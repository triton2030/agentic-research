<!-- 1html-generated: shared visual explainer zone -->
# HTML Artifacts — Visual Explainer Zone

Каждая страница здесь — самостоятельный сайт на уже установленных локальных
библиотеках. Tailwind, Alpine и Lucide подключены, тема DaisyUI `cupcake` уже
стоит; Table, Mermaid, ECharts и React Flow лежат в `lib/` и `assets/shared/`,
точные теги каждого — в `COMPONENTS.md`. В сеть не ходи: страница обязана
открываться по `file://` без CDN, установки, сервера и сборки.

Дизайн и интуитивность держи по золотому стандарту ремесла: Дон Норман,
эвристики Якоба Нильсена, Дитер Рамс, «Refactoring UI», швейцарская сетка и
бенто, лучшие посадочные страницы. Понятность через показ — по тем, кто это
умеет: Дэвид Маколей, Изотайп Нейрата, Минар и Тафти, Брет Виктор,
скроллителлинг и принципы анимации, Presentation Zen и Дуарте, Фейнман.
Имена на самой странице не пишутся.

Три границы:

- текст — крайнее средство: всё показуемое показывается, словами остаются
  подпись, связка и короткий вывод;
- соседние HTML, `_template.html` и `assets/_template.css` — не образец облика:
  они могли быть собраны до текущей планки;
- своё не выдавай за факт: непроверенное, догадку, аналогию и собственный вывод
  помечай как свои.

Это одна shared zone на проект:

- новая страница — `<slug>.html`, её стили — `assets/<slug>.css`;
- не создавай per-page bundle directory и не копируй `lib/assets`;
- удалил или переименовал HTML руками — пересобери каталог скриптом скила
  `1html`;
- обычная работа заканчивается готовой страницей, без audit, check и finish.

Выбор носителя и его рабочие рецепты — в skill `1html` и его `references/`;
здесь их не дублируй.
