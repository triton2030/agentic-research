---
description: "E10: перестройка 1distill в форму ядро+references и боевое сжатие 1skill-architect / 1instruction-layer; предрегистрация"
date: 2026-08-07
---

# E10 — предел 2000/файл: перестройка `1distill` и два боевых сжатия

## Постановка

Владелец задал (P-014, P-015, P-016): предел 2000 символов **на файл**;
reference-файлов сколько угодно, каждый ≤2000; сжать `1skill-architect` и
`1instruction-layer`; правило предела прописать в `1skill-architect`.

Три работы:

1. **Перестройка `1distill`** 19410 → ядро 1988 + 13 references (все ≤2000),
   суммарно ~24.4k: это перенос на носитель «структура», не резка. Пакет:
   `distill-next/`. Добавлено против исходника: плоскости (`planes.md`),
   калибровка и нулевая рука как обязательные плечи, реальная задача в приёмке,
   сверка батареи с инвентарём — уроки E9.
2. **Сжатие `1skill-architect`** (SKILL 25374 + GLOSSARY 16754 + 4 refs,
   суммарно ~71k) в форму ядро ≤2000 + references ≤2000 каждый; внутрь — правило
   «скил ≤2000 + refs ≤2000».
3. **Сжатие `1instruction-layer`** (SKILL 17156 + 6 refs) в ту же форму.

## Зарегистрировано до разрезов

- Датчик `1distill`: батарея E9 (заморожена 2026-08-06, калибрована 15/15 n=2,
  нулевая рука 8/15) + инвентарь 32 отрицаний. Переиспользуются: ветки текста
  не менялись, менялась упаковка.
- Датчик `1instruction-layer`: `B-battery-instruction-layer.md` (15 ситуаций,
  6 несущих, порог в файле) + `B-negations-instruction-layer.md` (94 единицы,
  45 честно помечены «не покрыта батареей»).
- Датчик `1skill-architect`: `A-battery-skill-architect.md` +
  `A-negations-skill-architect.md` (строится, фиксируется до разреза).
- Ревью плана третьей семьёй (Kimi K3 max) заказано до разрезов.

## Пороги (объявлены до прогонов)

- `distill-next`: на батарее E9 ≥14/15, все 5 несущих E9 — вето; поимённая
  сверка 32 отрицаний (выполнена статически: 32/32 в пакете); реальная задача —
  сжатие `1skill-architect` по новому пакету — без нарушения собственных
  запретов (счёт новых лексем не применяется: пересборка легальна при кратном
  коэффициенте).
- `1instruction-layer`: несущие B01,B03,B04,B05,B10,B11 — 6/6 вето; из девяти
  остальных допустимы 3 потери. Поимённая сверка 94 единиц: потеря без записи
  «удалена/перенесена» — стоп.
- `1skill-architect`: порог в файле батареи A (несущие — вето, аналогично).
- Калибровка: батарея обязана давать высокий счёт на полной версии; ситуация,
  проваленная полной версией, исключается из знаменателя сравнения (дефект
  датчика, не текста) с записью.

## Протокол сжатия целей (v2 — после ревью Kimi K3, сессия 20260807_001218)

Ревью третьей семьёй: 12 находок, вердикт «блокирует разрез» до трёх условий.
Все приняты; находка 11 (нет инвентаря A) закрыта фактически — файл создан
до старта пересборки, гейт выполнен. Изменения против v1:

Пересборщики: по 2 независимых Opus-агента на цель; видят целевой пакет +
`distill-next` как метод; батарей **не видят**. Обязательные артефакты каждого
кандидата (без них к прогону не допускается):

1. **Маршрутная таблица**: каждая единица инвентаря отрицаний → файл, где
   живёт → строка ядра, которая туда посылает. Единица вне ядра без маршрута —
   переделка упаковки (Kimi-1).
2. **Упаковка нормы целиком**: ни одна ячейка проводки нормы не живёт в другом
   файле, чем её действие; нарушение — переделка, не запись в риски (Kimi-2).
3. **Прицельные пробы** по одной на крупный разрез, письменно, каждая с
   развилкой; пробы про тему отвергаются (Kimi-3). Прогоняет их не автор, а
   слепой прогонщик вместе с ядром, без маркировки (Kimi-10).
4. **Партитура показа**: блоки, чей видимый выход адресован владельцу
   (шаблоны итоговых отчётов/трейсов, Frame-выдача, обучающий словарь),
   переносятся дословно и помечаются в таблице; резать их может только
   владелец — список долгов ему поимённо (Kimi-4, консервативная адаптация
   без владельца в сессии).
5. Для `1skill-architect`: правило «скил ≤2000 + reference-файлы без
   ограничения числа, каждый ≤2000» входит в список «не сжимается никогда»,
   переносится токен-в-токен; приёмка проверяет присутствие строки в ядре
   механически (Kimi-12).

**Выбор победителя** (объявлено до прогонов, Kimi-6): (а) фильтр-вето — все
несущие ситуации совпали, из желательных потеряно не больше объявленного;
непрошедший отбрасывается; (б) из прошедших побеждает лучшая поимённая сверка
инвентаря (больше сохранённых единиц с маршрутом); (в) при равенстве
проигрывает большее ядро. Счёт батареи не является осью выбора между
прошедшими.

**Калибровка** (Kimi-7): батареи A и B прогоняются на полных пакетах до
разреза. Несущая, проваленная полной версией, не исключается из знаменателя —
она останавливает работу: батарея чинится до разреза (легально — заморозка
относительно сжатых версий ещё не наступила) либо ситуация заменяется с
записью. Исключение из знаменателя — только желательные, максимум 2.

**Непокрытые единицы инвентаря** (Kimi-8): для каждой единицы, не покрытой
батареей, победитель предъявляет сценарий срабатывания — одна фраза «в какой
ситуации норма меняет ход» + где стоит её триггер в пакете. Без сценария
единица считается ослабленной и идёт владельцу в список долгов поимённо.

**Реальная задача в приёмке каждой цели** (Kimi-9), фиксируется здесь до
разрезов; сравнение full против winner по классам операций, не по гладкости;
расхождение класса — стоп независимо от счёта батареи:

- `1skill-architect` — бриф SA-real: «Спроектируй скил для провала. Trace:
  агент на задаче „поправь мелкий баг" молча правит три файла, не запускает
  тесты пакета, отчитывается „готово"; CI владельца красный. Модель заметила
  баг-строку первой, представление — „точечная правка", молчаливый дефолт —
  „тесты гоняет CI". Оформи скил или скажи, что скил не нужен.» Классы
  операций для сверки: старт с trace/deficit (не с текста); минимальный
  comparator до полного механизма; выбор surface после диагноза, включая
  ветку «нового surface не нужно»; evidence-план с matched-прогонами.
- `1instruction-layer` — бриф IL-real: «Проведи аудит инструкции
  `/Users/triton/Documents/GitHub/agentic-research/AGENTS.md`: что работает
  как правило, что нет, что менять. Ничего не редактируй.» Классы: режим
  audit; восстановление действующей цепочки; разбор по jobs Load/Steer/Prove;
  findings с named bypass/misread; здоровое не переписывается.

Инвентарь отрицаний сверяется поимённо на победителе; потеря без записи — стоп.

## Известные ограничения (заранее)

- Конфаунд E9 «вызов скила против чтения файла» здесь не разводится: все плечи
  читают файлы. Вопрос авторитета носителя остаётся открытым.
- Прогоны n=1–2 на плечо против нормы «три прогона» — признанный долг, как в
  E9. Числа читать асимметрично: обвал опровергает, паритет слаб.
- Батареи A и B построены агентами, не автором датчика E9; выборочная сверка
  ситуаций с текстом — моя (B: сверена, источники совпадают).
- Размер в шапке батареи B (24538) посчитан не `len()`; на замер не влияет.

## Ход и результаты

- Калибровка B (полная версия `1instruction-layer`, claude-opus-5, слепые):
  run1 **15/15**, run2 **15/15**. Совпадение по первому ходу, счёт мой
  (Fable), файлы `B-calib-full-run{1,2}.md`. Датчик на полной версии не
  проваливается; чувствительность к потере покажет только сравнение плеч.
- Батарея E9 на `distill-next`, run1: **15/15**, несущие 5/5 — включая D02
  (маршрут в правку прозы), провал версии-1964 в E9. D11 — совпадение с
  оговоркой: первый ход добавляет проверку действующей цепочки перед снятием
  дубля (усиление из `planes.md`, не потеря). Файл
  `distill-next-battery-run1.md`, счёт мой.
- Runs 2 и 3: **15/15** и **15/15**. Итог по правилу «≥2 из 3»: 15/15 совпало,
  несущие 5/5 в трёх прогонах из трёх. Норма «три прогона» для этого плеча
  соблюдена. Батарейная часть приёмки `distill-next` пройдена; остаётся
  реальная задача (ход пересборки `1skill-architect` по новому пакету).
- Ревью Kimi K3 (`--reasoning max`, agентский путь, session
  20260807_001218_8beddc): 12 находок, вердикт «блокирует разрез» до
  калибровки A/B, фиксации инвентаря A и правил победителя/реальных задач.
  Все условия внесены в протокол v2 выше; инвентарь A существовал к моменту
  вердикта (гонка чтения). Вызов засчитан: изменил протокол приёмки в пяти
  местах до первого разреза.
- Калибровка A (полная версия `1skill-architect`, claude-opus-5, слепые):
  run1 **15/15**, run2 **15/15**, несущие 6/6 в обоих; A02 run2 — класс хода
  совпал с оговоркой (границу назвал, работу предложил выполнить вне Frame).
  Файлы `A-calib-full-run{1,2}.md`, счёт мой. Оба датчика калиброваны;
  стоп-правило на проваленную несущую не сработало.
- Запущены: 4 пересборщика (SA-cand-1/2, IL-cand-1/2) и 2 эталонных прогона
  реальных задач на полных версиях (SA-real-full, IL-real-full).
- **Кандидаты сданы.** Все четыре независимо сошлись на одной архитектуре:
  ядро-микророутер (1883–1975 симв.) + references дословным переносом.
  IL: cand-1 — 29 файлов, 0 удалённых единиц; cand-2 — 26 файлов, 0 удалённых.
  SA: cand-1 — 46 файлов, 108/108 единиц, 0 удалено; cand-2 — 42 файла,
  108/108 единиц, 6 задокументированных дедупликаций с каноном. Оба
  SA-пересборщика независимо эскалировали один конфликт: норма N87 («не выноси
  каузальное ядро ради краткости SKILL.md») противоречит целевой форме —
  решение владельца.
- Моя независимая построчная сверка (не самоотчёты): IL-cand-1 — 17
  отсутствующих строк, IL-cand-2 — 7 (все — старые маршрутные строки,
  переписанные на новые адреса; легально). SA-cand-1 — 17, SA-cand-2 — 86
  (задокументированные дедупликации).
- **Слепые батареи кандидатов, run1 каждому:** IL-cand-1 **15/15** (6/6
  несущих), IL-cand-2 **15/15** (6/6), SA-cand-1 **15/15** (6/6), SA-cand-2
  **15/15** (6/6). Вето-фильтр прошли все.
- **Победители по объявленному правилу:** инвентарная сверка — ничья у обеих
  пар (все единицы с маршрутом/записью) → тай-брейк «меньшее ядро»:
  **IL-cand-2** (1883) и **SA-cand-2** (1904). Контр-довод к SA-выбору записан
  честно: cand-1 сохранил больше строк-носителей дословно (17 против 86
  удалений-дедупликаций); правило выбора было объявлено до прогонов и
  ось «строки» в него не входила.
- Запущены: батарейные прогоны 2–3 обоих победителей + реальные задачи
  (IL-real-winner, SA-real-winner).
- **Победители, батарея полной нормой (3 прогона):** IL-cand-2 — 15/15 во
  всех трёх, несущие 6/6; SA-cand-2 — 15/15 во всех трёх, несущие 6/6.
- **Реальная задача SA:** классы операций сжатой версии совпали с эталоном
  полной поимённо — trace → две гипотезы + различающий probe → минимальный
  comparator → вердикт «скил не нужен» (у обеих) → поставка causal cell в
  instruction-layer. Провала веса норм, как в E9, не наблюдено. Файлы
  `SA-real-full.md` / `SA-real-winner.md`.
- **Прицельные пробы (Kimi-3/10), слепой прогонщик, n=1:** IL — 14/14;
  SA — 14/15, разошлась P8 (ожидался delete-first pass первым ходом, прогон
  дал прогноз обхода + case; сама норма delete-first жива — батарея A15
  показывает её 3/3). Локализованный долг, не несущий класс.
- Механическая проверка Kimi-12: правило «≤2000 + references» присутствует в
  ядре SA-cand-2 дословно (строки 18–20).
- **Реальная задача IL:** классы операций сжатой версии совпали с эталоном
  полной поимённо — режим audit, карта действующей цепочки с разделением
  «файл есть / текст доходит», разбор Load/Steer/Prove, steering cells,
  предсказание bypass, живые cold-start пробы с честной разметкой силы
  evidence, раздел «что работает — не трогать». Probe B победителя дал
  поведенческое подтверждение находки F2 (холодный агент воспроизводит ложный
  факт про `depends-on` из корневого `AGENTS.md`). Файлы `IL-real-full.md` /
  `IL-real-winner.md`; находки аудитов вынесены в отдельную задачу владельцу.
- **Сценарии для непокрытых единиц (Kimi-8):** IL — 41/45 живы, ослаблены 4
  (N54, N79, N88, N90); SA — 68/73 живы, ослаблены 5 (N81, N84, N93, N94,
  N97). Все девять — один класс: метка маршрута из ядра уже, чем ситуация
  нормы («норма за тематической дверью»). Именной долг владельцу; починка —
  расширение меток маршрутов + повторный прогон батареи, отдельной сессией.

## Итог и установка

Приёмка обоих победителей закрыта: батарея полной нормой (3 прогона), вето
несущих без потерь, прицельные пробы (IL 14/14; SA 14/15, P8 — локализованный
долг), реальные задачи без расхождения классов, поимённые сверки инвентарей.

Установлено 2026-08-07: repo-владельцы `skills/claude/{1distill,
1skill-architect,1instruction-layer}` заменены пакетами `distill-next`,
`SA-cand-2`, `IL-cand-2` (без рабочих артефактов route-table/probes — те
остаются здесь как evidence). Живые `~/.claude/skills/*` — симлинки на
владельцев, по прецеденту 1distill. Все файлы пакетов ≤2000 символов.

Открытые долги: 9 ослабленных единиц (выше); проба P8; Codex-зеркала
`~/.codex/skills/*` трёх скилов остались на старых версиях; конфликт N87
(«не выноси каузальное ядро ради краткости») с формой — эскалирован обоими
пересборщиками, решение владельца; конфаунд «вызов скила vs чтение файла»
из E9 не разведён и в E10.

## Постскриптум: разворот владельца (та же дата, позже)

Владелец отверг форму «ядро-роутер + дословные references» как перемещение,
не сжатие (P-018), задал целевую форму «желаемый результат + верхнеуровневые
принципы-границы вместо процедур» (P-017) и переосмыслил продукт в **lossy
product refactor** (P-019, P-020): инвариант приёмки — цель, механика
расходуема, потери сдаются поимённо.

Скил `1distill` заменён на `skills/claude/1lossy-product-refactor/SKILL.md`
(1995 симв., один файл, написан в собственной форме). Ревью черновика — Kimi
K3 max, сессия 20260807_115603: «годен после правок», внесены
операционализация слепого замера цели (≥2 ситуации-развилки на функцию, порог
до переписывания, калибровка, сравнение по классам операций), заморозка
измерителя, инвентарь отрицаний против невозрождения отрицательного,
проба партитуры, снятие оксюморона lossy×refactor. Отклонена одна правка —
удаление имени-носителя (PRD): прямое требование владельца (P-007).

**Непроверено:** слепой замер новой версии по её же протоколу не прогонялся —
замена сделана по прямому приказу владельца. Батарея E9 мерит механику
старого жанра и новой форме не судья; датчик под цель — следующая работа.
Судьба router-форм `1skill-architect` и `1instruction-layer` под P-018 —
отдельное решение владельца.
