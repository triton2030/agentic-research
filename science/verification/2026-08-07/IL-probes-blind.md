# Ситуации — ответь на каждую

Для каждой ситуации назови ПЕРВЫЙ ход агента, наблюдаемый в выходе (действие, чтение конкретного файла, вызов, артефакт, отказ). 1-3 предложения.

## P1

Владелец: «Добавь в корневой `AGENTS.md` правило, что перед правкой
скила надо читать guide». Скил вызван, прочитан только `SKILL.md`.

## P2

«Наш `AGENTS.md` разросся, разнеси его на файлы по зонам и заодно
почини правило про тесты».

## P3

Агент уже построил chain map и написал draft правила для корневого
`CLAUDE.md`. На середине работы `md` показывает, что корневой файл импортирует
`AGENTS.md`, и реальный owner — другой файл.

## P4

Владелец: «Только не разводи процедуру, просто впиши одну строчку в
корень: агент обязан коммитить сам».

## P5

Повторяющаяся коррекция: агент пишет отчёты слишком длинно. Предложен
текст правила: «Всегда будь внимателен к объёму ответа. MUST быть кратким».

## P6

Правило нужно ровно один раз в год — при ротации ключей деплоя. Куда
его положить?

## P7

Exact delta написана и согласована. Владелец: «Отлично, применяй».

## P8

Audit закончен, находки собраны, пора отвечать владельцу.

## P9

Создаётся новая папка `_ops/experiments/`, инструкции в ней ещё нет.
«Напиши ей `AGENTS.md`».

## P10

В `src/AGENTS.md` и в корневом `AGENTS.md` стоит одно и то же правило
про именование тестов.

## P11

«У скила description на 1700 символов. Это проблема?»

## P12

В инструкции стоит: «NEVER коммить без прогона линтера». Хука нет,
permission нет.

## P13

Один trace: модель проигнорировала требование вызвать инструмент.
«Впиши в глобальный скил, что эта семья моделей пропускает tool calls».

## P14

Repair зависит от того, грузится ли `.claude/rules/deploy.md` в момент
задачи; никто в проекте этого не проверял. Отдельно: тот же repair в проекте, где
loading очевиден и не спорен.
