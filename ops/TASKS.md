# Tasks

## In Progress

No active work right now.

## Next

- [ ] Capture the first 2-3 concrete cases where `ops` improved the result
  Done when: each case explains the situation, what changed, and why the difference mattered inside `knowledge/research/meta/learnings.md`.

- [ ] Draft a simple taxonomy of meta-agents by decision job
  Done when: each candidate type has a purpose, a trigger, an output, and a boundary inside `knowledge/research/meta/learnings.md`.

- [ ] Define the promotion rule from research folders into base files
  Done when: it is clear when a point should stay in a research track and when it should move into `AGENTS.md`, `knowledge/guides/perfect-skills.md`, or `knowledge/guides/perfect-system-prompts.md`.

## Blocked

No blocked work right now.

## Done

- [x] Restored `ops/` as the project working folder
  Verified by: `ops/ROADMAP.md`, `ops/TASKS.md`, and `ops/ISSUES.md`.

- [x] Moved agent and meta-agent research into a separate root project
  Verified by: `projects/meta/before-you-build--agent-codex/`, `meta/meta-thinking--skill-codex/`, and `knowledge/research/meta/`.

- [x] Grouped the repository into four top-level directions
  Verified by: `design/`, `dev/`, `business/`, and `meta/`.

- [x] Stabilized `ops/` as the operational layer instead of a research folder
  Verified by: the current structure of `ops/ROADMAP.md`, `ops/TASKS.md`, and `ops/ISSUES.md`.
