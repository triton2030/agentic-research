interface:
  display_name: "ClickUp Control"
  short_description: "Control ClickUp through the private API"
  default_prompt: "Use $1clickup to complete the requested ClickUp outcome autonomously and verify the result."

policy:
  allow_implicit_invocation: true
