interface:
  display_name: "ClickUp Control"
  short_description: "Control ClickUp through the private API"
  default_prompt: "Use $1clickup to inspect my ClickUp workspace and take the requested action safely."

policy:
  allow_implicit_invocation: true
