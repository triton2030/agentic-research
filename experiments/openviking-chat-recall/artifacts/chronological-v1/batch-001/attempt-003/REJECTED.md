---
kind: semantic-audit-verdict
batch: batch-001
attempt: 3
status: rejected
date: 2026-08-22
---

# Clean batch-001 attempt-003 — REJECTED, но это последний кандидат старой схемы

Candidate SHA-256
`78300121bd55015606eb8520024914ee5eaadc112d96072c8cb19ae32d10316a`,
prompt `wiki-writer.v3.md`, модель `gpt-5.6-luna/max`. 23 операции create,
32/32 записи used, пять групп повторов. Механический `--check-only` PASS.

## Пять осей, пять FAIL — но провал другого рода

| Ось | attempt-002 | attempt-003 |
| --- | --- | --- |
| source-fidelity | 9 major | 6 |
| owner-attribution | 7 | 4 |
| page-fit | 7, из них 3 blocker | 3, blockers нет |
| ia-findability | 6, blind 4/5 | 2, blind 5/5 |
| coverage-determinism | blocker + 3 | 2, blockers нет |
| Итого | 29 | 17 |

Полные пакеты: `_workspace/codex-artifacts/20260822T1030*`, бриф
`../audit/BRIEF-003.md`.

## Что процедурная спина починила

Промпт v3 вернул шесть фаз upstream OpenViking. Результат измерим:
пять групп повторов вместо одной, и **все пять легли каждая на одну
страницу** — фаза 3 действительно кормит фазу 4. Типы перестали схлопываться
в `concept`: семь `entity` против нуля. Дробление как класс исчезло: аудит
page-fit не отметил ни одной из шестнадцати одно-записных страниц.

## Семнадцать находок — пять причин

1. Одна страница несёт два вопроса (`chat-recall-quote-format`): формат цитаты
   и то, какие слова считать словами владельца. Нашли source-fidelity и
   page-fit независимо.
2. Пропущена группа повторов «в скиле только дельта»
   (`cr-9e42f2995175d537` + `cr-e0cc9eb95585f23d`). Нашли page-fit и
   coverage-determinism.
3. Модальность: критерий подан как решение, решение ослаблено до планирования.
   Нашли source-fidelity и owner-attribution.
4. Тип страницы: процедура записи цитат — `method`, конкретный reference-файл —
   `entity`.
5. Дейксис: имя предмета взято из служебного `context-note`, а не из цитаты
   (Playwright дважды, reference-файл).

## Решение владельца: слой заявки снят

Владелец 2026-08-22: «хорошо делай как ты рекомендуешь до конца не спрашивай
меня» — на рекомендацию снять changeset/manifest/materializer слой.

Основание рекомендации: за три попытки механический слой не поймал **ни одного**
дефекта, который решил судьбу кандидата. Все три прошли `--check-only` и были
зарублены смыслом. Единственная находка машинного слоя за день — незаполненный
`undefined` в нашем же служебном поле, то есть дефект формата, которого без
формата не существует.

Оправданной осталась одна часть: раскладку принятого текста по файлам делает
код, а не модель — это доказано находкой
`_ops/findings/2026-08-22-081813-1787368693.md`, где LLM трижды ошиблась в
служебной materialization-логике при верном тексте.

Дальше короткая схема: писатель пишет страницы прямо, `cp` кладёт их в вики,
смысловой аудит на `sol` решает, git даёт откат и провенанс. Контракт —
`prompts/wiki-writer.v4.md`.

Этот artifact — immutable failure evidence. Ручное исправление запрещено.
