{
  "name": "md-mcp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Unified MCP server for Markdown corpus tooling: md-navigator (semantic search, frontmatter map, hybrid retrieval) and md-graph (read-before-edit / edit-after-edit graph hygiene). Subprocess wrapper over the existing Python CLIs.",
  "bin": {
    "md-mcp": "./src/server.js"
  },
  "scripts": {
    "mcp": "node ./src/server.js",
    "smoke": "node ./test/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.1"
  },
  "engines": {
    "node": ">=22"
  }
}
