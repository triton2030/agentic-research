interface:
  display_name: "1design-review"
  short_description: "Plan-first visual review fanout with progress."
  default_prompt: "Use $1design-review after frontend work: inspect the UI, plan grouped screenshots, then fan out clean reviewers with progress."
policy:
  allow_implicit_invocation: true
