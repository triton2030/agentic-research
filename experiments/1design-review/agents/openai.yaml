interface:
  display_name: "1design-review"
  short_description: "Plan-first screenshot groups plus clean reviewers."
  default_prompt: "Use $1design-review after frontend work is complete: inspect the UI, write a screenshot plan, then run clean grouped reviewers."
policy:
  allow_implicit_invocation: true
