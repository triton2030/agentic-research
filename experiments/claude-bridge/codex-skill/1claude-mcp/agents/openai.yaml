interface:
  display_name: "1claude-mcp"
  short_description: "Independent Claude advice or session control"
  default_prompt: "Use $1claude-mcp for the requested Claude advice/review or Claude session inspection/control."

dependencies:
  tools:
    - type: "mcp"
      value: "claude-mcp"
      description: "Subscription-backed native Claude advisor"

policy:
  allow_implicit_invocation: true
