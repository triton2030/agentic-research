interface:
  display_name: "1-Совет Claude"
  short_description: "External Claude review or session control"
  default_prompt: "Use $1claude-mcp for a compact independent Claude perspective on this task or decision."

dependencies:
  tools:
    - type: "mcp"
      value: "claude-mcp"
      description: "Subscription-backed native Claude advisor"

policy:
  allow_implicit_invocation: true
