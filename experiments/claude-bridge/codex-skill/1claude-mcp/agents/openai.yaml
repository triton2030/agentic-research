interface:
  display_name: "Claude Advisor"
  short_description: "Independent Claude review or second opinion"
  default_prompt: "Use $1claude-mcp to get a compact independent Claude perspective on this decision."

dependencies:
  tools:
    - type: "mcp"
      value: "claude-mcp"
      description: "Subscription-backed native Claude advisor"

policy:
  allow_implicit_invocation: true
