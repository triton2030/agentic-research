interface:
  display_name: "1-Claude MCP"
  short_description: "Independent Claude Opus advice or session control"
  default_prompt: "Use $1claude-mcp for the requested Opus advice/review or Claude session inspection/control."

dependencies:
  tools:
    - type: "mcp"
      value: "claude-mcp"
      description: "Subscription-backed native Claude Opus advisor"

policy:
  allow_implicit_invocation: true
