interface:
  display_name: "Claude MCP"
  short_description: "Run Claude advisors, threads, audits, and scoped workers"
  default_prompt: "Use $1claude-mcp to call Claude through the managed bridge, choose the safe model/profile, preserve resumable thread state, and return evidence plus a terminal tail."

dependencies:
  tools:
    - type: "mcp"
      value: "claude-mcp"
      description: "Managed Claude Code bridge"

policy:
  allow_implicit_invocation: true
