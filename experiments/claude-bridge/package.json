{
  "name": "claude-advisor-bridge",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Claude Agent SDK advisor bridge with blocking ask and transient sessions.",
  "scripts": {
    "ask:mcp": "node ./src/ask-server.js",
    "ask:live": "node ./test/live-claude-ask.js",
    "ask:test": "NODE_ENV=test node --test ./test/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.263",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.1"
  },
  "engines": {
    "node": ">=22"
  }
}
