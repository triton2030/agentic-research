# Local Defender Raw Output

Role: `local-defender`

Independence label: `main-authored / not independent`

Purpose: make the positive case explicit so the challenger is not attacking a
void. This is not independent evidence.

## Defended Claim

MAVO's current corpus is business-auditable and contains a coherent current
model:

- current/future boundary is explicit;
- actor and money boundary are explicit;
- paid `Принять` is a stable owner invariant;
- buyer and studio paths are described;
- weak links are named instead of hidden;
- pilot validation gates are already defined.

## Positive Case

1. The model does not smuggle in current marketplace/commission logic. The
   current money event is studio-side paid `Принять`.
2. The corpus does not pretend real proof exists. It labels paid `Принять`,
   direct payment trust, SKU factory, economics, and segment/adoption as
   `гипотеза` or `допущение`.
3. The business protocol in `Бизнес_Анализ/AGENTS.md` is stronger than the v0
   audit and should be treated as MAVO's owner protocol.
4. The cheapest next proof is named: pessimistic unit table, factory/SKU run,
   first print-ready trial, and pilot funnel with accepted paid requests.

## Limit

This defense supports `documents are auditable`, not `business works`.
