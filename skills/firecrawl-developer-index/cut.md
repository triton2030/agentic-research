# cut — firecrawl-developer-index, 2026-08-23

Что не вошло в правку и почему. Оригинал до правки — `origin.md`.

## Снято из description

| Снято | Причина |
| --- | --- |
| `prefer this over a general web page` | Поведенческая инструкция в описании; описания грузятся всегда, а доктрина владельца 2026-08-22 — «описание = только триггер». Различение против веба сохранено как свойство корпуса: «где поведение библиотеки задано, а не пересказано». |
| `Use when the question is how a library or API behaves` | Требует уже сформулированного вопроса. Момент владельца — комфорт, где вопроса нет. Заменено на акт «назвать по памяти». |
| `what an error means` отдельной строкой | Поглощено «причиной ошибки»; отдельный носитель не добавлял решения. |

## Предлагалось и отвергнуто

| Предложение | Кто снял | Причина |
| --- | --- | --- |
| «строить своё поверх библиотеки, где готовое уже есть» | линза «потерянное и выдуманное» | Строка без источника: слова владельца 2026-08-23 13:31 про «прочитать инструкции или найти доки», не про поиск готовой библиотеки. Маршрута под это в теле нет. Случай покрыт «писать код против API, который не открывал в этой сессии». |
| «лимит», «флаг» в перечне | линза «лишнее» | «лимит» выводится из «дефолта», «флаг» — из «сигнатуры». |
| «новостей», «проектов без публичного репозитория и доков» в обратной границе | линза «лишнее» | Позитивной частью не притягиваются; неиндексированные проекты уже покрыты буллетом тела. |
| «даже если вопроса не чувствуешь» | линза «лишнее» | Ненаблюдаемо. Наблюдаемый близнец «даже если владелец ни о чём не спрашивал» говорит то же. Момент владельца («комфорт») переведён в наблюдаемое условие, а не потерян. |
| Снять из тела эхо `indexed: true\|false` как «ложный факт» | линза «потерянное и выдуманное» | **Факт оказался верным.** Эхо возвращается ровно при заданном scope. Мой вывод об обратном стоял на двух испорченных тестах: запрос без scope и обрезка вывода через `head -c 400` (эхо лежит после `results`). Правка отозвана. |
| «ключ не печатать», «со многих IP», «CLI ключ передавать не требует» | линза «лишнее» (принято частично) | Первое всегда-истинно → корень, не скил. Второе — хедж, решения не меняет. Третье оставлено: без него неясно, почему `firecrawl developer` работает без ключа. |
| Указатель на `credentials.json` | линза «лишнее» (отклонено) | Линза предлагала обойтись `firecrawl view-config`. Проверено: view-config печатает ключ маскированным (`fc-ea7…e5de`), значение оттуда не достать. Файл — единственный исполнимый маршрут. |

## Не делалось намеренно

- Связь с четвёркой книг в описание не тащим: она живёт в корневом `~/.claude/CLAUDE.md`, дубль завёл бы второго владельца смысла.
- Tracked owner в `skills/shared/**` не заводился — решение владельца 2026-08-23: править live, риск перезаписи принять. Эта папка — история, не source tree.
