interface:
  display_name: "1-Применение принципов"
  short_description: "Use before consequential work when approved product truth may govern; use 1product-shaping to change that truth"
  default_prompt: "Используй $1use-principles перед чистовой записью или существенным выбором, когда утверждённая продуктовая правда может влиять на текущую работу."

policy:
  allow_implicit_invocation: true
