# Cut — 1deep-agents (ревизия 2026-08-10, 373 → ~105 строк)

- Бриф субагента 43 строки + step IDs S1…Sn + topology linear/iterative →
  6-строчное требование следа. Риск: слабее контроль iterative-методов и
  повторных проходов — проверять прогоном.
- 9 условий приёмки trace → 1 цитатный гейт (шаг с task-specific
  преобразованием, использующим предыдущий). Риск: грубее сетка отказа.
- Selection table, внутренний ranking, pre/post-state receipts,
  mechanism-модель списком (search/abstraction/…) → снято как
  процедурный балласт; ядро «диагноз когнитивной работы» сохранено.
- Раздел Authority И Non-Composition → строки описания и «Не входит».
- Развёрнутые правила выбора 1–7 → зонтик 2 + строка диагноза; правило
  «≥1 domain-native для тех.задач» сохранено в критериях.
- Порог автономии не переписывается — адрес на корневое правило записи
  (вместо третьей формулировки; вердикт волны).
- Продукт «синтез для owner decision» → «принятое решение с основанием»
  (бриф владельца); raw traces публикуются по-прежнему.
- references сохранены без изменений (frameworks, runtime-orchestration,
  characterization).
