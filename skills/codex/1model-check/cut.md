# Cut

- Автоматический hook и self-invocation сняты: skill запускается только вручную.
- Отдельная видимая задача и внешний советчик сняты вместе с hook.
- Порог 3 снят; явный вызов разбирает до пяти ещё не разобранных собственных
  `⚡ UNEXPECTED`.
- Немедленный trigger на `⚡ UNEXPECTED (repeat)` снят; repeat считается одним
  marker в пятёрке.
- Словесная confidence-оценка снята: она не является evidence качества модели.
- Единая общая причина не обязательна: допустимы partial coverage, независимые
  причины и недостаточное evidence.
- References и scripts не добавлены. С 2026-08-31 один runtime-contract
  установлен в Claude и Codex; platform metadata различается только механизмом
  запрета неявного вызова.
