interface:
  display_name: "1model-check"
  short_description: "Manual working-model reassessment"
  default_prompt: "Используй $1model-check, чтобы вручную разобрать до пяти неразрешённых расхождений прогноза с наблюдением и связать проверку со следующим действием."

policy:
  allow_implicit_invocation: false
