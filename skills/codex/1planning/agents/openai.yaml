interface:
  display_name: "1-Планирование"
  short_description: "One living work contract: agent-to-agent instruction stepping toward GOAL"
  default_prompt: "Use $1planning when chosen work must survive sessions or compaction: get the owner's yes on a short justification (goal, value, mode, appetite) before creating any plan file, keep one living contract with provenance per requirement, honest Wayfinding/Execution mode, dependency-cut milestones, and current truth instead of chronology."

policy:
  allow_implicit_invocation: true
