interface:
  display_name: "Память чата"
  short_description: "Сохраняет полезные тезисы и читает их как контекст"
  default_prompt: "Используй $1chat-recall, чтобы сохранить самостоятельный полезный тезис или прочитать мои прежние слова как контекст для текущего решения."

policy:
  allow_implicit_invocation: true
