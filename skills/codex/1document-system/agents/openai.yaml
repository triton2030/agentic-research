interface:
  display_name: "1-Система документов"
  short_description: "Route mutable truth before choosing document form"
  default_prompt: "Use $1document-system to run the tomorrow-change test before choosing owners, types, projections, or topology."
policy:
  allow_implicit_invocation: true
