interface:
  display_name: "1-Система документов"
  short_description: "Give a project document a standard type name and hold it to what must be acted on"
  default_prompt: "Use $1document-system when about to write or substantially edit a project document."
policy:
  allow_implicit_invocation: true
