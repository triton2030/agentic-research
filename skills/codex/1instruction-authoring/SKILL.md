---
name: 1instruction-authoring
description: >-
  Use when a project's agent instruction files must be written or rewritten so
  each zone carries only what the agent would otherwise violate. Not a
  how-to-work guide for a zone.
---

# Агентные инструкции проекта

## Уникальный контекст

Агент делает в проекте то, что сильная модель делает по умолчанию, и нарушает
местную правду, которой не видел: правит артефакт мимо ведущего скила, пишет по
привычной системе, не читает предписанное. Критично то, чьё нарушение сталкивает
код с документацией или зону со скилом. Больше двадцати единиц агент не держит и
теряет их случайно, поэтому нагрузку делят по папкам, а часть обязательств
отдельной командой не становится: цель зоны порождает поведение сама, критерий
закрывает обход, и только механизм исполняет запрет вместо агента. Содержимое
зоны — код и документы — влияет на агента сильнее файла инструкций: когда они
говорят иное, побеждают они, а строка, спорящая с фактическим состоянием зоны,
мертва. Из оставшихся строк он выполняет те, чью причину понимает: правило без
названного «зачем» сильная модель обходит как чужое пожелание, и список таких
правил остаётся непрочитанным по существу.

## Задача

Войди в проект с названной предстоящей работой и действующим слоем инструкций,
если он есть, и оставь после себя слой агентных инструкций — корневую и
папочные — в файлах, которые рантайм проекта действительно читает.

## Цель

- В каждой зоне агент застаёт ровно ту местную правду, которую иначе нарушил бы
  или заново искал бы, и ничего сверх неё — считая вместе с тем, что приносит
  скил, к которому зона отсылает.
- Каждое обязательство слоя живёт там, где обходится дешевле всего: целью зоны,
  её критерием, механизмом, который его исполнит, — и строкой инструкции лишь
  тогда, когда больше негде.
- Каждая зона открывается целью, из которой агент выводит и работу, и покой, а
  из каждой её единицы видно, где эта правда лежит и чем её править.

## Критерии принятия

- Каждая единица, которую ты написал или принял после вычитания, предъявлена с
  адресом файла и дословной строкой, которая заявляет эту правду в проекте, и
  несёт в самой себе причину, по которой её нарушение бьёт по цели зоны;
  расхождение, которого не подтверждает ни один документ, стоит в вопросах
  владельцу, а не в слое. Единица без причины — не единица: причина, найденная
  при написании, сама снимает лишние строки.
- Бюджет каждой зоны назван честным числом и предъявлен владельцу, если
  превышает двадцать; он не сведён вырезанием единицы и не переносом её в цель
  зоны или её критерии там, где они это обязательство не исполняют.
- Единицы действующего слоя в зонах, для которых ты не записывал привычку и не
  проводил вычитание, сохранены и названы непроверенными.
- Обязательство, объявленное переданным механизму проекта, указывает на файл
  этого механизма и дословную строку, которая эту проверку выполняет.
- Успехом не считается пересказ документации, инструкция, которую сильный агент
  выполнил бы сам, и набор для работы, которой в проекте не предстоит.

## Протокол поведения

1. Прежде чем открыть файлы зон предстоящей работы, полностью прочитай
   `references/discovery.md` и выполни стадию целиком, а не один её раздел.
   Порядок обязателен:
   после чтения зоны собственную привычку в ней уже не восстановить, и стадия
   вернёт выдуманную добросовестность вместо расхождений.
2. Прочитай действующий слой инструкций целиком, выпиши его единицы по зонам и
   сохрани единицы зон, которых ты не обследовал. Замер дал 2 из 2 прогонов, где
   без этого шага корневые правила о зоне сливались в саму зону, которую агент
   так и не открыл.
3. Полностью прочитай `references/writing.md` и выполни стадию целиком, а не
   один её раздел: она выбирает каждому обязательству место жительства,
   размещает оставшиеся единицы по зонам, задаёт их форму и держит бюджет
   головы.

## Правила, действующие всегда

- **Тест снятия.** Каждую строку слоя оставляй по тесту, а не по вкусу, и
  оправдывай сохранение, а не удаление: назови обязательство, которое исчезнет
  вместе с ней; знание, которого у чистого агента нет; поведение, которое без
  неё устойчиво меняется и портит результат; либо необратимый вред, который надо
  остановить до действия. Ни одного из четырёх — строке в слое не место: по
  **Minimal Critical Specification** предписывают не больше абсолютно
  необходимого, а «цель это и так подразумевает» оправдывает что угодно и потому
  не проверяет ничего. Переполнение зоны этот тест не лечит: там действует
  объединение по `references/writing.md`.
- Инструкция может нести когнитивный протокол: назови скил, которым артефакт
  этой папки правится, и этим передай зоне его порядок работы, вместо того
  чтобы пересказывать его своими словами.
- **Содержимое старше строки.** Инструкцию, с которой спорит код или
  документы её зоны, не усиливай: агент прочтёт зону целиком и пойдёт за ней.
  Единица, которую документ проекта подтверждает, остаётся в слое по
  критерию 1, а расхождение содержимого с этим документом уходит владельцу
  вопросом или в `1findings`, потому что строка сама его не закроет.
  Повторившееся нарушение уже написанной строки разбирай через
  `1context-refactor` — он ищет причину в любом контексте, который агент держал
  до ошибки, включая саму строку.
