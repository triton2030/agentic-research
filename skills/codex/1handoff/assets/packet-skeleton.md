---
description: "Handoff <timestamp>: <одна строка о том, что лежит в этом пакете>"
model: <идентификатор модели, которая фактически создала этот пакет>
date: <тот же timestamp в форме YYYY-MM-DD-HHMMSS>
---

1. Read this file in full.
2. Live files and runtime state override this dated snapshot.
3. First state the next action and why, then compare the recorded HEAD with
   `git log`. If they diverge, re-derive the action from live state before
   starting it. Check `git status` against the recorded working tree before
   touching any uncommitted file.

<Сюда выпиши каждый veto, каждый блокер, каждое непроверённое состояние и
каждый вопрос владельцу, без ответа на который работа стоит, — всё, что меняет
продолжение работы и что ты не вынес в раздел `## Anchors`. Ни одно из них не
должно оказаться в середине файла.>

## Questions to the Owner

<Гейт: продолжение зависит хотя бы от одного ответа владельца.>

- <вопрос> — задан <когда> или не задан; отвечен <адрес ответа> или без
  ответа; пока ответа нет — <принятое допущение или независимая работа>

## Terrain Model

### <Одна независимая поправка местности — один такой адресуемый блок>

<исходная модель или действие -> evidence или результат -> получившаяся модель>

## Where We Are

<Опиши, где именно остановилась работа. У каждого утверждения о состоянии здесь
стоят два отдельных ярлыка.
`knowledge` — наблюдение, которым утверждение проверено, либо `unverified`.
`consequence` — `none`, `accepted assumption`, `blocker` либо `reframe`.>

## Next Step

<Одно следующее действие и причина, по которой верным является именно оно.
У причины, и у рекомендации, если она здесь есть, — ярлык `knowledge`: адрес
слов владельца, файла или наблюдения либо `unverified`.>

Остаток работы после этого действия: <путь к файлу задачи, который держит
заказ, критерий готовности и очередь и не противоречит этому пакету; либо
путь к файлу задачи, названное расхождение с пакетом и последствие; либо
`остатка нет: ближайшее действие исчерпывает заказ`; либо `владельца нет`,
адрес слов владельца о заказе или `no recall address`, и последствие —
этот блокер повторён в зоне сразу после преамбулы>

## Anchors

- HEAD: <sha коммита на момент записи этого пакета>
- working tree: <правки: `clean`, либо каждая группа незакоммиченных правок
  с пометкой `намеренно` и почему, `снять можно`, либо `чужое или неизвестного
  происхождения — не снимать`; отдельно — каждый живой фоновый процесс и
  временная папка с той же пометкой, либо `нет`>
- recall: <captured | no qualifying evidence | blocked, и причина, если blocked>
- no recall address: <Гейт: часть 1 назвала этот блокер. Предмет позиции
  владельца, закрываемая ею развилка и последствие блокера>
- cleanup: <названные изменения и/или handed off; либо nothing to clean
  в одиночку>
- предыдущий полученный хендоф: <Гейт: эта сессия унаследовала хендоф. Путь к
  нему>
- <точные живые адреса: файл, строка и чем именно этот адрес важен>

## Incidents

<Гейт: новый evidence обесценил уже начатую работу либо сменил маршрут.>

### <Один инцидент — один такой адресуемый блок>

- прежняя модель: <во что верили до того, как выяснилось обратное>
- триггер: <что именно показало, что прежняя модель неверна>
- оплаченная цена: <измеренное время | unknown>
- почему нашлось поздно: <причина | unknown>
- durable исход уборки: <что изменено и в каком файле>

## Advice to the Next Agent

<Гейт: у этой сессии есть новые наблюдения, привязанные к файлу или роду работ.>

### <Одно наблюдение — один такой адресуемый блок>
