interface:
  display_name: "1-МД Чтение"
  short_description: "Read decision-changing Markdown without context bloat"
  default_prompt: "Используй $1md-read, чтобы дать следующему решению минимальное достаточное Markdown evidence или честный gap, не превращая чтение в отдельный отчёт."

policy:
  allow_implicit_invocation: true
