# Origin — 1hermes Ox Alpha override

- Owner: user statements captured in `_ops/chat-recall/2026-08-22-063718-codex-01a0271b.md`.
- Need: use free `stealth/ox-alpha` through Hermes harness so it can inspect and,
  when explicitly authorized, edit project files.
- Selection: update `1hermes`; do not create another skill.
- Route: use the existing Nous Portal subscription; the earlier OpenRouter-only
  implementation was corrected after the owner pointed to Hermes' own catalog.
- Admission: use Ox Alpha only while every official pricing component returned
  by the actual provider is zero; any uncertainty disables the route.
- Write price: a Hermes-created worktree may contain local unpushed commits so
  Hermes 0.20.0 preserves the result; push remains forbidden.
