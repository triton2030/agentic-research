interface:
  display_name: "1index"
  short_description: "Use when preserving a confirmed source route after costly search, an owner pointer, or separating task-specific navigation from agent instructions."
  default_prompt: "Используй $1index, чтобы сохранить подтверждённый маршрут к источникам после поиска, указания владельца или при отделении навигации от агентных инструкций."

policy:
  allow_implicit_invocation: true
