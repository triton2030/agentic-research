interface:
  display_name: "1-Создание инструкций"
  short_description: "Use when project agent instruction files must be written or rewritten. Not a how-to-work guide for a zone."
  default_prompt: "Use $1instruction-authoring for the requested agent-facing project instructions."

policy:
  allow_implicit_invocation: true
