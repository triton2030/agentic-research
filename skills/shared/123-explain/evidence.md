# 123-explain — evidence

## Проверено

- «Да» владельца на точный полный текст — 2026-08-21, «да пиши»
  (`_ops/chat-recall/2026-08-21-005222-claude-8e9b40bf.md`, запись selection).
- Двухлинзовый независимый аудит черновика в отдельных окнах (лишнее /
  потерянное+выдуманное); находки предъявлены владельцу до «да», след — в
  `origin.md`.
- Routing-falsifier неприменим: `disable-model-invocation: true`, подъём
  только ручной — голая фраза и near-miss скил поднять не могут по построению.
- Структура: проекции отрендерены `sync_simple_projections.py --check`
  (tracked + installed, оба runtime).

## Дельта шагов 6–7 (2026-08-21)

- «Да» владельца на дельту и снятие субагентного аудита его словом «Да можно
  уже без субагентов» (тот же holder chat-recall). Проверка дельты — одна
  самопроверка главного окна: швы с шагом 2 и Границей просмотрены, конфликт
  не найден.

## Формулировочный рефактор 2026-08-21

- Граница владельца и точное «да» на полный v2:
  `_ops/chat-recall/2026-08-21-151338-codex-01a023cd.md`.
- Независимый fresh-window audit первого кандидата нашёл семь групп
  семантических регрессий; кандидат получил `REVISE` и не записывался. V2
  восстановил исходные смыслы по `origin.md` и полному holder-у
  `_ops/chat-recall/2026-08-21-005222-claude-8e9b40bf.md`.
- `sync_simple_projections.py 123-explain --check` подтвердил совпадение
  portable owner, tracked Codex/Claude и обеих installed-проекций. SHA-256
  всех пяти `SKILL.md`: `4d70069b5d47d475d1df4106ede932c081c5284d28339069ebfa032014a8a362`.
- `git diff --check` по трём tracked `SKILL.md` завершился без вывода.
- Fresh read-only вызов установленного v2 в отдельном окне оставил все семь
  следов: источники, сделанное, ментальную модель и сюрпризы, дефолт,
  нерешённое, три нумерованных варианта с одним выбором, пользу и ошибки.
  Ответ использовал карточки и продолжил работу read-only. Это доказывает
  возможность требуемого поведения на одном реальном случае, не стабильное
  улучшение.

## Правка честности влияний (2026-08-22)

- Повод: живой инцидент — агент под /123-explain прочитал GOAL.md только в
  конце работы и в отчёте заявил раннее чтение и влияние; слова владельца и
  его «да» на точный текст — holder
  `_ops/chat-recall/2026-08-22-073058-claude-cefd6997.md`.
- Одно независимое аудит-окно (короткий путь: правка внутри прежней работы)
  нашло противоречие «держал в контексте» vs «не читал — провал», отсутствие
  исхода «в контексте, следа нет» и двусмысленность «не засчитывай»; всё
  разрешено до «да» владельца.
- `sync_simple_projections.py 123-explain --write --install` + `--check`:
  пять копий — portable owner, tracked Codex/Claude, обе installed — SHA-256
  `b04356da9d88ab68bbc3c54efd72616a7cb56e166d28b0bd50abaedf5a7d1f09`.
- Поведенческий прогон нового шага 1 не выполнен; falsifier — следующий живой
  вызов /123-explain владельцем (критерий приёмки рамки).

## Ограничения evidence

- Matched old-vs-new comparator с одинаковыми model, settings и context не
  выполнен; claim «вероятность соблюдения не снизилась» остаётся непроверенным.
- `quick_validate.py` из системного `skill-creator` отвергает существующий
  frontmatter `disable-model-invocation`; ключ присутствовал в baseline и
  сохраняет утверждённый manual-only механизм. Несовместимость валидатора
  записана отдельно в `_ops/findings/2026-08-21-153146-50459-9974.md` и не
  исправлялась внутри wording-only scope.

## 2026-08-23 — стиль и удовольствие

Текст версии 3 утверждён владельцем: «да так и надо, результат этого скила
максимально понятное объяснение которое я бы читал с большим удовольствием».
Три итерации независимого аудита (architecture-critic, отдельное окно, полное
чтение пакетов 1skill-shaping и 1instruction-shaping): findings 1–7, A–F,
V1–V3 закрыты в тексте либо записаны в cut.md. Статус свойства «легко и
приятно читать» — candidate: у правил стиля нет синтетического falsifier,
доказательство — живой вызов /123-explain владельцем.
