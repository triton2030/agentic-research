# Authoring modes — 112-learning-mode — 2026-08-30

## `skill-short-description`

Exact description: `Use only when explicitly invoked.`

- Use: «Используй `$112-learning-mode` для этой текущей работы».
- Skip: «Объясни, что сейчас происходит в этой работе».
- Near-miss: «Используй `$123-explain` для статуса и причин решений».

Description содержит только English trigger; функция находится в body.

## `behavior-protocol`

Буквальные owner-механизмы:

> «если он пишет в чат, какие методики и почему он выполнял, тут он не сможет
> отвертеться, чтобы не исполнять эти методики»

> «этот скил не должен прерывать текущую работу и после его вызова надо
> продолжать работу»

Источники: `_ops/chat-recall/2026-08-29-203900-codex-01a04e29.md:15` и
`_ops/chat-recall/2026-08-23-143751-claude-64e02dcb.md:34`.

TWI-разрыв находится между подготовкой и действием: чистый агент может сначала
выбрать реактивный ход, затем подобрать метод. Текущий package закрывает его
различающим evidence до действия и связывает первый проверенный ход с
немедленным продолжением исходной работы.

## `reference-files`

`../portable/references/activation.md` — самостоятельная содержательная стадия.

- Вход: явный вызов режима.
- Выход: честный урок о текущей работе и названный следующий ход.
- Сессионный gate применяется только после выхода activation-stage.
- Reference имеет локальную `## Цель`, не читает другие references и не
  владеет session contract.

One-file clean-room topology отклонена preservation-check: она потеряла
немедленный activation lesson; его возврат снова создаёт самостоятельный режим.

## `agent-defaults`

| Добавка | Дефолт → механизм → решение → вред → цена строгости |
|---|---|
| Следующее узкое место и верхняя цель | Локальная проблема выглядит правильной задачей → стратегический checkpoint → продолжать ли её сейчас → аккуратная реакция выдаётся за стратегию → один checkpoint только на существенной развилке. |
| Жизнеспособная реактивная альтернатива и различающий evidence | Метод легко подобрать после решения → контраст до действия → сохранить или изменить ход → post-hoc рационализация → нельзя сначала действовать, затем объяснять. |
| Наблюдаемый результат против прогноза | Объяснение выглядит достаточным evidence → result comparison → закрыт ли причинный цикл → красивый рассказ без проверки → отсутствие результата остаётся честно `не проверено`. |
| Один полный activation lesson | Повтор обучающего блока выглядит полезным → stage boundary → повторять ли церемонию → режим тормозит работу → без нового вызова доступен только короткий след. |
| Точная цитата либо помеченный пересказ | Название метода допускает удобную выдуманную формулировку → attribution gate → можно ли предъявить фразу как книжную → ложное доказательство → требуется короткая честная атрибуция. |

Все пять цепочек меняют отдельное решение и имеют наблюдаемый failure mode;
новых добавок поверх текущего package не принято.
