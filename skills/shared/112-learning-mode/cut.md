# Cut — 112-learning-mode

## 2026-08-23 — черновик v1 → финал (две линзы)

- «Корню следуем ещё точнее» + пересказ корневой обязанности «имя метода и
  почему» — сняты: второй владелец всегда-истинного; осталась дельта
  (отвергнутая альтернатива).
- «Ритуальная цитата без применения — провал режима» — снята: вердикт без
  операции, дубль «Готово когда».
- «Сначала погрузись в контекст» — заменено наблюдаемым стоп-условием
  «назови поимённо, на чём строится блок».
- «Блок строится на реальных ходах, не на пересказе книг» — поглощено
  стрелочными формами разделов и гейтом.
- Хвост description «и продолжай прерванную работу в том же ходе» — сохранён
  вопреки cut-тесту линзы 1: несёт различие с дефолтом остановки; принятый
  риск.
- Число «четырёх» книг — снято: значение принадлежит корню и уже менялось
  (Кови → Далио в тот же день).
- Собственная редакция стиля — заменена указателем на владельца формы
  /123-explain с одной названной дельтой (имена и цитаты называются).
- Принятые риски по слову владельца: «отныне» = до конца сессии (граница
  названа в теле); цитаты по памяти остаются с пометкой и правилом адреса
  для дословных.
- Находка обеих линз «firecrawl-developer-index не установлен» — отклонена
  проверкой: пакет на месте в ~/.claude и ~/.codex.

## 2026-08-23 — снятие конфликтов со 123-explain

- Абзац формы получил пометку владельца и разрешающую строку про имена
  внутри объясняющего блока 123; «только здесь» уточнено до «только в блоке
  112» (второе прочтение — «в этом файле»).
- Коллизия двух «продолжай по X» при вызове обоих скилов в одном ходе —
  разрешена существующими строками (явное объявление изменившегося хода в
  112; «Коррекция Рамки» в 123); отдельная строка победителя не заводилась —
  принятое допущение: блок 112 производится в момент вызова, не каждый ход.
