# Проверка первой версии

Проверено 19 сентября 2026. Это границы evidence версии, не гарантия будущих
запусков. Подробные локальные материалы —
`_workspace/orchestration/2026-09-19-maxrevie/` в мастерской.

- Независимые проверки смысла и необходимости: Opus 5 High и Luna Max;
  существенные замечания исправлены. Неписавший auditor проверил применение,
  прямые артефакты мини-прогона и доставку.
- `python3 skills/shared/1-max-review/tests/test_coverage.py`: 14 тестов.
  Проверяются пропуски, unknown, изменённый вход, старый plan_hash,
  некорректный отчёт, пустой аудит и независимые повторные проверки.
- Учебный audit-only запуск: 8 входов, 15 требований, 3 пакета, 25 результатов;
  18 pass, 6 fail, 1 unknown, 0 отсутствующих ответов. Четыре разных дефекта.
  Хеши и состав семи исходных файлов не изменились. Статус incomplete сохранён.
- Независимая проверка списка выявила неточное назначение связей. При приёмке
  исправлены адреса в отчёте с сохранением оригинала и карты замен; в сводку
  добавлен пропущенный режим delegated. Сам счётчик эти ошибки не обнаружил.
- Обычный одиночный review нашёл те же четыре дефекта. Рост вероятности
  обнаружения ошибок не установлен; сравнение единичное, без resampling.
- Во время мини-прогона менялся протокол: P03 уже получил отдельный короткий
  reviewer.md; P01/P02 — прежний контракт. Это проверка частей и общего
  исполнения, не прогон одной неизменной финальной версии.
- Codex и Claude установлены; наборы и байты совпадают с portable+platform:
  соответственно 9 и 8 файлов, оба quick_validate успешны.

Не проверены: сотни LLM-запусков, Claude Sonnet 5 Medium, живая БД и полный
цикл repair. Requested Luna Max известна, resolved model среда не раскрыла.
Отдельная координация зависит от возможностей хоста; полного отсутствия
унаследованных инструкций скилл не обещает.

Ориентир 5–7 — инженерная настройка. [CSE](https://arxiv.org/html/2608.12426v1)
измеряет композицию синтетических ограничений вывода, а не оптимальный размер
пакета ревью. Изоляция контекста и адресуемые артефакты согласуются с
[подходом Anthropic](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents);
эффективность конкретно 1-max-review эти источники не доказывают.

## Уточнение имени и цели — 19 сентября 2026

Пакет переименован в `1-max-review`. Контекст и Цель теперь прямо называют
поправку к привычному ревью: больше проверяющих с простыми заданиями вместо
нескольких с большими сложными заданиями. Протокол и исполняемая логика
coverage.py сохранены; равенство AST без вводного docstring проверено.
Обе глобальные установки и tracked-проекции совпадают с владельцем; прежних
пакетов `maxrevie` в runtime-каталогах нет, внутренние ссылки разрешаются,
оба quick_validate успешны. Смысл, сохранность требований и применение
проверены тремя независимыми окнами без существенных находок; локальные
результаты — `rename/reviews.md` указанной выше истории. Синтетическая проба
распределения дала 12 небольших назначений для 35 требований и двух связей
при трёх слотах. Предметные проверки в этой пробе не запускались.
Прежний пилот не выдаётся за новый прогон этой редакции.

## Дополнение о смысловой проверке прозы

В Контекст и существующий reference распределения добавлены незаметность
смысловых потерь, оценка реального чтения и работы до назначения, покрытие
каждого предложения связными фрагментами. Контракт проверяющего уточняет
сопоставление смысла с источниками. Код helper не менялся.

Три независимых агента с явно выбранными Sol Medium не нашли материальных
дефектов этой дельты. Один выполнил пробу на четырёх предложениях: распознал
изменённое значение по умолчанию, пропущенного получателя и потерянное
условие повторного подтверждения; неизменённое предложение подтвердил.
Это локальная проверка сохранности смысла, не замер общей надёжности.
Результаты и ограничение пробы — `prose/reviews.md` локальной истории.
Обе установки синхронизированы и валидны, внутренние ссылки разрешаются.

## Выбор моделей в теле

Таблица моделей и запрет скрытой замены перенесены из runtime references
в SKILL.md до делегирования. Три Sol Medium проверки не нашли потерь;
отдельная проба по одному телу правильно восстановила Luna/max,
Sonnet 5/medium, действия при недоступности и модель координатора.
Проба подтверждает доступность выбора без references, не исполнение моделей.
Доставка в обе среды сверена. История: `body-models/cut-and-check.md`.


## Общий CLI — 19 сентября 2026

Добавлена команда `max-review` поверх установленного `codex exec`. Codex и
Claude передают ей пакеты; листовые проверки запрашивают `gpt-5.6-luna`
с усилием `max` через существующий ChatGPT-вход. Это текущий маршрут вместо
прежнего Sonnet в Claude. Очередь хранит отдельные попытки, ограничивает
параллельность и продолжает незавершённое без повторения успешных заданий.

- 30 тестов проходят: 16 для CLI и 14 для учёта покрытия. Проверены ошибки,
  таймаут, остановка, продолжение, JSON-вывод и ограничение параллельности.
- Независимая приёмка Sol Medium: 5 из 5 условий. Найденное принятие явно
  другой resolved model исправлено; adversarial fake и регрессионный тест
  подтверждают отказ при расхождении с Luna.
- Установленный launcher работает из чужой рабочей папки. Реальный одиночный
  запуск прочитал SKILL.md и вернул `1-max-review`; отчёт и события сохранены.
  Доступ к серверу и чтение файла подтверждены, resolved model CLI не раскрыл.
- Read-only sandbox задаётся дочернему CLI; пользовательский config отключён,
  API-ключи не передаются. Применимые инструкции остаются: даже короткий
  smoke получил около 17 тысяч входных токенов. Изоляция не означает пустой
  контекст или нулевую стоимость запуска.

Локальные свидетельства: `cli/acceptance.md`, `cli/smoke/` и
`cli/wrapper-smoke/` в указанной выше сессии. Результат параллельного прогона записан ниже.


## Реальный прогон одного файла

Выбран `portable/SKILL.md`, SHA-256
`949aea6f6c9ae38df75a1ae68366dc1bff56efc15cbaf90b9e5bf6418aa14523`.
Независимая сверка инвентаря привела к 89 единицам в 15 пакетах; реально
одновременно работали 5 CLI-процессов. Все 15 вернули отчёты: 3 первых
попытки превысили 600 секунд, одна повторная волна их завершила. Всего
18 попыток. 65 входов были свежими при приёмке и сохранены снимками.

Механический итог исходных ответов: 71 pass, 14 fail, 4 unknown, пропусков
и ошибок схемы нет. Это не готовый вердикт: проверка по полным источникам
отклонила 15 из 18 отдельных замечаний. Среди замечаний к выбранному файлу
подтверждённых дефектов не осталось. Два подтверждённых дефекта относились
к свидетельствам smoke и подсчёту объёма самого аудита. Узкие назначения
создали ложные выводы об отсутствии правил и четыре unknown; полные
источники разрешили эти unknown. Исходные ответы не переписаны в PASS.
Аудит не объявлен clean: он сохранил дефекты подготовки и evidence.

По 15 доступным usage records сумма input 8 410 595, cached input 7 387 904,
output 287 384, reasoning 222 189. Это накопленные счётчики CLI, не размер
одного контекста, не уникальные токены и не полная стоимость: три timeout
attempts не вернули usage. Прогон показывает исполнимость очереди, но также
высокие накладные расходы и ложные срабатывания. Рост качества не доказан.

Сводка и разбор: `cli/audit/summary.md`, `cli/audit/adjudication.md`,
снимки: `cli/audit/evidence-snapshot/`. Это локальные материалы, а не часть
установленного скилла. Сотня параллельных процессов и запуск оболочки из
реального Claude-сеанса не проверялись.

## Уроки после прогона

В существующих checks/ledger references уточнены границы вывода об отсутствии,
отдельные outcomes связанных требований, скрытое чтение всех briefs при
проверке плана, фиксация prompt bytes и растущих источников. Согласована
передача финального JSON через CLI. Тело и модельный выбор не менялись.
Эта дельта проверена отдельно; полный Luna-прогон к новым references
не переносится. Адресная приёмка и модельная проба сценария —
`cli/lessons/`; они не измеряют статистическую надёжность.


## Редакторская проверка Opus Max

Opus 5 с запрошенным effort max прочитал тело, шесть общих references и
оба runtime-маршрута. По его замечаниям ограничение вывода об отсутствии
доставлено самому leaf, контракт reviewer назван единообразно и передаётся
полностью, фиксированная модель CLI отделена от выбора координатора.
Уточнены момент repair, граница машинного clean и каталог материалов.

Три Sol Medium окна проверили адресную дельту. Проба с reviewer.md дала
unknown при неизвестном охвате, fail при пустом обязательном месте и fail
при полностью проверенных допустимых местах. Замечания о полном тексте
контракта и владельце полноты охвата исправлены; повторная узкая сверка
пройдена. Код не менялся, прежние 30 тестов не выдаются за новый запуск.
История и независимая стратегическая оценка: `opus-text-review/` в сессии.
Стратегические предложения не внедрены этой редакцией.
