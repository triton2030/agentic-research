{
  "type": "object",
  "properties": {
    "packet": {"type": "string"},
    "status": {"type": "string", "enum": ["clean", "findings", "unknown"]},
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "rule": {"type": "string"},
          "at": {"type": "string"},
          "defect": {"type": "string"}
        },
        "required": ["rule", "at", "defect"],
        "additionalProperties": false
      }
    },
    "gaps": {
      "type": "array",
      "items": {"type": "string", "minLength": 1}
    }
  },
  "required": ["packet", "status", "findings", "gaps"],
  "additionalProperties": false
}
