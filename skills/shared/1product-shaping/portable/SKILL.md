---
name: 1product-shaping
description: >
  Вызывается перед работой, строящей или заметно меняющей продукт; когда
  владелец просит сверить понимание; и когда он просто описывает продукт —
  как работает, для кого, что важнее чего, — даже не заказывая работу.
  Ловится верхнеуровневая правда, влияющая на многое; мелкий факт — не сюда,
  его пишет `1chat-recall`. Также при повторных эскалациях, конфликте верных
  принципов, артефактах врозь — это дыра в рамке; просьба прибраться в
  парах — дубли, конфликты, дрейф от формата — режим уборки. Обсуждаемая
  правка функции или документа противоречит действующему принципу — стоп:
  правка отклонена либо принцип переписан сверкой. Без сверки развилки
  решает статистика, и работа переделывается. Применение готовой пары —
  `1use-principles`; сама цель — `1goal`.
---

# Продуктовая правда

Владелец не видит разрыва между своей головой и твоим пониманием — по
определению. Ты не видишь развилок — правдоподобной выглядит одна трактовка.
Сверка переносит работу туда, где каждый силён: ты читаешь всё,
пересказываешь и признаёшься; владелец коротко правит и подписывает. Его
ответы становятся принципами, по которым развилки решаются без него.

## Цель

На руках — пара `Product Principles` + `Product Frame` своего scope,
привязанная к цели проекта: владелец подписал сцену, чистовики кристаллизованы
и чисты, обоснование с его дословными словами — в журнале, следующая сессия
решает закрытые развилки без него. Мера — длина автономного участка до первой эскалации или первого дрейфа.

Не входит: применение пары в работе (`1use-principles`) · сама цель (`1goal`)
· планы (`1planning`) · спеки и тесты — отсюда их критерии, сами они живут в
репо · структура папок и интеграция со скилами — зона агентной системы
проекта · решение за владельца.

## Критерии успеха

- До первого продуктового суждения прочитаны целиком все Frame + Principles
  продукта и `_ops/GOAL.md`; след — в сверке названы применённые и отклонённые
  пары. Заголовки, snippets и память чтением не считаются.
- Владелец узнал замысел в пересказе и подписал сцену; confirmation map
  закрыт: каждый claim — подтверждён · исправлен · unresolved. Молчание не
  подпись.
- Каждый заданный вопрос назвал класс решений, который его ответ схлопывает.
- Каждый дефолт предъявлен до работы; необратимая развилка, не разрешённая
  живыми документами, не задефолчена — запрошено решение или заглушка;
  кроме ветки, выводимой из предъявляемого корня, — она входит в показ
  корня (interview, тест заслуженности).
- Привязка Frame к цели несёт запрет: «служит X → из-за цели не делаем Y».
  Без названного запрета привязка не засчитана.
- Конфликт просьбы или пары с целью показан с двумя выходами: править пару
  или звать `1goal` менять цель — цель тоже эволюционирует.
- Прогон: чистая сессия по одной только паре решает уже закрытую развилку —
  выбранную без словарного пересечения с текстом пары — без вопроса владельцу.

## Инварианты

- Дословные слова владельца живут в журнале и recall; чистовик —
  кристаллизация, каждое содержательное изменение подписано владельцем.
- Строка чистовика без опоры на слова или «да» владельца не пишется:
  неполный чистовик честнее правдоподобного; опору несёт запись журнала, не
  пометка в чистовике.
- Тайбрейкер порождается только решением владельца (или явно разрешённым
  ограниченным экспериментом с границей в тексте правила) и обязан решать
  конфликты: назови развилку, которую он решает, и развилку, которую нет.
  Второй нет — это лозунг, не правило.
- Смена ответа — переписывание чистовика через сверку и запись журнала;
  частичных вытеснений в чистовике не существует, журнал задним числом не
  правится.
- Конфликт двух верных принципов, требующий жертвы (жертва = ход нарушает
  наблюдаемое требование одного из них), — материал минимальной сверки сразу,
  не после «нескольких эскалаций»: такой ответ переиспользуем по определению.
- Пара создаётся только для scope с возвратом в нескольких сессиях; одна
  обратимая развилка пары не заслуживает.
- Ни одной записи в файл до «да» владельца.

## Дельта

Пересказывать и спрашивать модель умеет. Она не умеет:

- породить вторую трактовку: правдоподобной выглядит одна, и развилка не
  рождается вовсе;
- удержаться от статистического разрешения развилки — типовое выглядит как
  ответ владельца;
- показать разумный дефолт заранее: разумность и есть форма, в которой
  приходит дрейф;
- свернуть десять мелких вопросов к одному корню и задать его на языке
  пользователя;
- сохранить добычу сверки: ответ сглаживается в правило без области
  действия, и закрытое эскалируется снова.

## Известные сбои

`когда → сбой → цена → куда`

- пересказ собран → отрицания утверждений не прочитаны → развилку решила
  статистика → [interview](references/interview.md)
- просьба владельца содержит способ → закреплена фича вместо цели → строим
  не тот продукт → [interview](references/interview.md)
- вопросы готовы → дежурная анкета или вопрос по одному случаю: ответ не
  схлопывает класс → внимание владельца сожжено →
  [interview](references/interview.md)
- ответы получены → сглажены в правило без области действия → закрытое
  эскалируется снова → [pair-format](references/pair-format.md)
- Frame собирается → правдоподобная строка без provenance → дрейф с печатью
  владельца → [pair-format](references/pair-format.md)
- привязка к цели → лозунг без запрета → ритуал вместо ограничения →
  [pair-format](references/pair-format.md)
- деталь нижнего слоя записана принципом → вторая правда → двойное чтение и
  расхождение → [pair-format](references/pair-format.md)
- кристаллизация без «да» → воля владельца сглажена агентской прозой → дрейф
  с печатью владельца → [pair-format](references/pair-format.md)
- журнал правится задним числом → история обоснований потеряна → закрытое
  оспаривается заново → [pair-format](references/pair-format.md)
- первая полная сверка → жанр анкеты вместо метода → час владельца впустую →
  [full-walkthrough](references/full-walkthrough.md) — вымышлен: бери
  операции и порядок, не факты
- нужен второй пример механизма → изобретение с нуля →
  [examples](references/examples.md)

## Механика

Жёстких перехода три:

1. **Чтение до суждения.** Найди project-local owner этих форм (дефолт —
   `_ops/product-frames/*`); все Frame + Principles продукта и `_ops/GOAL.md`
   прочитаны целиком до пересказа, дефолтов и вопросов: принцип соседнего
   scope меняет границу задачи. Антипример: сформулировать правдоподобную
   сцену и открыть пару лишь для подтверждения — файлы прочитаны, рамка уже
   выбрана жанром. Существующая пара уже разрешает вопрос — форма «только
   чтение»: применяй и работай, владельца не дёргай.
2. **Дефолты до формы.** Разбор опасных дефолтов и переворот пересказа
   предъявлены вместе со сценой, не припомнены после: припомненный разбор
   защищает уже выбранное.
3. **«Да» до записи.**

Шов с `1document-system`: очередная пара в уже принятом root и вся
кристаллизация — этот скил. Новый root пар, их перенос или смена формата
семейства — стоп, ход `1document-system`; он решает только admission и
home. Read-only карта или projection поверх пар — не вмешательство.

Порядок по умолчанию: форма (полная · минимальная · только чтение · уборка;
минимальная — одна сцена-пересказ на подпись, когда Frame после чтения
отсутствует, а неизвестность способна изменить результат, обещание клиенту,
архитектуру или заметный объём работы) → поиск разрывов; прежние слова владельца по теме
развилки — точечным запросом `1chat-recall` через его скрипт, не общим
поиском: вопрос, уже закрытый его прежними словами, не задаётся, а цитата
идёт в `P-*` как источник → акт 1: сцена с выводимой проверкой · понятно ·
двусмысленно · опасные развилки с дефолтами → акт 2: вопросы с тестом
заслуженности, потолок десять, + аппетит (бюджет · жертвы · событие
пересмотра) → одна форма, один заход → confirmation map → запись: кристаллизация по
pair-format — переписанные чистовики (принципы и Frame) целиком на «да»
владельца, после «да» чистовики + запись журнала одним ходом
→ карта устаревшего: «<артефакт> противоречит <P-*/сцене> → <что сделать>»;
обратимое в разрешённой тебе зоне чини сам → прогон.

Форма «уборка» — по слову владельца или когда кристаллизация задела несколько
пар: ничего нового не спрашивается; все чистовики перечитываются против
формата записи, друг друга и цели — дубли, конфликты пар, разрыв привязки
пары к цели, детали чужого слоя, материализованные пустые слоты, отсутствие
имени или старшинства. Редакционное чинится по исключению pair-format —
слияние без смены смысла, с записью в журнал; смысловой конфликт и дрейф с
печатью владельца — минимальной сверкой. Выход — карта
«находка → ремонт | сверка» и прогон.

## Завершение

Владелец подписал сцену; пара записана и привязана к цели; карта устаревшего
выдана; unresolved-claims названы списком — молчание не подпись.

Сдача: пара + какие развилки теперь закрыты без владельца + чем проверено +
что осталось gap. Не вошедшее — строкой с причиной в сдаче: с маршрутом к существующему
владельцу либо пометкой «owner-а нет — gap»; отдельный файл не создаётся.
