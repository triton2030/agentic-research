# Cut — 1planning

## 2026-08-26 — раскройка на тройку (1planning · 1plan-map · 1plan-task)

Решение владельца — `_ops/chat-recall/raw/2026-08-26-220614-claude-4ee6bbef.md`;
полная построчная карта вердиктов — `skills/1planning/split-map-2026-08-26.md`;
снимок пакета до раскройки — `skills/1planning/tracked-owner-2026-08-26/`.
Снятое (кандидаты, слово владельца окончательное):

- **Delta — 8 слабостей модели**: три пережили как строки протокола
  (хронология-раскрой, zombie-patch, лимит 7 подзадач против хвоста списка);
  остальные пять сняты — компенсируются изоляцией задач и fresh-reader check.
- **Known failures — таблица 14 маршрутов** `when → failure → cost → route`:
  роутинг-леса старого монолита; каждое живое правило осталось у своего
  владельца, сама таблица снята.
- **modes.md: полная типология ready/blocked/fog/resolved** — осталась одна
  строка «fog не становится подзадачами»; **contrast cases** (premature
  execution, planning theater, easy frontier) — иллюстрации, выводимы из probe
  и правила Next.
- **decompose.md: адрес каталога скосов** архива 1break-down
  (`skills/1break-down/tracked-claude-2026-08-08/references/decomposition-failures.md`)
  — из тела снят, адрес сохранён здесь.
- **delegation.md целиком**: волновая механика уже жила у `1orchestration`
  (brief, write ownership, приём возвратов, wait/probe/repair); план-сторона
  (запрет записи план-файлов воркерами, `_evidence/**`, инвалидация, «верь
  git») перешла в `1plan-task`. Снята строка `волна: чтение · запись ·
  проверка` в отчёте подзадачи и порядок чтения recovery.
- «Названные гейты сильнее выводов из книг», «полный план + перепланирование
  дешевле пошагового контроля», Covey/commander's intent как именованные
  зонтики — выводимы из новых общих строк.
- Пост-аудит пары субагентов (сократитель + проверщик): из тела 1planning
  сняты вступительная маршрутка семьи (дубль Протокола 5), «chat-only → no
  plan file» (выводимо из «plan files only after the yes»), вторая
  формулировка unknown≠premise, «a name without a point is not application»
  (живёт в глобальной инструкции и каноне); «never chronology» уточнено
  «настоящая зависимая цепочка — ось зависимостей, назови её так» против
  релейблинга. Взамен добавлены нефейкуемые следы: цитата ограничивающих
  строк инструкций (или имена прочитанных файлов), probe режима письменно в
  чат, имена принципов как след `1use-principles`, «да» владельца — именно
  на показанную декомпозицию.

## 2026-08-25 — post-audit correction

- Снято `prove it in chat`: имя методики и самоотчёт не являются proof;
  сохранён адресный trace `пункт методики → элемент плана`.
- Не добавлен общий lifecycle в оба скила: planning сохраняет admission-гейт,
  planning-specific transfer/archive живёт у `1interview-tool`.
- Не возвращены question-note, questions-folder или отдельный checklist:
  task хранит только адрес интервью, а Wayfinding — только agent-resolvable
  вопросы.

## 2026-08-25 — вопросы переданы `1interview-tool`

- Удалён `portable/references/questions.md`: отдельная question-note и её
  lifecycle создавали второго владельца формы рядом с `1interview-tool`.
- Из карты удалены questions-folder, её checker-обязанность и view
  «Открытые вопросы». Вопросы больше не являются частью map/dashboard
  topology.
- В `1planning` сохранён один гейт: оставшийся после derivation вопрос идёт в
  `1interview-tool` и останавливает только затронутую ветку. Форма, хранение,
  перенос ответа и архив принадлежат `1interview-tool` и папке interviews.
- Question-note frontmatter, deadline и автоматический default по истечении
  срока не перенесены: planning-вопрос живёт по readiness/archive lifecycle
  интервью; незатронутые ветки продолжаются.
- Повторы этого гейта сняты из Invariants, Known failures, decompose,
  delegation и modes; в contract/map остались только их собственные схемы —
  граница task-файла и обозначение остановленной ветки.
- Приём ответа через любой канал и отдельный question-note шаг записи в
  chat-recall не дублируются: ответ проходит существующий lifecycle
  `1interview-tool` — «проверь» → перенос к owner-у → архив; общий same-turn
  capture владельца остаётся обязанностью `1chat-recall`.

## 2026-08-24 — English v3.1: agent-reader reframing (recall agentic-research `_ops/chat-recall/raw/2026-08-24-181208-claude-13fd1279.md`)

- Вся инструкционная проза переведена на английский по слову владельца
  («полностью перевел на английский язык»). Токены схемы — frontmatter-ключи,
  заголовки разделов («Зачем»/«Принципы»/«Подзадачи»…), словарь статусов,
  имена файлов дашборда и YAML `.base` — оставлены литералами: их
  байт-в-байт проверяет прибор живой карты mavo-short2 (`check-map.py`) и
  читает владелец; в тело добавлен явный запрет их переводить
  («Schema tokens are literal»).
- Верх тела перестроен: план-файл — интерфейс агент→агент, скил —
  оркестрация последовательной автономной работы агентов («файлы
  планирования нужны не мне, они нужны агентам… автономные работы по
  порядку агентов»). «Дельта» переименована в перечень компенсируемых
  слабостей и связана с верхом.
- Предохранитель «имена — ярлыки, ничего сверх названного» заменён по слову
  владельца («многие инструкции можно схлопнуть, назвав книгу или
  методологию»): имя несёт «как», записанные гейты/формы/инварианты сильнее
  подразумеваемого книгой. Прежний риск (ярлык рождает вторую обязанность,
  снятую 2026-08-23) закрыт этой оговоркой.
- Cold-start проверка усилена до fresh-reader check свежим субагентом
  («файлы планирования должны проверяться свежими субагентами на предмет
  неправильного чтения»); добавлено требование назвать двусмысленные строки;
  fallback без subagent runtime — холодная перечитка. Дубль критерия — в
  Success criteria тела (помечен ссылкой на contract).
- Новые зонтики, сжавшие прозу: Miller's span (3–7), single source of truth,
  two-way doors, waterfall как имя анти-паттерна хронологии, requirements
  traceability у акта вывода. Прежние ярлыки (commander's intent, Кови,
  Shape Up, tracer bullet, just-in-time, riskiest first) сохранены.
- Не тронуто: абсолютный путь в decompose «Contested frontier» (известный
  residual).

## 2026-08-24 — v3.1 coherence-проход: методы как chat-evidence (та же recall-сессия)

- В тело добавлен абзац «plan by the masters» с observable-следом по слову
  владельца («чтобы он прям в чате называл, что он их действительно
  использовал при планировании, как доказательство»): обоснование в чате
  называет методы, реально сформировавшие план; страховка от декорации —
  «имя считается только вместе с изменённым решением» (P-005: пересказ ≠
  adherence). Состав обоснования в Mechanics дополнен «the methods that
  shaped the cut and the order» с пометкой «edit together».
- Порядок разделов: «Goal» поднят перед «Structure» (назначение → продукт →
  раскладка); прежний порядок вводил структуру раньше цели.
- Снята коллизия слова delta: в миссии «writes only the delta» заменено на
  «adds only what no book fixes», раздел «Delta — the weaknesses…» остался
  единственным носителем термина.
- Абзац-предохранитель про имена книг слит в метод-абзац (был отдельным
  хвостом абзаца про autonomy) — минус независимый пересказ.

## 2026-08-24 — v3.1 follow-up: агрессивный trigger и миссия (та же recall-сессия)

- Description заменён по слову владельца («вызываться при любом упоминании
  планировании или даже мысли о том, что делать дальше»): триггер расширен
  до выбора следующей задачи, проверки траектории и большой ошибки
  («прочитать планы — вдруг это не ошибка, а что-то, что мы оставили на
  потом»); перечисление прежних триггеров свёрнуто. Тот же триггер дописан
  в default_prompt Codex `openai.yaml` (карточка дополнена, не заменена).
- В верх тела добавлена миссия: взгляд на проект свысока — понять, что
  важно сейчас, что отложить, как разбить, и записать; «модели умные,
  пишем только дельту» — формы, гейты и инварианты, которых не фиксирует
  ни одна книга.
- В Invariants поднято непересечение: эпики — порядок работ и не
  пересекаются; задача не ворует работу у другой задачи — внутри эпика и
  между эпиками (владелец правила — map; в map уточнено «in its own epic
  or in any other»).

## 2026-08-23 — именующая вставка (Кови / commander's intent)

- Условный абзац про планы в глобальной инструкции с гейтом «Never re-plan
  without 1planning» — снят владельцем («Мы думаем… Нет давай тогда добавим
  4 книгу просто»); позже слот книги в глобальной строке занял Далио, Кови
  остался ярлыком здесь.
- Клауза «замысел сильнее шагов при ре-плане» — снята самопроверкой по
  паттерну аудита 1use-principles: новая обязанность под видом ярлыка, второе
  чтение «пропусти шаги, если замысел велит»; правила ре-плана остаются у
  references/modes.md.

## 2026-08-23 — рефактор связности (две линзы)

- «Закрыты все видимые вопросы» в гейте — заменено указателем на словарь
  блокировки questions.md: пересказ словаря в теле давал прочтение «ждём
  ответов на всё» против «мелкие пачкой без стопа».
- Поля `планов:`/`планы:` в теле — сняты как словарь конкретного проекта:
  их нет ни в шаблоне эпика, ни в приборе map.md.
- Маршрут пустого `рамки` («вызов 1product-shaping, не вопрос владельцу») —
  снят из тела: владелец маршрута map.md, у него уже третий исход
  «нет рамки — причина».
- Второе употребление «tracer bullet» в map.md — снято: имя закреплено за
  осевой нитью карты.
- Списки триггеров чтения task.md/context.md в теле и modes.md — заменены
  указателем на contract.md (раздел context.md); канонический список
  пополнен «при возврате в задачу» (жил только в теле).
- Папка вопросов добавлена в цикл modes.md — тело называло её копией гейта,
  которой в оригинале не было.
- Статистика «до пятой части вопросов…» в questions.md — снята как
  безысточниковая (тот же класс, что снимался 2026-08-18); правило срока
  получило носитель — поле `срок:` во frontmatter заметки.
- Две строки карты сбоев «эпик не перечитан» — слиты в одну.
- «Plan Mode → молчи» в Допуске сохранён вопреки линзе 2: закрывает Plan
  Mode, включённый после подъёма скила (аргумент линзы 1); description гасит
  только вызов.
- Residual, не тронуто: абсолютный локальный путь в decompose.md «Спорный
  фронтир» — внешняя зависимость portable-пакета.

## 2026-08-24 — дельта «Дашборд владельца»

- `columnSize` и `displayName` mavo-образца — косметика колонок, отдана
  инструкции проекта («Не входит» тела).
- Frontmatter самого `Дашборд.md` (`описание`, `обновлено`) — рукописные
  значения на чистой проекции; ни один контракт их не читает.
- Пункт красного прибора «нет файлов дашборда» — снят обеими линзами: не
  инвариант эпика/связи, ни один чекер его не мерил; обязанность несёт
  триггер достройки в «Дашборде владельца».
- Замена Codex `openai.yaml` целиком — ярлык «протухло» опровергнут
  построчно (4 из 5 клауз жили в v2); карточка дописана, не заменена.
- Широкий родительский `inFolder` mavo-образца — заменён `or` двух явных
  папок (Codex-аудит: скрытая зависимость от топологии mavo, посторонние
  заметки в выборке); mavo может оставить свой фильтр как проектную вьюху.

## 2026-08-24 — v3: задача одним файлом (recall mavo-short2 `_ops/chat-recall/2026-08-24-130300-claude-7756c5b8.md`)

- `status.md`, `context.md`, `modules/` и вехи — сняты владельцем («слишком
  разрослось на множество файлов, агенты множество файлов не читают»);
  статус и evidence — в тогл-отчётах подзадач, контекст — в «Зачем», брифы
  воркерам — эфемерные в run_dir, возвраты — `_evidence/` эпика.
- Лимит строк файла задачи (50) — снят владельцем («задачи могут быть без
  лиммитов, ведь у каждой подзадачи скрытые участки отчёта»); размер держит
  число подзадач 3–7.
- Жёсткий WIP-лимит — не вводился; вместо него предупреждение прибора о двух
  🔨 задачах («отличный знак что задачи по хроно были созданы не правильно…
  или конфликт»).
- Колонка-зеркало раздела «Принципы» в дашборде — снята владельцем («нет не
  надо»).
- Атрибуции IMP и SAFe — сняты линзой «лишнее»: правило сформулировано
  полностью, имя не сжимало; предохранитель «имена — ярлыки» возвращён.
- Протокол probe/repair зависшего воркера — свёрнут к владельцу
  `1orchestration`; в delegation осталась только дельта планирования.
- Строка сбоя «стык не назван» — поглощена правилом стыка contract.
- Дубль decompose «Очевидный один шаг … — пропуск» рядом с «К очевидным шагам
  церемонию не добавляй» — оставлен: разные роли (допуск/форма), находка
  линзы отклонена.
