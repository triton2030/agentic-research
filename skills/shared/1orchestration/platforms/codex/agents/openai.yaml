interface:
  display_name: "1-Оркестрация"
  short_description: "Coordinate independent subagent streams into one result"
  default_prompt: "Use $1orchestration to coordinate the requested subagent wave and return one integrated result."

policy:
  allow_implicit_invocation: true
