# Первый независимый аудит исполнения

Источник: сообщение auditor /root/tree_check_execution в текущей сессии; роль не могла писать файл, запись сделана родителем.

Итог auditor: incomplete. E01 unknown — --output-dir одновременно назначал cwd/-C; при evidence внутри другого репозитория события не доказывали отсутствия конфигурации предков. Рекомендация: отделить рабочий каталог от evidence и проверить.

E02–E18 pass: версия по hashes, Sol low в argv, полный скилл, JSON имён/типов, сборщик без чтения содержимого, симлинки без обхода, evidence вне базы, argv без shell, лимиты/ошибки без частичного успеха, контроль turn.completed и tool actions, live-run без инструментов, сохранённые входы/результат, неизменность старого тела/reference, ограничение names-only, общий CLI, независимый повтор 7 тестов.

Принято E01: финальная дельта создаёт отдельный TemporaryDirectory независимо от пути evidence. Tests-final.txt проверяет пустой cwd и отдельный output; новый holdout-run использует эту версию. Полное отсутствие базового контекста Codex не заявляется.
