# Адресная правка: порядок, который видно по именам

Маршрут: адресная правка по `1skill-creation/refactor.md`; владелец назвал
дополнение и утвердил текст (`mavo3/_ops/chat-recall/2026-09-18-175527-Claude-6ecac4b3.md`,
записи «Да, где порядок есть», «Согласен: текст добавлений в скил»,
«Утвердить и проверить»). Вход — `before/`, выход — `candidate/`.

## Сохранено

Весь прежний текст SKILL.md и справочника без изменений формулировок: предмет
и группировка, три отношения вложенности, один владелец правил, запрет
смешивать файлы и подпапки, критерий имени, пять условных наборов, скрипт
`check_tree.py`. Фраза «порядок работы и потребление результата сами по себе
не устанавливают родителя» сохранена дословно; добавление ниже её уточняет,
не отменяя.

## Добавлено

| Касание | Смысл | Основание |
|---|---|---|
| Контекст, первая фраза | владелец читает имена в порядке показа и ждёт понять, с чего начать | слова владельца 18.09: «если читать только имена файлов и не открывать файлы, а это именно то, как я использую и продвигаюсь по навигации» |
| Третий критерий приёмки | имена детей в порядке отображения дают пересказать содержимое, а где порядок чтения есть — и что читать раньше | наблюдение: каждый адрес `Путь дизайна` проходил критерий имени, а папка целиком не читалась; то же в шести ветках (`mavo3/_ops/findings/2026-09-18-181739-49393-5555.md`) |
| «Когда выбираешь родителя и связи», конец первого абзаца | порядок чтения соседей делают видимым в отображении, а не вложенностью, способом инструкции базы; где порядка нет, его не изобретают | точка неверного прочтения фразы о родителе; способ местный по правилу самого скила о карте базы |
| Справочник, «Порядок, который видно без открытия» | пример с цифрой, обзором `0` в том же виде, что и соседи, и папкой без порядка | защита от применения цифр везде и от файла-обзора рядом с подпапками |

## Не добавлено (протокол допуска)

«Процесс может быть предметом» — уже в Контексте дословно. «Путь через
предметы — документ» — выводится из одного владельца и «действие без
самостоятельного предмета остаётся частью ответа». Новый тип папки — отклонён
владельцем («слишком много всяких типов… надо более гибко»). Общее правило
о цифрах — способ принадлежит инструкции базы, скил независим от проекта.

## Незатронуто

`description`, триггеры, `check_tree.py` и его промпт (новый критерий попадает
в проверку через полный SKILL.md), Codex metadata в `platforms/codex/`.

## После проверки (волны 1 и 2)

Принятые исправления: условие «где порядок чтения есть» внутри критерия (intent-1, F2, trajectory-1); «Порядок чтения при этом…» (F3); обзор `0` «в том же виде, что и соседи» (intent-2, F1); последнее предложение примера снято как повтор (F4); «в отображении, а не вложенностью» (волна 2, необходимость). Отклонено: пятое касание — ссылка на пример из раздела имён (trajectory-3, низкая важность). Финальные байты — `final/`; карта приведена к ним 2026-09-18.

Состояние: `рефактор завершён` — проверка версии в `validation.md`, доставка по `install-approved.md`.
