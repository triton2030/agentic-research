# Проверка редакции 2026-09-18 «порядок, который видно по именам»

## Версия
Финальные байты — `final/` (SKILL.md md5 d652dbe9445c33dae6257170811a2abb, справочник a0ba026efd6cc4d8f3d97fb7372bb5d3); `check_tree.py` не менялся (bf4d5c67…). Установлено во владельца `skills/shared/1folder-tree/portable/`, tracked-проекции `skills/claude/`, `skills/codex/` и установленные `~/.claude/skills/`, `~/.codex/skills/` через `sync_simple_projections.py --write --install`; `--check` и md5 всех шести копий совпали.

## Пробы поведения
Вход каждой пробы — штатный `check_tree.py --dry-run`: имена дерева в JSON плюс полный SKILL.md как критерии, без содержимого и инструкции базы. Codex CLI на машине не установлен (`codex is not on PATH`), поэтому вход отдавался Claude Opus в изолированном субагенте с запретом читать что-либо кроме файла входа; изоляция удостоверена только поручением и одним обращением к файлу (`tool_uses: 1`), `events.jsonl` нет. Сырые ответы — `probes/raw/`, пометки автора — `probes/*.answer.md`.

| Проба | Дерево | Версия | Наблюдение |
|---|---|---|---|
| before-путь | `_docs/Путь дизайна` (алфавит поперёк процесса) | before | порядок назван одной строкой среди затруднений, к критерию не отнесён; обзор предложено вложить стадии внутрь |
| candidate-путь | то же | v1 | алфавит поперёк процесса назван на двух уровнях и отнесён к новому критерию; способ показа не изобретён, отнесён к инструкции базы |
| before-риски | `_docs/Бизнес/Риски` (порядка нет) | before | о порядке ни слова |
| candidate-риски | то же | v1 | охрана «не изобретают» сработала, цифры не предложены; критерий вынесен в расхождения по половине «что лежит» |
| digits-v2 | макет утверждённого дерева с цифрами `probes/mock-digits/` | v2 | цифры прочитаны как видимый порядок, не как «версии, даты»; принят ли способ в базе — помечено непроверенным; ложного срабатывания нет |
| риски-v2 | `_docs/Бизнес/Риски` | v2 | порядок не в расхождениях; «если порядка нет, отсутствие видимого порядка не недостаток»; цифры не предложены |

## Волна 1 (три роли, Opus, свежие окна)
`review-intent.md`, `review-instructions.md`, `review-trajectory.md`. Принято: условие «где порядок чтения есть» внутри критерия; «Порядок чтения при этом…»; обзор `0` «в том же виде, что и соседи»; снятие последнего предложения примера; проба дерева с цифрами; сырые ответы отдельно. Отклонено: пятое касание (ссылка на пример из раздела имён). Две роли судили до появления ответов проб (штатные запуски упали без codex).

## Волна 2 (та же тройка, только изменённые строки)
`review-intent-wave2.md`, `review-instructions-wave2.md`, `review-trajectory-wave2.md`. Возражений к тексту нет. Принято: «в отображении, а не вложенностью» (три слова, защита решения владельца против папки-процесса); карта `cut.md` приведена к финальным байтам; проба риски-v2. Замечание о производственной модели — см. границы.

## Границы
- Производственная модель двери `check_tree.py` (`gpt-5.6-sol low`) не проверена ни разу; входы сохранены дословно (`probes/*/prompt.txt`, `probes/*.prompt.txt`) для прогона, когда `codex` появится.
- По одному прогону на клетку: заявляется возможность показанного поведения, не частота соблюдения.
- Справочник ни в одной пробе не участвовал и в скрипт не попадает; узаконивание цифр в теле отсутствует, на Opus ложного срабатывания не было.
- Двери «куда положить файл» и «как назовём папку» в свежем окне не открывались.
- Установленные копии совпали побайтно; поведение установленного пакета подтверждается только пробами выше.
