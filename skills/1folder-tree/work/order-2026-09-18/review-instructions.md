# Роль «необходимость требований» (check-instructions), Opus, 2026-09-18

## Вердикт
Все четыре добавления несут дельту: до правки в скиле не было ни одного упоминания порядка отображения (`grep` по `before/`: единственное «порядок» — в запрете «порядок работы не устанавливает родителя»). Смысл этой фразы сохранён и усилен: новый абзац объясняет, куда девается отвергнутый сигнал. Граница выдержана: diff даёт ровно четыре куска, `check_tree.py` совпадает байт в байт, `before/` совпадает с установленным пакетом. Роль видела пробы пустыми (запуски `check_tree.py` упали без codex); ответы проб записаны позже в `probes/*.answer.md`.

## Находки
- **F1.** «Обзор папки получает `0`» допускает запрещённое смешение (`candidate/references/как надо и как не надо.md:114`). Основание: запрет владельца 18:11:38 и первый критерий. Правка: «…а обзор целого получает «0» в том же виде, что и соседи: файл среди файлов, подпапка среди подпапок».
- **F2.** У третьего критерия нет условия применимости «где порядок есть» (`candidate/SKILL.md:48–51`). Основание: решение владельца 18:25:09. Прогноз сбоя: `check_tree.py` подаёт SKILL.md как список критериев, проверяющая модель разбирает единицы по одной — замечание «порядок не виден» на папке независимых предметов. Правка: «…позволяют пересказать, что в ней лежит, а где порядок чтения есть — и что читать раньше».
- **F3.** «Порядок при этом не пропадает» цепляется к порядку работы из предыдущей фразы (`candidate/SKILL.md:100`). Правка: «Порядок чтения при этом не пропадает».
- **F4.** Последнее предложение примера — третье повторение «порядок не делает родителя» (`…как не надо.md:116–117`); пример не создаёт соблазна, от которого оно сторожит. Правка: снять.

## Проверено, находки нет
Контекст (невыводимое — ожидание владельца «с чего начать»); абзац о видимом порядке — самая сильная единица (без него критерий закрывали бы папкой-процессом, отклонённой владельцем, или собственным способом в общем скиле); способ у базы, не у скила; «процесс может быть предметом» уже в строке 18; приёмка по именам не подрывает «имена без чтения не доказывают полноту»; «дубли-линзы» верно не внесены (владелец: «надо подумать»).

## Возможная потеря вне скила
Решение «путь это документ-линза, новых папок-путей не делаем» (18:29:47) выводимо внутри скила; но новая формула «обзор целого предшествует частям» слегка усиливает тягу завести папку-обзор. Дом решения — инструкция базы mavo3 (`_docs/AGENTS.md`), проверить запись там при переносе. Прогноз.

## Решение автора
F1–F4 приняты; F1 и F4 объединены: строка про обзор получает уточнение вида, последнее предложение примера снято. Замечание о доме решения о линзах передано в задачу переноса mavo3.
