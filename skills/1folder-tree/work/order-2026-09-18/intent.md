# Адресная правка: порядок, который видно по именам

Заказ владельца 2026-09-18 (mavo3, `_ops/chat-recall/2026-09-18-175527-Claude-6ecac4b3.md`):
дерево читается по именам без открытия файлов, и по именам не чувствуется порядок;
новых типов папок не добавлять («надо более гибко… больше про порядок знаний»);
цифра в начале имени принята как способ показать порядок «где порядок есть»;
текст четырёх добавлений утверждён («Утвердить и проверить»).

Граница: адресная правка `skills/shared/1folder-tree/portable/`. Четыре касания:
Контекст (чтение имён в порядке показа), третий критерий приёмки (пересказ по
именам), абзац о видимом порядке соседей в «Когда выбираешь родителя и связи»,
раздел «Порядок, который видно без открытия» в справочнике. Остальное без изменений;
`check_tree.py` не меняется: он передаёт весь SKILL.md как критерии.

Не добавлено по протоколу допуска: «процесс может быть предметом» (уже в Контексте),
«путь через предметы — документ» (выводится из одного владельца и «действие без
предмета — часть ответа»), общее правило о цифрах (способ местный, у инструкции базы).

Наблюдение-основание: в `_docs` mavo3 в шести ветках вход ветки и первый шаг стоят
не первыми из-за алфавита (`_ops/findings/2026-09-18-181739-49393-5555.md`).

Состояние: `готов полный черновик` — кандидат в `candidate/`, снимок в `before/`.
