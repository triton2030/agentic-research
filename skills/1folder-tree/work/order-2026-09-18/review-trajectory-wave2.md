# Волна 2, роль «применение и результат» (check-trajectory), Opus, 2026-09-18 — предмет: изменённые строки candidate-v2 и проба digits-v2

## Вердикт
Правка адресная: `check_tree.py` во всех трёх версиях побайтно одинаков. Условие «где порядок чтения есть» теперь доезжает и до приёмки, и до двери скрипта (внутри критерия; скрипт вкладывает `SKILL.md` целиком — `probes/digits-v2/prompt.txt:164`). Проба digits-v2 закрывает находку 2 первой волны только на Opus и одним прогоном: цифры прочитаны как исполнение критерия, приёмка способа отнесена в непроверенное. Набор действующих требований у других дверей не вырос.

## Проверено механически
`probes/digits-v2/prompt.txt` и `tree.json` совпали с повторным `--dry-run`; `probes/digits-v2/skill.md` = `candidate-v2/SKILL.md`; `check_tree.py` одинаков в before/candidate/candidate-v2; четыре установленные копии равны `before`, симлинков нет. Оговорка о виде обзора согласуется с критерием смешения и допуском папки с одним файлом; макет это реализует.

## Находки
1. **Дверь проверена не своей моделью** (`run.json: model_requested gpt-5.6-sol`, ответ — Opus). В доставленном входе слова «цифр» нет, запрет «версии, даты» есть (`prompt.txt:249`): отсутствие ложного срабатывания — свойство прочтения модели, не текста. Правка: прогнать тот же вход на `gpt-5.6-sol low` до установки; текст трогать только при срабатывании. **Решение автора:** `codex` на машине не установлен; установка выполняется с этой границей, вход сохранён дословно (`probes/digits-v2/prompt.txt`) для прогона, когда `codex` появится; текст не трогается по правилу самой находки.
2. **Папка без порядка с v2 не прогонялась**; улучшение — прогноз. **Принято**: проба `probes/риски-v2` запущена, результат в validation.md.

## Остаётся непроверенным
`gpt-5.6-sol low` на обоих деревьях; по одному прогону на клетку; изоляция окна пробы удостоверена только пометкой автора (для проб Claude нет `events.jsonl`); двери «куда положить файл» и «как назовём папку» в свежем окне не открывались; доставка в четыре копии `SKILL.md` — проверяется при установке `--check`.
