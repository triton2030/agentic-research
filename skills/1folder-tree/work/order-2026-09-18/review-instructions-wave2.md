# Волна 2, роль «необходимость требований» (check-instructions), Opus, 2026-09-18 — предмет: изменённые строки candidate-v2

## Вердикт
F1–F4 реализованы минимально, без потери существенных условий владельца: запрет смешения усилен там, где автор сам ошибся в эскизе; условие «где порядок есть» встало внутрь критерия, где его читает `check_tree.py`; фраза о порядке работы осталась дословно. Повтора между критерием и абзацем о родителе нет: разные точки отказа. Объём совпадает с заявленным; `check_tree.py` побайтно равен `before/`.

## Находка (одна, низкая важность, прогноз)
Снятие F4 убрало единственное прямое «порядок ≠ родитель» в справочнике; тело отвергает как родителя порядок работы, а про порядок чтения не говорит; база может счесть вложенность способом показать порядок. Закрывается выводом из закрытого списка трёх отношений — снятие безопасно. Дешёвая страховка в теле: «…делают видимым в отображении, а не вложенностью, способом, который задаёт инструкция базы». **Принято** — три слова в уже изменённом абзаце, прямая защита решения владельца против папки-процесса; реализовано в `final/`.

## Непроверенное (названо ролью)
F2 не проверен пробой на папке без порядка (закрывается пробой `риски-v2`); основание F2 — прогноз, не наблюдение (в пробе v1 охрана сработала, в «Расхождения» попала половина «что лежит»); F1 живёт только в справочнике, который не участвует ни в одной пробе и не попадает в скрипт.

## Проверено, находки нет
F3 не рвёт мост к отвергнутому сигналу; F1 согласован с допуском папки с одним файлом; F4 — единственное снятие утверждённого текста, но снятое было пересказом сохранённого правила, а решение владельца «Утвердить и проверить» допускало исправление по проверке; безусловность Контекста оставлена намеренно — факт о читателе, не требование.
