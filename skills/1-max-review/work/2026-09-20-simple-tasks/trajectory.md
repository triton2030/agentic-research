# Проверка направления

Режим `named`, роль `trajectory-critic`. Вопрос: ведёт ли оставшаяся работа к упрощению Max Review, которого просит владелец, или её захватили необязательные детали? Назови следующий необходимый ход и границу завершения. Только исследование и отчёт, без правок, внешних действий и новых агентов.

Точные слова владельца: `_ops/chat-recall/2026-09-20-103907-Codex-01a0b9fe.md:15–17`. Он просит множество терминальных субагентов, каждому очень простое задание и короткий ответ. Исходная цель полного распределённого обзора сохранена; это редакция глобального скилла, не повторный аудит MAVO.

Объект: текущие семь Markdown-файлов в `skills/shared/1-max-review/portable/{SKILL.md,references/}` и `platforms/{codex,claude}/references/runtime.md`; версия записана в `draft-v1.json` рядом с этим пакетом. Рамки мастерской — `_ops/GOAL.md` и `_ops/product-frames/agentic-research*.md`.

Доступные свидетельства: `cut.md`, `state.md`, `probe-summary.md`, `probe/results.json`, `probe/run/attempts/*/0001/report.txt` рядом с пакетом. Четыре реальных запуска без schema завершились; один прочитал глобальное правило письма. Количественного доказательства роста надёжности нет. Старый аудит MAVO остаётся incomplete.

Предлагаемое продолжение, ещё не выполнено: короткие правки по трём независимым рецензиям; развести новый plain-text контракт и прежний JSON-контракт сохранённой команды prepare; проверить существующие тесты и доставить в Claude/Codex, затем точный коммит. Старый prepare сейчас ссылается на переписанный reviewer.md, хотя сам требует JSON. Приёмка редакции по местному скиллу включает три роли и не более двух волн; первая волна завершена. Отдельного улучшения транспорта, измерения масштабирования или повторного MAVO-review не запланировано.
