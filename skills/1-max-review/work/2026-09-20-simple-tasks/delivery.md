# Доставка

20 сентября 2026. `sync_simple_projections.py 1-max-review --write --install`
записал tracked и installed проекции Codex/Claude; отдельный `--check`
подтвердил совпадение с владельцем. Финальные байты owner — `final.json`.
quick_validate обеих установок: Skill is valid. Все локальные Markdown-ссылки
установок разрешаются. `git diff --check` живых пакетов не сообщил ошибок.
Дословные prompts пробы сохраняют пробел после номера пустой строки;
его не удаляли, чтобы не менять проверенные байты и их хеши.

Установки: `/Users/triton/.codex/skills/1-max-review` и
`/Users/triton/.claude/skills/1-max-review`.

24 теста `python3 -m unittest discover -s skills/shared/1-max-review/tests -v`
прошли за 14,992 с после compatibility-правки; последующие изменения только
в прозе и метаданных. Пробы прямого CLI и границы подтверждения описаны
в `probe-summary.md`. Третьей волны рецензентов и повторного MAVO-аудита нет.

Автор сверил две последние формулировки repair с источником: исправляет
главный агент после собственного подтверждения. Четыре модельные пробы
до этой формулировки доказывают только неизменившийся прямой CLI-маршрут.
