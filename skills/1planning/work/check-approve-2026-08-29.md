# Check-approve — 1planning — 2026-08-29

## Цикл 1

Проверен полный русский черновик вместе с product frame, cut, refactor map и
соседями семьи. Независимые окна: literal checker `/root/check_literal`,
trajectory checker `/root/check_trajectory`; внешний совет — `claude-opus-5`,
session `7fc9d0ba-3bbd-47c7-a1b9-81d39df72afe`.

Принято и исправлено:

- native Plan Mode больше не выключает допуск, а только запрещает durable-write;
- owner-gate относится к материальным выборам, а не к десяти полям
  сериализации;
- видимый след метода, порядок и предел усилий возвращены в срез;
- английский `invention probe` заменён наблюдаемой русской пробой первого хода;
- после `GOAL.md`, эпика и задач читаются только применимые инструкции и скилы;
- перегруженный монолит раскроен на admission, cut и approval.

Не принято:

- превращать контекстный ориентир в hard split или обязательный точный счёт;
- сохранять silence в native Plan Mode только потому, что он был в старом
  контракте: после отделения durable writers он отключал уникальную функцию.

Цепочки новых жёстких ограничений `default → механизм → вред → цена` записаны
в [карте рефактора](refactor-map-2026-08-29.md#цена-строгости).

## Цикл 2

Точная portable-версия `de6fc4bd…` прошла финальный барьер:

- содержательные references получили локальные `## Цель`; router остался
  служебным без декоративной цели;
- один обновляемый пакет решения заменил четыре одновременно сохранявшихся
  результата стадий;
- контекстный бюджет отделён от материального утверждения, а выводимые
  field-checklists сняты;
- literal checker: максимум `19`, русский текст/links/descriptions — PASS;
- trajectory checker: chat-only ownership и map/task seams — PASS;
- clean executor: полный маршрут A не пишет файлов и передаёт один approved
  packet — PASS.

Fresh Eyes остановил третий архитектурный цикл. Opus/Premortem finding о
parallel orchestration отклонён по прямому owner-evidence о последовательных,
не параллельных агентах
(`_ops/chat-recall/2026-08-24-181208-claude-13fd1279.md:23`).

Hard lines оставлены только для одного файла стадии, Plan Mode, запрета записи,
owner routing и наблюдаемого перехода; остальное ведут цель и контекст.
