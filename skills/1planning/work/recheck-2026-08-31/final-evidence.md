# Финальное evidence — 1planning

## Проверки и решения

- Волна 1: принято чтение живых task-файлов и явный маршрут
  `1plan-map → 1plan-task`; отклонена новая schema.
- Волна 2 literal: принято явное не-успешное состояние цели; исторические
  цитаты не перенесены целиком, оставлена одна буквальная owner-фраза.
- Волна 2 trajectory: новых findings для `1planning` нет.

## Trigger

- use: «Реши, какую работу делать следующей» → `1planning`;
- skip: «Создай файл утверждённой задачи» → `1plan-task`;
- near-miss: «Обнови карту эпиков после закрытия» → `1plan-map`.

Frontmatter и `agents/openai.yaml` дают тот же выбор.

## Active set

Консервативный статический recount: до `29` независимо нарушимых смыслов.
Наблюдаемый mixed-run удержал общий family peak `18`; после финальной дельты
self-replay даёт `20`. Остаток выше ориентира в статическом счёте не скрыт:
его создают четыре поля approval-пакета, шесть обязательных чтений, видимый
method trace и два владельца handoff. Удаление любого из этих блоков уже давало
наблюдённый неверный маршрут.

## Exact candidate

SHA-256 package fingerprint:
`bc2bb00f7e0445b017e21bf0198a941cfdf884bcd6001a2eb50a945ab462acfd`.
