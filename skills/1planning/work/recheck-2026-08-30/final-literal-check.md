# Final literal exact-byte check

Проверены неизменяемые candidate directories по канону: отсортированные пути с
ведущим `./`, затем `path NUL exact-bytes NUL`, SHA-256 всего потока.

| Package | SHA-256 | Атомарный маршрут |
| --- | --- | ---: |
| `1planning` | `91b4cc7db09fb5909a69cf4ff6c02830f361b30ffebaef3122f86a13ab031ee4` | 28–31 |
| `1plan-map` | `57f9fadb7eafbaf60a4a1fff105c9c3ec3e818dffe1b99c1fc391e0bde0e47b1` | 30–32 |
| `1plan-task` | `0b90fe7ab5af763258d42fe69a9c17f159d9e87f3b8a6eb8a3efb26bc2216e05` | 39–40 |

`quick_validate.py` и строгий YAML parse прошли. Во всех пакетах только
`SKILL.md` и `agents/openai.yaml`, runtime references нет; instructional body
русский, description — короткий English trigger-only.

Шесть несущих шлюзов сохранены: planning читает применимые корневые/папочные
инструкции, называет книгу и метод, утверждает полный пакет передачи; карта
хранит топологию и сначала исправляет её; задача требует допустимый вход,
единственного автора и причину отложения. Допуск остаётся у `1planning`, а
карта не пишет файлы задач. Чтение корневых/папочных инструкций не переносится
в карту/задачу: это граница владельца решения `1planning`.

Числа выше ориентира `~20`: два консервативных разбора независимо нарушаемых
смыслов дали указанные диапазоны, а не один канонический счёт. Они не были
механически урезаны, поскольку включают явно сохранённые границы полномочий и
безопасности.
