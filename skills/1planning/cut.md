# Cut — рефактор 2026-08-09

Таблица потерь пересборки тела SKILL.md.

## Живёт у владельца, из тела снята копия

- «да» до файла: 3 носителя → 1 (Механика, жёсткий переход)
- условия входа «пока верно X» → схема `references/contract.md`
- отложенный состав / акт вывода → `references/contract.md`
- зомби-план, текущая правда, классификация свидетельств → цикл в
  `references/modes.md`
- возврат в Wayfinding при новом материальном неизвестном →
  `references/modes.md`
- стыки и критерий потребителя → `references/contract.md`
- детали `context.md`: 4 носителя → владелец `references/contract.md` +
  строка постусловий
- `1use-principles` на развилке: 2 носителя → 1 (Инварианты)
- «вехи от оси, не хронологии» в Критериях → `references/decompose.md`

## Несёт только имя (указатель из тела)

cold-start-проверка · форма файлов · цикл корректировки · ось разреза.

## Потеряно совсем

Абзац вступления «проза дешевле кода, подробный план ощущается прогрессом —
самообман жанра»: работу делают первая строка Дельты и «Театр планирования»
в `references/modes.md`.

## Принятые риски

- (закрыт в тот же день, ревью Kimi) «условия входа» и «отложенный состав»
  без строки в карте сбоев — возвращены строкой «правлю чужой контракт по
  памяти».
- «Статус — текущая правда без хроники» — намеренная помеченная копия
  смысла modes.md: здесь состояние файла, там процедура.

## 2026-08-18

- Раздел «Очередь владельца» в task.md — заменён заметками-вопросами
  (questions.md): владелец файлы плана не открывает.
- Монофайл task.md на 10 разделов — заменён папкой task/status/context:
  разные читатели и темп изменения.
- Вехи-карта всего запуска внутри плана — знаменатель уходит карте эпиков;
  перенос — отдельным ходом при создании карты.
- Придуманные аудитом-2 числа и пороги кандидата (2–4 варианта, «считанные
  дни») — сняты как безысточниковые.
- 2026-08-18 (усиление план⟷эпик): точка «перечитай эпик» в Завершении снята
  независимым аудитом — дубль contract.md с конфликтующим лекарством; смысл
  живёт один раз в contract.md.
- 2026-08-18: обязательная норма map «текст эпика называет свой живой план» —
  отклонена владельцем: связь эпик→план уже пишет прибор в колонку «планы»,
  текст эпика правится при перечитке, когда план меняет его правду.
- 2026-08-18 (эпики не удаляются): фраза «архив план-файлов не уносит след —
  итог записан в эпике» снята аудитом — выводима из «✅ только с evidence» и
  «затронутые эпики обновлены тем же ходом»; «эпиков определённое количество» —
  решением владельца обоснование запрета удаления, не ограничение на состав.
- 2026-08-18 (связка с 1product-shaping): слово «материальные» заменено на
  «нужные» по букве владельца; именная связка «эпики карты» в 1product-shaping
  отклонена владельцем — безымянная («артефакты с маршрутом к паре»), граница
  «Не входит» скила рамок не тронута.

## 2026-08-22 — terminal repair и внешний runtime

- Строка «2–3 круга verify→fix … стоп, блокер владельцу» снята: у неё не было
  первого no-delta terminal, а любой технический blocker безусловно уходил
  владельцу.
- Копия runtime lifecycle в шаблоне `status.md` не добавлена: смысл живёт один
  раз в `references/contract.md`, а status хранит только датированный snapshot.
- Явный root-takeover технического модуля после двух repairs не добавлен:
  наблюдавшиеся сбои требовали terminal state и честного owner-handoff;
  `status.md` уже требует blocker и один `Next`. Кто исполняет последующую
  диагностику, остаётся открытым residual, не выдуманным правилом.

## 2026-08-22 — v2

- «Ось карты названа» сохранена (драфтер снимал — возвращена линзой 2, A1).
- Lifecycle plans/backlog/_archive сохранён в колоцированной форме (A4/B1);
  «второй plans/backlog/archive-дом не создаётся» — снято как выдумка драфтера.
- Слово «слой» в определении эпика не используется (коррекция владельца:
  эпик = большая конечная работа, не слой системы).

## 2026-08-24 — repair v3

- Снята формула процента эпика: при just-in-time создании задач её знаменатель
  открыт, поэтому `100%` означал завершение только уже созданных задач.
- Процент задачи сохранён: он показывает прогресс текущего явного списка из
  3–7 подзадач, а не закрытие задачи.
- Новый знаменатель эпика не добавлен; ответ «сколько осталось» остаётся у
  полосы статусов в порядке исполнения.
- Перестановка эпиков и автоматическое закрытие Runtime по `1/1` созданных
  задач сняты: этот счётчик не доказывает критерий эпика.
- Яркий queue-token `🟡 надо сделать` снят; `◽ в очереди` сохраняет смысл,
  но не конкурирует визуально с текущей работой.

## 2026-08-25 — repair v4

- Перестановка Runtime раньше frontier и автоматическое `✅` по `1/1`
  созданных задач снова сняты: оба хода меняют состояние по метрике другого
  уровня.
- Закрытые task-файлы не скрываются из dashboard; им выделена отдельная view,
  где процент остаётся task-level историей.
- Вопрос с дефектным `тип` или `статус` не отдаётся на волю dashboard-фильтра:
  checker перечисляет top-level Markdown с диска до разбора envelope.
- Одно поле `опора`, меняющее смысл после ответа, снято. Неизменяемое
  происхождение вопроса и `ответ-опора` не требуют угадывать lifecycle по
  значению одного slot-а.
- Двухосевая модель `owner intent × dependency readiness` не введена в эту
  правку: текущий scalar status получил только явное precedence `⏳` над
  dependency-derived `🔒`; более тяжёлая модель требует отдельного evidence.

## 2026-08-29 — граница planning-family

- Долговечные map/task-файлы вынесены владельцам `1plan-map` и `1plan-task`;
  `1planning` оставляет только допуск и видимый срез задачи в чате.
- Reread/rebuild/lifecycle сняты из planning: это обычное исполнение
  `1plan-task`; композиция и состояние эпика принадлежат `1plan-map`.
- Ось, цель, траектория, граница, посылка, критичный контекст и evidence-target
  сохранены как landing values, иначе решение исчезает между окнами.
- «До 20» не превращено в безусловный split: около 20 — сигнал проверить
  упаковку; неделимая цель остаётся целой с числом и причиной.
- Один task-владелец не означает запрет bounded evidence-probes: помощник не
  принимает решения и не пишет контракт.
- Native Plan Mode больше не выключает допуск: он запрещает лишь durable-запись
  из этого скила.
- Допуск, срез и approval раскроены на самостоятельные стадии с одним reference
  в активном наборе; владелец утверждает материальный выбор, а не сериализацию.
- Русское тело заменило прежний English-контракт по более свежему слову
  владельца; English остаётся только в `description` и литеральных токенах.
- `description` сокращён до trigger-only; установка тройки разрешена после
  clean acceptance вместо отдельного approval packet.

### Commander intent pass

- Скептический допуск, один автономный срез и запрет долговечной записи
  перенесены в «Уникальный контекст» и «Цели владельца»: они должны направлять
  решение без чек-листа.
- Содержательные references получили локальную `## Цель`; field-checklists и
  повтор вывода сняты, router остался служебным.
- Жёсткими оставлены только выбор одного файла стадии, видимый переход,
  Plan Mode и владельцы map/task: это невыводимые маршруты и safety boundary.

Детальная трассировка:
[refactor-map-2026-08-29.md](refactor-map-2026-08-29.md).

## 2026-08-31 — рефактор по доктрине композиции

Пакет пересобран clean-room от нового commander's intent
(`work/refactor-2026-08-31-doctrine/intent.md`). Кандидат —
`versions/candidate-2026-08-31-doctrine/`. Тело: 18 единиц, reference-файлов нет.

### Сохранено полностью

Все 15 механик карты `_workspace/planning-refactor-2026-08-31/mechanics-map.md`
(P1–P15) несёт новый текст, включая семь, которые жили только в неустановленном
кандидате 2026-08-31: чтение карты и живых задач, спор с владельцем, показанные
шаги, маршруты в обоих соседей, явное исключение работы вне живой цели.

### Добавлено чистой комнатой

- Соперник берётся с живой карты, а не из разговора: придуманный соперник звучит
  так же убедительно, но проверить его владелец не сможет.
- Обоснование опирается на открываемые адреса: проверяемость означает «можно
  открыть и не согласиться», а не «хорошо аргументировано».
- Пограничный случай решается в сторону карты, потому что цена двух ошибок
  несимметрична.
- Отклонённое возражение уезжает вместе с работой, иначе следующая сессия
  переспорит его заново или примет невидимый риск.
- Возражение делается один раз: блокировка не превращается в осаду.
- Устаревшая карта чинится, а не защищается.
- Собственный фальсификатор и строка старшинства.

### Снято

- Формулировка «относящееся состояние» как вход решения — заменена названными
  адресами `_ops/GOAL.md`, карты и живых задач: неоднозначность здесь дороже
  длины.
- Обобщённое «применимые skill-инструкции» — сильная модель читает применимые
  инструкции, находясь в контексте задачи; строка не переживала третье сито.

### Правки после волны проверки 2026-08-31

Два проверяющих вернули 25 находок, 23 приняты.

**Основание счёта единиц.** Считается каждый независимо нарушимый предикат, а
не пункт нумерации. Раздел «Пересмотреть, если» в счёт не входит: он не
предписывает действия в предметной области скила, а называет условие пересмотра
самого текста. По этому основанию тело — 19 единиц. Прежняя запись «18» считала
пункты и была неверна.

**Снято с причиной, чего раньше в этой карте не было:**

- второй триггер живого `description` «or reassessing work after a major error»
  возвращён в `description`, а не снят: его отсутствие в первом черновике было
  молчаливой потерей, не решением;
- Codex-метаданные пакета не потеряны: кандидат несёт `agents/openai.yaml` с
  переписанными `short_description` и `default_prompt` под новый момент запуска.

**Отклонено с причиной:**

- Требование убрать литеральные адреса `_ops/map`, `_ops/GOAL.md` как ломающие
  переносимость глобального скила. Основание отклонения — решение владельца
  `2026-08-31-202257-claude-86d85f7e.md#L25`: точная схема mavo-short2 объявлена
  общим стандартом, и цена этого выбора была показана ему до решения. Наблюдение
  внутри находки принято отдельно: скил теперь отрабатывает случай, когда карты
  в проекте ещё нет.
- Предложение слить единицы «решение уходит целиком» и «отклонённое возражение
  уезжает с работой» в одну формулу высшего порядка. Обобщать можно только вниз
  по цене последствий; у второй свой дорогой отказ — следующая сессия
  переспорит на пустом месте или примет невидимый риск, — и из общей формулы
  чистый агент его не выведет.

**Соответствие механик единицам:** P2 → «обоснование адресами» и «Старшинство»;
P4 → «отсутствие дубля проверено по именам задач»; P8/P9 → «Протокол поведения»,
пункт 1; P10 → «Пакет несёт четыре вещи вместе»; P11 → «Решение становится
решением после явного „да“».

**Принятое отступление от `goal-context.md`.** Уникальный контекст этого пакета
длиннее ориентира в 500 символов. Решение владельца 2026-08-31
(`_ops/chat-recall/2026-08-31-202257-claude-86d85f7e.md#L32`): контекст не
жать и ориентир в `goal-context.md` не менять. Следующему аудитору: это
названное решение, а не недосмотр.

## 2026-08-31 — форма «контекст · задача · цель»

Установлена версия `versions/candidate-2026-08-31-genie-v2/`. Предыдущая
редакция той же формы (`candidate-2026-08-31-genie`) была установлена без волны
проверки и снята после неё: два проверяющих вернули 23 находки, все приняты.

**Основание формы.** Слова владельца о природе агента: «агенты созданы достигать
цели, но как сломанный джин они выполняют буквально… цель выполнить в любом
случае и любой сложности, тут главное правильно сформулировать задачу и цель»
(`_ops/chat-recall/2026-08-31-202257-claude-86d85f7e.md`, запись
2026-08-31T23:15:05). Отсюда цель пишется достижимой руками агента, с точкой
насыщения, и так, чтобы дешёвый путь к ней совпадал с правильным.

**Счёт.** Единиц в теле: 19 → 7. Основание счёта то же, что у версии doctrine:
независимо нарушимый предикат; раздел «Пересмотреть, если» в счёт не входит.
Прежняя запись «19 → 6» считала по разным основаниям и была неверна.

**Растворено в контексте — 11 механик:** соперник с карты, обоснование адресами,
проверка дубля, оба маршрута к соседям, пограничный случай, передача целиком,
запрет фиксировать состав, неверная и отсутствующая карта, короткий взгляд на
предмет, живая цель проекта как мера.

**Снято с причиной — 1:** состав пакета передачи (результат, граница,
доказательство, критичный контекст) принадлежит теперь `1plan-task`, который сам
требует их от себя. Здесь остался предмет утверждения: работа, её эпик и
обоснование выбора.

**Осталось семь единиц:** след «книга и метод» с пределом глубины разбора;
вердикт свежести; явное «да» на названный предмет; настояние не заменяет
утверждения; работа вне планов всё равно становится эпиком или задачей;
отклонённое возражение уезжает с работой; старшинство.

Единица «возражение делается один раз» перестала быть инструкцией: она встала в
раздел «Твоя задача» вместе со своей причиной. Без причины проверяющий показал,
что она читается как исчерпанная квота и гасит не только осаду, но и уточнение
возражения.

**Что нашла волна в предыдущей редакции этой формы.** Записываю, потому что это
повторяемые ошибки, а не частности:

- самый дешёвый путь к цели вёл к согласию: цель не содержала сравнения, ради
  которого скил существует, и одобрение удовлетворяло её быстрее возражения;
- `_ops/GOAL.md` исчез как вход решения, а карта потерь утверждала полную
  сохранность — я проверял себя иглой, которая проходила по соседнему тексту;
- «неделя» как цена неверного старта была придумана мной: у владельца о цене
  сказано только «мы просто это потеряем». Число несло всю асимметрию, из которой
  выводится поведение, и снято как безысточниковое;
- две разные записи владельца были склеены в одну цитату через многоточие, и
  выпала ровно та середина, где он требует шаги. Восстановлены раздельно;
- «может открыть» проверялось у адреса, а не у прочтения: адреса можно
  перечислить, не открывая. В цели теперь «ты открыл сам».

**Принятое отступление от `goal-context.md`.** Авторские утверждения о ценах
ошибок стоят в Уникальном контексте, хотя стадия требует там только слова
владельца. Это названное решение владельца (`…-86d85f7e.md`, запись
2026-08-31T23:31:54), а не недосмотр: форма выводит поведение именно из цен.
Уникальный контекст также длиннее ориентира в 500 знаков — отдельное решение
владельца, записанное выше.

**Ядро — 2087 знаков** против 1000 в эксперименте. Разница — цена того, что
первая редакция была компактной за счёт потерянного.

**Фальсификатор компрессии.** Сжатие 19 → 7 ничем не проверено, кроме волны
проверяющих. Пересмотреть его, если агент под этим текстом хотя бы раз одобрит
работу, не назвав соперника с карты.

## 2026-09-02 — причина у каждой единицы

Точечная правка по словам владельца о том, что агент выполняет инструкции,
смысл и важность которых понимает
(`_ops/chat-recall/2026-09-02-151925-claude-0c84f259.md#L22`, `#L24`). Три
единицы раздела «Что миром не выводится» стояли без причины и получили её
внутри той же единицы: явное «да» (иначе блокировка снимается молча, старт по
инерции — дорогая ошибка), настояние не заменяет утверждения (оно отвечает на
спор, а не называет эпик и обоснование), старшинство (иначе срочность
переспорит рамку, а скил решит за соседей). Счёт единиц не изменился: 7.
Структура тела не менялась; порядок Задача, Цель, Зачем, Критерии — при
следующем полном рефакторе. Волна проверяющих не проводилась.
