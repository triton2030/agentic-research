interface:
  display_name: "1-Формирование продукта"
  short_description: "Use when a recurring scope needs a new or changed Principles + Frame pair; use 1use-principles for the current choice"
  default_prompt: "Используй $1product-shaping перед работой над новой или изменённой возвращающейся областью продукта, либо когда утверждённая пара конфликтует или дрейфует."

policy:
  allow_implicit_invocation: true
