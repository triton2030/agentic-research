# Authoring decisions and loss map — 2026-08-31

Состояние: `готов финальный проверенный черновик`; две reviewer-волны
израсходованы.

Authoring baseline при финальной проверке:
`/Users/triton/.codex/skills/1skill-creation/SKILL.md`, SHA-256
`11e82449797be9615b92976f2fe33f8677f74429a10fc2564a91b7a09fae344e`.
Ранний intent фиксировал прежний SHA `6e6b93e9…`; параллельное изменение live
не переносилось в candidate, а применимые authoring rules были перечитаны.

## Trigger

- use: «Я делаю приложение для детей»;
- use: «Эта продуктовая рамка больше не направляет новые решения»;
- skip: «Исправь опечатку в Product Frame»;
- near-miss: «Как P-003 влияет на текущий экран?» → `1use-principles`.

## Принятый когнитивный протокол

Owner-граница: главный фокус — «принципы Рэя Далио», «вещи высокого уровня» и
«когнитивный протокол грамотного абстрагирования»; хороший принцип порождает
«кучу автономных решений»
(`_ops/chat-recall/2026-08-30-181729-codex-01a052cb.md:18`).

Протокол исправляет четыре недефолтных провала: вкус модели вместо основания;
частное правило или общий лозунг вместо различающей абстракции; ложная
переносимость, проверенная автором на собственных примерах; каталог следствий
вместо немногих порождающих принципов.

## Карта потерь

### Сохранено

- Creator/Applicator: `1product-shaping` создаёт источник суждения,
  `1use-principles` выводит конкретное решение.
- `GOAL`, все живые пары и точечный recall образуют основание до shaping.
- High-leverage вопрос закрывает несколько классов решений.
- Уровень принципа проверяется разумной противоположностью.
- Порождающая сила проверяется изолированным чистым агентом на несхожих
  нижних слоях и конкурирующем принципе.
- Канонический адрес — только `<project-root>/_ops/product-frames/`; пара
  получает authority только после exact approval всей версии.
- Конфликт с `GOAL` имеет два явных маршрута: изменить пару или отдельно цель.

### Поглощено

- Confirmation map поглощена разделением owner-основания, синтезированного
  кандидата и точного approval.
- Interview-акты, квота вопросов и walkthrough поглощены тестом high-leverage
  вопроса и наблюдаемым clean run.
- Failure lists поглощены тремя фальсификаторами: разумная противоположность,
  несхожие слои и конкурирующий принцип.
- Мера «длина автономного участка» поглощена наблюдаемой порождающей силой
  пары в clean run.

### Снято

- «Чистая текущая правда» как центральный продукт скила: current canon остался
  authority-boundary, но основной продукт — качество верхнеуровневых принципов.
- Универсально обязательные разделы Frame, лимит тайбрейкеров, длина принципа
  1–3 строки и обязательные `Старшинство`/`Аппетит`: живой MAVO-корпус не
  подтверждает общую форму, а она вытесняла смысл.
- Требование, чтобы агент не формулировал принцип до owner-approval: агент
  синтезирует кандидат, владелец даёт authority полной паре.
- Все runtime references: отдельный `pair-contract.md` после wave 1 удалён;
  ни один reference не предотвращал вред сверх шести операций тела.

## Wave 1 decisions

- `принять`: разрешить начало с одного owner-сигнала.
- `принять`: заменить обязательные поля Frame результатным критерием.
- `принять`: добавить изолированный прогон только по паре.
- `принять`: подтверждённый конфликт нового приоритета решает владелец.
- `принять`: восстановить два маршрута конфликта с `GOAL`.
- `принять`: заменить ошибочные counts и текущий authoring SHA.
- `принять`: снять `pair-contract.md`, потому что его 36 предикатов снова
  делали форму продуктом.

## Wave 2 decisions

- `принять`: holdout-развилки выбираются независимо от пары и не пересказывают
  её или исходные примеры; прежний probe остаётся доказательством возможности,
  но не сильного переноса.
- `принять`: при update прежняя одобренная пара остаётся evidence, а владелец
  видит каждый изменённый или снятый смысл до approval.
- `принять`: независимые предикаты вынесены на отдельные строки.
- `принять`: прежний active-set count ≤14 был ошибочным и снят.

## Counts и оставшаяся сложность

- `SKILL.md`: 3 routing-различия, 4 свойства результата и 38 независимо
  проверяемых protocol-предикатов; 15 когнитивных операций, без runtime
  references.
- Ledger по пунктам протокола: `3, 2, 2, 3, 1, 2, 1, 2, 3, 5, 1, 3, 3, 3,
  4` — сумма 38.
- `platforms/codex/agents/openai.yaml`: 4 независимо проверяемых поля.
- Консервативный terminal active set больше ориентира 20; это явно оставленный
  когнитивный риск, а не скрытый результат укрупнения или дробления по
  references. Следующее удаление снимает named source, falsifier, update-loss
  guard либо authority boundary.

Конкретный вред, оправдывающий оставшуюся сложность: без основания модель
присваивает владельцу свой вкус; без абстрагирования выдаёт фичу или лозунг;
без clean run сама подтверждает собственный тезис; без минимизации записывает
следствия как принципы; без approval/canonical boundary кандидат становится
неподтверждённой властью.
