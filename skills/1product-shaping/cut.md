# Не вошедшее при переработке 2026-08-08

Оригинал — `live-claude-2026-08-08/SKILL.md` (36.6KB). Формула судеб: тело →
семь разделов; операции → references; сосед → `1use-principles`.

- Раздел «Работа по паре» целиком (эскалация через тайбрейкеры, адресное
  чтение `P-*`, «комментарии лечат симптом») — территория `1use-principles`
  по решению владельца «Да, разделяем»; механизм «повторные эскалации → дыра
  → минимальная сверка» сохранён (инвариант тела + пример 6).
- Правило «каждая задача плана называет сцену Frame и применяемые `P-*`» —
  шов `1planning`/`1use-principles`; решить при их следующем касании, здесь
  ему владельца нет.
- Абзацы-рационализации, описывающие артефакт читателю («Естественное сжатие
  теряет самую дорогую добычу…», «Скилл переносит работу туда, где каждая
  сторона сильна…» в полной форме) — адресат-нарушение (аудит 3а); суть сжата
  во вводку и причины при правилах.
- Шаг 12 walkthrough «Применить пару в следующей развилке» + первая половина
  примера 6 (применение тайбрейкера в рантайме) — демонстрировали работу
  соседа; вырезаны при разведении швов.
- Повторы read-gate в трёх разделах оригинала — один жёсткий переход
  «Чтение до суждения».
- «Не гоняй владельца переподписывать сцены ради каждой задачи» — покрыто
  формой «только чтение»; отдельная строка не пережила сжатие.
- Подробности минимальной формы («если ответ рождает долговечный принцип —
  зафиксируй в паре») — выводимо из инвариантов пары; строка не удержана.
