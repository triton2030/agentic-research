# Evidence — рефактор 2026-08-09 «чистовик + журнал»

## Исполняемой проверкой (до рефактора, диагноз)

- 51 заголовок `P-*` в четырёх парах mavo-short2, из них 4 надгробия;
  у 30 из 47 содержательных блоков нет `Commitment` (grep, два независимых
  прогона: Claude + Codex-parser).
- Корпус принципов — 7 327 слов (`wc -w`).
- Скриптов-потребителей формата пар нет (grep по scripts/apps/packages).

## Чтением

- Все четыре Frame целиком, принципы прототипа целиком, оба скилла со всеми
  references, эталон компиляции `1use-principles`, GOAL.
- Независимый разбор Codex (gpt-5.6-sol, xhigh, blind — без выводов Claude):
  вердикт «архитектура не удовлетворяет назначению», сошёлся с диагнозом
  Claude; материальные добавки — глобальная неадресуемость `P-001`,
  протухающие sync-рёбра «Влияет на», противоречие чистовика и Frame в паре
  кабинета (подтверждено первичкой). Тред
  `019fe664-0827-7323-8c46-b9c6dd6a417f`, run
  `_workspace/codex-artifacts/20260809T115902Z-8b68fd51` (mavo-short2).

## Прогон нового формата (2026-08-09, после записи)

Setup: два свежих субагента, одна вымышленная задача (сервис доставки
цветов, три подтверждённых ответа владельца, «запиши Principles + Frame»).

- **Без скилла:** цитаты владельца внутри принципов, блоки «следствия —
  выведено», статусы в frontmatter, открытые развилки в Frame — летопись,
  свидетельство + регулятор + карта состояния в одном файле.
- **Со скиллом (SKILL.md + pair-format):** confirmation map до записи;
  принципы — три правила по 1–3 строки с границей «не решает»; Frame —
  сцена + приёмка + привязка с запретом + однострочные тайбрейкеры (P-N);
  дословные цитаты — только в записи журнала; деталь «30 минут» не попала
  в чистовик, сохранена в журнале; неназванный аппетит честно опущен.

Вывод: решение под скиллом различимо отличается от прогона без него —
критерий прогона `1skill-shaping` выполнен.

## Не проверено

- Триггеринг по description не перепроверялся: description не менялся.
- Выигрыш свипа `1use-principles` на реальном корпусе — проверится после
  миграции пар mavo-short2.
