interface:
  display_name: "1-Находки без отвлечения"
  short_description: "Record a side signal and immediately resume the interrupted work"
  default_prompt: "Используй $1findings, чтобы побочный сигнал остался доступен для будущего разбора, не стал новой целью и не прервал движение к основной."

policy:
  allow_implicit_invocation: true
