interface:
  display_name: "1-Находки без отвлечения"
  short_description: "Record a side signal and immediately resume the interrupted work"
  default_prompt: "Use $1findings to record one observed side signal without investigating it, then immediately resume the interrupted action."
