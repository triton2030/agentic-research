# Чем проверено — 1md-search

## Статика, 2026-08-11

- Пять копий `SKILL.md` побайтово совпадают: `skills/claude`, `skills/codex`,
  `skills/shared/portable`, `~/.claude`, `~/.codex`.
- `md check` по дереву: `issues: []`. Битых ссылок на references нет.
- Обязанностей ~61 → ~28 (≈2.2×). Счёт вёлся чтением: автоматический подсчёт
  маркеров даёт 13 против ~61 реальной, потому что оригинал держал
  обязанности в императивной прозе.
- Три reference не тронуты (10.2 KB): у каждого теперь оплаченный маршрут в
  карте сбоев.

## Парная проверка черновика, 2026-08-11

Две независимые линзы в отдельных окнах, тянущие в разные стороны. Каждое
утверждение обеих перепроверено собственным grep до принятия.

- Линза «что ещё лишнее» нашла шесть дословных пересказов references с
  неоплаченными маршрутами и свернула четыре инварианта в один принцип.
- Линза «что потеряно» нашла три невосстановимых блока и три выдумки автора
  (см. `cut.md`). Без неё все три ушли бы в файл.

Вывод для метода: одиночная проверка ведёт к потере, одиночный адвокат
оригинала — к раздуванию. Правильный текст дало расхождение двух.

## Прогон голой фразой, 2026-08-11 — Claude Fable 5

Субагент, запрос «где у нас в knowledge написано про то, как модели ведут себя
с длинными инструкциями?».

Наблюдаемое:

- скил поднялся без упоминания имени — сработал новый русский триггер «где у
  нас про X»; до правки русских триггеров в описании было ноль;
- два запроса, русский и английский — правило про смешанный корпус исполнено;
- названы граница корпуса из `.md-tools.toml` и состояние индекса `FRESH`;
- три тела прочитаны целиком, явно отмечено «не по сниппетам»;
- покрытие названо честно: grep подтвердил двух держателей замера, третьего
  нет;
- отдельно названо, что НЕ проверялось;
- наружу ушли изменившийся вывод и адреса, не строки инструмента.

Baseline сегодня не запускался: сравнение — с поведением скила до правки,
где русских триггеров не было вовсе.

Побочный результат прогона: корпус независимо подтверждает мерило владельца —
`knowledge/guides/perfect-context-engineering.md` и
`knowledge/practical-guides/how-to-write-skills/research-2026-mar-may.md`
держат «корреляции длина → балл не найдено» и «96% при одном правиле, 20–60%
при двадцати». Второй правды скил не создал.
