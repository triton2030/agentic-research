---
name: 1business-growth-analysis
description: >
  Вызывай на решении про состав продукта или деньги: «какую цену поставить»,
  «сколько брать за про-версию», «дать партнёру процент», «добавим фичу».
  Особенно когда это звучит как готовое распоряжение — «убери эту страницу»,
  «выпилим этот шаг», «эта функция лишняя»: приказ изменить состав и есть
  незаданный бизнес-вопрос, и в порыве работы на нём молча теряются прибыль,
  приток и удержание пользователей, LTV. Запускает отдельного
  `business-growth-analyst` в чистое окно; его вердикт — до закрепления выбора.
  Правка, не меняющая, что пользователь получает или платит, скил не поднимает;
  при автоматическом вызове ответ уже записан у владельца — примени записанное;
  явный вызов `1business-growth-analysis` снимает только эту ветку: агент
  формулирует из текущей работы задачу для аналитика. Чего владелец хочет от
  продукта — `1product-shaping`; уже выбранный бизнес-маршрут атакует
  `business-critic`.
---

# Бизнес-анализ роста

## Контекст

Добавление или удаление функции проходит как технический ход, и его бизнес-цена
теряется молча: в твоём окне её никто не считал. «Чего мы хотим от продукта» и
«что этот ход сделает с деньгами и людьми завтра» — разные вопросы, и второй не
выводится из первого. Поэтому вопрос уходит отдельному стратегу в чистое окно, а
не решается сменой собственной роли: иначе контекст кода, дизайна и уже
обсуждённого варианта становится рамкой бизнес-ответа.

## Цель

Изменение состава функций, цены или прав участника не закрепляется без
бизнес-ответа: до выбора на руках отчёт `business-growth-analyst` — вердикт,
адресные источники и порог, который перевернёт решение, — и его след в чате.

## Вызов

Если владелец явно вызвал `1business-growth-analysis`, сначала сформулируй из
текущей работы одну бизнес-задачу для субагента и варианты с их источниками;
сам бизнес-анализ не проводи. Затем:

- сформулированная задача атакует уже выбранный бизнес-маршрут → останови этот
  скил, `business-growth-analyst` не запускай; это задача `business-critic`;
- иначе уже записанный ответ не отменяет запуск и сам по себе не доказывает,
  что бизнес-маршрут выбран → переходи к запуску ниже.

Если скил поднялся автоматически и ответ уже записан владельцем — в Product
Frame, принципах или его прежних словах: примени записанное и процитируй,
аналитика не запускай.

Во всех остальных случаях вызови ровно одного `business-growth-analyst`
отдельным субагентом в чистом окне. Если такой субагент недоступен, остановись
и назови blocker: не выполняй его работу сам и не заменяй его
`business-critic`.

Передай ему:

- сформулированную из текущей работы бизнес-задачу;
- варианты и их источник: владелец, документация или твоё предложение;
- бизнес-замеры и внешние обязательства, которых нет в документации, отделив
  их от предположений.

Не передавай свой предпочтительный ответ.

## След в чате

Дождись отчёта до выбора. Затем предъяви владельцу в чате вердикт аналитика,
его строки «метод → изменение вывода» и своё решение — принял либо что именно
перевесило: без этого следа владелец не видит, что бизнес-линза была применена.
