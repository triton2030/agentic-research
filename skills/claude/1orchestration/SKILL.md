---
name: 1orchestration
description: >
  Вызывается, когда владелец просит оркестрацию, волну или разделить
  работу между субагентами; всегда перед запуском двух и более
  субагентов на одну работу; и когда на одну задачу — даже
  последовательную — сходятся три и более моментных инструкционных
  владельца сверх постоянного слоя (поднятые скиллы, рамки задачи,
  критерии плана — корень не считается). Оркестратор делит работу
  на посильные конкретные задачи и при каждой выдаче фокусирует
  субагента на инструкциях его момента; сам держит траекторию с
  высоты птичьего полёта и бережёт своё окно. Без скилла одно окно
  берёт всё разом и тупит: пропущенные требования, самопроверка
  исполнителя, перетёртые правки. Выбор модели и семьи — мостовые
  скилы (`1codex`/`1hermes` у Claude, `1claude-mcp`/`1hermes` у
  Codex); форма план-файлов → `1planning`; линзы и контракт свежести
  критиков → `1fresh-eyes`.
---

# Оркестрация

Окно, слушающее весь свод разом, исполняет каждую инструкцию хуже.
Оркестратор — технический директор волны, хранитель её траектории
и пользы: делит работу на посильные задачи, при выдаче фокусирует
каждого на инструкциях его момента, своё окно держит для сверки
хода с целью проекта и волевых решений.

## Цель

На руках — работа волны субагентов, полезная проекту, а не только
буквальная: каждый субагент получил посильную задачу с фокусом,
оркестратор с высоты птичьего полёта держал траекторию и волевые
решения, а обрыв сессии стоил одну волну, не работу.

Не входит: выбор моделей и семейств (`1codex`, `1hermes`) · форма
план-файлов (`1planning`) · линзы, зоны и контракт свежести критиков
(`1fresh-eyes`).

## Критерии успеха

- Карта волны предъявлена в чате до запуска: задача → кому →
  фокус-адреса → что предъявить в возврате.
- Каждое задание посильно: одна зона чтения либо один
  непересекающийся футпринт записи; границы и форма возврата
  названы в самом задании.
- Тяжёлые классы инструкций получили приём по таблице цены; лёгкие
  не повторялись.
- Исполнитель и проверяющий — разные окна; видение — у советников.
- Ход сверен с целью проекта, не только с буквой задания:
  расхождение или невыполнимость закрыты волевым решением — оно
  проверено субагентами, записано в реестр решений и названо в
  отчёте; необратимое или меняющее долговечную правду ушло
  вопросом владельцу.
- Окно оркестратора сбережено: корпус не читал, в футпринты не
  писал.
- Отчёт волны выдан в чат: агент → задача → фокус → возврат → чем
  проверено.
- После обрыва новое окно продолжило с носителя режима, не с нуля.

## Инварианты

- Свод инструкций не отменяется — оркестратор фокусирует: задание
  называет адреса инструкций момента и что предъявить в возврате
  как след их применения. Фокус — адреса, не пересказы: пересказ
  создаёт вторую правду.
- Оркестратор бережёт окно: в детали работы не уходит, тяжёлую
  генерацию — применение принципов, экстраполяцию — отдаёт
  советникам; волевое решение по конфликтам, пустотам и возвратам —
  его, как и синтез итогового ответа. Корневые обязанности
  отвечающего — recall-цитаты, продуктовая правда — закрываются
  возвратами советников и входят в отчёт волны.
- Исполнитель не принимает свою работу; советник не проверяет
  построенное на его видении.
- Оркестратора тоже проверяют: траекторию волны судит внешнее окно.
- Параллельная запись — только непересекающиеся футпринты; иначе
  один писатель.

## Дельта

Модель умеет запускать субагентов. Она не умеет:

- разделить работу на посильные задачи — даёт одному окну всё сразу;
- сфокусировать — вываливает весь свод или молчит про инструкции
  вовсе;
- взвесить инструкцию — даёт пишущему и экстраполяцию, и приёмку;
- повторить критичную инструкцию в момент её действия — надеется
  на память окна;
- сберечь окно оркестратора — читает корпус сама «для скорости»;
- потребовать след — принимает «учёл» без отчёта волны.

## Известные сбои

`когда → сбой → цена → куда`

- задание выдано без фокуса → субагент слушает весь свод разом →
  соблюдение каждой строки падает кратно (96% → 20–60%) → карта
  волны (Механика, шаг 1)
- инструкции розданы без веса → тяжёлая исполнена как ритуал или
  забыта → [instruction-cost](references/instruction-cost.md) —
  без таблицы цены все инструкции весят одинаково
- дешёвые окна на лёгком классе без перекрытия зон → слепая зона
  одного окна невидима снаружи → правило перекрытия в той же
  таблице цены
- задание написано слабыми формулировками → субагент исполняет
  форму, не решение → переделка волны → `1instruction-shaping` →
  references/wording.md
- оркестратор ушёл в детали → окно съедено, волна без директора →
  инвариант сбережённого окна
- запись без предполёта → пересечение футпринтов, перетёртые
  правки → [preflight](references/preflight.md)
- проверило то же окно или та же семья → дыра невидима до
  владельца → внешняя проверка (Механика, шаг 4)
- возврат принят, переход не записан → свежее окно продолжает с
  позавчера → носитель режима (Механика, шаг 5)

## Механика

**Порог.** Волна разворачивается, когда есть любое из: две и более
независимые зоны чтения · два и более непересекающихся футпринта
записи · три и более моментных инструкционных владельца сверх
постоянного слоя. Ни одного — один воркер плюс внешняя проверка;
причина строкой.

**Режим.** С живым планом состоянием волны владеет план: форма
задания, носитель возврата, бюджет и круги ремонта — по контракту
`1planning` (delegation); этот скилл добавляет фокус-адреса,
порядок, барьеры и лимиты параллельности; реестр решений живёт в
план-папке. Без плана — папка волны
`<корень проекта>/_workspace/orchestration/<дата-слаг>/`:
контекст-файл (зачем волна, проблема, цель, границы — без дублей
канона и без процедур), возвраты (лёгкий — thread, тяжёлый —
файлом) и `decisions.md` — реестр волевых решений: решение ·
обоснование · чем проверено. Реестр — след волны, не канон:
решение, переживающее волну, уходит в план или вопросом владельцу.
Папка волны добавляется в `.gitignore` при создании.

1. **Карта волны** — до запуска, в чат: задача → кому →
   фокус-адреса → что предъявить в возврате; вес инструкций и
   приёмы тяжёлых классов — по таблице цены (карта сбоев). Формат
   задания: посильная задача · фокус-адреса · границы · форма и
   адрес возврата; формулировки — по правилам wording (карта
   сбоев).
2. **Чтение и советники** — параллель по зонам; советники
   возвращают видение: применение принципов, экстраполяцию,
   варианты решения. →• оркестратор принял отчёты с адресами.
3. **Запись** — предполёт футпринтов пройден; один писатель по
   умолчанию; упавшего будят репликой, не респавнят. →• возвраты
   лежат по носителю режима.
4. **Проверка** — параллельно, окнами, не писавшими работу: работа
   против критериев задания и плана · сверка с принципами и
   цитатами владельца — `1use-principles` в непишущем окне
   советника · траектория волны и самого оркестратора — внешнее
   окно; линзы и контракт свежести критиков назначает
   `1fresh-eyes`; кросс-семейный прогон — по решению владельца.
   Возвраты — разница списков, не мнение.
5. **Принятие** — только оркестратор: пакеты дословно, не в
   пересказе; конфликт, пустоту и развилку закрывает волевым
   решением — строкой в реестр в том же ходе — или вопросом
   владельцу, вопросы пачкой; развилка, меняющая ветку,
   останавливает зависимую ветку немедленно; переход записан в
   носитель режима в том же ходе.
6. **Отчёт в чат** — таблица: агент → задача → фокус → возврат →
   чем проверено. Без отчёта волна не закрыта.

**Ремонт** (бесплановый режим; с планом — по delegation) — первый
круг репликой прогретого воркера; второй — свежим окном с заданием
и возвратом; третьего нет — очередь владельца.

## Завершение

Волна закрыта: отчёт волны в чате; возвраты приняты либо отклонены;
переход записан в носитель режима; очередь владельца выдана; строка
`1findings` — что в делении работы и фокусировке сработало, что
нет. Порог не достигнут — волна не разворачивается, причина
строкой.
