# Evidence — 1codex

Только измеренное. Самоотчёт агента и «читается лучше» сюда не попадают.

## Вмешательство в идущий ход работает (2026-08-16)

**Вопрос:** можно ли развернуть уже идущий turn Codex, не начиная заново?

**Пробник 1 — голый SDK.** Ход на долгую задачу, реплика через
`TurnHandle.steer()` на 25-й секунде. Лента хода:

| t | что |
|---|---|
| 17.9s | `agentMessage`: «Начинаю с `codex_review.py`…» — работает по исходному |
| 25.2s | `userMessage` — реплика вошла в ход |
| 31.0s | `agentMessage`: «STEER-OK \| файл в момент отмены: SKILL.md» |

Ход закрылся `completed` за 31 секунду. Модель не просто приняла сообщение —
переопределила задание за 6 секунд.

**Пробник 2 — сквозной, через мост.** Фоновый `codex_review.py` + команда
`codex_progress.py "$RUN_DIR" --steer "…"` из другой сессии:

```
01:31:24  steer_requested   7882a7f76211
01:31:25  steer_accepted    turn_id 01a00831-…
```

Финал прогона: `STEER-LIVE-OK | последний разобранный файл: ни один`,
`status=completed ok=true`. Доставка через диск заняла секунду.

**Границы, прочитанные в схеме SDK, не в документации:** неуправляемы ровно два
вида хода — `review` (нативный `--mode diff`) и `compact`. `expectedTurnId` —
заявленная предпосылка запроса: опоздавшая реплика в следующий ход не попадёт.

## Триггер: было 0 из 3, стало 2 из 2 (2026-08-16)

Одинаковые условия до и после: `claude -p "<фраза>" --max-turns 3`, cwd — корень
проекта, голая фраза без подсказок и без имени скила в двух из трёх.

| Фраза | Старый пакет | Новый пакет |
|---|---|---|
| «кодекс работает уже час, что делать» | не поднялся → `ListAgents`, `ps`, `ls ~/.codex` | **`Skill/1codex`** |
| «сомневаюсь в своём выводе, нужна проверка» | не поднялся | **`Skill/1codex`** |
| «поправь опечатку в README» | не поднялся (верно) | не поднялся (верно) |

Что изменилось в `description`: добавлен случай «запущенный прогон идёт долго —
понять, туда ли идёт, вернуть на курс, исправить недопонимание в результате».
Его в старом описании не было вовсе.

Оговорка о силе замера: по одному прогону на фразу, отбор фраз мой. Это
доказывает, что скил стал подниматься там, где не поднимался, но не измеряет
частоту ложных срабатываний на широком корпусе задач.

## Подробности прежнего замера

`claude -p "<фраза>" --max-turns 3`, cwd — корень проекта, голые фразы без
подсказок. Установлен старый пакет (тело 15 120 символов, `description`
889).

| Фраза | Поднялся `1codex`? | Что сделал вместо |
|---|---|---|
| «кодекс работает уже час, что делать» | нет | `ListAgents`, затем `ps` и `ls ~/.codex` |
| «сомневаюсь в своём выводе, нужна проверка» | нет | ориентировался по файлам; из scratchpad-прогона — `1chat-recall` |
| «поправь опечатку в README» | нет (верно) | правка без скилов |

Первая фраза — прямой момент надзора, второй строкой стояла в старом
`description` («когда сомневаешься в своём выводе или подходе») и всё равно не
сработала. Первый случай в описании отсутствовал вовсе и добавлен.

Отдельная ирония первой строки: агент пошёл в `ListAgents`, хотя тело старого
скила ровно про это и предупреждало — «Codex в списке агентов не виден». Правило
существовало и не сработало, потому что скил не поднялся: это и есть разница
между наличием правила и его действием.

## Что НЕ доказано

- Что вмешательство репликой выгоднее перезапуска. Доказано только, что оно
  технически работает. Различающий замер — три парных прогона на одном задании
  («соблазнительный второстепенный баг», «долгое правильное молчание»,
  «недопонимание в финале»), мерить время до принятого результата и объём
  текста, попавший в окно оркестратора.
- Что сокращение тела вдвое улучшило исполнение уже поднятого скила. Измерялось
  только срабатывание.
