---
name: 1codex
description: >-
  Use when work is nearing done, a route is not yet chosen, heavy work needs
  other hands, a Codex run is in flight, or a Codex verdict conflicts with
  Claude's. Claude's own subagents are 1orchestration.
---

# 1codex — чужие глаза, чужая голова, чужие руки

## Уникальный контекст

У владельца есть мост к Codex — второй модельной семье со своим окном и
оплаченной подпиской, кредиты которой не переносятся. По его наблюдению Codex
внимательнее Клода и превосходно работает аудитором: видит в работе Клода то,
чего не видит сам Клод. Претензия владельца в том, что Клод зовёт Codex редко и
почти всегда руками — исполнителем уже принятого решения, а не аудитором своей
работы и не советником до неё. Владелец хочет видеть в Клоде оркестратора, а
живых агентов — своими глазами.

## Твои главные цели

1. Codex входит в работу до того, как твоё суждение закрылось: аудитором —
   прежде чем ты назовёшь работу готовой, советником — прежде чем выберешь
   маршрут; прогон, лишь подтверждающий твой готовый вывод, успехом не считается.
2. Каждый оплаченный прогон закрывается названным эффектом — что он изменил в
   твоей работе или решении либо почему не изменил; запуск, пересказ ответа и
   «учёл» эффектом не являются.
3. Владелец в любой момент видит сам, кто из агентов жив и над чем работает, а
   чужая работа доезжает до проекта целиком; потерянные правки, висящие деревья
   и незакрытые треды успехом не считаются.

## Когда читать reference-файлы

Открывай ровно один файл — тот, чей момент ты наблюдаешь сейчас; к следующему
переходи, когда сменился сам момент, а не когда захотелось подробностей.

| Момент | Файл |
|---|---|
| отдаёшь Codex проверку уже сделанного: работы, текста, дифа, вывода | [`audit.md`](references/audit.md) |
| отдаёшь суждение до реализации: маршрут, план, прочтение намерения, взгляд на траекторию сессии | [`advise.md`](references/advise.md) |
| отдаёшь саму работу: исследование в его папку либо правку файлов проекта | [`delegate.md`](references/delegate.md) |
| прогон уже идёт: жив ли, туда ли идёт, вернуть ли репликой, остановить | [`watch.md`](references/watch.md) |
| тред пережил свой прогон: чужой, старый, оборванный, занятый | [`threads.md`](references/threads.md) |
| твой вывод и его вывод разошлись — или совпали, и ты счёл это доказательством | [`arbitrate.md`](references/arbitrate.md) |

## Правила, действующие всегда

- Любой оплаченный turn запускай через `Bash` с `run_in_background: true`:
  заранее ты не знаешь, что окажется долгим, а форграунд вешает сессию на минуты.
- Не вешай пайпы-фильтры на фоновую команду (`| head`, `| grep`, `2>&1 | …`):
  закрытый пайп ронял прогон и подменял готовые результаты exception-записями.
  После финиша читай канон с диска — `result.json` и головы файлов, не stdout.
- В задание ЛЮБОГО прогона клади запрет обратного вызова — «не вызывай
  инструменты claude-mcp, работай файлами и shell»: у Codex есть блокирующий
  `claude_ask`, отвечать на него некому, и замер 2026-07-28 дал 9 часов тишины.
- Успехом оплаченного прогона считай только `status=completed` и `ok=true`:
  failure или частичный ответ не синтезируй как готовый результат, а артефакт,
  записанный до отказа, засчитывай лишь прочитав его сам и назвав, чем проверил.
- Запустив прогон, назови владельцу роль, задачу, обе ручки — `RUN_DIR` и id
  фоновой задачи — и место результата, и предупреди, что Codex **в списке агентов
  не виден**: это фоновый процесс, а не субагент. Без id задачи ты после
  пробуждения не откроешь и не остановишь нужную карточку.

## Правила момента «собираюсь запускать»

- Момент упущен — сперва по-настоящему переоткрой готовность или маршрут
  вопросом, способным опровергнуть твой вывод; если вывод уже не может
  измениться, не запускай прогон ради отчётности и не называй подтверждение
  успехом.
- До запуска назови объект, который ответ ещё вправе изменить — решение,
  артефакт или ограниченный кусок работы, — и наблюдаемый результат, по которому
  прогон можно будет закрыть; названный после ответа эффект придумывается под
  ответ.
- Пиши задание разделами **Контекст → Цель → Границы → Готово, когда**, отдавая
  первичные артефакты путями: твой пересказ передаёт Codex твою же ошибку вместе
  с задачей.
- Ставь `--run-dir` вне проекта, если его тесты или хуки чистят `_workspace/`:
  по умолчанию туда ложится канонический ответ прогона, и чужая уборка убивает
  уже оплаченный ход. Признак хорошего пути один — он переживёт сессию.
- Запрещай side effects словами задания в любом непишущем прогоне: `read-only` —
  профиль моста, а не неподвижность диска, внешние MCP живут вне sandbox. И
  наоборот, на пишущем профиле фраза «ничего не меняй» прав не отнимает — там
  границу держит только список файлов.
- Назови явно принятые решения и неочевидную project-дельту, каждую один раз:
  **самодостаточность — единственное, чего Codex не восстановит сам**, потому что
  диалога с владельцем он не видит. Остальное — его работа, а не твоя опека.

## Правила момента «прогон вернул ответ»

- Разложи ответ на материальные выводы и закрой каждый одним из трёх исходов;
  пересказ не закрывает ни один.

  | Исход | Чем закрывается |
  |---|---|
  | **принят** | внесён в названное решение или артефакт, и ты показал evidence изменения |
  | **отклонён** | контрдоказательством из первички, а не твоим несогласием |
  | **не решён** | остаётся явно открытым, а не превращается в «менять нечего» |

- Работал Codex вне текущего состояния проекта — перечисли и забери всё, что он
  создал, прежде чем закрыть работу.
- Проверяй интегрированное состояние проекта, а не рабочее дерево исполнителя:
  результат, живущий только у Codex, не завершён.
- Закрой либо осознанно сохрани тред и временное рабочее состояние прогона и
  назови владельцу финальный статус; недоказуемая чистота оставляет прогон
  открытым, а не закрытым.

Мост: `/Users/triton/Documents/GitHub/agentic-research/experiments/codex-bridge`,
python — `<backend>/.venv/bin/python`. Ярус по умолчанию верный — `gpt-5.6-sol` и
усилие `xhigh`, передавать ничего не надо; `gpt-5.6-luna` существует для тупого и
большого, где нужна пропускная способность, а не суждение. Бесплатны и полезны до
траты: `codex_review.py --doctor --project "$PWD"`, когда мост не работает, и
`/Applications/ChatGPT.app/Contents/Resources/codex debug prompt-input` (в PATH
его нет), когда неясно, что Codex получит на вход помимо твоего задания.
Остальная runtime-правда (ledger, профили sandbox, версии) живёт в
`<backend>/README.md` и меняется вместе с кодом: сюда её не копируй, устареет
молча.
