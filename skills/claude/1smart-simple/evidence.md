# Чем проверено — 1smart-simple

## Статика, 2026-08-11 — методы в теле, references сняты

- Tracked owner, `~/.claude` и `~/.codex` побайтово совпадают:
  SHA-256 3ac9b8666f6bfa0747405cbd838e0e6fb8d8f9287634519a36a1d51bc3bbfd9c.
- Пакет 29.8 KB → 9.0 KB (3.3×): тело 7467 → 9044 байт, `references/` 22301 → 0.
  Правил в теле 38 → 33.
- Висячих ссылок на `references/` в теле нет (grep = 0).
- Codex `agents/openai.yaml` не ссылался на снятые файлы, оставлен как был.

Замер сделан по всем файлам пакета: перенос текста между файлами сжатием не
считается.

## Прогон голой фразой, 2026-08-11 — Claude Fable 5

Субагент, голый запрос «сожми этот документ, он слишком длинный: <path>».
Жертва — продуктовый документ 2600 байт с мета-текстом, парафразами и пятью
числами договора.

Наблюдаемое:

- скил поднялся без упоминания имени;
- след по всем одиннадцати методам с цитатами; два честных «пусто» с причиной;
- evidence защищено: «15% от оборота», «до 10 числа», «5000 рублей» и
  контраст-маркер «не фиксированную сумму, а 15%» перенесены дословно;
- ядро 192 символа, остановка на вопросе «Это та самая суть?»;
- оригинал не изменён.

Baseline сегодня не перезапускался; прогон 2026-08-10 зафиксировал немедленное
сокращение без остановки. Новое здесь — именованный след и защита evidence: в
теле до 2026-08-11 их не было.

Открытый вопрос из прогона: на коротком тексте критический и обоснованный
уровни рискуют совпасть — нижняя граница применимости скила не названа
(`_ops/findings/2026-08-11-064439-44476-26628.md`).

## Статика, 2026-08-10

- Tracked owner, Codex и Claude SKILL.md побайтово совпадают:
  SHA-256 3cafcbf51403a69c74c241bd787273d87921adc6ef066fdd76c09ca268fbe6f4.
- md check на tracked SKILL.md: issues=[].
- YAML frontmatter трёх копий и Codex agents/openai.yaml разбирается без ошибки.
- Старый Codex launch-prompt заменён минимальной проекцией утверждённой цели.

## Поведенческий A/B — Claude Fable 5

Support envelope: Claude Code 2.1.221, model fable, effort high, только tool
Skill, no session persistence. Один запрос в обеих ветвях; baseline запущен с
--disable-slash-commands.

Запрос: «Сократи текст: …» на продуктовом абзаце из четырёх функций, механизма
ценности и двух ограничений.

- Baseline сразу вернул готовое сокращение.
- Со скилом Claude восстановил функцию, дал ядро в 185 символов, спросил
  «Это та самая суть?» и не заменил оригинал.
- Дефект: перед ядром выведены служебная вводка и строка «Функция текста».
  Требование механики «Покажи только» не соблюдено.

Вывод этого прогона: активация и смена решения доказаны; точный контракт первого
выхода не выдержан.

## Codex CLI

Support envelope: Codex CLI 0.147.0-alpha.6.5, model gpt-5.6-sol, /tmp,
read-only sandbox, ephemeral session.

Явный запрос $1smart-simple вернул «недоступен в этой сессии». Поэтому совпавшие
ответы automatic / --disable skill_search доказывают только, что этот
CLI-harness не загрузил desktop-skill; поведение Codex app ими не проверено.
