# Cut — рефактор 2026-08-09 (baseline 95.6KB → см. evidence)

Таблица потерь. Baseline цел в git-истории этого коммита.

## Живёт (перенесено/сохранено)

Все механизмы утверждённых групп: инверсия шаблона (усилена против
SECTION-STATUS-центричной модели) · один владелец · зоны и алгоритм папок ·
каталог 15 типов + alias-роутинг · жанровые баны всех шаблонов (у 13 —
выведены Opus-агентом из Purpose/near-miss, помечено в отчёте) · BDR-профиль
целиком · ядро метадаты + description-контракт · compaction: entry gate,
frozen owners, coverage test, не-дубли, loss ledger · delegation DAG и
контракты воркеров · mode gate + два жёстких перехода.

## Несёт только имя (сжато до имени/строки)

Стандартные разделы всех шаблонов (модель знает жанр — принцип владельца) ·
процедурные пересказы system/direct mode · Coverage-ledger детали (строка) ·
approval-нюансы (3 строки) · md-tools adapter (абзац) · пять контрактов
retrieval/identity (слиты в инварианты тела).

## Потеряно (осознанно, с причиной)

- Полные таблицы Core Sections 15 шаблонов — защищали пустоту, «пустой
  шаблонный слот ничего не защищает» (SEC MAVO).
- «Shape Without Ceremony» как раздел — стал одним инвариантом (инверсия).
- Reader/multiplicity таблица каталога — свёрнута в строку дефолтов.
- Central Model / Evidence and Stop разделы старого тела — жанр не канона;
  смыслы живут в Инвариантах и Completion.
- Русский язык references — решение владельца: пакет полностью английский.

## Новое (источник — владелец 2026-08-09 либо полевое MAVO)

Аксиома прайора во вводке · инверсия шаблона как глобальный дефолт ·
исполняемый гейт против дрейфа прозы · бюджет обвязки · границы домена с
meta-refactor.md (compaction-safety).
