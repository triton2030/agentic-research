# Карта потерь — 1document-system v2

Выход `refactor.md` «После полного черновика» §1–§2. Старый пакет перечитан
целиком по `old-mechanics-map.md` (68 механизмов) и сверен с
`work/v2/draft/SKILL.md`.

Итог: **68 механизмов → 4 живут, 21 поглощены, 42 сняты с названной причиной,
1 потеря конечного состояния.** Работа остановлена на этой потере.

## Снято решением владельца — 34 механизма

Владелец отменил их прямо, и по §2 они потерями не считаются.

| Механизмы | Что снято | Основание |
| --- | --- | --- |
| 19–27 | Каталог 15 типов, alias-роутинг внутри каталога, форма шаблона, все 15 шаблонов, профиль BDR | «не называть прям буквальные документы а дать имя методики выбора нужной документации для стртапа… чем фиксировать список» — `#L19` |
| 28–34 | Системный режим целиком: порядок проектирования, greenfield, карта системы документации, карта диспозиций рефактора, closeout | «Только один документ» — `#L20` |
| 51–55 | Топология: зоны `canon/_ops/projections`, алгоритм папок, роутер, дома по умолчанию | Там же: предмет — один документ, а не корпус |
| 56–64 | `compaction-safety` целиком: entry gate, замороженные владельцы, coverage test, что не дубль, loss ledger, калибровка | Там же |
| 65–68 | `delegation` целиком: fan-out gate, staged DAG, контракты пяти воркеров | Там же |
| 4 | «Живой локальный контракт побеждает целиком; скил — fallback» | «Жанр всегда, имена — уступает» — `#L18` |
| 11, 13, 14, 23 | Бюджет слоя управления, восемь маршрутов known failures, mode gate, зарезервированный тип DOCS | Следствия снятых режимов: маршрутизировать больше не к чему |

## Живут дословно — 4 механизма

| № старый | Где в новом |
| --- | --- |
| 1 · аксиома прайора | Уникальный контекст + шаг 8 |
| 7 · инверсия шаблона | Шаг 9 |
| 15 · local first | Шаг 1 |
| 17 · допуск до материализации | Шаг 5 |

## Поглощены — 21 механизм

Смысл сохранён, носитель другой. Каждая строка проверена вопросом: выведет ли
чистый агент прежнее поведение из нового текста.

| № старый | Чем поглощён |
| --- | --- |
| 2 · цель типизированного корпуса | Цели 2 и 3 |
| 3 · пять критериев успеха | Цели 1–3 + шаги 2, 9, 12, 14 |
| 5 · семья чужого скила — живой контракт | Всегдашнее правило A2 |
| 6 · порядок claims → owner → carrier | Шаги 3→4→6: вопрос → тип → имя |
| 9 · атомарный допуск + независимый шов | Шаг 5 (атомарность) + шаг 3 (повторяющийся вопрос вместо шва) |
| 12 · дельта модели | Обоснования внутри шагов 2, 9, 10, 13 |
| 18 · completion | Шаг 14 |
| 20 · каталог-селектор, один шаблон | Шаги 3–4: выбирается термин, а не файл |
| 21 · один активный артефакт на тип+scope | Шаг 2 |
| 22 · alias-роутинг в чужие скилы | A2 |
| 24 · гейт допуска локального типа | Шаги 3 и 5 |
| 35 · один артефакт, не авто-декомпозиция | Момент вызова: один документ |
| 36 · курс direct-mode | Шаги 1–14 |
| 37 · разделы как чеклист в голове | Шаг 9 |
| 40 · два сигнала перепроверки | Шаг 14, два других сигнала |
| 43 · description как поверхность retrieval | Частично: адрес переехал в имя файла. **Уменьшение названо ниже** |
| 45 · approval только про текущий артефакт | Не поглощён. **Названо ниже** |
| 26 · режимы владения OWNER/REFERENCE/LOCAL | Шаг 12 несёт запрет держать чужое; указательная форма упрощена. **Уменьшение названо ниже** |
| 38 · одна нормативная репрезентация | Шаг 12 + цель 2 покрывают одно место; форма внутри файла не покрыта. **Названо ниже** |
| 8 · различимость правды, истории, вида, ops, evidence | Шаг 12 держит различимость правды, истории и evidence по владеющему жанру. **Вид не покрыт — см. потерю** |
| 10 · исполняемый гейт против дрейфа прозы | Не поглощён. **Названо ниже** |

## Снято по трём ситам — 8 механизмов

| № старый | Причина снятия |
| --- | --- |
| 41, 42, 44, 46 · ядро метадаты, поля, адаптеры сканера | Ни одно поле не заслужило единицу: адрес даёт имя файла. Живой реестр с front matter принимается шагом 1 |
| 39 · lifecycle draft/active/superseded | Статус документа — семья, прямо запрещённая шагом 11 как дверь провенанса |
| 21 (часть) · дома по умолчанию | Правила папок исключены: адрес — имя, поиск идёт сквозь папки |
| 37 (часть) · прогрессивное покрытие большого источника с ledger | Сильный агент делает это сам, стоя в задаче |
| 63 · форма после реза | Частный случай шага 9 |

## Названные уменьшения — не потери конечного состояния

Владельцу они предъявляются, но работу не останавливают: конечное состояние
старого пакета достигается, хотя механизм слабее.

- **`description` как отдельная поверхность поиска.** Старый пакет требовал
  писать `description` так, чтобы агент выбрал файл среди соседей до чтения
  тела; `1md-search` индексирует это поле отдельно. Новый пакет отдаёт адрес
  имени файла. Конечное состояние «найти владельца, не открывая лишних
  файлов» достигается, канал уже.
- **OWNER / REFERENCE / LOCAL.** Шаг 12 запрещает держать чужое содержимое, но
  не описывает форму указателя. Сильный агент, отправив содержимое владельцу,
  ставит ссылку сам.
- **Одна нормативная репрезентация.** Дублирование одного смысла прозой,
  таблицей и машинной спекой внутри одного файла новым текстом прямо не
  запрещено; шаг 10 снимает его как блок без собственного вопроса.
- **`approved` как личная метка владельца.** Механизм личный, эвиденса в его
  словах нет — родился синтезом агента 2026-08-09. Снят вместе с метадатой.
- **Исполняемый гейт против дрейфа прозы.** Старый инвариант 10: «проза держит
  стиль, скрипты держат структуру», из полевого MAVO. Требовать
  project-local скрипт в каждом проекте противоречит стартап-направлению;
  шаг 14 держит проверку прозой.
- **Список сознательно не написанного в Completion.** Был доказательством
  инверсии. Заменён сигналом «все слоты заполнены» по готовому файлу.

## Потеря конечного состояния — 1

### Производный вид больше не отделён от правды

**Что потеряно.** Механизмы 47–50 и часть 8. Старое тело называло это прямо:
«promotion of any view into a second truth is the central failure this system
exists to prevent». Пакет держал это тремя вещами:

- проекция несёт `derived-from` на источник и `depends-on` на каждую секцию,
  изменение которой её фальсифицирует;
- проекция может переупорядочить, подчеркнуть и объяснить, но не может
  добавить изменяемую правду, продвинуть гипотезу в факт, скрыть противоречие
  или переопределить владельца;
- проекция никогда не источник для правды, и дефолтный поиск её игнорирует.

**Почему это не покрыто новым текстом.** Читательский вид — тоже документ,
поэтому он попадает в новый момент вызова «любое письмо документа». Шаг 12
разводит содержимое по владеющим жанрам, но вид не является чужим жанром: он
пересказывает ту же правду для другого читателя, законно и намеренно. Цель 2
даёт «один ответ — одно место», и сильный агент из неё выведет, что вид не
владеет правдой. Но она не даёт **устаревания**: когда источник изменился, вид
остаётся выглядеть текущим, и следующий агент читает его как правду.

**Почему это конечное состояние, а не механизм.** Старый пакет объявлял
предотвращение второй правды смыслом собственного существования. Ни одно
решение владельца этого не отменяло: он снял корпусную работу и каталог типов,
о проекциях не говорил.

**Цена восстановления.** Одна-две единицы в теле — вид называет источник, и
изменившийся источник делает вид устаревшим до перепроверки. Бюджет позволяет:
сейчас 16 из 20.

**Состояние.** `нужен ответ владельца`.
