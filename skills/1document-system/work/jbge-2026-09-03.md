# Адресная правка — 2026-09-03

Пользователь утвердил пять предложенных изменений и установку: «Да, отлично,
вноси изменения». Выбранный текст сохранён как selection в
`_ops/chat-recall/2026-09-03-160008-codex-01a066ea.md:17`; исходная речь — строка 16.
Scope: обязательный триггер любой документации; необходимая читателю дельта
вместо всего установленного; мотив против достраивания; условие остановки;
применение к документам других скилов с сохранением их контрактов.

Маршрут адресный: согласованы пять точечных изменений, включая сохранение
правила уточнять существующий абзац. Остальной пакет остаётся в силе.
Требование повторного approval из 1skill-creation уступает прямому разрешению
пользователя внести эти изменения; новый рефактор структуры не заказан.

Проверяемое свойство: достаточность определяется задачей читателя. Фальсификатор:
правка одного факта создаёт справочные разделы, новый документ заполняется ради
шаблона либо документ другого скила освобождается от минимальной достаточности
или теряет обязательное поле. Эти три случая входят в независимую проверку.
Новая задача согласует тело с расширенным триггером; правка new-home убирает
тот же запрет на ссылку по одному признаку чужого скила. Решение и причина
по-прежнему живут раздельно; архив и датированные записи не переписываются.

## Проверка

Одна волна: `jbge_instructions` и `jbge_trajectory`, отдельные чистые окна.
Первый нашёл шестой критерий против лимита пяти и прежний порядок разделов.
Шестой сохранён по прямому утверждению владельца; лимит пяти не выполнен.
Перестановка прежних разделов отклонена как незаказанный рефактор.
Второй не нашёл новых регрессий в трёх случаях, но указал на старое требование
копировать весь набор разделов соседа. Принято уточнение «обязательный набор»:
обычный абзац остаётся достаточным без заполнения необязательных слотов.
Это независимый прогноз по тексту, не поведенческий прогон модели.

Механическая сверка: description совпадает с утверждённым (157 символов),
семь адресов стадий существуют; existing-home, derivative и overgrown
побайтно сохранены, как и правило о раздельной записи решения и причины.
Codex UI prompt синхронизирован с новым триггером любой документации.

Установлено в Claude и Codex через `sync_simple_projections.py --write --install`;
`--check` подтвердил полный состав и байтовое совпадение обеих tracked и обеих
installed проекций с owner. Все локальные ссылки проверены в пяти копиях.
