# Решения допуска — 1document-system v3

Выход `behavior-protocol.md` плюс разбор волны проверки. Пакет — один файл
`draft/SKILL.md`, sha256 `8eeef44d…`: 5 критериев принятия и 4 единицы
протокола, 93 строки. `name` и `description` английские по
контракту `skill-short-description.md` (199 знаков из 200), тело русское по
решению владельца (`#L26`).

## Метод отбора, заданный владельцем

Владелец дал два правила, и оба применялись к каждому кандидату:

- «очень много можно удалить если просто дать цель… ведь агент знает о них»
  (`#L53`) — механизм остаётся, только если **спорит** с дефолтом модели, а не
  совпадает с ним;
- «Даже если я дал буквально какие то протоколы или инструкции не значит что мы
  их не имеет права отменять в случае если уже текст цели и задачи уже
  подразумевает что то что я говорю будет выполнено… протокол просто потеря
  внимания агента и даже вред» (`#L54`) — его собственные способы проходят тот
  же отбор.

Третье правило — из самого `behavior-protocol.md` §2: критерий принятия
предпочитается инструкции, потому что не даёт агенту отвертеться, но оставляет
выбор способа.

## Девять способов владельца — ни один не выжил как протокол

| Способ | Адрес | Судьба и основание |
| --- | --- | --- |
| Мыслить как информационный архитектор, когда дома нет: какую папку создать по смыслу, какой файл с каким именем и почему | `#L42` | **отменён.** «лежит там, где её будет искать следующий агент» уже требует выбирать дом по вопросу читателя. Неочевидная часть — не изобретать свою схему имени — стала критерием 2 |
| Неуверенное не пишется в файл, а называется в чате одной строкой | `#L38` | **уже критерий**, третий; инструкцией не становится |
| Причина решения пишется тем же вызовом вторым файлом | `#L48` | **результат оставлен, маршрут отменён.** «причина не живёт в документе с решённым» выводится из критериев 1 и 2. «Два файла в один ход» — тот самый лишний протокол: в проекте, где типа записи решения нет, он заставит агента его изобрести вопреки критерию 2 |
| Чистка перед записью ограничена разделом, куда встаёт новое | `#L45` | **отменён.** Стоит в Цели (третье предложение) и в критерии 4 |
| Признак грязного: противоречие и устаревшее, чужой жанр, структурная грязь | `#L46` | **поглощён.** Устаревший ответ — критерий 4; чужой жанр — критерий 1; структурная грязь — критерий 5 |
| Границу правки чужих файлов агент выводит из цели | `#L50` | ✓ инструкцией и не был; граница стоит в третьем предложении Цели |
| `1ia-audit` вливается целиком и снимается | `#L39` | решение о пакете, не о поведении |
| Имя `1document-system` сохраняется | `#L52` | решение о пакете |
| Пакет по-русски | `#L26` | решение о пакете |

## Критерии принятия — что в каждый вошло

| № | Ядро | Что поглотил | Что закрывает |
| --- | --- | --- | --- |
| 1 | Термины и дробность — владельца; тело не пересказывает разговор | M2 (тело говорит своим жанром), M4 (пиши то, что владелец узнает как своё), H19 | Провал «строка, которой ты не говорил» и провал «файл в серой зоне» с одной стороны |
| 2 | Имя в одном ряду с соседями того же класса; без соседей — ближайший стандартный термин | M1, A1, A2, B3, B6 | Провал «файл в серой зоне» со стороны имени и провал «записалось не туда» |
| 3 | Ход закрыт перечнем: что установлено и где легло; не легло — какого владельца не нашлось | M5, M7, A9, G16, H5 | Лазейку «записать одну строку и объявить остальное неразмещённым»; делает критерии 1, 4 и 5 проверяемыми снаружи |
| 4 | Дифф несёт только текст этого ответа; расхождение не закрывается удалением | M3, A6, C4, F24, G3, C9 | Лазейку «переписать чужой большой документ» и невидимый выбор за владельца |
| 5 | Прирост объёма равен установленному; обрамление не содержание | M9, A5, A8, H8, H9, H12, B5 | Лазейку «заполнить пустые разделы типа» и двигатель раздувания по инерции |

## Допущенные инструкции — 4 единицы

Только то, что не выводится из намерения и что критерий этого хода не ловит:
провал наступает позже во времени либо в зоне чужого владельца.

| № | Дефолт агента | Материальный вред без неё | След исполнения |
| --- | --- | --- | --- |
| 1 | Пишет отчёт или выжимку, не называя, откуда и на каком состоянии источника она снята | Пересказ становится второй правдой; через месяц оба документа выглядят верными, и следующий агент действует по короткому | В теле пересказа назван источник и его состояние на момент снятия |
| 2 | Считает пересказ текущим, пока файл выглядит свежим | Источник ушёл вперёд, а действуют по пересказу — он короче и понятнее. Свежая дата и целые ссылки этого не ловят | Пересказ, чей источник изменился, помечен требующим перепроверки либо перепроверен |
| 3 | Типизирует и перекраивает чужие артефакты, раз момент вызова широк | Ломаются контракты `1planning`, `1plan-task`, `1findings`, `1product-shaping`, `1instruction-authoring` | Содержимое отправлено во владеющий скил; форма чужой семьи в диффе не менялась |
| 4 | Чинит переросший файл письмом — сжимает или дописывает; либо дробит по заголовкам | 267 KB в mavo-short2 письмом не лечится, а дробление по темам даёт много слабых поверхностей правды вместо одной сильной | Разделение прошло отдельным ходом; у каждой новой части назван свой читатель, владелец и проверка |

Единицы 1 и 2 — восстановленное владельцем правило (`#L27`), потерей быть не
могут. Единица 3 — A11 v2 дословно. Единица 4 добавлена по ответу владельца на
найденную потерю: разделение переросшего документа осталось без владельца после
снятия `1ia-audit`.

## Почему clean-room retry не выполнялся

`refactor.md`: «Поздняя локальная правка сохраняет этот результат, пока не
могла изменить функцию, commander's intent, карту потерь или clean-room
смысловую структуру». Единица 4 добавлена после возврата чистой комнаты и:
функцию не меняет; намерение не меняет (Уникальный контекст, Задача, Цель и
Критерии остались утверждёнными байтами); карту потерь **закрывает**, а не
переписывает; смысловую структуру чистой комнаты не трогает — среди её восьми
механизмов разделения не было и быть не могло, потому что намерение о нём не
говорит. Retry по §2 разрешён, но причинно не затронут ни один более ранний
этап.

## Правки намерения после его утверждения — полный перечень

Прежняя редакция этого раздела называла две правки. Проверяющий доказал, что
изменены **все пять критериев принятия**, а не только Цель. Ниже — построчно;
владельцу предъявляется целиком, его утверждение прежней редакции этих байтов
не покрывает.

### Цель

| Было утверждено | Стало | Почему |
| --- | --- | --- |
| «Место записи чисто» | «Рядом с новым текстом не осталось прежней версии того же ответа» | Четвёртый внутренний конфликт: прежняя формулировка требовала чистить соседний устаревший абзац про другое, а критерий 4 это запрещал |
| — | добавлено «среди живых поверхностей правды… архив и датированные записи прошлого расхождением не считаются» | M6 чистой комнаты: буквальное «в проекте не осталось» либо запускает обход всего репо, либо приводит к правке архива и записанных слов владельца. **Owner-источника у строки нет — вынесено владельцу отдельным вопросом** |
| — | добавлено «решённое — в документе своего класса, причина решения — в документе класса „решение“, если такой класс в проекте есть» | Восстановление решения владельца `#L44`, которое я ошибочно объявил выводимым |

### Критерии принятия

| № | Было утверждено | Стало | Почему |
| --- | --- | --- | --- |
| 1 | «Каждая добавленная строка восходит к словам владельца или к уже принятому решению проекта» | добавлены адрес источника вне тела и жанровый запрет (мнение, хроника работы, пересказ разговора) | M2 без носителя атрибуции непроверяем; жанровый запрет — P-003, «сердце скила», снятый мной без нового evidence |
| 2 | «По имени файла и его первому экрану видно, какой это деловой тип» | имя, обязательная шапка и набор разделов как у соседей; клауза greenfield; гейт на новый тип | `#L23` уступает реестру имена, **разделы и метадату** — в черновике была только первая треть. `SEC — Canon Section Contract` в MAVO прямо адресуется этому скилу |
| 3 | «либо названо владельцу в чате одной строкой… порог уверенности не повод закрыть ход пустым» | перечень в чате; строка для неуверенного; предлагаемый дом; где искал прежний ответ | Канал владельца `#L38` был снят и его причина прямо запрещена; поиск по вопросу числился поглощённым, но в байтах отсутствовал; порог «большинство» — числовой бюджет, против которых возражает этот же файл |
| 4 | «Правка, объяснимая чистотой файла вообще, — не успех» | радиус: за пределами раздела не тронуто, внутри — чистка разрешена и называется в перечне | Я **инвертировал** `#L43`/`#L45`: владелец дал ограниченную радиусом обязанность чистить, черновик выдал безусловный запрет |
| 5 | «Рост без нового охвата — не успех» | список обрамления плюс исключение для разделов, обязательных по контракту типа | Модель не классифицирует обрамление как прирост; без исключения критерий 5 давал основание не писать обязательные заголовки |

## Trigger-проверка

Выход `skill-short-description.md` §3–4. `description`: «Use when recording
anything that outlives the conversation into a project document: put it where it
will be looked for and write only what was established. Not chat, code, or other
skills' artifacts.»

| Фраза | Ожидание | Результат |
| --- | --- | --- |
| «запиши всё, что мы обсудили про интерфейс» | use | вызывает: долгоживущее идёт в документ проекта |
| «обнови спеку — мы поменяли схему оплаты» | use | вызывает: запись в существующий документ |
| «зафиксируй правила скидок в документации» | use | вызывает |
| «поправь опечатку в README» | skip | не вызывает: долгоживущего не появилось |
| «объясни в чате, как работает роутинг» | skip | не вызывает: «Not chat» |
| «добавь комментарий к этой функции» | skip | не вызывает: «Not code» |
| «запиши это, я только что решил» | near-miss против `1chat-recall` | **оба срабатывают, и это верно.** `1chat-recall` сохраняет дословные слова владельца в корпус, этот скил кладёт установленную правду в документ своего класса. Границу держит третья единица протокола: записанные слова владельца — чужая семья и им не типизируется |
| «отметь эту проблему, разберёмся потом» | near-miss против `1findings` | уходит к соседу: находки названы чужой семьёй, и «разберёмся потом» — не долгоживущая правда |
| «запиши задачу по этой работе» | near-miss против `1plan-task` | уходит к соседу фразой «other skills' artifacts» |
| «раздели этот огромный документ на части» | near-miss | **вызывает этот скил** и упирается в четвёртую единицу: разделение попутно с записью не делается, работа называется строкой перечня. Осознанно — прежний адресат `1ia-audit` снят решением владельца |

## Разбор волны проверки

Обе волны израсходованы. Вернулись 27 находок: 10 от проверяющего траектории,
17 от буквального. **Принято 27, отклонено 0.** Ни одна не оказалась
неподтверждённой, и ни одну не удалось отклонить прямым evidence.

Три находки исправляют мою собственную ошибку метода: я применил правило
владельца об отмене протоколов (`#L54`) к трём его решениям, которые намерение
на самом деле не подразумевало, — чистка раздела (`#L43`, `#L45`), разделение
решённого и причины (`#L44`), уступка разделов и метадаты реестру (`#L23`).
Проверка на живом корпусе mavo-short2 показала, что ни одно из них не
выводится.

Четвёртая ошибка того же рода — снятие жанрового запрета P-003. Основанием было
«модель это знает». `knowledge/practical-guides/how-to-write-skills/authoring-canon.md`
называет это прямо неверным критерием: «„модель это знает" не является критерием
удаления правила. Различай knowledge-gap и behavior-gap: если модель может
пересказать правило и всё равно нарушает его, разрыв дефолта реален». Жалоба
владельца — «агенты начинают писать какие-то свои мнения или превращают документ
спецификации в документ летописи» — это behavior-gap. Запрет возвращён в
критерий 1 вместе с предъявляемым артефактом, которого требует тот же канон.

## Вопросы, вынесенные владельцу

- Строка Цели «среди живых поверхностей правды; архив и датированные записи
  прошлого расхождением не считаются» owner-источника не имеет: она пришла из
  чистой комнаты, а не из его слов.
- Его дословное «нельзя писать новое в грязные документы всё должно быть чисто»
  (`#L43`) сужено его же позднейшими ответами `#L45` и `#L46` до радиуса
  раздела. Разворот предъявляется прямо.
- Право единолично ввести новый тип документа там, где проект не поставил
  гейта: критерий 2 сейчас это разрешает.

## Сознательно не допущено

| Отклонено | Причина |
| --- | --- |
| Reference-файл выбора типа | Сведён в клаузу критерия 2 по методу владельца (`#L53`): именованный стандарт вместо выписанной методики |
| Каталог типов документов | Отменён владельцем 2026-08-31 (`#L24`) |
| Обязательные поля метадаты и схема провенанса | Проектная конвенция; критерий 2 её принимает, а буквальная схема сделала бы скил проектно-зависимым |
| Отдельный протокол уточнений у владельца | Отменён им внутри той же реплики: «на каждый тогда чих будет меня спрашивать» (`#L34`) |
| Ссылка на «лучшие практики информационной архитектуры» голым текстом | Именно этот прайор даёт Diátaxis, docs-сайт, таксономию и полноту покрытия — источник раздувания, из-за которого заказан рефактор. Уникальный Контекст называет расхождение с ним вместо ссылки |
| Числовые бюджеты длины | Произвольны между проектами, создают добивание до числа |
| Цикл ревью готового документа | Момент вызова — запись; критерий 3 закрывает ход перечнем, а не приёмкой |
