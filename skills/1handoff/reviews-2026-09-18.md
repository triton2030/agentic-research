# Проверка адресной правки — 2026-09-18

Заказ: четыре правки после пробы свежим агентом на реальном пакете
(`draft-2026-09-18/probe-2026-09-18.md`). Владелец: «Понял, давай исправляй
все четыре пункта» (`_ops/chat-recall/2026-09-18-165101-claude-9f9b4643.md#L18`).
Три проверяющих `1skill-creation` в свежих окнах, Claude Opus 5, первая волна;
затем одна волна проверяющего траектории по затронутым единицам.

## Находки первой волны и решения

| Находка | Кто | Решение |
| --- | --- | --- |
| У остатка работы нет исхода «остатка нет»: короткая сессия заводит пустой файл задачи или лжёт блокером | смысл, требования, траектория | принято: третий исход в действии 4 и в строке скелета |
| У чужих незакоммиченных правок нет своей пометки; «остаток, который можно снять» читается как разрешение их снести | смысл, траектория (прецедент `_ops/handoffs/2026-08-25-115213-opus-5.md#L18`) | принято: третье значение «чужое или неизвестного происхождения — не снимать» в части 3, части 5 и якоре |
| «Знает, что делать… или находит файл» позволяет выписать очередь в пакет | смысл | принято: критерий части 5 требует файл, подтверждение или блокер |
| «Заведи через `1plan-task`» без условия допуска: агент строит эпик на закрытии или всегда отговаривается | требования, траектория | принято: файл только там, где планирование живёт и заказ допущен словом владельца; иначе блокер с адресом слов о заказе |
| Действие 4 не требует сверить найденный файл задачи с сделанным; проба показала лгущий статус | траектория | принято: сверка статуса и счёта подзадач в действии 4 |
| Вставка про причину Next Step внутри п. 4 части 4 расширяет п. 5 на второй ярлык | смысл, требования | принято: отдельный п. 7, только `knowledge`; распространён на рекомендации |
| У правки про Next Step нет зеркала в части 5 | траектория | принято: критерий evidence расширен на причину и рекомендации |
| Упакованный предикат в критерии части 5 про вопросы | требования | принято: отдельный буллет |
| Перевывод действия по рабочему дереву будет срабатывать почти всегда | требования (прогноз) | принято: перевывод по HEAD, `git status` — сверка перед правкой незакоммиченного |
| Статус вопроса не несёт, на чём сессия шла без ответа | траектория | принято: допущение или независимая работа в критерии и строке скелета |
| `handed off` получил второй смысл; исход действия 4 не вписывается в `cleanup` | траектория | принято: `владельца нет` + фраза, что исход живёт в строке остатка |
| Останавливающий вопрос получил третий дом | траектория | принято: фраза в п. 3 части 4 |
| Вложенный плейсхолдер в строке вопроса | смысл | принято |
| Числа пробы не воспроизводятся (67 и 52) | траектория | принято: проба исправлена на пересчёт (103 пакета; 18 из 22 текущей формы) |
| Первая половина действия 4 дублирует действия 1–3 | требования | принято частично: невыводимый случай впереди, сверка оставлена как restatement в точке действия по наблюдению пробы |
| Четвёртый предикат в первом пункте цели пользователя | требования (заметка) | оставлен сознательно как формулировка цели правки |

## Что не проверялось

Реальный хендоф по новому тексту ещё не делался; поведение под ним — `candidate`
до следующего пакета и повторной пробы потребителя тем же способом.

## Вторая волна — проверяющий траектории по затронутым единицам

| Находка | Решение |
| --- | --- |
| Обязательный recall-адрес заказа противоречит контракту `1chat-recall`: обычный заказ в корпус не идёт | принято: либо адрес, либо блокер `no recall address` из части 1 с возвратом туда |
| «Допущен словом владельца» расходится с `1plan-task`: допуск даёт `1planning` или эпик | принято |
| Сверка файла задачи без маршрута починки; правка выпадала из `cleanup` | принято: починка через `1plan-task`, правка остаётся в `cleanup`, в строку остатка идёт только владелец |
| У заблокированной починки нет значения в строке остатка | принято: четвёртое значение — путь, расхождение, последствие |
| Блокер «владельца нет» садится в середину файла | принято: повтор в зоне после преамбулы |
| Слово «остаток» в двух смыслах | принято: пометка дерева переименована в `снять можно` |
| Якорь `working tree` не помечает процессы и папки; `clean` закрывал всё | принято: правки и процессы разведены |
| Верхняя зона скелета молчит о вопросе владельцу | принято |
| Часть 5 требовала состояние каждого вопроса, часть 3 пропускала только меняющие продолжение | принято: критерий сужен |
| «Каждой рекомендации» в Next Step легализует очередь в разделе | принято: «если рекомендация в разделе всё же есть» |
| Вопрос от владельца, оставленный сессией без ответа, дома не имеет | отклонено: за границей заказа, проба такого случая не показала; кандидат в находку при первом наблюдении |
