# Read it as the next agent

Drop what you know from this chat and read the file you just wrote as the agent
who will receive it: this repository, this path, no conversation.

1. The packet passes only when that reader can name and begin the correct next
   action and say why it is correct.
2. It passes only when that reader can tell a dated claim from live truth and
   find the evidence for material state.
3. It passes only when every pending decision, veto, blocker, risk and
   unverified assumption that can change continuation is visible to them.
4. It passes only when that reader avoids every dead end this session paid for
   and every false reading of the terrain that survived it, and can locate the
   current owners of live truth.
5. Having written all the named sections is not a stop condition: while any of
   the above is still false, repair the packet or report the exact blocker.
6. Return the exact packet path, or the exact blocker, plus one line describing
   the packet when it exists.
7. Report each independent outcome once — `recall` as `captured`,
   `no qualifying evidence` or `blocked`; `cleanup` as the named changes,
   `nothing to clean` or `handed off`; `packet` as the exact path or `blocked`;
   `consumer check` as `passed` or the exact blocker — and remember that a
   blocked outcome does not erase a successful one.
8. State residual risk only for context you could not reach or claims you could
   not verify, and stop when more text or more cleanup cannot change what the
   next agent will do.
