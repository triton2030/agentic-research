# Product Frame — 1chat-recall

## Цель

Когда существенное решение может зависеть от прежних слов владельца, будущий
агент восстанавливает не набор похожих цитат, а временный decision-ready
контекст: применимую позицию, минимальную контекстную дельту, вытесненное более
раннее решение, scope, provenance и честные gaps. После этого агент либо
продолжает работу без повторного вопроса владельцу, либо abstain-ится, если
применимость или хронология не разрешены.

Потребитель продукта — агент; выгодоприобретатель — владелец, которому не нужно
повторять уже сказанное. Цитаты, hybrid retrieval, `context-note`,
`session-context`, timeline и repair — обслуживающие возможности. Мера успеха
— корректное продолжение текущей работы, а не количество найденных совпадений
или полнота quote dump.

Acceptance retrieval: default query возвращает один маршрут — `holders`,
ранжирующий session-файлы по полному `session-context` и буквальным цитатам.
Каждый holder даёт `semantic_rank`, полный `session_context`, относительный
возраст, сильнейшую matching quote и counts по type/topic. Промежуточного слоя
пересказов между запросом и словами владельца не существует: агент приходит
прямо к цитате. Проверка более нового evidence и живого project-local owner-а
обязательна; самодостаточная цитата не требует чтения полного holder-а, holder
открывается, когда применимость зависит от сцены или chronology. Длина файла,
сырое число цитат и counts сами по себе ranking не повышают.

Для важной темы главный агент сначала выполняет собственный поиск и может в
начале либо по ходу работы отправить ровно одного фонового субагента за
независимым verdict. Основной ход его не ждёт; вернувшиеся новые адреса
учитываются в следующем относящемся к теме решении.

Рабочий контекст остаётся transient synthesis с source evidence. Он не создаёт
постоянный «профиль владельца» и не превращает датированный recall-лог в
текущий канон.

Acceptance capture: агент сначала решает, является ли реплика самостоятельным
важным тезисом или содержательным выбором. Принятая реплика атомарно сохраняется одной записью — второго
шага, меняющего другой файл, у capture больше нет. Качество поиска покупается
на записи, а не производным слоем: перед каждой quote/selection-записью агент полностью
читает `_ops/chat-recall/topics.md` и выбирает тему по границе предмета;
`context-note` остаётся короткой keyword-like поисковой поверхностью из
недостающих именованных референтов, имён артефактов и полезных синонимов;
отменяющая реплика несёт адрес отменённой, а неразрешённый конфликт помечается
вместо молчаливого выбора одной из двух позиций владельца.

Provenance сообщения и авторство фрагмента — разные проверки. Прямое
user-visible сообщение может содержать вставленные документы, целые разговоры
и слова других людей или агентов; tool/controller/subagent follow-up не
становится речью владельца из-за carrier-а `role=user`. В corpus попадают только
подтверждённые owner-authored фрагменты, сокращённые исключительно удалением с
сохранением слов и порядка. Неясное авторство означает gap, а не догадку.

## Тайбрейкеры

1. При конфликте false application и missed recall побеждает abstention: лучше
   переспросить, чем уверенно применить stale или wrong-scope позицию.
2. При конфликте semantic rank и применимости побеждают scope, commitment,
   chronology и более свежие слова. Owner evidence — буквальная цитата;
   `session-context`, тема и scores только предлагают маршрут чтения.
3. При конфликте recall и более свежего project-local owner-а побеждает live
   owner; расхождение остаётся видимым.
4. По умолчанию ищется только project-local corpus. Cross-project recall
   разрешён лишь явным scope и не смешивается молча с локальной позицией.
5. У каждой owner-записи (`quote` или `selection`) `context-note` обязателен и короток: это keyword-like
   noun phrases — недостающие именованные референты, устойчивые имена артефактов
   и полезные поисковые синонимы; у непрозрачного `selection` он называет
   выбранный вариант. Это не проза, summary или текущая правда;
   повтор/перефразирование цитаты и расширение scope запрещены. Если
   неповторяющиеся поисковые ключи нельзя назвать, агент возвращается к source
   context, а не выдумывает их и не оставляет поле пустым.
6. При конфликте удобства пересказа и надёжности evidence побеждает цитата:
   производный слой между запросом и словами владельца не заводится. Отмена
   прежней позиции живёт указателем на самой записи, а неразрешённый конфликт
   помечается, а не решается молча.
7. Transcript helper подтверждает message-level provenance, timestamp и source
   address, но не внутреннее авторство каждого вставленного фрагмента. Последнее
   остаётся решением агента по deletion-only границе Capture.

## Аппетит

- Local/offline retrieval обязателен; цитаты не отправляются в сеть.
- Не вводить второй truth-store, ссылки на transcript или постоянный профиль
  владельца. Цитаты — единственное evidence; `topics.md` владеет только
  словарём тем и позиций не хранит.
- Перенос слов владельца в план, документацию или спецификацию — отдельный
  режим отдельной сессии, а не побочный эффект capture и не производный слой
  рядом с корпусом.
- Runtime требует `session-context` только для новых и затронутых сессий;
  отсутствие у untouched legacy-файла само по себе не diagnostic. Одноразовый
  owner-requested backfill допустим как сборка реального evaluation corpus:
  карточка выводится после чтения полного transcript, записи цитат не меняются.
- Допустимы одноразовый model/cache bootstrap и несколько секунд на
  consequential query; дешёвый lexical fallback и честный abstain сохраняются.
- Обычный Retrieval читает только сохранённый quote corpus. Native transcript
  доступен лишь для явно заказанного владельцем Repair/backfill named session и
  не является fallback пустого поиска.
- Пересмотреть Frame при повторном применении stale/wrong-scope evidence,
  quote-dump вместо решения, систематических лишних вопросах владельцу или
  смене рабочего model set.

Исходная рамка подтверждена владельцем 2026-08-04; tie-breaker 5 скорректирован
владельцем 2026-08-05; `session-context` подтверждена 2026-08-14;
один фоновый субагент подтверждён владельцем 2026-08-25. 2026-08-28 владелец
снял производный слой тем целиком («сам протокол был неправильный»,
`_ops/chat-recall/2026-08-28-061115-claude-d84f79af.md:22`), оставив прямой
поиск по цитатам, карту тем как словарь и подписав усиление `context-note`
вместе с полями отмены и спорной позиции.

2026-08-29 владелец усилил Capture: перед quote-записью надо полностью читать
существующую карту тем, потому что плохая metadata делает цитату практически
потерянной; `context-note` должен быть коротким и похожим на набор ключевых
слов для текущего lexical search, а не на прозаический контекст —
`_ops/chat-recall/2026-08-29-150002-codex-01a04cf3.md:19` и `:20`.

2026-08-31 владелец уточнил источник Retrieval: обычный агент читает только
ранее записанные цитаты из quote corpus, а не native session transcript —
`_ops/chat-recall/2026-08-29-150002-codex-01a04cf3.md:28`. После проверки
реалистичных сценариев он одобрил corpus-only цель и установку — там же, `:30`.

Позднее 2026-08-31 владелец уточнил границу самой цитаты: его сообщение может
содержать вставленный документ или разговор, поэтому сокращение допустимо
только удалением; user-visible сообщение от другого агента не является его
цитатой. Он также потребовал сверить терминальные команды и ответы helper-а с
runtime-контрактом — тот же holder, `:31` и `:32`.

Позднее в той же сессии владелец выбрал независимую reference-topology и
распространение metadata-протокола на Repair и `selection` —
`_ops/chat-recall/2026-08-29-150002-codex-01a04cf3.md:21`. Живая граница
запрещает превращать прежнюю hot-body реализацию в вечную архитектуру, если
компактная topology наблюдаемо сохраняет инвариант надёжного исполнения —
`_ops/chat-recall/2026-08-29-163434-codex-01a04d4a.md:15`.

Аппетит не является стоп-линией. 2026-08-20 владелец снял его запреты как
препятствие: «если идеи есть то мы просто отменим все запреты и рамки которые
были до этого» — `_ops/chat-recall/2026-08-19-221010-claude-3a023b71.md:26`,
после того как агент остановился на запрете вместо предъявления идей. Запреты
остаются действующим выбором и продолжают требовать обоснования при снятии, но
агент обязан сначала показать идею и её цену, а не вернуть вопрос. Рабочие
targets: GPT-5.6 и общий Claude core для Opus 5/Fable 5; projections сохраняют
один продуктовый и causal contract.
