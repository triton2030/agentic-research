Вердикт по механизму: **невыполним в текущей Codex-сессии** — callable schema не содержит операций создания и повторного обращения к проектному треду; дополнительно найдены воспроизводимые расхождения команд с текстом роли. Это не доказывает отсутствия механизма в других конфигурациях Codex.

**Находки**

1. [SKILL.md:55](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/SKILL.md:55) → **обязательный транспорт недоступен** → В текущей схеме и доступном каталоге инструментов отсутствуют создание проектного треда, отправка сообщения в тред другой сессии, чтение его ответа и `wait_threads`. Есть `collaboration.spawn_agent`, `followup_task`, `send_message`, `wait_agent`, но их контракт адресует субагентов текущего дерева, а не произвольные сессии проекта. Схема не обещает для них `clientThreadId` / `threadId`; `wait_agent` возвращает уведомление об обновлении, без `afterCursor`. [execute.md:95](/Users/triton/.codex/skills/1orchestration/references/execute.md:95) описывает ожидаемую механику, но не предоставляет отсутствующие операции. → **Минимальная правка:** явно ограничить маршрут средой с подтверждёнными операциями создания, продолжения и чтения проектного треда; при их отсутствии сразу применять состояние «психолог недоступен». Одной текстовой правкой обеспечить транспорт здесь нельзя.

2. [SKILL.md:63](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/SKILL.md:63) → **занятость треда разрешает разрушить общий адрес психолога** → Текст ставит «занят» рядом с «закрыт» и «не найден» и предписывает создать новый тред. При одновременных вопросах двух сессий это разрешает второй сессии заменить указатель на работающего психолога. Аналогично две первые сессии без файла могут независимо создать два треда. Это противоречит «один на проект». → **Минимальная правка:** занятость означает ожидание освобождения; перед первым созданием согласовать единственного создателя и повторно проверить указатель. Замену разрешать после подтверждения невозможности продолжить прежний тред.

3. [agents/psychologist.md:112](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/agents/psychologist.md:112) → **предписанная команда не запускается под текущим sandbox, а `--lexical` не устраняет эту причину** → Даже `uv run --locked --script …/chat_digest.py --help` завершился с кодом 2: `Failed to initialize cache …/.cache/uv`, `Operation not permitted`. До обработки флагов скрипта выполнение не дошло. Через `python3 -B` справка, timeline, show и lexical работают. По коду `--prepare` допускает загрузку модели из сети; hybrid также открывает записываемые lock и SQLite-кеш. → **Минимальная правка:** назвать требования к записываемым кешам и сети для подготовки; при блокировке самого `uv` дать проверенный запуск через `python3 -B` для timeline/show/check и `--lexical`. Не обещать, что один `--lexical` исправляет запуск через заблокированный `uv`.

4. [agents/psychologist.md:117](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/agents/psychologist.md:117) → **`--show <адрес>` неверен** → Проба с `2026-08-20-100000-claude-0f0f0f0f.md#L21` вернула `record_id не найден`. С `cr-e7b40a04a766ee06` команда успешно вернула полную запись. [chat_digest.py:2119](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/scripts/chat_digest.py:2119) сравнивает аргумент именно с `record_id`. → **Минимальная правка:** написать `--show <record_id>`; отдельно указать, что полный разговор читается непосредственно из файла.

5. [agents/psychologist.md:65](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/agents/psychologist.md:65) → **отсутствие общего усечения не гарантирует полного чтения слоя личности** → `--type обо-мне,предпочтение --timeline --max-chars 20000` показал `3/3 records shown`, но обрезал две цитаты многоточием. Причина — независимый `--head`, по умолчанию 110 символов. Увеличение `--limit` и `--max-chars` его не меняет. → **Минимальная правка:** для полного чтения этих записей явно добавить `--json`, расширяя лимиты до `truncated:false`, либо открывать полные записи/файлы.

6. [agents/psychologist.md:91](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/agents/psychologist.md:91) → **`--since` не является проверкой всех изменений корпуса после прошлого ответа** → Фильтр сравнивает дату исходной реплики, включительно. Проба `--since 2026-08-24` вернула записи за 24 и 25 августа; timestamp со временем отвергнут с требованием `YYYY-MM-DD`. Позднее добавленный backfill старой реплики этот фильтр пропустит, как и изменения метаданных старой записи. → **Минимальная правка:** назвать формат и включительную границу; оговорить, что после backfill/repair нужно перечитать затронутые разговоры независимо от `--since`.

7. [SKILL.md:50](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/SKILL.md:50) → **абсолютный запрет чтения корпуса противоречит другим режимам** → [capture.md:46](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/references/capture.md:46) требует от сохраняющего агента прочитать текущий файл целиком; Integrity требует сравнения сохранённого с источником. Психолог при этом не меняет файлы. → **Минимальная правка:** уточнить, что исключительность психолога относится к retrieval прежней позиции; назвать техническое чтение для Capture и явно заказанного Integrity исключениями.

**Держится**

- Передача полного текста роли, абсолютного `TARGET_PROJECT_ROOT` и первого вопроса достаточна для явного задания роли: автоматическая регистрация `agents/psychologist.md` здесь не требуется.
- На тестовых шести записях `--type обо-мне,предпочтение --timeline --json --max-chars 20000` вернул все три подходящие записи с `truncated:false`. При 512 символах честно сообщил усечение.
- `--show <record_id>` возвращает полную запись; лексический запрос «заявка клиента» нашёл нужный разговор. Скрипт и lock-файл совпадают с прежним установленным вариантом.
- Файл-указатель как локальное хранилище идентификатора допустим. **Доказательств, что именованный тред или нативный реестр здесь доступны и лучше, нет.** Но файл не делает идентификатор адресуемым: это должен подтверждать транспорт. Формат строки стоит уточнить как `<threadId><TAB><дата>`, чтобы дата не попала в аргумент операции.
- В свежей сессии **без файла** выполнение здесь остановится перед созданием психолога; **с файлом** — перед отправкой вопроса. Уже предусмотренный [retrieval.md:44](/Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/references/retrieval.md:44) сохраняет честное поведение: зависимое решение остаётся открытым, независимая работа продолжается.

**Непроверяемое из read-only**

Видимость психолога в списке сессий, возможность владельца писать ему, фактические возвраты `clientThreadId` / `threadId`, готовность треда и доставка ответов между сессиями не подтверждены.

Вопрос закроет один сквозной запуск в среде с нужной schema: сессия A создаёт психолога и подтверждает готовый идентификатор; владелец видит чат; свежая сессия B обращается к тому же треду и получает ответ с сохранённым контекстом. Дополнительный одновременный вопрос проверит поведение занятого треда. Hybrid требует отдельной пробы с разрешёнными кешами и подготовкой модели.

Файлы не изменял, треды не создавал, Claude-инструменты не вызывал.