# Аудит A — выполнимость Codex-варианта скила 1chat-recall в Codex

## Контекст
Скил `1chat-recall` (глобальный, стоит в `~/.codex/skills/` и `~/.claude/skills/`) хранит дословные слова владельца проекта в `_ops/chat-recall/` и возвращает агенту его позицию перед существенным решением. Сегодня владелец сменил механизм: корпус читает только отдельный агент-психолог (роль в `agents/psychologist.md`); в Codex психолог — фоновый тред проекта, к которому обращаются все сессии проекта, видимый владельцу как сессия. Черновик писал Claude и не может прогнать его в Codex. Ты — Codex; читай черновик как агент, которому предстоит им пользоваться.

Первичные артефакты (read-only):
- Черновик Codex-варианта: /Users/triton/Documents/GitHub/agentic-research/skills/1chat-recall/work/owner-picture-2026-09-08/draft/codex/ — SKILL.md (раздел «Психолог»), agents/psychologist.md, references/retrieval.md, references/capture.md, references/integrity.md, scripts/chat_digest.py (`--help`).
- Слова владельца о механизме: /Users/triton/Documents/GitHub/agentic-research/_ops/chat-recall/2026-09-08-161113-claude-4c325be0.md строки 18–24.
- Скил владельца о фоновых тредах Codex: ~/.codex/skills/1orchestration/SKILL.md и references/execute.md («Нативная механика»).
- Прежний установленный Codex-вариант для сравнения: /Users/triton/Documents/GitHub/agentic-research/skills/codex/1chat-recall/.
- Тестовый корпус для живой пробы инструмента: /tmp/recall-probe/_ops/chat-recall (6 записей); реальный корпус — /Users/triton/Documents/GitHub/agentic-research/_ops/chat-recall (319 файлов).

## Цель
Найти, что в Codex-варианте не выполнимо или неоднозначно для Codex-агента как есть, и чем это видно. В частности: (1) какими именно нативными операциями Codex-сессия запускает фоновый тред проекта, отправляет вопрос в уже существующий тред другой сессии и читает его ответ — существуют ли они в твоей callable schema, как называются, что возвращают (`clientThreadId` / `threadId`), нужен ли `wait_threads`; (2) увидит ли владелец такой тред в списке сессий Codex; (3) выполнима ли роль психолога из своего текста: `uv run --locked --script .../chat_digest.py` под sandbox Codex, `--prepare`, `--type обо-мне,предпочтение --timeline --max-chars`, `--since`, `--show`; (4) файл-указатель `_ops/chat-recall/psychologist-thread.txt` — рабочая ли это форма адреса треда, есть ли лучшая (именованный тред, реестр); (5) что произойдёт при первом вызове скила в свежей Codex-сессии проекта без файла и при втором вызове с файлом. Разрешён пробный запуск `chat_digest.py` на тестовом корпусе для проверки инструмента.

## Границы
Read-only: ничего не изменяй в репозитории и в ~/.codex; не создавай фоновые треды и не пиши файл-указатель; не вызывай инструменты claude-mcp и `claude_ask` — работай файлами и shell. Не переписывай скил и не предлагай новую архитектуру: аудит, не переделка. Если раздел выполним как есть — скажи «держится» с тем, чем это видно; выдуманные находки хуже пустого списка.

## Готово, когда
Один ответ по схеме: сначала строка «Вердикт по механизму: выполним / выполним с правками / невыполним» с одной фразой основания; затем список находок, каждая — `адрес (файл:строка) → что не выполнено или неоднозначно → чем это видно (операция, схема, поведение, прогон) → минимальная правка текста`; затем блок «Держится» — что проверено и не требует правок; в конце — «Непроверяемое из read-only» с тем, какой живой запуск закрыл бы вопрос.
