# Волны проверки — 2026-09-08

## Волна 1 — на v1 (до смены механизма владельцем)

Две роли из чистых окон: `check-instructions`, `check-trajectory`. Пока они
шли, владелец сменил механизм (`#L20–L22`), поэтому часть находок закрыта
переходом на психолога, а не строчной правкой.

| # | Роль | Находка | Решение |
| --- | --- | --- | --- |
| 1 | instr | Ссылки `#L16` бьют в пустую строку | Принято: helper сдвинул строки при добавлении типа в шапку; адреса пересчитаны (L18–L24), дефект известен (`_ops/findings/2026-09-06-142100-…`) |
| 2 | instr | Цитата «между предложениями…» не в корпусе | Принято: сохранена (`#L23`); добавлен более сильный источник `2026-08-13-120315-claude-8e9d58e3.md:21` |
| 3 | instr | `origin.md` не в папке-истории | Принято: путь исправлен на `skills/shared/1chat-recall/origin.md` |
| 4 | instr | P-004/P-005 не совпадают с принципами скила | Принято: уточнено — принципы проекта `_ops/product-frames/agentic-research.principles.md` |
| 5 | instr | Вопрос владельца о методиках без адреса | Принято: сохранён (`#L24`) |
| 6 | instr | Description не по форме «Use when…», снята граница, нет use/skip/near-miss | Принято: форма проверяющего с `attitudes`; use/skip/near-miss в intent.md; граница снята ради 200 символов — near-miss записан |
| 7 | instr | Одно знание трижды (SKILL.md, retrieval.md, случай) | Принято: в retrieval.md Зачем осталась механика режима; мир — в теле; копия в роли психолога оправдана отдельным окном |
| 8 | instr | Старшинство без причины | Принято: причина добавлена в роль психолога («вывод — реконструкция, они — сказанное») |
| 9 | instr | Нагрузка при первом запросе названа неверно | Снято сменой механизма: вызывающий не ищет; нагрузка психолога посчитана в intent.md |
| 10 | instr | «Необратимое → вопрос» — авторское примирение | Принято: вынесено в «Авторские решения к показу владельцу»; граница уточнена по находке traj-6 |
| 11 | instr | Состояние не заменено, проверка потерь пуста | Принято: state.md и раздел потерь в intent.md |
| 1 | traj | Механизм уже отменён владельцем (L20–L22) | Принято целиком: психолог в `agents/`, retrieval = вызов, «Независимый поиск» и «дешёвая модель» сняты, Codex — тред |
| 2 | traj | Нет прямой дороги к слою личности (`--type обо-мне,предпочтение`) | Принято: bootstrap психолога |
| 3 | traj | «Ни опор» — дешёвый выход | Принято: критерий психолога — только после запроса на уровне ценности и слоя личности, границы названы |
| 4 | traj | Два scope у картины | Принято: картина строится при первом вызове всегда; Цель retrieval без ограничения «где слов нет» |
| 5 | traj | «Записанная позиция» читается как запись в корпус | Принято: формулировка «названная в ответе»; Среда — выводы не сохраняются `note` |
| 6 | traj | «Необратимое» без границы возвращает ритуальные вопросы | Принято: граница — разрушительное, платное, внешнее действие; обратимый выбор по выводу; показано владельцу как авторское |
| 7 | traj | Дубли SKILL.md ↔ retrieval.md | Принято (см. instr-7) |
| 8 | traj | Адрес сбоя `#L16` неверен | Принято (см. instr-1) |

Без находок (обе роли): имена методов с инвариантами; Capture не сломан новым
акцентом; description — решение владельца о триггере; неназванные части
дословны.

## Comparator старого и нового текста retrieval (до смены механизма)

Синтетический корпус `/tmp/recall-probe` (6 записей: стартап, «скорость важнее
тестов», страх застрять на инфраструктуре, «главное — чтобы салон увидел
заявку»); вопрос без прямой цитаты — серверная обработка заявки: валидация,
очередь, ретраи, идемпотентность, дубли, тесты. Модель haiku, по два прогона.

| Версия текста | Очередь/ретраи/идемпотентность | Тесты | Состояние, названное агентом |
| --- | --- | --- | --- |
| старый, прогон 1 | нет | нет | «Применимое evidence» |
| старый, прогон 2 | нет | нет | «Применимое evidence», «решение выведено из общей позиции» |
| новый, прогон 1 | нет | нет | «Применимое evidence» |
| новый, прогон 2 | нет | 3–5 на критический путь | «Выведенная позиция… названа выводом агента» |

Вывод: на корпусе из шести записей обе версии выводят «минимум» из ценностей;
сбой владельца не воспроизводится, когда весь корпус читается одним запросом.
Он ожидаем на реальном корпусе (этот проект — 319 файлов, ≈1.75 МБ), где агент
ищет по фразе и читает узко — довод за отдельное окно психолога, а не за
строчную правку. Только один прогон из четырёх пометил позицию как вывод.

## Волна 2 — на v3

См. ниже (дописывается по возврату ролей).
