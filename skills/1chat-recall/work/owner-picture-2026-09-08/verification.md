# Проверка — 2026-09-08, v3

## Тесты пакета на черновике

- `draft/claude`: `python3 -m unittest discover -s tests -p 'test_chat_*.py' -q` — 122 OK.
- `draft/codex`: тот же прогон — 125 OK.
- Изменены два теста топологии в обоих runtime (см. intent.md «tests»).

## Корпус и указатель Codex

`chat_digest.py --check --strict` на корпусе с посторонним файлом
`psychologist-thread.md` и `.txt` — «OK: 6 записей без diagnostics» (оба
варианта игнорируются проверкой).

## Codex-механизм

`codex exec resume <SESSION_ID>` подтверждён документацией
(developers.openai.com, «Resume a non-interactive session»), но снят: `which
codex` → not found, бинарь только в `/Applications/ChatGPT.app/Contents/Resources/codex`
(0.153.4); «фоновые треды» владельца (`#L22`) — нативная механика Codex
(`~/.codex/skills/1orchestration/references/execute.md`, «Нативная механика»:
`clientThreadId → threadId`, `wait_threads` с cursor; имена операций — из
callable schema). Раздел переписан под неё. Не проверено из Claude: запуск
треда-психолога, отправка вопроса в тред из другой сессии, видимость в списке
сессий владельца, sandbox/approvals для `uv run` и `--prepare`.

## Живой прогон психолога на реальном корпусе

`Agent` (general-purpose, модель родителя) с ролью `draft/claude/agents/psychologist.md`
inline, `TARGET_PROJECT_ROOT` = этот репо (319 файлов, ≈1.75 МБ).

Вопрос 1 — при каких условиях владелец хотел бы вопроса перед действием по
одному выводу. Результат: состояние «прямые слова»; прочитаны ленты
`agent-autonomy` (50), `owner-intent` (16), `обо-мне` (9), `предпочтение`
(102), 30 разговоров целиком; ответ по форме с ~40 дословными опорами и
адресами; 270K токенов субагента, 7 минут, 35 вызовов инструментов. Позиция
владельца (его словами, с адресами): внутри проекта при свежем GitHub —
действовать и коммитить; спрашивать только когда ход выходит из восстановимой
зоны (прод, удаление без сохранёнки, ОС —
`2026-08-11-183344-claude-6bb7881b.md#L21`, `2026-08-11-050000-claude-97cc05a9.md#L22`),
когда долговечная запись не уверена в том, что он это имел в виду — тогда одна
строка в чате (`2026-08-31-223050-claude-9ee6a678.md#L35`, `#L39`), при
реальном конфликте позиций (`2026-09-05-183706-codex-01a071c7.md#L22`) и «если
почувствуешь что-то странное» (`2026-09-03-154042-codex-01a066d9.md#L19`).
Эта граница заменила авторскую («разрушительное, платное, внешнее») в
retrieval.md и роли — теперь она опирается на слова владельца.

Наблюдение: `SendMessage` в этой сессии (desktop app) отсутствует и в
deferred-списке — повторный вопрос тому же агенту здесь проверить нельзя. В
SKILL.md добавлен запасной путь: новый `Agent` с той же ролью и прежними
ответами психолога.

Вопрос 2 (запасной путь: новый `Agent`, роль + краткий перенос ответа 1) —
как владелец хотел бы поступить с непроверенным Codex-вариантом. Результат:
состояние «прямые слова + вывод по сборке»; опёрся на переданные адреса и
прочитал ещё 20 разговоров; 157K токенов, 5 минут, 31 вызов. Позиция: оба
варианта ставятся вместе одним «да» перед установкой
(`2026-08-21-010201-codex-01a020be.md#L16`, `2026-08-22-124048-codex-01a02869.md#L22`);
«не проверено» в артефакт не пишется — сомнение живёт в чате
(`2026-08-09-150249-codex-019fe5f7.md#L22`); проверка скила Claude —
когнитивный аудит мощным Codex «с точки зрения агента, который бы это
использовал» (`2026-08-28-193846-claude-76aa0843.md#L26–27`,
`2026-09-05-183706-codex-01a071c7.md#L19`), максимум две волны
(`2026-08-31-001517-codex-01a05418.md#L23`); установка — единственный гейт,
где он хочет быть спрошен (`2026-08-29-152721-codex-01a04d0e.md#L24`).
Запасной путь работает: картина перенесена, ответ по форме, дешевле первого.

## Аудит Codex через мост

- `codex-audit/run-A` — gpt-6-astra, 191 с, 467K токенов, `status=completed ok=true`:
  вердикт «невыполним в текущей Codex-сессии» по транспорту (нет операций
  проектного треда в схеме bridge-сессии); семь находок, все приняты (см.
  reviews.md). Живая проба Codex: `uv run --locked` под sandbox падает на кеше,
  `python3 -B` с `--lexical`, `--timeline`, `--show <record_id>` работают.
- `codex-audit/run-B` — gpt-5.6-sol, 842 с, 2.6M токенов, `status=completed ok=true`:
  вердикт «не годен» по нагрузке набора роли и аварийным режимам; семь находок,
  приняты полностью или частично (reviews.md); расхождения с решениями автора
  вынесены владельцу (intent.md, пункты 8–9 и «Нагрузка»).
- Проверка после правок v6: тесты обоих вариантов OK; `python3 -B … --lexical`
  и `--show cr-…` проверены на тестовом корпусе.

## Что закроет только живой запуск из Codex-сессии

Сессия A создаёт психолога как проектный тред и получает готовый `threadId`;
владелец видит чат в списке сессий; свежая сессия B отправляет вопрос в тот же
тред и получает ответ с сохранённой картиной; одновременный вопрос проверяет
поведение занятого треда; hybrid-поиск — с разрешённым кешем и `--prepare`.
