# Результат ремонта 1chat-recall

Установленная версия — [installed-2026-09-22-stable-records](../../versions/installed-2026-09-22-stable-records/). Владелец поручил ремонт 22 сентября; значимый выбор — постоянный Markdown-заголовок рядом с цитатой и идентичность исходного ответа вместо сравнения слов. Функция датированного контекста и прямого чтения сохранена.

| Что изменилось | Чем проверено |
| --- | --- |
| Постоянный anchor и отдельный line_anchor | Рост шапки, новый topic и backfill не меняют адрес или имя holder |
| Два одинаковых ответа | Разные source-ref, включая части одного сообщения, создают разные записи; повтор возвращает прежнюю |
| Общая запись | 12 конкурентных процессов, новые темы и rollback не теряют соседние записи |
| Legacy и repair | Голая строка не считается identity; проверенный hash связывает цитату; plan/apply отвергает изменённый файл |
| Чтение по полному адресу | --show принимает record_id и file.md#recall-ID; неоднозначность отвергается |
| Доставка | Все 24 файла Codex и 25 Claude совпадают с принятой версией, включая новые модули; installed.json |

Полные финальные наборы: **160 Codex, 157 Claude — OK**. Команда в каждом owner-е: `python3 -m unittest discover -s tests`; неизменённые логи — `tests-accepted-codex.txt` и `tests-accepted-claude.txt`. Дополнительно `quick_validate.py`: обе версии valid. Три новые --show regression и прежние восемь сначала падали; ещё пять сценариев отдельно падали на временных копиях before. Исходные снимки не редактировались.

Первая фиксация и её логи сохранены отдельно; финальные байты — [accepted-candidate-sha256.json](accepted-candidate-sha256.json), доставка — [installed.json](installed.json). Claude — существующий symlink в tracked owner; Codex получил атомарно заменённые изменённые файлы. Новый source module доставлен до зависящих от него entrypoints.

Три независимые роли и разбор замечаний — [reviews.md](reviews.md), прямая межмодельная проверка — [Opus](opus-trajectory.md). Вторая волна завершена: обе роли Codex вернули pass, [Opus закрыл семь прежних находок](opus-acceptance.md), включая прямые пробы установленного Capture и --show в обеих средах. Проверяющие не писали исходники.

Границы: блокировка защищает сотрудничающие helpers, а не ручные редакторы или старые writers. Semantics/hybrid не менялся; новые ручные поисковые пробы использовали lexical, полный suite покрывает прежний код. Исправность всех исторических цитат не заявляется. Старые материалы MAVO и их адреса хранятся в самом MAVO, вне глобального пакета.
