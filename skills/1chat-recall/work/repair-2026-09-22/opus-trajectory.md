## Граница проверки

Роль — применение и результат (`check-trajectory.md` + `agent-defaults.md`): маршруты, состав действующих требований в момент выбора, наблюдаемое поведение. Смысл формулировок и необходимость требований — не мой предмет. Уточнения, что правки 11:07:53 были узкими (quote/selection и legacy hash в `capture.md`, предел блокировки в `integrity.md`, только help в `chat_capture.py`), я получил после проб; сами байты 11:07:53 я видел и прогнал, но сравнить их с версией 11:02 не мог — копии той версии в рабочей папке нет. Ничего не менял: исходники, installed и MAVO не тронуты, все записи — в `/tmp`.

## Что я реально выполнил

**Тесты.** `test_identity_repair.py` (codex) отдельно — 18 OK. Полные наборы по текущим байтам: codex **157 OK**, claude **154 OK**. Совпадает с вашей цифрой; это моё независимое наблюдение, а не пересказ логов.

**Пробы во временном проекте** `/tmp/recall-probe-Qc3AHk` (скрипты кандидата `skills/codex/1chat-recall/scripts`):

| Вход | Действие | Результат |
|---|---|---|
| `--source-ref <msg>#goal` и `<msg>#scenes`, обе цитаты «да», разные topic/context-note | два Capture | две записи, `#recall-42496aac…` и `#recall-770cbfb4…`, оба `deduplication: source-ref` |
| дословный повтор второго вызова | Capture | `already-present`, тот же anchor, записей по-прежнему 2 |
| `--new-topic layout` + backfill реплики 2025-01-05 в тот же holder | два Capture | имя holder-а не изменилось (`2026-09-22-150000-codex-11111111.md`), frontmatter вырос, первая запись уехала L17 → L20, `#recall-42496aac…` тот же |
| `--query "сцены каталог" --lexical --json` | поиск | `strongest_quote.address = …#recall-770cbfb4…`, отдельный `line_anchor …#L24`; sha256 всех файлов корпуса до и после — **идентичны** |
| `--address 'legacy…:10'` без sha | repair | rc=2, `unverified line address` |
| `--address 'legacy…:10 sha:<64hex>' --plan-out` | repair dry-run | план с `old_address #L10` и новым `anchor`; файл побайтово не изменился |
| чужой writer дописал строку → `--apply-plan` | repair apply | rc=2, `changed since plan`, файл не изменён |

Все шесть заявленных свойств из таблицы «Сохранность» в этих пробах ведут себя как заявлено.

**Проба по installed Codex** (`/tmp/recall-codexinstalled-jKJqX7`, скрипты `~/.codex/skills/1chat-recall`): `--source-ref` — `unrecognized arguments`; два «да» с разными topic и context-note → второй вернул `already-present`, в корпусе **одна** запись; `--supersedes '<file>.md#recall-<32hex>'` → rc=2 «must address one record as `<file>.md:<line>`».

## Находки

**1. Доставка в Codex не состоялась; installed-пакет — дословно `before`.** `~/.codex/skills/1chat-recall` = `work/repair-2026-09-22/before/codex` байт в байт (21 файл, 0 различий). Отсутствуют `scripts/chat_repair.py`, `scripts/record_identity.py`, `tests/test_identity_repair.py`; расходятся 8 файлов, включая `SKILL.md` и все три reference. `~/.claude/skills/1chat-recall` при этом совпадает с кандидатом (25/25, 0 различий). Основание — `install-approved.md`: «Сверь состав и содержимое каждой проекции с этой версией… Назови среду, в которую доставка не состоялась». Свидетельство неверного результата — проба выше: в Codex-рантайме второй «да» до сих пор теряется. Минимальная правка: доставить те же байты в `~/.codex/skills/1chat-recall` и перепроверить состав, либо явно объявить Codex недоставленной средой.

**2. Пока доставка половинчата, общий корпус MAVO уже несёт адреса, которые installed Codex отвергает.** В `mavo3/_ops/chat-recall` 56 записей получили `### recall-…`. Codex-помощник по этим адресам даёт rc=2 и требует `<file>.md:<line>` — ровно ту форму, которую ремонт объявил не-идентичностью. Это не прогноз: команда выполнена, ошибка приведена дословно.

**3. Момент предъявления цитаты называет только новую форму адреса.** `references/retrieval.md:90-91`: «устойчивый `address` из выдачи (`<файл>.md#recall-<id>`)». Оговорка про legacy живёт в другом разделе (`:68-71`). Фактическое распределение: в `agentic-research/_ops/chat-recall` устойчивых заголовков **0 из 2251**, в `mavo3` — **56 из 2809**. То есть почти в каждом реальном случае выдача даёт `address` вида `<file>.md:<line>` (проверено: у legacy-записи в пробе `"address": "legacy-2026-01-02.md:10"`), и инструкция в момент применения буквально неисполнима. Минимальная правка: в `:90-91` назвать обе формы или сослаться на правило legacy рядом.

**4. `--show` не принимает тот адрес, который агент несёт.** `--show '<file>.md#recall-<id>'` → `record_id не найден`; работает только голый `recall-<id>`; `--show '<file>.md#L24'` тоже отказ. `chat_digest.py:2179` сравнивает `record["record_id"] == args.show`. Help для `--show` форму не называет, а `SKILL.md:56-57` объявляет `--help` владельцем схемы и расхождение справки с поведением — дефектом инструмента. Минимальная правка: принимать полный адрес (отбрасывать `<file>.md#`) либо назвать ожидаемую форму в help.

**5. Манифест и логи больше не относятся к поставляемым байтам.** `candidate-sha256.json` (11:02:38) расходится с текущими байтами в 6 файлах (`references/capture.md`, `references/integrity.md`, `scripts/chat_capture.py` — в обоих рантаймах, mtime 11:07:53). `green-codex.log`/`green-claude.log` (10:57/10:58) отчитываются о 155/152 тестах, текущие наборы — 157/154. Свойство само по себе в порядке (я прогнал текущие байты), но артефакт приёмки этих байт не подтверждает. Минимальная правка: пересчитать манифест и перезаписать green-логи по финальной версии.

**6. Красная база покрывает 8 из 18 identity-тестов.** `red-runtime.log`: `Ran 8 tests`, `FAILED (failures=8)`. Без записанного красного остались в том числе `test_two_yes_answers_in_one_message_are_distinct_source_fragments`, `test_exact_timestamp_fallback_distinguishes_context_in_same_message`, `test_date_only_without_source_ref_does_not_claim_idempotency`, `test_relations_keep_target_after_manual_insertion_and_header_growth`, `test_failed_transaction_cannot_rollback_another_writers_capture`. Для тестов вокруг `chat_repair.py` красный прогон невозможен по существу (скрипта не было); для перечисленных пяти — возможен. Основание — `agent-defaults.md`: нужен «результат проверки, способной обнаружить ошибку». Минимальная правка: дописать красный прогон по тем пяти на `before`-коде либо назвать их непокрытыми красным.

**7. Ремонт применён к живому корпусу MAVO, и часть входящих `#L`-ссылок в действующих документах осталась и теперь бьёт мимо.** `mavo-dirty-before.patch` не содержит ни одного `+++ b/_ops/chat-recall/` и ни одного `+### recall-` — на момент снимка оба файла были чистыми. Сейчас: `M 2026-09-20-111334-Codex-01a0bd58.md` (+60), `M 2026-09-21-180842-Claude-11606db7.md` (+50). Перенос ссылок выполнен и учтён (`verification.json`: 101 устойчивая ссылка, 25 документов, 26 исправленных целей), но в недействующих архивом местах остались старые адреса:

- `_ops/handoffs/2026-09-22-103915-claude-fable-5-1.md:14,18` → `…11606db7.md#L43`; строка 43 сейчас **пустая** (в HEAD там была цитата «да нет я не уверен надо проверить…», на которую ссылка и опирается);
- `_ops/handoffs/…:58` → `…01a0bd58.md#L29`; сейчас это `### recall-a4ed7789…` (в HEAD — цитата от 2026-09-21T13:33:37);
- `_workspace/orchestration/2026-09-19-catalogue-metadata/state.md:15` → `…01a0bd58.md#L20`; сейчас `### recall-11826f47…` (в HEAD — цитата от 2026-09-20T11:13:34);
- `price-acceptance.md:3` → `…01a0bd58.md#L18`; строка 18 — заголовок файла и была им до ремонта, то есть ссылка была неверна и раньше.

Копии под `_workspace/_archive/…` `integrity.md` оставлять разрешает. Основание — `integrity.md`: «вместе с ними перенеси проверенные входящие ссылки… Покажи охват переноса и **оставшиеся старые ссылки**». Охват показан, список оставшихся — нет ни в `verification.json`, ни в `state.md`. Минимальная правка: перенести три ссылки в двух действующих документах и записать перечень намеренно оставленных.

## Непроверенное

- Не сравнивал байты `chat_capture.py` 11:07:53 с версией манифеста — сохранённой копии нет, поэтому «только help» подтвердить не могу; подтверждаю лишь, что текущие байты дают 157/154 OK.
- Семантический/hybrid маршрут не трогал: все запросы шли через `--lexical`, чтобы остаться офлайн. Bootstrap модели и поведение dense-канала не наблюдал.
- Ручные пробы вёл на codex-копии скриптов. `chat_capture.py`, `record_identity.py`, `chat_repair.py` у двух рантаймов совпадают по хешу, поэтому на Claude результаты переносятся по тождеству байт, а не отдельным наблюдением; отдельно по Claude наблюдал только его собственный набор (154 OK).
- `--check --strict` по реальному корпусу не запускал; заявка `new_diagnostics: 0` мною не проверена.
- Конкуренцию сверх тестовых 12 процессов не пробовал; надёжность при ручных редакторах, как и сказано в intent, не проверяется ничем.
- Мутационных проб не делал: для десяти identity-тестов без красной базы я не устанавливал, что они способны упасть.
