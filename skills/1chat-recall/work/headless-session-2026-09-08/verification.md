# Проверка — психолог Claude как headless-сессия, 2026-09-08

## Тестовый корпус (haiku, /tmp/recall-probe)

- T1 создание с `--session-id`: 5 ходов, `chat_digest.py` через Bash без
  запросов разрешений (`--permission-mode dontAsk`, `--allowedTools
  Bash,Read,Glob,Grep`), ответ по форме с адресами, `session_id` = назначенный.
- T2 `--resume` тем же uuid: 1 ход, без повторного чтения, ответ по форме с
  адресами из первого хода — контекст пережил границу процессов.

## Реальный корпус (fable, этот репо, 319 файлов)

- Q1 создание: вопрос о модели психолога. 30 ходов, 204 с, $3.81, ответ 3185
  знаков по форме: состояние «смешанное», позиция «ставь самую сильную модель»,
  8 опор с адресами (в т.ч. «моделями ниже опус я не пользуюсь»,
  `2026-08-06-215508-claude-224b8c51.md#L21`; «не думай про лимиты»,
  `2026-08-06-113200-claude-712763b3.md#L25`). `--model fable` в команде
  скила опирается на этот ответ.
- Q2 `--resume`: см. ниже.
- Q2 `--resume` тем же uuid: вопрос о числовом лимите длины ответа. 2 хода,
  52 с, $0.38, ответ 2687 знаков по форме, состояние «смешанное», 7 опор с
  адресами; опирался на слой личности, прочитанный в Q1, дочитал четыре
  разговора о лимитах. Картина пережила границу процессов на реальном корпусе.
  Содержание: числовой лимит не вводить — «экономить смыслы, а не буквы»
  (`2026-08-28-193846-claude-76aa0843.md#L37`), правила краткости в корне он
  снимал, потому что «он из-за этого тупит» (`2026-08-08-135137-claude-457be55d.md#L29`).
  Роль оставлена с «по делу, опоры три–семь».

## Итог

Механизм «сессия на проект» для Claude работает в этой среде на реальном
корпусе: создание с инструментами, продолжение с сохранённой картиной, ответ
по форме. Стоимость fable: первый вопрос ≈ $3.8 и 3–4 минуты (чтение слоя
личности), следующие ≈ $0.4 и меньше минуты.
