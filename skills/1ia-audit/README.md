# История `1ia-audit` — скил снят 2026-09-01

Скил информационной архитектуры влит в `1document-system` и снят решением
владельца: «Слить информационную архитектуру целиком, `1ia-audit` снять — при
названных издержках: переписать маршруты в `1smart-simple`, `1goal`,
`1md-graph`, `1md-search` и втянуть его 662 строки»
(`_ops/chat-recall/2026-08-31-223050-claude-9ee6a678.md#L39`).

Tracked owner-а у него не было: живые установленные пакеты были единственной
правдой, поэтому сняты они, а не проекция.

| Путь | Содержимое |
| --- | --- |
| `live-claude-2026-09-01/` | Пакет из `~/.claude/skills/1ia-audit/` целиком, 8 файлов |
| `live-codex-2026-09-01/` | Пакет из `~/.codex/skills/1ia-audit/` целиком, 9 файлов |

Разбор его 105 механизмов и их судьба —
`skills/1document-system/work/v3/old-mechanics-map.md` и
`skills/1document-system/cut.md`.

**Что из него живёт в `1document-system`.** Одна правда и запрет на два
independently editable ответа; вид не становится правдой; смешение функций в
одном файле; жанровый наполнитель; предохранитель от слепого дробления при
разделении документа.

**Что снято вместе с ним.** Режим аудита готовой формы без новой информации на
руках — момент, у которого теперь нет владельца ни в одном скиле. Шесть
маршрутов из четырёх скилов переписаны по смыслу на `1document-system` там, где
он несёт нужную операцию, и сняты там, где не несёт.
