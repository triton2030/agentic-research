# Проверка смысла разделов

## Вердикт

В границах check-intent адресуемых находок нет. Проверены байты `draft/SKILL.md` с SHA256 `fafd0fdcd516a232b2553792fd3f60dedd182d8882fc0fcd842c4bf3d76b92b8`, совпадающим с `draft-sha256.json`.

Контекст (`draft/SKILL.md:11–16`) сообщает неизвестные будущему читателю свойства инструмента: обычная выдача является картой кандидатов, а успешный поиск может быть только лексическим. Их последствие раскрыто рядом: по карте нельзя обосновать ответ (`:20–21`, `:69–81`). Задача требует найти, прочитать и использовать фрагменты в исходной работе с проверяемой связью вывода и источника (`:18–21`). Без скила сильный агент мог бы выполнить смысловой поиск, но не восстановить именно этот маршрут `md`, проверку dense-канала и переход от карты к телу. Цель (`:23–28`) выбирает достаточное прочитанное основание и открытый существенный вопрос; она не приравнивает близость эмбеддингов к истине или авторитету. Отдельного раздела критериев нет; остаточную лазейку, для которой он необходим, я не нашёл.

Реалистичное понимание читателя: при вопросе о правиле в неизвестном Markdown-корпусе он выбирает разрешённую область, проверяет фактический scope, ищет через `md search-read`, читает исходный текст и устанавливает его статус по контракту корпуса. Если dense-канал не работал или разрешённые источники не закрывают вопрос, он называет ограничение, а не выдаёт карту попаданий за доказательство (`draft/SKILL.md:30–54`, `:69–88`, `:104–113`). Это соответствует принятому автоматическому подключению при поиске по смыслу (слова владельца в `/Users/triton/Documents/My_projects/md-tools/_ops/chat-recall/2026-09-21-223200-codex-01a0c503.md`) и зафиксированному в `intent.md` уточнению об одном корневом индексе с выбираемыми папками.

Проверка двух синтетических сценариев в `evidence/scope-after.md` и `evidence/cold-warm-after.md` показывает чтение действующего файла и ограничение scope. Она не устанавливает надёжность автоматического выбора скила в произвольной будущей задаче или рост вероятности успеха.
