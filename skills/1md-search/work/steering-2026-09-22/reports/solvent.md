assumption_untested

| Допущение | Цена удержания | Мир без него | Условие удержания |
| --- | --- | --- | --- |
| **Постановка:** неизвестный Markdown по смыслу требует отдельного semantic route (`before/SKILL.md:3-6`). | Установка, индекс, возможные внешние вызовы и обучение агента маршруту. | Карта файлов и точный поиск снимают задачу о `1md-search` лишь если столь же надёжно приводят к действующему источнику; известный замер дал 6/6 промахов shell-поиска (`md-tools/_ops/product-frames/md-tools-agent-runtime.md:24-32`). | Принятое владельцем ядро — truth retrieval (`md-tools/_ops/product-frames/md-tools-agent-runtime.md:34-41`); пересмотр нужен при сопоставимом опровергающем replay. Снятие сейчас не обосновано. |
| **Устройство:** `description` и `allow_implicit_invocation` приводят Codex и Claude к скиллу при неизвестном содержании (`before/SKILL.md:3-6`, `before/agents/openai.yaml:6-7`). | Можно улучшить body, который агент ни разу не загрузит. | Если вызов не происходит, проблема переходит в discovery metadata и установку; исходная задача не исчезает. | На непоказанных задачах оба агента выбирают route; это часть `md-tools/_ops/GOAL.md:49-58`, но пакет прямо не содержит измерения автоактивации (`packets/solvent.md:21-22`). |
| **Устройство:** один корень корпуса и его фильтры задают область истины (`before/SKILL.md:30-48`). | Нужны поиск корня, проверка scope и осторожность с include, объединяемыми через OR. | Поиск без явной границы может выдать исключённые или производные тексты; перенос проблемы в оценку авторитета каждого результата. | Корпус задаёт `.md-tools.toml`, а CLI добавляет фильтры к baseline (`md-tools/_ops/product-frames/md-tools-agent-runtime.md:38-41`; `md-tools/src/navigator/config.py:181-196,239-249`). Уточнение владельца о выбранных папках согласуется с этим, не требует второго вложенного индекса (`packets/solvent.md:17`; `before/references/index-lifecycle.md:5-20`). |
| **Инструмент:** индекс может обновляться и отправлять текст провайдеру; неявный вызов скилла сам не даёт полномочий (`before/SKILL.md:37-48`). | Проверка разрешения иногда задерживает поиск, а отказ ведёт к файловому маршруту. | Если убрать проверку, агент может отправить Markdown за пределы разрешения; если запретить все вызовы, semantic route теряется. | Runtime действительно отправляет uncached query и фрагменты (`md-tools/docs/architecture-lock.md:148-153`); проверять уже данное разрешение, не спрашивать повторно. Dry-run/confirm относится к `transaction_required`, а не к одному лишь `cost_bearing` (`md-tools/docs/cli-conventions.md:45-51`). |
| **Инструмент:** карта кандидатов требует чтения тела и проверки статуса источника (`before/SKILL.md:63-85`). | Дополнительное чтение и иногда второе ранжирование. | Если считать сниппет доказательством, вывод может опереться на нерелевантный или устаревший фрагмент; задача получения evidence остаётся. | `search-read` по умолчанию отдаёт карту, `--expanded` — тело; score не вероятность истины (`before/references/retrieval-engine.md:5-12,28-34`). |

- `type`: assumption_untested · `assumption`: неявный вызов реально выбирается на новых смысловых вопросах · `location`: `before/agents/openai.yaml:6-7`; `md-tools/_ops/GOAL.md:49-58`; `packets/solvent.md:21-22` · `diagnosis`: разрешение неявного вызова в metadata не доказывает выбор маршрута, поэтому улучшение body может не дойти до агента · `cost_of_change`: короткий replay на ранее не показанных вопросах для Codex и Claude с записью первого выбранного route; при провале — правка discovery metadata или установки · `falsifier`: оба агента стабильно загружают `1md-search` на неизвестном смысловом запросе и оставляют known-address/exact запросы на соответствующих маршрутах · `alternative`: держать текущий semantic route и проверять discovery до дальнейшего усложнения управляющего текста.
