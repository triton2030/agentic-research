different_class_exists
Найти неизвестный локальный источник для вопроса, прочитать достаточное основание и не выйти за разрешённую область.

| world | who · what · paid · when · source | divergence · transfer_condition · recommendation |
| --- | --- | --- |
| Соседняя индустрия: корпоративные знания | **Microsoft 365 Copilot Retrieval API** возвращает текстовые выдержки с адресами из SharePoint, OneDrive и коннекторов; проверяет доступ вызывающего пользователя и допускает фильтр области до выполнения запроса. Цена: зависимость от разрешений и индекса Microsoft 365; некорректный синтаксис фильтра может привести к поиску **без фильтра**. Проверено по действующей документации 2026-09-22. [API](https://learn.microsoft.com/en-us/microsoft-365/copilot/extensibility/api/ai-services/retrieval/copilotroot-retrieval?pivots=graph-v1). | Их граница обеспечивается платформой, тогда как локальный `md` и текст скилла не являются системой доступа. Переносится при условии, что runtime показывает и проверяет **фактическую** область запроса, а агент сверяет её до использования выдачи. В текущем скилле уже есть проверка `path_scope`; сохранить её, но не обещать, что одно описание скилла гарантирует границу корпуса. |
| Другой масштаб: поиск по множеству репозиториев | **GitHub Code Search** сужает поиск через `repo:` и `path:` прямо в запросе; GitHub отдельно перечисляет неиндексируемые файлы и ограничение выдачи 100 результатами. Цена: неполное покрытие, в том числе исключение больших, бинарных, generated и vendored файлов; исчерпывающий поиск не поддерживается. Проверено по действующей документации 2026-09-22. [Синтаксис](https://docs.github.com/en/search-github/github-code-search/understanding-github-code-search-syntax), [ограничения](https://docs.github.com/en/search-github/github-code-search/about-github-code-search). | Это поиск кода, а не смысловое чтение Markdown; `path:` работает внутри уже определённого репозитория и не решает выбор авторитетного документа. Переносится правило различать **область запроса**, **покрытие индекса** и **прочитанный источник**. Текущий скилл уже проводит это различие; пустую выдачу не стоит превращать в тезис об отсутствии знания. |
| Другой класс системы: чтение по требованию | **Anthropic / Claude Code** сочетает заранее заданные проектные инструкции с навигацией по путям, `glob`/`grep` и чтением файлов по мере работы; Anthropic прямо описывает этот путь как обход устаревшего индекса и признаёт цену: дополнительные шаги и риск бесплодного поиска без хороших инструментов и эвристик. Проверено по действующим материалам 2026-09-22. [Context engineering](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents), [инструменты и границы путей](https://code.claude.com/docs/en/tools). | Код обычно содержит имена и точные строки; неизвестный Markdown может называться иначе, чем понятие в вопросе, поэтому `glob`/`grep` не заменяют semantic retrieval во всех корпусах. Переносится при малом или обозримом корпусе, известных терминах либо отсутствии разрешения на индекс/отправку данных. В скилле уже есть exact/filesystem fallback; оставить его полноценным маршрутом и проверять автоматический выбор на задачах, где смысловой поиск действительно нужен. |
