interface:
  display_name: "1-Создание инструкций"
  short_description: "Use when creating or rewriting project instructions"
  default_prompt: "Use $1instruction-authoring to create or rewrite project instructions as a minimal routed graph, prove the behavioral delta, and install only the authorized exact package."

policy:
  allow_implicit_invocation: true
