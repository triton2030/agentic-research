# Фактическая траектория чистого исполнителя — кейс «taskflow» (2026-08-29)

Черновик: `../../../versions/draft-2026-08-28/SKILL.md` (версия после правок прогона 3 и снятия
canon). Исполнитель — чистое окно, референсы недоступны; файлы не создавал.

## Траектория (пронумерованные реальные шаги)

1. Прочитал SKILL.md целиком.
2. Шаг 1 — цель восстановлена из слов владельца; interview не потребовался
   (точку открытия референса зафиксировал).
3. Шаг 2 — карта зон: web (UI), api (рантайм-проверка), specs (владелец
   тарифных границ), adr, plans.
4. Шаг 3 — корень-роутер; развилку «куда класть правило владельца» решил по
   телу (veto-class, кросс-зонное → корень, «Нерушимо:»); это единственная
   точка, где недоступный placement.md мог изменить результат.
5. Шаг 4 — инструкции получили web, api, specs (свои правила игры); adr и
   plans — склады, только адреса в корне. Связки входа проверены.
6. Остановка: два невыводимых правила не изобретал, вынес вопросами владельцу
   (прятать Pro-фичу или показывать disabled; нужна ли явная виза на правку
   спецификации).
7. Шаг 5 — пробник спроектирован (зона web, задача с известным дефолтом
   «захардкодить план в компоненте», критерии провала по diff, не самоотчёту);
   имитирован, не прогнан → весь пакет — candidate.
8. Завершение — жёсткая остановка: без «да» владельца файлы не пишутся, пакет
   сдан черновиком.

## Счёт формы (лимиты ≤20 единиц, разделы ≤500 символов)

| Файл | Единиц | Цель | Контекст |
| --- | --- | --- | --- |
| корень | 6 | 185 | 191 |
| apps/web | 3 | 124 | 222 |
| apps/api | 3 | 121 | 198 |
| docs/specs | 2 | 131 | 198 |

«Нерушимо:» — одна метка (границы тарифов только через specs); девальвацию
метки исполнитель сам назвал ценой.

## Сравнение с целью пользователя (шаг 7 check-approve)

Материальных расхождений нет: минимальная загрузка (склады без инструкций,
«подозрение — причина не писать» применено), важность различима (одно
«Нерушимо:»), цель написана в каждом файле, лимиты соблюдены, гейт «да»
сработал. Полезное поведение сверх ожиданий: невыводимые правила ушли
вопросами, а не выдумкой.

Кандидаты исполнителя честные: пробник имитирован, длинная траектория и
непробованные зоны не покрыты.
