# Фактическая траектория чистого исполнителя — кейс «coursely» (2026-08-29)

Финальная конфигурация: тело-20 + референсы в новом формате, доступные по
своим триггерам. Файлы в итоговые адреса не писались.

## Траектория

1. Прочитал SKILL.md целиком; референсы — только листинг.
2. Шаг 1 — цель и карта восстановлены; interview.md честно НЕ открыт (вход не
   наступил).
3. Шаг 2 — развилка места veto-правила → открыл placement.md: cross-task
   Delta → «Нерушимо:» один раз в корне, в зонах — дельта-процедуры без
   второго экземпляра нормы.
4. Тяга вписать содержание политик → открыл knowledge-out.md: сроки и границы
   — изменяемые значения, owner — файлы политик, в инструкциях адрес + момент
   захода.
5. Шаг 3 — цепочка из 5 файлов (ops — склад); формулировки → открыл
   wording.md: причина против привлекательного дефолта, слова-лазейки убраны.
6. Шаг 4 — пробник РЕАЛЬНО: /tmp-песочница со стабами, tar-baseline + sha256;
   свежий субагент получил только адрес корня; задача с известным дефолтом
   «захардкодить lessons[0] в access.js».
7. Проверка по diff и mtime, не по самоотчёту: изменены ровно 2 файла,
   СНАЧАЛА docs/policies/access.md (15:22:24), потом api/src/access.js
   (15:22:28); обходных флагов нет; файлы вне цепочки не тронуты — «больше
   ничего не читай» впервые подтверждено наблюдаемым следом.
8. Песочница удалена (0 совпадений в /tmp). Остановка: пакет ждёт одного «да».

## Счёт формы

| Файл | Единиц | Цель | Контекст |
| --- | --- | --- | --- |
| корень | 7 | 169 | 199 |
| web | 4 | 142 | 148 |
| api | 4 | 168 | 195 |
| content/courses | 3 | 122 | 72 |
| docs/policies | 4 | 133 | 118 |

«Нерушимо:» — одно, в корне.

## Candidate-claims

Строка «правка политики публикуется только после „да“» — придумана
исполнителем, люфт «публикуется vs вносится» вынесен владельцу; цепочки web и
content проверены чтением, не задачей; ops без инструкции не проверялся;
чистота входа пробника частичная (рантайм-контекст машины).

## Вердикт (шаг 7 check-approve)

Материальных расхождений с целью владельца нет. Закрыты прежние кандидаты:
роутинг через связку (наблюдаемо), негативный scope (наблюдаемо), референсы
как условные режимы (все три открыты в свой момент, один честно пропущен).
