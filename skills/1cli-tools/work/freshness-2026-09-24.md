# Проверка актуальности 1cli-tools — 2026-09-24

Заказ: проверить актуальность скила и обновить нужные инструменты. Адресная
правка справок; назначение, триггер и тело SKILL.md сохранены.

## Установлено

| Инструмент | Было | Стало |
| --- | --- | --- |
| agent-browser | 0.34.0 | 0.38.1 |
| ast-grep | 0.45.1 | 0.45.3 |
| npm | 12.0.2 | 12.1.0 |
| Node.js | 26.7.0 | 26.9.0 |
| uv | 0.12.5 | 0.12.18 |
| GitHub CLI | 2.97.0 | 2.101.0 |
| Vercel CLI | 59.1.4 | 59.26.0 |

Homebrew обновлён до выбора кандидатов. Его dry-run показал зависимости
Node; установка дополнительно обновила stylelint до 17.15.0. Автоматическая
очистка старых версий отключена. После обновления Node ссылки npm/npx стали
вести в Cellar/node/26.9.0, active npm оказался 11.19.1. Последующая установка
глобального npm восстановила 12.1.0.

TypeScript 7.0.2 и FFmpeg 9.0.2 уже соответствовали кандидатам менеджеров.
md-tools 0.7.0 — editable installation из локального проекта, его источник
не заменялся пакетом из registry. Его текущий каталог проверен напрямую.

## Проверено

- Итоговые active versions; scoped `npm outdated` и `brew outdated` пусты.
- Справка всех описанных семейств CLI; каталог и сигнатуры десяти команд md.
- Реальный ast-grep outline на TypeScript-функции через stdin.
- В отдельной browser session локальная data-страница: первый snapshot
  `kind: full`, второй `kind: unchanged`; первый screenshot `changed: true`
  с path, второй `changed: false` без path. Сессия закрыта.
- esbuild из Vercel преобразовал TypeScript в JavaScript после точечного
  разрешения установочного скрипта. Авторизованные API/deploy не запускались.
- Три независимых проверяющих проверили сохранность намерения, необходимость
  добавлений и исполнимость маршрутов. Существенных замечаний нет; два новых
  bundled skill проверены дополнительной адресной волной.

## Сохранность и доставка

Сохранены все прежние маршруты. Обновлены датированные версии; добавлены
snapshot delta, условные screenshots и два новых bundled skill. Уточнены
сеть/warmup у md search и переключение npm при обновлении Node.
Снятых требований нет; переработка функции скила не проводилась.

Правка начинается в tracked owner `skills/claude/1cli-tools/references/`.
Те же справки доставлены в существующие пакеты Claude и Codex; новый
параллельный source tree не создаётся. Доставка проверяется побайтно.

Ограничение: предупреждения npm о диапазоне Node у @renovatebot/pep440 и
устаревшем tar сохранены в findings. Полная проверка всех рабочих сценариев
Vercel не проводилась. Историческое переключение npm подтверждено выводом
Homebrew и промежуточным `npm --version` основного исполнителя; независимые
проверяющие обновление повторно не выполняли.
