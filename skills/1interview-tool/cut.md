# Cut — пересборка 2026-08-13

## Снято полностью

- `references/llm-divergences.md` — общий каталог модельных перекосов не меняет
  собственное решение интервью и дрейфовал между runtime-копиями.
- «Максимум одна активная форма» — источник неизвестен; правило глобально
  блокировало независимые интервью.
- Предел `5–7` секций по `3–5` полей — источник неизвестен; адрес и цена ошибки
  уже ограничивают форму по смыслу.
- Развёрнутый chat-response template — результат задан Целью, Механикой и
  Завершением.
- Собственная permission-модель и `patch-ready synthesis` — полномочия
  принадлежат caller/runtime, не интервью-скилу.
- «Уточни одним адресным вопросом» — неподтверждённый процедурный бортик;
  материальная непротиворечивость остаётся критерием результата.
- Отрицательный перечень незавершённости — повтор положительного контракта
  Завершения.
- Три повторных напоминания в cookbook — честный gap, адрес каждого
  refactor-элемента и воротный порядок уже принадлежат `SKILL.md`.
- `Критерии успеха` — полностью повторяли Механику и Завершение.
- `Дельта` как отдельный раздел — проблема сжата в обязательный Контекст.
- `Инварианты` как отдельный раздел — уникальные бортики перенесены в точку
  действия Механики, повторы сняты.
- Внутренние строки карты сбоев про воротный порядок и lifecycle — повторяли
  Механику без отдельного reference-маршрута.

## Слито

- Адрес вопроса, цена ошибки и проверка различающихся ответов — один инвариант.
- Перенос и архив — одно постусловие и один order-sensitive переход.
- Single-choice и multi-choice — один блок cookbook с одной дельтой.

## Сохранено

- Самостоятельный поиск фактов до вопросов.
- Порог не менее четырёх связанных user-owned решений.
- Воротный вопрос до зависимой формы.
- Рекомендация с основанием либо честный gap.
- Граница голосов и уникальные headings ответов.
- Слабые defaults только после общей отметки готовности.
- Отдельный блок каждой атомарной единицы refactor-а с default `Оставить`.
- Перенос к owners до архива.
- Ленивое создание `_ops/interviews/` и отказ писать в `/`.

## Новое, утверждённое владельцем

- Автоматический вызов в Codex и Claude.
- Единый tracked owner в `skills/shared/1interview-tool/`.

## 2026-08-23 — рефактор-сокращение + сита и якоря вопросов

Курация владельца («да» на карту инвентаря, 52 обязанности), пересборка от
оригинала, две линзы в отдельных окнах. Счёт: 52 → ~45, из них ~6 — новые
одобренные (сита допуска, якоря); чистый рефактор старого 52 → ~39; cookbook
153 → 86 строк.

Снято по курации: воротный шаблон cookbook (строка таблицы дельт вместо
полного каркаса); «создавай папки лениво» и «никогда не пиши в /» (модели
2026 делают сами, сбоя в истории нет); число «4» в пороге — заменено
свойством «ответы уходят к разным owners либо зависят друг от друга».
Слито: четыре шаблона блока → базовая форма + таблица дельт; тройной
lifecycle-контракт → Механика владеет, Цель и Завершение — постусловия;
гейт согласия + блок готовности → одно правило; «дополняй owner» + «новый
файл по трём условиям» → одно правило. Разметка (H3/H4, дословное решение
владельца 2026-08-13) переехала к владельцу — в cookbook; тело держит смысл
и указатель.

Добавлено по слову владельца 2026-08-23: сита допуска вопроса (копии
1use-principles / 1planning/questions / 1product-shaping, помечены «правь
вместе»); якоря «The Mom Test» (вопрос-факт) и «Specification by Example»
(вопрос-требование), разведённые по типам вопроса; «мир владельца» — копия
«Поднимай язык» 1product-shaping; след `снято:` в блоке «Уже проверено».

Снято линзами из добавленного: «Lean Startup» как правило порядка —
в асинхронной форме порядок независимых вопросов не меняет ни одного
ответа, воротная развилка уже держит единственный платящий случай;
«генератор кандидатов» — без источника; «рекомендация живёт под вопросом» —
форма блока принуждает; сужение «следствие для кода» в копии «Поднимай
язык» — восстановлена формулировка источника.

Residual: на 1interview-tool не ссылается ни один скил; симметричная строка
в 1product-shaping («Одна форма, один заход» → сборка формы этим
инструментом) — отдельная задача по слову владельца.
