# Cut — пересборка 2026-08-13

## Снято полностью

- `references/llm-divergences.md` — общий каталог модельных перекосов не меняет
  собственное решение интервью и дрейфовал между runtime-копиями.
- «Максимум одна активная форма» — источник неизвестен; правило глобально
  блокировало независимые интервью.
- Предел `5–7` секций по `3–5` полей — источник неизвестен; адрес и цена ошибки
  уже ограничивают форму по смыслу.
- Развёрнутый chat-response template — результат задан Критериями и
  Завершением.
- Собственная permission-модель и `patch-ready synthesis` — полномочия
  принадлежат caller/runtime, не интервью-скилу.
- «Уточни одним адресным вопросом» — неподтверждённый процедурный бортик;
  материальная непротиворечивость остаётся критерием результата.
- Отрицательный перечень незавершённости — повтор положительного контракта
  Завершения.
- Три повторных напоминания в cookbook — честный gap, адрес каждого
  refactor-элемента и воротный порядок уже принадлежат `SKILL.md`.

## Слито

- Адрес вопроса, цена ошибки и проверка различающихся ответов — один инвариант.
- Перенос и архив — одно постусловие и один order-sensitive переход.
- Single-choice и multi-choice — один блок cookbook с одной дельтой.

## Сохранено

- Самостоятельный поиск фактов до вопросов.
- Порог не менее четырёх связанных user-owned решений.
- Воротный вопрос до зависимой формы.
- Рекомендация с основанием либо честный gap.
- Граница голосов и уникальные headings ответов.
- Слабые defaults только после общей отметки готовности.
- Отдельный блок каждой атомарной единицы refactor-а с default `Оставить`.
- Перенос к owners до архива.
- Ленивое создание `_ops/interviews/` и отказ писать в `/`.

## Новое, утверждённое владельцем

- Автоматический вызов в Codex и Claude.
- Единый tracked owner в `skills/shared/1interview-tool/`.
