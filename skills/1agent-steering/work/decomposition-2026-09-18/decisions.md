# Декомпозиция вместо чистого агента, снятие мета — 2026-09-18

## Заказ и граница

Три реплики владельца: `_ops/chat-recall/2026-09-18-230156-claude-5d0fbacb.md#L16`,
`#L18`, `#L19`. Адресная правка владельца `skills/shared/1agent-steering/portable/`:
снять приём сравнения с чистым агентом, добавить декомпозицию с целью,
границами и итогом, убрать мета-обсуждения. Принципы P-009, P-010.

## Карта потерь

Снято как приём чистого агента: сравнение с сильным агентом без текста и
истории как процедура; «дорогое снятие требует сравнения действий с условием
и без»; абзац о проверке эффекта усиления прогонами с ним и без.
Снято как мета: оговорки о границах DriftBench/CSE и Meincke в теле;
«метод требует пересмотра, если…»; «деление не гарантирует соблюдения»;
«инженерная адаптация Superpowers» в теле; «состав и стоимость проверки
определяет контракт авторинга». Границы исследований остались в сносках.
Перенесено: источники KAOS, Anthropic, DriftBench, CSE — в сноски;
«достаточный контекст между хрупким сценарием и расплывчатым пожеланием» —
в абзац Контекст. Добавлено: абзац «Декомпозиция»; «subagent briefs»
в description.

## Проверка

Одна волна трёх ролей (Opus, свежие окна): смысл, необходимость, применение.
Принято: «сильный агент» в планке дельты; проверка «если одновременно
необходимое не уменьшилось, разбивка осталась оформлением»; причина у
передачи механизму; определение границ; критерий остаётся при сломанном
джине или по контракту носителя; «семь названий — не набор для заполнения»;
снят повтор «каждый лишний критерий ослабляет»; снята фраза о Superpowers;
фрагмент-держатель смысла в проверке текста; поручение субагенту в description.
Отклонено: возврат оговорок «деление не гарантирует соблюдения» и «один случай —
одна инструкция не обязательная схема» — хеджи, снятые по реплике L19;
повтор критерия в SKILL.md вне границы заказа.

Проба применения (проверяющий, без изменения исходников): поручение субагенту
и файл задачи на проверку ссылок в `knowledge/` собрались из контекста, цели,
границ и итога без списка критериев; в файле задачи остался один критерий по
C1 `1plan-task`. Одиночная проба одной модели; сопоставимый прогон по старой
редакции не проводился, рост соблюдения не измерен.

Структурный валидатор skill-creator: pass. `git diff --check`: pass.
Побочная находка о потребителях, которые несут прежний приём сравнения:
`_ops/findings/2026-09-18-231500-4152-29053.md`.

## Дополнение того же дня

Реплика `_ops/chat-recall/2026-09-18-230156-claude-5d0fbacb.md#L20`, P-011:
в «Декомпозицию» добавлены файлы как инструкции исполнителю, слои инструкций
и скилов как нагрузка, фокус задачи вместо урезания слоёв, контекст о
параллельных исполнителях. Правка адресная, без новой волны проверяющих;
валидатор и синхронизация проекций пройдены.
