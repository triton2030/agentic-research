# Состояние — 1orchestration, круг 2026-09-01 «композиция поручения»

Состояние: `установлено`. Владелец безусловно утвердил точную версию
(«Да, устанавливай», 2026-09-01T20:03:36) и отдельно закрыл вторую половину
входа в дверь адресной правки («Да, остальное не трогаем», тот же момент,
`_ops/chat-recall/2026-08-31-222311-claude-a7a340d8.md`). Пакет записан в шесть
поверхностей: tracked owner `skills/shared/1orchestration/`, проекции
`skills/claude/1orchestration/` и `skills/codex/1orchestration/`, живые пакеты
`~/.claude/skills/1orchestration/` и `~/.codex/skills/1orchestration/`.

Хэши после записи: `SKILL.md 5a9b75a967ec…` во всех пяти файлах,
`references/brief.md 33a6cb75c517…` во всех пяти, `openai.yaml 179adf85f0e2…`
в трёх местах, где он предусмотрен. Состав живых пакетов: claude — `SKILL.md` +
`references/brief.md`; codex — те же плюс `agents/openai.yaml`. Ссылка из тела
резолвится в обеих средах, цепочек reference-файлов нет.

Цепь израсходованных состояний: `нужен полный авторский черновик` →
`готов полный черновик` → `рефактор завершён` → `установлено`.

## Маршрут и его дефект

Вход `refactor.md` → **«Адресная правка намерения»**. Дверь требует двух
дословных половин от владельца в этом разговоре про этот пакет.

- **Конкретное изменение — есть.** «Да, делай»
  (`_ops/chat-recall/2026-08-31-222311-claude-a7a340d8.md`, запись
  2026-09-01T19:29:10, `kind: selection`) — согласие на предъявленный в чате
  разбор, где форма изменения названа поимённо: отдельный reference-файл,
  читаемый в момент написания поручения.
- **«Остальное остаётся в силе» — получено после разбора.** «Да, остальное не
  трогаем» (2026-09-01T20:03:36). До этого половина отсутствовала: обе проверки
  первой волны
  установили это независимо: строка «В тело — нельзя… а влезет — сломает то, что
  чинили» написана агентом, а не владельцем, и говорит она о запрете на тело, а
  не о том, что остальное остаётся в силе. В корпусе дословных слов владельца по
  этой половине нет.

**Следствие.** Круг шёл по адресной двери условно, с компенсацией: тело v14
сверено с черновиком побайтно, снятого нет. Дефект был предъявлен владельцу
разбором до вопроса об установке, и он закрыт его собственным ответом, а не
моим выводом. Порядок остаётся дефектным: половина получена после написания
черновика, а не до входа в дверь.

Это четвёртый круг подряд, где ни одна из двух дверей `refactor.md` не
моделирует происходящую работу; относится к поданной находке
`_ops/findings/2026-08-31-235506-54071-487.md`.

**Намерение скила не затронуто.** Уникальный контекст, Задача, Цель и Критерии
принятия совпадают с v14 дословно, поэтому пункт 2 «Адресной правки» не
срабатывал и `goal-context.md` не читался. Единственная правка тела — раздел
«Когда читать» из одной строки, которого требует `reference-files.md`, шаг 3.

## Предыдущий круг

`refactor-2026-09-01-criteria`, состояние `установлено`,
`SKILL.md 2ad3d04f5cd00c98c620ff298abff1bf36df3547b12f07903497ba7d4b88c883`,
`openai.yaml 179adf85f0e2e5024e03829e3c7c32115e5f76a04c4d4d29bbeb605a93942f94`.
Он и есть старый пакет этого круга.

## Основание изменения

Решение владельца 2026-08-31T22:31:53 — «Да и как раз надо использовать знания
из файла … how-to-command-agents-with-text.md», записано принятым в
`work/refactor-2026-08-31-doctrine/intent.md:76`. В v13 и v14 оно доехало
составом частей поручения в одной клаузе Цели; способ производства каждой части
не доехал. Доктрина — проектный файл, а скил глобальный, поэтому она вошла
выведенным знанием без путей и без имени файла
(`work/refactor-2026-08-31-doctrine/intent.md:79`).

## Состав утверждаемой версии

- `portable/SKILL.md` — v14 плюс раздел «Когда читать»
- `portable/references/brief.md` — новый файл стадии
- `platforms/codex/agents/openai.yaml` — без изменений
