# Запрет worktree для фоновых сессий

Источник: `_ops/chat-recall/2026-09-10-154426-codex-01a08ae8.md#L15`.
Прямое поручение владельца выполнено в Codex runtime owner.

Изменение: `references/choose.md` заменяет предпочтение local и широкое
разрешение worktree при пересечении файлов запретом с единственным исключением:
необходимая одновременная правка одних и тех же файлов пишущими агентами.
`references/background-sessions.md` доставляет правило к созданию сессии.
Решение добавлено в существующие продуктовые принципы пакета.

Две независимые read-only проверки: буквальная проверка не нашла противоречий
или недоступных ссылок; проверка траектории предсказала local для читателей,
писателей разных файлов и последовательных правок одного файла, допустимость
worktree только при необходимой одновременной записи в те же файлы.
Предсказания не являются наблюдением реальных запусков. Замечаний и отклонений нет.

Проверка доставки: `sync_simple_projections.py 1orchestration --write --install`,
затем `--check` — все tracked и installed projections совпадают с owners.
`git diff --check -- skills/codex/1orchestration` — без ошибок.
Чужие незакоммиченные изменения сохранены; других смысловых правок не вносилось.

Граница доказательства: изменён текст скила, runtime-блокировка не добавлялась;
старшие инструкции среды продолжают определять разрешённые операции.
