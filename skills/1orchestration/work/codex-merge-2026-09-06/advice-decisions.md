# Решения по Opus-совету

Результат: runs/02-opus-advice.json в папке оркестрации. Validated model claude-opus-5; requested opus/xhigh; native session сохранена.

- Принято: обычный Codex-субагент не заменяет Opus; добавлено в choose.md.
- Принято: возможность маршрута проверяется до его выбора; choose.md уже содержит точную границу callable schema, без выдуманного разрешения.
- Отклонено: «автономная работа» сама открывает create_thread. Прямо противоречит старшим инструкциям, запрещающим этот tool для внутренних подзадач.
- Принято: Opus по существенной развилке/аудиту, а не каждую волну; это условие в choose.md.
- Принято: измерение экономии и research citation не нужны в рабочем тексте; остаются в истории.
- Принято: preserve прямой запуск/продолжение фона и lifecycle; description явно называет background Codex tasks, accept.md различает запуск и управление.
- Принято: инверсия local/worktree, не рекурсивное поручение, память и GitHub; сохранены в текущих байтах.
- Не принято удаление стадий целиком: это условные моменты загрузки невыводимого знания, а не фиксированный мыслительный алгоритм. choose даёт распределение/параметры, execute — адресуемые возвраты, accept — проверенное принятие. Возврат в нужную стадию по состоянию, без повторного прогона всех стадий. Три reference вместо предложенных двух предотвращают одновременное чтение native + session + приёмки в одном файле; стоимость проверят два независимых reviewer.
- Сохранён критерий восстановимости: владелец прямо повторил датированную папку и автономную длительную работу. Снятие ради generic/non-generic потеряет его условие.
