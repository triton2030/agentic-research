# Итог независимой волны и локальных исправлений

Два terminal возврата получены: runs/03-instruction-review.md и runs/04-trajectory-review.json в папке оркестрации. Оба читали неизменный v1. Второй волны не было; локальные поправки проверены root на текущих байтах candidate-v2. Новых дорогих снятий обязательств нет.

## Instruction review

- I1 принято: причина каждой строки восстановлена в execute/Передача работы.
- I2 принято: accept использует выбранных проверяющих и существующий прогон; короткому поручению достаточно ответа с адресами.
- I3 принято в части подтверждённых дублей: удалены повторные execute-критерии, единственный контракт перенесён в поручение/прогон; повтор критерия памяти из тела снят, знание осталось в execute и входе после сжатия. Требования полей памяти не сняты ради счёта. Оценка v1 (24/22/72/37 независимо нарушимых предикатов) сохраняется как предупреждение, а не исчезает от переноса абзацев; редакционный ориентир 20 не объявляется доказанным бюджетом головы.
- I4 принято: property-evidence.md различает наблюдаемые артефакты и прогноз, финальная карта перенесена в cut.md.
- I5 принято: название раздела исправлено.

## Opus trajectory

- F1 принято: description начинает с large / many files / sessions до решения делегировать.
- F2 принято по существу: недоступность одного транспорта не отменяет остальные. Формулировка «снимаемый владельцем» отклонена: текущие старшие инструкции не снимаются одним user-предпочтением.
- F3 принято: Opus закреплён в приёмке существенных развилок и итога большой работы; Codex-окно не считается заменой. Повтор пояснения в choose снят, выбор Opus там сохранён.
- F4 принято: множество писателей ИЛИ изменяемых файлов явно включает Luna-маршрут.
- F5 частично принято: один табличный отчёт всей волны разрешён, но остаётся счёт КАЖДОГО поручения и root. Предложение считать только самое нагруженное отклонено: теряет прежнее требование и требует сравнительного эксперимента. Такого снятия не делали.
- F6 принято без потери выбора модели: маршруты условные ненумерованные; execute сразу различает заказ только на запуск и приёмку результата. Выбор исполнителя не пропускается, когда ещё не сделан.

## Граница доказательства

Текущие файлы перечитаны целиком, ссылки и schema проверены. Реальная консультация Opus дала исправления; clean-room Luna восстановил распределение из намерения. Автоматический discovery, выигрыш цены/времени и разрешённый live background-run не измерены. Высокий granular счёт полей длительной памяти остаётся потенциальной нагрузкой; его соблюдение нельзя объявить проверенным по длине файла.
