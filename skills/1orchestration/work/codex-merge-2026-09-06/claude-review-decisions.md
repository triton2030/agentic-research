# Claude: решения по второй волне

Буквальный reviewer: явно назвать отсутствие Claude end-to-end — принято; проверяется текст/структура и parity, не runtime-поведение.
Opus trajectory (validated claude-opus-5, native session в runs/06):

- F1 принято: choose явно связывает старый приоритет ролей 1codex с работой, которую Claude ведёт сам; для этой оркестрации новый owner задаёт Codex исполнителем и native Opus проверяющим. Сам пакет 1codex сохранён.
- F2 принято по наблюдаемому выбору яруса: модель задаётся явно инструментом или профилем. Непроверенное обобщение о наследовании каждым типом агента не записывается как факт.
- F3 принято: общая папка относится к своим исполнителям; изоляцию Codex bridge определяет 1codex. Первое решение владельца без worktree было явно Codex-specific, последующее Claude-уточнение переносит память, но не отменяет гарантии моста.
- F4 принято: description называет распределение между Codex и собственными агентами; прямой запуск/наблюдение Codex остаётся 1codex.
- Старые references удаляются только после сравнения со снимком, новый состав устанавливается существующим sync.

Оба terminal ответа получены. Это вторая и последняя волна текущей работы; локальные исправления перечитаны и механически проверены root. Поведенческий прогон из Claude не выполнен.
