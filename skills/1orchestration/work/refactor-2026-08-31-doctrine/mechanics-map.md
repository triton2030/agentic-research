# Механика старого пакета, разобранная до сути — 1orchestration v10

Карта root-а для сверки потерь. Исполнителю чистой комнаты не передавалась.
Источник: `skills/shared/1orchestration/portable/SKILL.md`
(`0dab19d7bf285693f84f4eebac9ca2733698a9d0abb40fd604c61215a6edbf7e`) и
`platforms/codex/agents/openai.yaml`; история снятий — `cut.md`, происхождение
правил — `origin.md`.

## Что пакет заставляет root делать

| № | Механизм v10 | Суть: что он реально предотвращает | Что ломается без него |
| --- | --- | --- | --- |
| M1 | trigger «перед назначением субагента или разделением перегруженной работы» | скил читается до написания промпта, а не после | поздний вызов: промпт уже отправлен, оценка бессмысленна |
| M2 | root — технический директор: польза, траектория, решения, интеграция, приёмка | root не растворяется в исполнителях и не отдаёт наружу приёмку | никто не владеет общей целью; сумма возвратов не складывается в результат |
| M3 | остальные участники удерживают только выполнимые наборы | асимметрия ролей: тяжесть общей картины не размазывается | исполнителю выдают контекст, который ему не нужен и который он не удержит |
| M4 | цель: активные наборы каждого actor и следующего решения root выполнимы | посильность считается с обеих сторон границы | оптимизируют только исполнителя, root тонет |
| M5 | цель: brief не становится owner-ом правды, адресованной в `read` | промпт не превращается во вторую копию канона | дубль и drift: файл изменился, промпт остался прежним |
| M6 | цель: зависимый ход только после `pass` по актуальному evidence | барьер перед зависимой работой | downstream строится на непроверенном или устаревшем основании |
| M7 | шаг 1: до brief root читает load-bearing owner/authority источники; делегируемое evidence читает исполнитель | root знает, что вообще влияет, прежде чем делить | brief неполон: root не знает, какие критерии существуют |
| M8 | шаг 2: интерфейс `outcome · done_when · evidence на критерий · read (точные адреса) · delta` | пять полей поручения как один неразрывный контракт | исполнитель не знает done, куда смотреть и что здесь нового |
| M9 | шаг 3: после brief оценить активный набор каждого предполагаемого actor | оценка идёт по готовому поручению, а не по замыслу | считают воображаемую задачу, а не выданную |
| M10 | шаг 4: отдельно оценить активный набор root в его следующем решении | root не забывает про себя | root перегружен интеграцией и теряет траекторию |
| M11 | шаг 5: активная единица — одна независимо забываемая вещь | счёт идёт по смыслам, а не по строкам | перегрузка маскируется форматированием |
| M12 | шаг 6: ориентир около `20` — мягкий сигнал, не предел | порог не превращается в механический split | либо дробят без нужды, либо порога нет вовсе |
| M13 | шаг 7: простейшая форма, удовлетворяющая целям | сложность оправдывается только целями | оркестрация ради оркестрации |
| M14 | шаг 8: делегировать только через границу, после которой кто-то перестаёт удерживать часть набора | запрет декоративной делегации | root получает прежнюю работу плюс интеграцию сверху |
| M15 | шаг 9: утверждение, отчёт о ходе и «done» не evidence | приёмка по наблюдаемому, не по самоотчёту | ложный `pass`, обнаруженный поздно и последствиями |
| M16 | шаг 10: изменился влияющий источник или вход — вернуть работу к первому устаревшему результату | откат вверх по потоку, а не просто блокировка нового хода | уже выполненный downstream молча остаётся построенным на старом входе |
| M17 | platform metadata: `display_name`, parity `short_description`, русский `default_prompt`, `allow_implicit_invocation: true` | скил виден и вызывается сам, а не только руками | скил существует, но не срабатывает |

## Что v10 уже потерял против слов владельца

Проверять на стадии сверки потерь; в чистую комнату не передавалось.

| Потеря | Слова владельца | Когда исчезло |
| --- | --- | --- |
| наблюдаемое доказательство владельцу в чате | «Я бы хотел бы туда добавить вывод прямо в чат, ну, как доказательство работы агента» | `cut.md`, v7 clean-room: «не возвращены launch map…»; отмены владельцем не было |
| взвешивание когнитивной цены инструкции и силы модели | «понимать когнитивную нагрузку от каждой инструкции… с какими инструкциями справятся, какие модели» | в v10 нет ни строки о модели исполнителя; в v11/v12 вернулось в Уникальный контекст |
| фокусировка внимания вместо запрета | «в инструкциях мы можем как раз-таки сфокусировать его… это его сейчас фокус» | v10 держит `read` и `delta`, но не подсветку важного; запрет физически невозможен, а замены нет |
| условный durable след волны и реестр волевых решений | «реестр его самостоятельных принятых решений с его обоснованием» | `cut.md`, v5: безусловный carrier снят; v7 снял и условный. Владелец 2026-08-31 вернул условно |

## Что снято обоснованно и возвращать не следует

Из `cut.md`, без owner-возражений: статическая таблица ролей и прав, wave-папка
как безусловная обязанность, шесть references v7 и route-cascade, state machine
и общий runtime lifecycle, обязательный verifier и executor/verifier split,
числовые пороги `3+ владельца` / `3–4 окна` / `>4`, статическая таблица
instruction-cost, hard cap `20`, LLM-судья и метрики оркестрации, universal A2A
и always-on supervisor, полный `count → budget` реестр как безусловный режим,
authoring checkers и approval-gate внутри runtime-пакета.

## Внешние швы, проверенные чтением, а не README

`skills/1orchestration/README.md` называет два шва, которых больше нет:
`skills/claude/1codex/references/{orchestration,fleet}.md` и
`skills/shared/1planning/portable/references/delegation.md`. Оба файла удалены
при перекройке своих пакетов. Находка записана —
`_ops/findings/2026-08-31-224500-orchestration-stale-seams.md`.

Живые швы на 2026-08-31:

| Шов | Адрес | Что он утверждает |
| --- | --- | --- |
| отрицательный routing из `1codex` | `skills/claude/1codex/SKILL.md:6` | «Claude's own subagents are 1orchestration» — субагенты Claude уходят сюда, а не в Codex-флот |
| правило одного писателя | `skills/claude/1codex/references/delegate.md:80` | «Два воркера не правят один файл… это runtime-исполнение общего правила `1orchestration`» — сосед ссылается на общее правило, которого в v10 нет |
| специализированные волны | описания `1fresh-eyes` и `1deep-agents` | их волны этим скилом не перехватываются |

Второй шов — открытая развилка стадии поведения: либо правило одного писателя
возвращается сюда, либо строку правит владелец `1codex`. Владелец касался
предмета в соседнем контексте: «мы стараемся не использовать отдельные ветки
Work 3. Только если доказано, что реально файлы между собой пересекаются»
(`_ops/chat-recall/2026-08-29-203235-codex-01a04e23.md`, 2026-08-29).
