# Check-approve — 1plan-map — 2026-08-29

## Цикл 1

Проверен полный русский черновик вместе с product frame, cut, refactor map и
соседями семьи. Независимые окна: literal checker `/root/check_literal`,
trajectory checker `/root/check_trajectory`; внешний совет — `claude-opus-5`,
session `7fc9d0ba-3bbd-47c7-a1b9-81d39df72afe`.

Принято и исправлено:

- форма состояния стала владельцем общего словаря и нормализации снимка;
- при отсутствии пары сначала идёт `1product-shaping`, и лишь его отрицательный
  результат разрешает `нет принципа`;
- bootstrap точно называет корень карты, прибор, проектную инструкцию и команду;
- composition, epic-state и dashboard стали самостоятельными write-stages;
- map-event меняет только эпик; статус task-файла остаётся у task-writer.

Не принято:

- Opus finding о молча исчезающем эпике: `запуск` обязателен, сломанную схему
  отвергает прибор;
- Opus finding о ничейном снимке как owner-gap: owner и нормализация уже были у
  карты, исправлен только routing;
- разрешить несколько активных эпиков по умолчанию. Прямые решения владельца
  называют эпики порядком и автономную работу последовательной, не параллельной
  (`_ops/chat-recall/2026-08-24-181208-claude-13fd1279.md:23,29`).

Цепочки новых жёстких ограничений `default → механизм → вред → цена` записаны
в [карте рефактора](refactor-map-2026-08-29.md#цена-строгости).

## Цикл 2

Точная portable-версия `3f3811db…` прошла финальный барьер:

- содержательные references получили локальные `## Цель`; router остался
  служебным;
- перегруженные стадии раскроены по существующим швам: композиция/форма,
  словарь/фронтир, structural/state validation, bootstrap/update дашборда;
- один пакет карты заменяет результаты прошлых стадий; новая карта записывается
  только вместе с прибором и сразу проверяется;
- literal checker: максимум `20`, русский текст/links/descriptions — PASS;
- trajectory checker: человеческая карта, последовательный frontier и разные
  writers — PASS;
- clean executor: invalid live task вне frontier блокирует перестановку и
  валидацию; ложный status не записывается — PASS.

Hard lines оставлены для схемы, write ownership, frontier, команды прибора,
структурной/state проверки и независимой acceptance; топологию и аудиторию
ведут локальные цели.
