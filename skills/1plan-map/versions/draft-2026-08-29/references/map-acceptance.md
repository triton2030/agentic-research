# Стадия приёмки карты

## Цель

Чистое окно принимает смысл карты после структурной проверки и не пропускает
пересечение задач, ложную траекторию или пересказ документации.

1. Больше одной задачи `🔨` в эпике возвращает границы в `1planning`; это не
   штатный режим.
