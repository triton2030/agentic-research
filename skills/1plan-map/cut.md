# Cut — рефактор 2026-08-29

## Оставлено

- вывод эпиков из Product Frames, принципов и `GOAL.md`;
- непересечение, зависимости, порядок, frontier и evidence-закрытие;
- постоянная история закрытого и отложенного;
- dashboard и машинный прибор.

## Перенесено

- допуск и срез следующей задачи → `1planning`;
- запись и обычное исполнение task-файла → `1plan-task`.

## Снято

- frontier больше не разрешает карте самой придумать и создать JIT-задачу;
- событие состояния не может попутно менять композицию;
- эпик не дублирует цель, критичный контекст и ограничения исполнения задачи.

## Новый seam

Форма карты владеет общим словарём статусов и нормализацией `эпик-снимок`;
task-форма зеркалит их, а прибор проверяет parity. Цена — явная зависимость
task-схемы от map-схемы вместо ничейного контракта.

Композиция, состояние эпика, dashboard и validation стали самостоятельными
стадиями с одним reference в активном наборе. Событие карты теперь меняет
только эпик; статус task-файла остаётся у текущего task-writer.

Snapshot теперь содержит только composition-owned смысл: обычный state-event
не инвалидирует task-контракт. `description` стал trigger-only; русский runtime
сохраняет литеральными имена, команды, schema keys и код.

## Commander intent pass

- Человеческая аудитория карты, последовательная композиция и раздельные
  писатели перенесены в «Уникальный контекст» и «Цели владельца».
- Содержательные references получили локальную `## Цель`; выводимая процедура
  снята, а перегруженные проверки разнесены по уже существующим швам.
- Жёсткими оставлены схема, фронтир, разделение composition/state, прибор и
  порядок стадий: они задают точные write/safety/acceptance seams.

Детальная трассировка:
[refactor-map-2026-08-29.md](refactor-map-2026-08-29.md).

## 2026-08-31 — рефактор по доктрине композиции

Кандидат — `versions/candidate-2026-08-31-doctrine/`. Тело 20 единиц,
`references/epic-schema.md` — 12 единиц.

### Сохранено

M1–M14, M16, M17 карты механики. M6 (запрет второй карты) поглощён точной
схемой размещения: у второй карты больше нет легального адреса. M12
(последовательный frontier) перенесён в схему, где живут `порядок` и `запуск`.

### Возвращено из слов владельца

- Чтение **всех** продуктовых рамок и **всех** принципов до состава эпиков —
  буквальной цитатой, потому что владелец назвал это обязательным дважды.
- `_ops/GOAL.md` как основание, когда рамок в проекте нет вовсе.
- Вызов `1use-principles` и адреса рамок и принципов внутри эпика.
- Сверка показанного состояния с живыми файлами задач при аудите.
- Возврат `изменений нет` как доказанного вывода.

### Добавлено чистой комнатой

- Проверка выводом: из написанного эпика выводится одна задача и называется одна,
  которую границы обязаны остановить. Единственная наблюдаемая проверка первой
  цели.
- Эпик не наследует переписку, в которой писался.
- План исполнения в эпик не кладут: он замораживает решение раньше времени.
- Бесконечный эпик не имеет исхода и навсегда оседает в остатке.
- Закрытие по достигнутому исходу, а не по исчерпанию списка задач.
- Форма карты не переделывается попутно с работой, за которой позвали.

### Снято

- Условная ветка про ссылку на INDEX-раздел: цена нарушения низкая, а постоянное
  место в бюджете внимания она занимала.
- Абсолютный путь к файлу доктрины: глобальный скил не может зависеть от
  соседнего репозитория на конкретной машине; нужная дельта внесена текстом.

### Глобализировано из mavo-short2

По решению владельца `2026-08-31-202257-claude-86d85f7e.md#L25` схема стала
общим стандартом: колокация задач внутри папки эпика, 13 ключей frontmatter,
словарь статусов, отсутствие процента у эпика, Obsidian Bases. Источник —
`_workspace/planning-refactor-2026-08-31/live-schema.md`, снято с работающих
файлов, не сочинено.
