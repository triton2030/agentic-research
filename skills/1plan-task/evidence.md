# Evidence — 1plan-task

## 2026-08-26 — рождение при раскройке тройки

Обязательная пара субагентов с чистыми окнами (одна на весь рефактор тройки,
прогон по канону `1skill-creation`):

- **Сократитель**: счёл тело ~29 единиц — самый плотный слой дублей
  тело↔форма. Применено правило владения: словарь provenance, «ровно три
  секции», 3–7, «[x] с доказательством», «не воровать чужие подзадачи»,
  «адрес вместо пересказа», определения режимов — владеет task-form; тело
  держит момент, изоляцию, mode-flip, zombie-rebuild, «two retellings»,
  fresh-reader. Внутрифайловые дубли формы сняты.
- **Проверщик ухудшений**: функция совпала с закреплённой владельцем
  («скилл изолированности задач», raw 2026-08-26-220614 #L25–L26);
  тогл-отчёты и «конкретика только здесь» подтверждены. Гейт «цитата
  критерия эпика в „Зачем"» признан настоящим нефейкуемым следом. Закрыто по
  вердикту: неопределённый `эпик-снимок` (нормализация — map-form), деферал
  задачи `⏳` и re-entry (lifecycle формы). Пол 3–7 подзадач оставлен —
  записанное правило Миллера старого контракта; решает владелец.

Support envelope: файл живёт в связке с картой (`1plan-map`) и инструментом
проекта; вне их работает форма и изоляция, счётчики деградируют в ручные.
Полигон — mavo-short2.
