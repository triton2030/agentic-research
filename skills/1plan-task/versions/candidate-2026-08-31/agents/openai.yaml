interface:
  display_name: "1-Задача плана"
  short_description: "Use for one approved task file"
  default_prompt: "Используй $1plan-task для одного утверждённого файла задачи одного агента."

policy:
  allow_implicit_invocation: true
