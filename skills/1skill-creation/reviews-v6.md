# Прогоны проверки — v6

## Круг 1, 2026-08-28

Черновик: `draft-v6/` (оверлей из 5 файлов на момент прогона).

### check-instructions (7 находок)

1. check-approve шаг 2 пересказывает содержание agent-файлов + дубль «пустой
   список законен» → **принято частично**: ярлыки-описания сняты; «каждый
   возвращает список правок; пустой список законен» оставлен как контракт
   родителя (у субагентов — своя норма в их файлах).
2. check-instructions шаг 1: семь файлов хвостом одной строки → **принято**,
   разбито списком.
3. check-harm шаг 1: шесть файлов одной строкой → **принято**, разбито.
4. check-harm шаг 3: три каталога одной строкой → **принято**, разбито.
5. Снять перечитку `writing-rules.md` из шага 1 check-approve как дубль
   проверки субагента → **отклонено, в разбор**: правка утверждена владельцем
   в этой сессии («4 согласен»); функции разные — доставка правил пишущему в
   точке действия против независимой проверки.
6. `user-said.md` по имени → «буквальные цитаты пользователя в папке-истории»
   → **принято** (коллизия с замороженным `_ops/user-said/`).
7. Пронумеровать «Сначала прочитай writing-rules» шагом 1 → **отклонено, в
   разбор**: противоречит правилу владельца «режимное правило не нумеруется»
   (user-said, сессия 9bb215e3 L28).

### check-harm (1 находка)

1. Шаг 1 check-approve «собери черновик целиком» не имел следа: draft-v6 был
   оверлеем из 5 файлов, состав объяснялся субагентам устно → **принято**:
   draft-v6 дособран до полного пакета копиями неизменённых файлов portable.

### Исполнитель (пробный случай «рефактор 1findings»)

Шаги: SKILL.md → refactor.md → 1findings целиком → goal-context; УК и ЦП
предложил сам, остановился на обсуждении с пользователем — совпадает с целью
режима. Отклонение: `writing-rules.md` прочитал после файлов режима, не
первым (сам это заметил и назвал). Наблюдение в разбор: прозаичная строка
«Сначала…» исполняется с запаздыванием, но маршрут не исказила.

## Круг 2, 2026-08-28 (полный черновик после правок круга 1)

### check-instructions (7 находок)

1. Тело роняет случай «сталкивается с соседом» из входа `routing.md` →
   **принято**, случай добавлен в строку режима.
2. Разбить «На разделе читай только…» на две строки (три «его») →
   **отклонено**: ось однозначности без наблюдаемого неверного разбора —
   исполнитель круга 1 прочёл строку верно; v5 уже отклонял этот класс
   («налог на строгость»).
3. «Глубоко пойми» → «называй, что он решает» → **отклонено**: тот же класс,
   строка пережила веер v5, нового сбоя нет.
4. Снять «максимально понятно, простыми словами» из разбора → **отклонено
   сверкой с историей**: дословная формулировка владельца (raw L27), урезание
   уже отклонялось в v4.2 — рецидив, гейт шага 4 сработал.
5. Абстрагирование стрелками → нумерованный подсписок → **принято**
   (порядковое нумеруется — формат-грамматика владельца).
6. В списке чтения check-harm нет `reference-files.md` и `routing.md`, их
   шаги не уличить в невыполнении → **принято**, добавлены.
7. Раздел «Соседи» дублирует «когда использовать» → **отклонено**: конвенция
   пакета из v5, не введена v6.

### check-harm (2 находки)

1. Ссылка `../../1context-refactor/references/check.md` в `refactor-check.md`
   бита из tracked owner → **отклонено**: проверено `ls` — резолвится в обеих
   живых раскладках (`~/.claude/skills/`, `~/.codex/skills/`), где скил
   исполняется; несовпадение слоёв tracked/live — свойство проекции.
2. «Сначала прочитай writing-rules» не подпёрт следом; исполнитель круга 1
   реально прочитал его с запозданием → **в разбор**: правка меняет поведение
   гейта, решает владелец.

## Внешнее ревью (другой агент, через владельца), 2026-08-28

Решения владельца по дельтам ревью: повтор шагов 2–3 с исполнителем —
принято; след исполнителя в сдаче — принято; передача контекста субагентам —
принято; позитивная матрица + проверка добавок у check-instructions —
принято; «не ослабляй намеренную строгость» в harm — отклонено владельцем
(«лишнее усложение»); механические починки соседей — приняты и выполнены
(1instruction-authoring, 1context-refactor, 1local-rules оба рантайма,
sync зелёный; `/raw/` убран из user-said/origin; ссылка в refactor-check
переписана прозой). Потери ревью, не взятые в черновик: сверка «потерянного
нет», «снятие — лучший исход», проверка шагов протокола по артефактам —
сохранены из нашей версии. Мандат check-harm переписан по новым словам
владельца: оценщик строгости, два уровня (цель→поведение · протокол↔цель).

## Круг 1 нового захода (после дельт владельца и ревью)

### check-instructions (5 находок)

1. Строка режима behavior-protocol в теле 231 символ (>200) → **принято
   частично**: хвост сокращён до «не просил — раздела нет» (~190), явный
   запрет раздела сохранён — правило владельца «протокол только по явному
   намерению» не выводится из отсутствия чтения.
2. «Референс читается только при необходимости» — запрещённое сводом
   расплывчатое слово → **принято**, строка начата с «Важная логика…».
3. «…refactor-failures; перед сдачей — refactor-check» читается как одно
   условие → **принято**, разведено на две строки.
4. Команда чтения базы в refactor-check спрятана в прозе → **принято** в
   адаптированной форме: bullet без возврата относительной ссылки (проза
   владения сохранена по решению владельца о механической починке).
5. Шаг 1 check-approve — четыре глагола хвостом → **принято**, нумерованный
   подсписок.

### check-harm (2 находки)

1. В списке чтения check-harm нет `agents/check-instructions.md` — вредную
   строку в файле напарника не увидеть → **принято**, добавлен.
2. Не оговорён счёт повторов при смене мандата проверяющих → **отклонено, в
   разбор**: юридическое уточнение края («налог на строгость»); действующее
   правило «новые решения владельца → новый заход, счёт с нуля» покрывает
   случай по аналогии с правкой после «да».

### Исполнитель (пробный случай «скил уборки Downloads»)

Шаги: SKILL.md → writing-rules первым → проверка папки-истории (нет) →
goal-context → остановка на шаге 1 с вопросами пользователю (контекст,
таксономия, перемещение/копирование, конфликты). Расхождений с целью нет;
чужие референсы не открывал.

## Круг 2 нового захода (повтор шагов 2–3 по изменённому черновику)

- check-instructions: **находок нет** (строка behavior-protocol — 190 симв.;
  компактные конструкции сверены с нормой пакета falsifier-ом; ссылки
  резолвятся).
- check-harm: **находок нет** (все шесть изменённых мест проверены; вопрос
  счёта повторов уже в разборе, повторно не поднят).
- Исполнитель (пробный случай «рефактор 1html»): writing-rules прочитан до
  режима; скил и все references прочитаны целиком; tracked owner проверен по
  адресным правилам; остановка на предложении УК/ЦП владельцу с вопросом о
  снятии скила («снятие — лучший исход») и о восстановлении папки-истории.
  Расхождений с целью нет.
