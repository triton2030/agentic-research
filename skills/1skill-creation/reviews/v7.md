# Проверка v7 — 2026-08-29

## Первый круг

- Буквальный проверяющий нашёл несамодостаточные цели, reference→reference
  переходы, перегруженные тело и `check-approve.md`, отсутствие v7-следа.
- Проверяющий траектории нашёл те же переходы, слишком широкий триггер
  протокола и ритуальные обязанности рефактора.
- Holdout `1dependency-decision` прочитал четыре references цепочкой и создал
  дублирующий «Протокол поведения»; след —
  `/tmp/1skill-creation-holdout.Dortm8/skills/1dependency-decision/`.

Принято: самодостаточные три цели; тело как единственный router; references с
собственными выходами; протокол только для обязательного пути; рефактор только
с refactor-specific логикой; независимые сборка, проверка и установка.

## Финальный круг

- Буквальный проверяющий: `CLEAN`.
- Проверяющий цели и траектории: `CLEAN`.
- `rumdl`: 12/12 файлов без замечаний.
- `quick_validate.py`: `Skill is valid!`.
- Финальный holdout прочитал по команде тела `goal-context.md` →
  `skill-short-description.md` → `writing-pass.md` → `check-approve.md`; ни
  один reference не открыл другой.
- Holdout не создал «Протокол поведения», не задал выводимых вопросов и не
  открыл `agent-defaults.md`; тестовый скил содержит шесть активных единиц.
  Фактический след —
  `/tmp/1skill-creation-holdout.Dortm8/postfix/skills/1dependency-decision/`.

Остаток: один holdout подтверждает исполнимость и выбранную траекторию, но не
измеряет вероятность соблюдения на разных моделях.

## Коррекция после слов владельца L26–L30

Этот круг отменяет прежний `CLEAN`: после него владелец вернул свой текст и
уточнил смысл reference-режимов и единицы инструкции.

- Дословно возвращены авторские Уникальный контекст и три цели.
- `writing-pass.md` снят; проверка и установка снова разведены по решению
  пользователя, поэтому `install-approved.md` оставлен самостоятельным режимом.
- Тело стало единственным одноуровневым router-ом: один reference за раз, без
  reference→reference переходов.
- Пункты всех references и agents логически разнесены; пояснение остаётся
  внутри инструкции, только пока не создаёт второго выбора.
- Проверяющий траектории после правок: `CLEAN`.
- Буквальный проверяющий подтвердил ссылки и отсутствие цепочек, но не принял
  бюджет: при строгом сложении всех предикатов тела и выбранного reference
  активные стадии превышают 20.
- Holdout `1meeting-decisions`: 29→15 единиц; авторский текст сохранён; nested
  review не запустился из-за `agent thread limit reached`.
- Holdout `1incident-update`: 27→4 инструкции; наблюдаемый прогон смешал один
  факт с решением, поэтому результат `UNKNOWN / NOT APPROVED`, а не pass. След:
  `/tmp/skill-creation-v7-holdout2-20260829/`.
- `rumdl`, `quick_validate.py` и проверка внутренних ссылок прошли на 11 файлах.

Остаток до утверждения: владелец должен зафиксировать, бюджет 20 относится к
самостоятельным поведенческим инструкциям текущего режима или к строгой сумме
всех фактов, целей, условий и ограничений тела вместе с reference. Версия не
установлена.

## Решение владельца и установка

- Владелец выбрал первый вариант: «Да, установи первый вариант»
  (`_ops/chat-recall/2026-08-28-180725-codex-01a04879.md`, L31). Бюджет
  относится к самостоятельным поведенческим инструкциям, которыми агент
  фактически руководствуется в текущем режиме; пояснения одной инструкции и
  обязательства завершённых стадий повторно не складываются.
- Возражение буквального проверяющего про строгую сумму не является дефектом
  принятой версии: оно использовало отвергнутую владельцем модель счёта.
- Операционный счёт утверждённой версии: `SKILL.md` 19 · `agent-defaults` 13 ·
  `behavior-protocol` 9 · `check-approve` 17 · `goal-context` 18 ·
  `install-approved` 10 · `refactor` 13 · `reference-files` 15 ·
  `skill-short-description` 14 · `check-instructions` 17 ·
  `check-trajectory` 20.
- Неизменённый `мой draft-v7` записан в shared owner, tracked Claude/Codex и
  обе live-проекции. `writing-pass.md` удалён из активного пакета; Codex-only
  `agents/openai.yaml` сохранён.
- `diff -qr` подтвердил равенство draft и portable owner;
  `sync_simple_projections.py --check` — равенство owner и проекций; `rumdl` и
  `quick_validate.py` прошли для всех пяти копий; все внутренние маршруты
  существуют.

Остаточный риск: два holdout-прогона показывают исполнимость маршрута, но не
вероятность одинакового соблюдения на разных моделях; один прогон правильно
остановился с `UNKNOWN / NOT APPROVED` после смешения факта с решением.
