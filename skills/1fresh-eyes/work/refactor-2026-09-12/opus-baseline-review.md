# Opus baseline review

Model: claude-opus-5
Session: fe57aa49-de92-44a3-933a-513693eea101

Read-only review complete. Five findings, ordered by impact on the authoring route; then what I deliberately left unchanged, then limits.

## Findings

**1 · The live Product Frame is two generations stale and would mislead this very refactor (highest impact).**
`skills/claude/1fresh-eyes/product-frame.md:12,18,22` still states "Расхождение отчётов сильнее консенсуса: совпадение — сбой запуска", "Cross-model — `1codex`" (Claude-only), and "Тело ≤ ~100 строк: три зонтика + два наблюдаемых гейта". The shipped body contradicts all three: `references/synthesis.md:11` permits an identical outcome when verification paths differ; premortem is symmetric per runtime (`claude/references/premortem.md:18` → `$1codex`, `codex/…:18` → `$1claude-mcp`); `SKILL.md` is a 33-line six-step controller with no umbrella/gate structure. The frame matching the shipped bytes exists but was never promoted: `skills/1fresh-eyes/draft-2026-08-29/product-frame.md:1-28` self-identifies as "installed generation" with package fingerprints, and commit `1c0fb9c4` touched only SKILL/references. Also `product-frame.principles.md:35` (FE-4, "Codex в панель не входит", commitment `decision`) contradicts FE-7 at `:64-69` with no reconciliation note; the later candidate frame added exactly that note (`draft-2026-08-30/product-frame.md:50-52`). Concrete failure: an autonomous refactor reading the live frame as governing intent "restores" совпадение=сбой-запуска and Claude-only `1codex`, and may drop the cross-family premortem as FE-4 says Codex isn't in the panel — undoing adopted decisions. Minimal correction: promote the 2026-08-29 frame (tiebreakers + Владение) over the live pair, carry the FE-4→FE-7 note into the principles file. Source support: the two draft frames + `draft-2026-08-30/cut.md:61-66`.

**2 · `claude/references/synthesis.md:7` calls the cross-family report native.** It reads "Вход: четыре терминальных родных отчёта", while `claude/references/premortem.md:7` says the premortem output is "терминальный отчёт Codex". The Codex twin is correct: `codex/references/synthesis.md:7` — "три родных отчёта и терминальный отчёт Claude". Provenance is a one-sided edit: both drafts said "четыре … родных" at `draft-2026-08-30`, and only the codex file was fixed in `candidate-final-2026-08-30`. Concrete failure: a Claude-side controller whose synthesis input contract demands four *native* reports can satisfy it with the native `premortem` lens that exists in the roster (`~/.claude/agents/premortem.md`), which is the substitution FE-7 forbids; short of that, it normalizes the foreign report into native form and loses "родную запись каждой линзы" (`synthesis.md:11`). Minimal correction: mirror the codex wording.

**3 · Synthesis states the divergence condition but names no terminal for its failure.** `claude|codex/references/synthesis.md:11` says an identical outcome is full-value "только при разных путях проверки", and the only output it defines is the handback at `:14`. Nothing tells the controller what to return when the four reports are materially the same. Both later frames name it: `draft-2026-08-29/product-frame.md:19` ("одинаковые метод, источник и следствие — провал запуска") and `draft-2026-08-30/product-frame.md:28-29` ("дают точный `panel_incomplete`"); `origin.md:19-20` records it as the second observable gate ("приёмка: уникальная строка из каждого отчёта"). Concrete failure: contaminated or interchangeable packets return four convergent reports, and the controller delivers a confident `unchanged` handback built on a failed launch — the exact deficit that created the skill (`origin.md:3-5`). Minimal correction: one line in `synthesis.md` making materially identical method/source/consequence a `panel_incomplete` terminal rather than a handback. Panel and named modes already carry explicit terminals; synthesis is the only stage without one.

**4 · Frame files sit inside the runtime owner folder, so the repo's own projection tool fails this package (confirmed by execution).** `skills/shared/sync_simple_projections.py` builds a runtime-owner manifest from every file under `skills/<runtime>/<skill>`; `python3 sync_simple_projections.py 1fresh-eyes --check` returns exit 1 with `installed claude/1fresh-eyes: missing product-frame.md` / `…principles.md`. The "fix" the tool offers (`--write --install`) would copy ~105 lines of owner history and quotes into the live Claude package, violating the adopted boundary "История и Product Frame не устанавливаются в runtime package" (`draft-2026-08-29/product-frame.md:27-28`). Convention supports the move: 20+ frames live in `skills/<skill>/`, shared owners keep theirs at `shared/<skill>/` root outside `portable/`. Minimal correction: move both files to `skills/1fresh-eyes/` (which already owns `origin.md`, `cut.md`, `evidence.md`); this also lands finding 1 in one operation.

**5 · `claude/references/steering.md:13` lost the continuation mechanism its Codex twin keeps (prediction).** Codex `:13` names both (`send_message` for a running target, `followup_task` for an idle one); Claude says only "Передай сохранённому нативному `Agent`-потоку". `cut.md:65-67` records that the Send/resume mechanics were *Claude's* and deliberately not carried to Codex, and `draft-2026-08-29/cut.md:20` says the matrix was compressed *into* `steering.md` — so the Claude side's compression dropped what the entry says it kept. Predicted failure: the controller re-launches the lens with a fresh `Agent` call instead of messaging the retained agent id, creating the new cognitive voice `steering.md:11` forbids, and the "начальный вердикт · вмешательство · пересмотренный вердикт" record becomes unobtainable while the correction looks complete. Minimal correction: name the retained-agent-id continuation in `:13`, mirroring the Codex line.

## Justified unchanged

- **`SKILL.md:20-24`, the two bare owner quotes.** They duplicate content already in `:16` and `panel.md:13`, but the roster-closure quote is the package's strongest anti-substitution authority anchor and substitution is the named failure (`panel.md:16`). The 2026-08-30 checker pair's opposite ruling belongs to a candidate that was abandoned (`candidate-final-2026-08-30/STATUS.md`). Keep.
- **Premortem-before-panel ordering** (`SKILL.md:31`, `premortem.md:15`). Serializing the slow cross-family leg costs wall-clock, but the order is load-bearing in adopted reasoning ("критичный порядок уже запускает его первым", `draft-2026-08-30/cut.md:40`; `intent.md:69`), and "параллельно" was deliberately cut earlier as a harness default (`cut.md:41`). No change.
- **Named-roster asymmetry** (Claude has `smith`, Codex has `trajectory-critic`, neither ships a definition in the package). `named.md:3` handles it honestly as an exact `blocker` without substitution, and TB4 keeps role method out of the skill. No change.
- **Package duplication itself.** Only ~9 body lines differ across the two owner folders, and the `1deep-agents` precedent (`shared/1deep-agents/portable/references/runtime-orchestration.md:6-23`) shows a single portable core with one runtime-delta reference. It would remove the drift class that produced finding 2 — but it is a topology change beyond "рефактор в пределах функции Fresh Eyes" (`work/refactor-2026-09-12/state.md:6-7`). I flag it as an owner decision, not a defect.

## Limits

- Read-only; I ran nothing but `sync_simple_projections.py --check`. Findings 1–4 are verified against file bytes and tool output; finding 5 and all behavioral consequences are predictions, not measured.
- Role definitions (`~/.claude/agents/*.md`, `~/.codex/agents/*.toml`) are outside the two owner folders and outside your stated route. One boundary note without a demand: `~/.claude/agents/premortem.md:74-88` carries its own cross-family mechanics (hardcoded `experiments/codex-bridge/codex_review.py`, path verified present) and re-states the three verdict strings from `premortem.md:20` — the freshness contract lives in two places with two different transports, which TB4 exists to prevent.
- Installed projections match their owners byte-for-byte apart from the frame files in finding 4; `candidate-final-2026-08-30` equals the live package except the later deliberate `openai.yaml` display-name change (commit `b07a15c5`). No pending unapproved repair remains.

