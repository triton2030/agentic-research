# User said — 2026-05-28

* 01:30:26 — "Скил воркревью должен заставить агента в очень коротком виде честно признаться о каких папочных и корневых инструкциях он забыл что должен был сделовать. И какие скилы он упустил хотя надо было их использовать. А также сказать в чём он реально сомневается в контексте цели проекта, то есть на сколько близко то что он сделал к цели проекта или на сколько он не уверен вообще в правильности своей работы." контекст: правка work-review skill
* 01:52:46 — "нам надо в глобальной инструкции сказать чтобы ты прям вслух писал свой ход мыслей и главное мотивы твоего поведения которые должны быть основаны на инструкциях как корневых так и папочных" контекст: правка глобальной инструкции Codex
* 02:00:11 — "Наш скил [$1instruction-layer](https://github.com/triton2030/agentic-research/blob/main/Users/triton/.codex/skills/1instruction-layer/SKILL.md) должен более жёстко говорить что такое вообще файл инструкций, у меня очень частая проблемма что файлы инструкций превращаются в документы с информацией а не набор влияющих на поведение вещей" контекст: правка 1instruction-layer
* 15:05:41 — "инструкции для агентов, они эффективнее, чем меньше они по размеру" контекст: обсуждение rules vs skills vs hooks

## 2026-05-28T16:01:14+05:00 | long user prompt | turn 019e6e3e-d8a5-7723-b4f1-cf3f736daeba

* cwd: `/Users/triton/Documents/GitHub/agentic-research`
* chars: 818
* source: `UserPromptSubmit`

```
Отлично. Теперь поговорим насчёт папки «Правила», которую мы, кстати, должны переименовать в папку rules

И я б хотел с тобой именно это обсудить по лучшим практикам написания агентских инструкций. То, что агентские инструкции должны быть инструкциями к поведению, а все дополнительные знания и правила должны быть ссылками. И поэтому папку «правила» я думал создать так, что там мы будем хранить менее важные правила, которые агент должен знать, но по случаю. То есть в агентских инструкциях прописать, что когда ты будешь делать что-то-то что-то-то читая папку правила, например. 

Ещё папка «Правила» классная тем, что папочные инструкции тоже могут ссылаться на разные файлы из папки «Правила». И вот тут мне надо обсудить эту идею с тобой. С точки зрения управления языковыми моделями, насколько это хорошая идея?
```

## 2026-05-28T16:06:38+05:00 | long user prompt | turn 019e6e43-cc20-78c0-a2ce-007225732900

* cwd: `/Users/triton/Documents/GitHub/agentic-research`
* chars: 673
* source: `UserPromptSubmit`

```
Да, отлично. И вот тут как раз-таки Я думаю, этим должен заниматься скилл слоя инструкций. 

И мы сейчас должны его отредактировать. Чтобы он следил за тем, чтобы все инструкции были строго инструкциями, не энциклопедиями. 

Какие-то дополнительные знания, правила и так далее должны храниться в правилах. 

Чтобы он обязательно говорил, что инструкции должны хранить в себе только самое главное. Чем короче инструкция, тем она эффективнее. 

Также это означает, что скилл стратегии документации тоже должен знать про папку правил: чтобы дорожная карта не хранила в себе правила, чтобы файл цели не хранил в себе правила, чтобы корневой Ridme тоже не хранил в себе правила.
```

## 2026-05-28T16:14:47+05:00 | long user prompt | turn 019e6e4b-448a-7ff3-8d0f-231f44cad006

* cwd: `/Users/triton/Documents/GitHub/agentic-research`
* chars: 3531
* source: `UserPromptSubmit`

```
Да, ты прав. Обновлённый живой каталог сейчас показывает **31 инструмент**. Важное изменение: многие команды теперь по умолчанию дают **короткий безопасный output**, а полный материал включается через `--expanded`.

**Главные новые / важные**
- `md search-read` — найти по смыслу. По умолчанию даёт карту релевантных секций и snippets; с `--expanded` добавляет тела секций.
- `md walk` — пройти цепочку мысли от конкретной секции по anchored wikilinks. Без эмбеддингов, просто читает связанные блоки.
- `--expanded` — новый общий паттерн для больших ответов: normal = компактно, expanded = подробно.

**Ориентация и чтение**
- `md orient` — первый обзор корпуса: статус, важные файлы, краткая карта. `--expanded` даёт более полный пакет.
- `md corpus-scan` — показывает, где в репо есть Markdown-корпуса и где `.md` ещё без индекса.
- `md status` — говорит, свежий ли индекс и нужен ли `md index`.
- `md ls` — список файлов с `description`, title и числом заголовков.
- `md toc` — оглавление папки/файла со стабильными id секций.
- `md extract` — достаёт выбранные файлы или секции из результата `ls` / `toc`.
- `md search` — semantic search, когда нужны только ранжированные handles/snippets.
- `md search-read` — semantic search + ladder чтения; `--expanded` добавляет content.
- `md read-related` — соседний контекст файла: ссылки, backlinks, связанные документы. `--expanded` добавляет тела.
- `md walk` — читает последовательность связанных секций по anchored wikilinks.
- `md importance` — центральные документы по графу ссылок.

**Граф и безопасность правок**
- `md preflight` — перед правкой файла: что прочитать, что потом обновить, есть ли blockers.
- `md edit-context` — preflight + related context в одном вызове.
- `md impact` — что заденет удаление/переименование файла.
- `md section-blast-radius` — hard graph слой + semantic слой для секции.
- `md deps` — прямые и обратные зависимости файла.
- `md check` — битые wikilinks, anchors и markdown links.
- `md scan` — проблемы frontmatter.
- `md health` — общий graph-health: coverage, hubs, orphans, cycles, broken links.
- `md cycles` — циклы зависимостей.
- `md changed` — проверяет изменённые `.md` через preflight.

**Семантика, дубли, IA**
- `md overlaps` — похожие пары секций; по умолчанию группирует, `--expanded` даёт полный список.
- `md repeated-concepts` — повторяющиеся идеи на уровне концептов.
- `md audit` — большой аудит корпуса: health, findings, severity; `--expanded` даёт полное evidence.
- `md query-by-type` — секции по типам: rules, decisions, definitions, open questions.
- `md refactor-candidates` — предложения по реорганизации, без авто-правок.
- `md profile-sections` — классифицирует секции для `query-by-type` и `refactor-candidates`.

**Изменяющие инструменты**
- `md index` — создаёт/обновляет embedding index.
- `md init` — добавляет недостающий graph-frontmatter.
- `md strip` — удаляет legacy/unknown frontmatter поля.

Для этих трёх и `profile-sections`: сначала `--dry-run`, потом confirm через `_envelope.lock.transaction_id`.

**Диагностика**
- `md ping` — проверяет, что CLI/backend живы.

**Служебные команды**
- `md tools --json` — живой каталог инструментов.
- `md tools <name>` — описание одного инструмента.
- `md selftest --json` — быстрая проверка установки.
- `md doctor` — диагностика окружения.
- `md --version` — версия CLI.

---

Давай подумаем, как мы могли бы усилить или какие комбинации команд или структурно, как мы могли бы воспользоваться всеми инструментами и усилить наш скилл информационной архитектуры?
```

## 2026-05-28T16:32:33+05:00 | long user prompt | turn 019e6e5b-8920-7b82-a9a6-4189fb052ad9

* cwd: `/Users/triton/Documents/GitHub/agentic-research`
* chars: 626
* source: `UserPromptSubmit`

```
Так, отлично. Теперь очень важно ээ-э в скилле папочных инструкций тоже сказать, чтобы он смотрел и вызывал скилл слой инструкций, чтобы каждая папочная инструкция использовала этот шаблон. 

Но скилл папочных инструкций в графе «роль» должен указывать наиболее релевантную роль, относящуюся к этой папке. Потому что у нас папочная инструкция может быть по маркетингу, папочная инструкция может быть по финансам. Это значит то, что по маркетингу роль очень крутого маркетолог, который знает такие-то, такие-то методологии. А если он создаёт папочную инструкцию в папке «Финансы, то тогда роль, которая релевантна к этой папке.
```

## 2026-05-28T16:52:47+05:00 | long user prompt | turn 019e6e6e-0e1f-7e33-a39d-a9853ba46d0d

* cwd: `/Users/triton/Documents/GitHub/agentic-research`
* chars: 808
* source: `UserPromptSubmit`

```
Слушай, в нашем инструменте есть режим чтения по цепочке Вики-ссылок, блоков, который называется walk Так вот, я хочу его заменить на cohearance-audit
Ну, или типа того, может быть, я неправильно написал. Смысл вот в чём. То, что если мы работаем над одним файлом, мы вызываем этот аудит, и он обогащает все Ввиики-ссылки на текста внутри вики-ссылок. 

Смысл такой, то, что ты отредактировал текст. Внутри этого текста есть Вики-ссылки, которые ведут на какие-то конкретные блоки текста на других страницах. И-и-и когда ты закончил редактировать, чтобы проверить Вики-ссылки, ты берёшь и заменяешь Вики-ссылки буквально на те блоки текста, на которые он ссылается, И ещё раз переписываешь уже большой крупный файл. Таким образом ты проверяешь связанность этого документа с ссылками, на которые он упирается.
```
