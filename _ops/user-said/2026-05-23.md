# User said — 2026-05-23

- 00:10:46 — "Файл: .md-tools.toml. Cleanup отдельным коммитом, порядок жёсткий: сначала убрать дубли walker/filter, потом добавлять config. CLI поверх config: append — project config задаёт baseline, CLI добавляет временное сужение. Секции: сразу [index] и [graph], без [walk]."
  контекст: md-tools per-project filter config — четыре выбранных решения после Brooks/dev-critic review
- 12:38:50 — "Это получается, что в скиллах, по поводу инструкций и скилле по поводу контракта папки надо сделать так, что каждая инструкция должна быть до 200 строк на файл."
  контекст: обсуждение лимитов инструкций Codex/Claude
- 12:43:49 — "Я не говорю редактировать сам-саму длину скиллов. Я говорю, чтобы эти скиллы учили агента, что когда создаёте инструкции, они не должны быть длиннее д200 строк."
  контекст: коррекция scope правки Claude skills
- 23:33:30 — "Я бы делал рефактор, но не “v2 rewrite”. Лучший путь: **stabilization refactor текущего `md` CLI**, чтобы он стал нормальной агентной оболочкой вокруг библиотечного ядра."
  контекст: выбор подхода к md CLI refactor
