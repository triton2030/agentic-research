# Находка — 2026-09-01 — запись:193230-7124-7833

- 19:32 — первоисточник подтвердил рекомендацию про мост: авто-unarchive при resume у вендора НЕТ, и соседний продукт (Paseo) держит ровно наш дефект — архивирует нативный тред на закрытии, а на resume снимает только своё состояние; их принятый корень: у AgentClient есть archiveNativeSession, но нет unarchiveNativeSession. Наш эквивалент — codex_orchestrate.py:271-282, ветка thread_resume при переданном task.thread_id | https://github.com/getpaseo/paseo/issues/1435 и https://github.com/openai/codex/issues/35091 (дословный текст ошибки: session <id> is archived. Run `codex unarchive <id>` to unarchive it first) | если возьмёмся чинить мост, ловить archived на resume и звать thread_unarchive с одним ретраем — тогда правило из delegate.md исчезает вместе с граблями; там же отмечено, что сам `codex unarchive` умеет падать на битой локальной state-БД (codex doctor)
