SendMessage tool is absent in this Claude desktop-app session: ToolSearch "select:SendMessage" returns nothing and it is not in the deferred list, although the Agent and ListAgents tool descriptions refer to it; a spawned subagent cannot be re-asked from the same session here. | ~/.claude/skills/1chat-recall (draft agents/psychologist.md fallback), ~/.claude/skills/1orchestration, ~/.claude/skills/1fresh-eyes | Observed in session 4c325be0 on 2026-09-08 (desktop app). Any skill that relies on re-addressing a subagent needs a fallback (1chat-recall draft got one: new Agent with prior answers). Interpretation, not verified: availability may differ between CLI and desktop builds.
