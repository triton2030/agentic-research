# Находка — 2026-09-01 — запись:003301-58970-4104

- 00:33 — Адреса вида `<файл>.md#L<N>` в корпусе `_ops/chat-recall/` систематически указывают не на ту строку: шапка файла разговора (карточка сессии) переписывается и растёт при каждой новой записи, а все прежние цитаты смещаются вниз. Измерено по git-истории `2026-08-31-160620-claude-97782077.md`: шапка до первой цитаты была 16, затем 19, 19 и 20 строк | обнаружено обоими проверяющими на волне v15 `1skill-creation` независимо друг от друга: в `skills/1skill-creation/product-frame.principles.md` P-009 и P-010 ссылались на `#L19` и `#L21` файла из 18 строк, P-003 на `#L16` при фактической 20; локально исправлено только в папке `skills/1skill-creation/`, дрейф в остальном репо не проверялся | адрес по номеру строки — основной способ ссылаться на слова владельца во всех продуктовых рамках, decisions-файлах и инструкциях, и он перестаёт быть проверяемым: гейт, который держится на адресе, не отличит настоящее разрешение владельца от заявленного. Владельцу `1chat-recall` решать, получают ли записи стабильный якорь вместо номера строки и надо ли обойти существующие ссылки
