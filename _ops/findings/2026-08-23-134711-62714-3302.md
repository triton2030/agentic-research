# Находка — 2026-08-23 — маршрут Ox молчит при отказе

- 13:47 — ИСПРАВЛЕНО 2026-08-23: первая редакция утверждала, что обёртка 1hermes
  выбрасывает stderr Hermes CLI. Это неверно. Обёртка кладёт непустые строки
  stderr в `warnings` (hermes_advisor.py:231–241), игнорируя единственный
  benign-префикс. Проверка сырых `.err` застрявших прогонов волны показала ноль
  байт: CLI при отказе маршрута не печатает ничего.
- Что остаётся настоящим сигналом: при отказе маршрута создаётся сессия из двух
  сообщений, ноль вызовов API, provider не разрешён, ответ пуст — и ни одного
  диагностического сообщения ни от CLI, ни от провайдера | волна Ox 2026-08-23,
  примерно один прогон из трёх | причина недоступна с нашей стороны без
  verbose-режима самого CLI; обход — повтор по отпечатку в
  experiments/hermes-ox-alpha/route_started.py
- ДОПОЛНЕНО 2026-08-23: четыре verbose-прогона CLI подряд прошли успешно, отказ
  не воспроизведён. Убито второе ложное умозаключение: `message_count: 2` не
  означает отсутствия ответа ассистента — на успешном прогоне CLI пишет ровно
  то же «cleanup ... with 2 message(s)». Причина отказа остаётся неизвестной.
- Побочно: у самого CLI бюджет `api_calls=1/500`, независимый от нашего
  `--max-turns`; меньший потолок не наш.

