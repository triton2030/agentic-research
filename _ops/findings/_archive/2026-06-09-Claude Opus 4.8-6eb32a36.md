# Findings — 2026-06-09 — Claude Opus 4.8 — sess:6eb32a36

- 16:29 — md: индекс раздваивается по корню — knowledge даёт text-embedding-3-small/NEEDS_WARMUP, knowledge/agents даёт bge-m3/HEALTHY; те же файлы, две несравнимые модели, молча | md status (verified) | агент получает результаты из разного индекса в зависимости от того, какой корень назвал
- 16:29 — md corpus-scan --json из корня репо крашится usage_error NoneType — самый частый холодный первый вызов 'что где проиндексировано' падает | md corpus-scan | cold-start агента ломается на первом же вызове
- 16:29 — md read-related/edit-context отвергают ПАПКУ как target: read-related лжёт 'Markdown file does not exist', edit-context роняет неперехваченный IsADirectoryError в JSON-конверт; при этом orient/ls папку принимают | folder-as-target (verified) | агент мыслит папками, грамматика target несогласована между sibling-командами
- 16:29 — md read-related --semantic-radius: соседи это file-level заглушки — relative_path + opaque section_id + heading_chain=заголовок ФАЙЛА (не найденной секции); НЕТ snippet; section_id в другом namespace чем md toc, не резолвится | semantic neighbors (verified) | отвечает 'какой файл', а не 'какую секцию открыть' -> агент делает 2-3 лишних вызова
- 16:29 — md deps/impact/read-related слепы к prose-backlink: файл с 6 реальными входящими ссылками вида backtick file.md в теле возвращает confident-empty 'nothing depends/only self'; корпус линкуется прозой, граф индексирует только wikilink+frontmatter | graph half blind | агент переписывает файл думая что он остров, гниют cross-links
- 16:29 — self-learning: грамматика 'где корпус' рассогласована между md командами — positional corpus vs --scan vs --paths, status ОТВЕРГАЕТ --scan, search/search-read делают --query флагом не позиционным, --top vs --limit; почти каждый субагент угадал не тот арг и ретраил | весь CLI | главное agent-UX трение, чистый налог не логика
- 18:59 — md INDEX_CONFLICT самовосстанавливается: любая команда с target/scope в под-папке (knowledge/agents) авто-засевает там .md-navigator + индекс на bge-m3, конфликтующий с корневым text-embedding-3-small; чистка базы необходима но недостаточна, корень — anchoring индекса, не stale DB | index_resolution (verified, чистил дважды за сессию) | semantic reads умирают INDEX_CONFLICT после первого же sub-scope чтения
- 18:59 — FIX before/after: новый md semantic-neighbors закрыл #5 (page-as-query 2->4/5) — несёт snippet+start_line+mdref(file:LN)+matched_target+obligation/graph_edge, принимает папку, ~0.27s; конфликт теперь ДЕТЕКТИТСЯ а не молча берёт чужую модель | re-test 6 субагентов | главная исходная жалоба решена
- 18:59 — FIX НЕ затронул: #1 грамматика корпус-аргумента (+ новое: search ключ results vs search-read sections; semantic-neighbors требует позиционный corpus, deps его отвергает; md search exit 0 на usage error), #4 prose-backlink слепота (deps/impact/edit-context/read-related = 'nothing depends' на файле с 4+ prose-ссылками -> ложное 'safe to edit'), #6 snippet обрезан, #3 corpus-scan --json краш, #7 pagerank start_here врёт, #11 map->extract (toc один путь, id сброс, subtree-дубли, 3 файла=6+ вызовов) | re-test | средний agent-UX почти не сдвинулся, сдвинулся только page-as-query
- 18:59 — self-learning: UX-тест корпуса должен быть read-only и root-pinned — формулировка 'refactor the folder' заставила субагентов РЕДАКТИРОВАТЬ канон (откатил 5 файлов agents/), а sub-scope read-команды плодят shadow-индекс который потом ломает semantic reads; будущий agent-retrieval-test слой не должен мутировать корпус и не должен ходить под-scope корнями | тестовый харнесс | иначе fixtures сами портят baseline
- 19:16 — ПОПРАВКА к прошлой находке про #8: канон НЕ knowledge/3-small. По .md-tools.toml (только на репо-руте, include=knowledge/**) канонический корпус = РЕПО-РУТ на bge-m3 (./.md-navigator, 31МБ); он уже полностью покрывает knowledge/** (md index repo-root --dry-run --path-include knowledge/** -> pending_chunks:0, cost:0). knowledge/.md-navigator на text-embedding-3-small — сам non-canonical shadow. Миграция на канон стоит /bin/zsh | dev-critic поймал мой frame-lock (verified dry-run) | я в прошлый ход удалил bge-m3 nested (model-correct), оставил 3-small (model-wrong) — перевёрнуто
- 19:16 — self-learning: adversarial субагент (developer-critic) сломал мою рамку 'knowledge canonical' — frame-lock держался 3 хода, evidence (4 индекса, 2 модели, repo-root как parent) был доступен но я его не запросил пока критик не заставил; вербализация 'где я могу ошибаться' недостаточна без адверсариального чтения кода | design-фаза | критик-семья окупается именно тут
