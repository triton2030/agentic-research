`1-max-review` (`scripts/max_review.py`, `_codex_path`) и проверка `1folder-tree` (`scripts/check_tree.py`) ищут `codex` только через `shutil.which`, а на этом Mac его нет в PATH даже в login shell (замер 2026-09-23: `zsh -lic 'which codex'` → not found). Движок лежит в `/Applications/ChatGPT.app/Contents/Resources/codex` (0.155.0-alpha.16); мост и md-scout берут его оттуда сами. Из сессий Claude оба инструмента, похоже, живьём не запускаются; работают ли они из Codex, где PATH может быть другим, не проверено. Замечено при переводе моделей на поколение 6.

Разобрано 2026-09-23: оба скрипта после PATH берут движок ChatGPT.app по настоящему пути; живые прогоны `max-review` (gpt-6-luna) и проверки дерева (gpt-6-sol) прошли. Итог — `skills/shared/1-max-review/validation.md` и `skills/1folder-tree/cut.md`, разделы 2026-09-23.
