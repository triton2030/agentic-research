# Finding — 2026-07-26 — capture:165207-36615-32449

- 16:52 — ОТОЗВАНО (проверка не подтвердила): я записал, что `md overlaps --max-heading-level` инертен, опираясь на отсутствие ключа в args (`api_audit.py:296-306`). Параметр идёт другим путём — через `_index_context_kwargs` в `_sections_index_context` → `build_map(corpus_root, max_heading_level or 6)` (`api_index_context.py:151,188,276`), то есть влияет на состав секций. Замер «2 vs 6 даёт идентичную выдачу» скорее всего объясняется тестовым корпусом без заголовков глубже h2, а не инертностью флага.
- 16:52 — остаётся к проверке отдельно: falsy-zero в `api_audit.py:298` (`int(kwargs.get("min_tokens") or 30)`) — тот же паттерн назван и для `threshold`, и дрейф default `top` (schema заявляет 10, код берёт 20) | /Users/triton/Documents/My_projects/md-tools/src/navigator/api_audit.py:296-306 | параметры молча возвращаются к дефолту, агент делает вывод из неизменившейся выдачи
