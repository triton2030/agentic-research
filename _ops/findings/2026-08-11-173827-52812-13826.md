# Находка — 2026-08-11 — запись:173827-52812-13826

- 17:38 — Поле sandbox_mode=read-only в профилях критиков Codex не связывает: треды самих критиков шли с danger-full-access в 49 из 55 замеров | замер по отдельным rollout-логам субагентов ~/.codex/sessions/2026/08 (у субагента свой файл по agent_thread_id; в его системном промте видно 'sandbox_mode is X'); read-only и workspace-write встречались только там, где родителем был bridge-прогон со своим профилем | побеждает живая песочница родителя (глобальный config = danger-full-access), совпадает с предупреждением доки OpenAI; read-only критиков держится ТОЛЬКО прозой инварианта 'более широкие parent permissions её не расширяют' — сам инвариант написан верно, но поле в TOML создаёт ложное ощущение принуждения
