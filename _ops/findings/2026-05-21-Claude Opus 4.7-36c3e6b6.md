# Findings — 2026-05-21 — Claude Opus 4.7 — sess:36c3e6b6

- 20:19 — profile-sections --mode llm --force на full corpus hung 10+ min без output (только 'using OpenRouter key from...'). Killed manually. Default timeout=60s в urllib не всегда срабатывает на TLS handshake. Fix: switch to httpx/requests с hard timeout, OR ThreadPoolExecutor.submit().result(timeout). Plus добавить batch commits каждые N sections — release SQLite write lock между calls, иначе block md_search/md_section_blast_radius via 'database is locked'.
- 20:31 — md_refactor_candidates bias: 3 из 5 top proposals → platform-deltas.md как owner. Composite signal слишком weight'ит на pagerank+in_degree относительно semantic alignment subject. Multi-topic files (которые имеют broad cosine similarity к many) выигрывают unfairly. Tuning candidates: повысить uniqueness_threshold с 0.35 → 0.30 (более strict), повысить owner_confidence_threshold с 0.45 → 0.60, OR изменить composite weights в owner_detector.py чтобы cosine больше веса vs graph centrality. Verify через editorial session 10 proposals.
