Scope-чек исследователя моста (`experiments/codex-bridge/codex_investigate.py`) роняет `ok`, когда вызывающий Claude сам правит или коммитит проект, пока идёт ход: чек не различает авторов, а коммит даёт `head_changed=true` при пустом `out_of_scope_files`. Наблюдалось минимум дважды: run `20260918T184500Z-steer-external-probe` (запись в `skills/claude/1codex/evidence.md`) и `20260923T002233Z-image-probe-gpt6` (Claude правил `skills/claude/1codex/SKILL.md`, картинка при этом годна). Скил `1codex` велит запускать прогоны в фоне и продолжать работу, но об этой ловушке молчит; механика записана только в README моста (абзац после «Картинки»). Не решено, нужно ли предупреждение в скиле или изоляция исследователя от правок вызывающего.

Дополнение 2026-09-23: картинкам исследователь больше не нужен — мост собирает их и у ревьюера (`skills/claude/1codex/references/delegate.md`). Ловушка остаётся для прочих прогонов `investigate`.
