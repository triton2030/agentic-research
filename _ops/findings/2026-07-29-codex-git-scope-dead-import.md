# Мёртвый импорт в codex_git_scope.py

- **Где:** `experiments/codex-bridge/codex_git_scope.py:22` — импортирует
  `PROJECT_ARTIFACTS_SUBDIR` из `codex_run_ledger` и нигде не использует.
- **Чем плохо:** лишняя зависимость модуля git-скоупа от журнала; имя
  подсказывает связь, которой нет.
- **Источник:** замечен Opus-субагентом при рефакторе 2026-07-29 (вне его
  scope, файл не трогался), подтверждён grep-ом.
- **Фикс:** удалить импорт, прогнать тесты моста.
