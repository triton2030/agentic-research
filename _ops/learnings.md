# learnings

- 2026-04-22 · Expected: owner-chain держит `_ops/PROJECT-PLAN.md`, `_ops/INTERVIEW.md` и `_ops/learnings.md` как живой truth layer для fresh-session работы. · Actual: `learnings.md` отсутствовал, а `instruction-layer` не требовал явно защищать частую пересинхронизацию `_ops/`. · Delta: считать холодный или неполный `_ops/` upstream drift и пересинхронизировать нужный файл в том же ходе; instruction layer должен защищать этот ритм, а не жить поверх stale truth. · Applies to: PROJECT-PLAN §1. Стабилизировать живую форму репы [~].
- 2026-04-22 · Expected: `task-contract` выводит критерии только из утверждённой траектории плана. · Actual: в старом `contract-mode` ещё жил fallback `weak strategic grounding`, и off-plan ask мог получить тонкий контракт вместо отката вверх. · Delta: без approved `PROJECT-PLAN.md` или без честной привязки ask к Goal + активному Stage + текущему Step criteria не генерировать; сначала `project-strategy`, потом `task-contract`. · Applies to: PROJECT-PLAN §1. Стабилизировать живую форму репы [~].
- 2026-04-24 · Expected: task-файлы и PROJECT-PLAN остаются синхронными благодаря owner-chain. · Actual: один task owner пытался держать и preflight, и write-check, и review, поэтому триггерная поверхность размывалась. · Delta: вынести моментные инжекции в `before-work`, `before-write`, `work-review`; оставить `task-contract` владельцем task-файла; закрепить evidence reconciliation у `project-strategy` и routing у `instruction-layer`. · Applies to: PROJECT-PLAN §1. Стабилизировать живую форму репы [~].
- 2026-04-24 · Expected: `instruction-layer` только роутит к `brooks`, не сужая его роль. · Actual: Brooks был ошибочно ограничен code-backed surfaces. · Delta: Brooks routing должен покрывать whole-system structural critique для документов, визуалов, бизнес-планов, instructions, repo-shape и связности системы; роль Brooks не переписывать внутри `instruction-layer`. · Applies to: PROJECT-PLAN §1. Стабилизировать живую форму репы [~].
