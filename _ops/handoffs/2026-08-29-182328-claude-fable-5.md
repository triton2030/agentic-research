---
description: "Handoff 2026-08-29-182328: рефактор 1instruction-authoring завершён — v3 (стадии, зоны-рёбра, Пять нерушимых) установлен, логически закрыт Codex-ом; открытые кандидаты названы"
model: claude-fable-5
date: 2026-08-29-182328
---

1. Read this file in full.
2. Live files and runtime state override this dated snapshot.
3. First state the next action and why, then compare the recorded HEAD with
   `git log`. If they diverge, re-derive the action from live state before
   starting it.

Veto: установленный пакет `1instruction-authoring` меняется только новым «да»
владельца после полного цикла проверки — это нерушимое правило 5 самого скила
и контракт `1skill-creation`; правка в обход равна нарушению обоих.

Непроверенное: рабочее дерево держит незакоммиченные правки ПАРАЛЛЕЛЬНЫХ
сессий (1chat-recall draft-2026-08-29, 1orchestration draft-v6, 1plan-map,
1fresh-eyes и другие — `git status`); эта сессия их не трогала и не проверяла.
Не откатывай и не коммить их как свои.

## Terrain Model

### Файл зоны — рёбра, а не учебник зоны

Исходная модель: инструкция папки = контекст + правила работы в ней ->
владелец: «мы в слое инструкций не пишем, как работать в этой зоне… слой
решает несвязанность зон» (corpus 2026-08-28-193846:32), прогоны подтвердили
-> файл зоны несёт рёбра к другим зонам (адрес + момент срабатывания) и только
неочевидные правила своего поддерева; общеизвестное ремесло запрещено.

### Спор о счёте единиц разрешён замером, не аргументом

Исходно два несовместимых счёта: строка-мысль (моё) против независимого
предиката (Codex; тело = 69) -> замер неожиданности на обеих целевых моделях
(`checks-2026-08-28/surprise-audit.md`): только 8 строк из ~50 «в базе» у
каждой модели, половина пакета — класс B «знаю, но съехал бы» -> резать по
известности почти нечего; лимит 20 держится доктриной владельца «высшая
инструкция подразумевает низшие», а право строки на подразумеваемое
доказывается пробником, не декларацией.

### Склейка через «·» — не объединение

Исходно я ужимал файлы, склеивая независимые правила в одну строку -> владелец:
«жульничество»; Codex подтвердил числом -> честная единица = одна мысль на
строку полными предложениями; правило «экономь смыслы, не буквы» живёт в
`wording.md` пакета.

### Пробник — red-green для слоя инструкций

Исходно проверка инструкций = прочитать и оценить -> прогоны coursely и
relayhub: свежий субагент получает только файл входа, задачу с известным
расходящимся дефолтом, судится по diff и mtime (политика правлена раньше кода)
-> доставка по связкам и негативный scope доказуемы наблюдаемо; методика
закреплена стадией `probe`.

### Codex как логический аудитор жёстче in-family проверок

Мои субагенты-ревьюеры пропустили то, что Codex нашёл протокольным чтением:
11 логических конфликтов -> три раунда (11 → 3 → 0) -> вердикт «пакет
логически закрыт» (артефакты `_workspace/codex-artifacts/20260829T125342Z-*`,
`…130803Z-*`, `…131703Z-*`).

## Where We Are

- Скил v3 установлен на пять поверхностей: tracked owner
  `skills/shared/1instruction-authoring/portable/`, проекции
  `skills/{claude,codex}/1instruction-authoring/`, живые
  `~/.claude/skills/…` и `~/.codex/skills/…`.
  `knowledge`: link-check всех поверхностей чист, `git log` до HEAD ниже.
  `consequence`: none.
- Счёт файлов: SKILL 20 · intent 6 · zones 9 · assembly 7 · probe 5 ·
  placement 16 · wording 7.
  `knowledge`: пересчитан вручную после финальных правок; предикатный счёт
  Codex выше и признан владельцем не мерилом.
  `consequence`: none.
- Логика пакета закрыта Codex-ом, поведение доказано семью исполнителями
  (сильнейший след — `checks-2026-08-28/trial-relayhub.md`).
  `knowledge`: артефакты прогонов и trial-файлы в
  `skills/1instruction-authoring/checks-2026-08-28/`.
  `consequence`: none.
- Прежние файлы interview.md и knowledge-out.md удалены со всех поверхностей
  (поглощены intent/assembly).
  `knowledge`: ls references всех поверхностей.
  `consequence`: none.
- История скила актуальна: origin.md несёт запись v3, goal-context.md
  синхронизирован с финальной парой, user-said.md держит цитаты 15–37.
  `knowledge`: правки этой сессии, коммит 67868384.
  `consequence`: none.
- Полное покрытие зон пробниками, абляция «пакет против голой пары УК+Цель» и
  Product Frame скила — не сделаны.
  `knowledge`: unverified (это и есть непроверенное).
  `consequence`: accepted assumption — владелец знает, зафиксировано в
  reviews.md заходах 7–8.

## Next Step

Ничего достраивать в этом репо не нужно — скил живой. Верное следующее
действие: при первом реальном вызове `1instruction-authoring` на живом проекте
наблюдать прохождение стадий и вернуть находки в
`skills/1instruction-authoring/checks-2026-08-28/reviews.md`; это закрывает
главный непроверенный claim «обязательство держится на длинной траектории»,
который короткие пробники покрыть не могли.

## Anchors

- HEAD: 67868384
- recall: captured — записи владельца этой сессии лежат в holder-е
  `_ops/chat-recall/2026-08-28-193846-claude-76aa0843.md`, строки 15–37
- cleanup: названные изменения — origin.md получил запись v3, goal-context.md
  синхронизирован с финальной парой, находка о дважды оплаченном уроке —
  `_ops/findings/2026-08-29-182328-handoff-links-rule.md`
- живые адреса:
  - `skills/shared/1instruction-authoring/portable/SKILL.md` — источник правды
    скила; живые пакеты — проекции
  - `skills/1instruction-authoring/checks-2026-08-28/reviews.md` — журнал всех
    решений рефактора (заходы 1–8), читать при любой будущей правке скила
  - `skills/1instruction-authoring/user-said.md` — дословные тезисы владельца;
    менять скил против них нельзя без его новых слов
  - `skills/1instruction-authoring/checks-2026-08-28/surprise-audit.md` —
    методика и результат замера неожиданности (переиспользуемая)
- открытые кандидаты (не блокеры, решение владельца): абляция «пакет против
  пары»; пробники остальных зон; Product Frame скила; placement.md как самый
  тяжёлый файл пакета (16 единиц)

## Incidents

### Пакет-протокол `1skill-creation` обновился посреди рефактора

- прежняя модель: протокол проверки стабилен на время сессии
- триггер: владелец: «скилл обновился, действуй строго по протоколу»; в пакете
  сменились check-approve, агенты-проверяющие, исчез canon.md
- оплаченная цена: перезапуск проверки по новому протоколу (~один полный заход)
- почему нашлось поздно: параллельная сессия правила пакет, у этой сессии не
  было сигнала об изменении
- durable исход уборки: обязанность «цитируй канон» снята с адресом причины в
  `skills/1instruction-authoring/cut.md`; мёртвые пути вычищены

### Мой счёт единиц оказался счётом контейнеров

- прежняя модель: строка с «·»-перечислением = одна единица
- триггер: владелец назвал это жульничеством; предикатный пересчёт Codex дал
  тело 69 вместо заявленных 20
- оплаченная цена: две полных пересборки тела (заходы 4–5)
- почему нашлось поздно: я оптимизировал число под потолок вместо честного
  вопроса «что такое единица»
- durable исход уборки: правило «одна мысль — одна строка полными
  предложениями» в `wording.md`; доктрина счёта записана в reviews.md заход 6

## Advice to the Next Agent

### Проверочные прогоны — после обсуждения, не после каждой правки

Правило владельца (corpus :24). Эта сессия до коррекции сожгла ~3 лишних
полных прогона субагентов на промежуточных версиях. Польза соблюдения — минуты
и токены за каждый цикл.

### Замер неожиданности — дешёвый и переиспользуемый инструмент

Один чистый субагент на модель + шкала A/B/C по строкам решает споры «нужна ли
строка» фактом, а не вкусом. Стоимость — один прогон на модель; методика в
`surprise-audit.md`.

### Пробник по методике probe.md запускается за минуты

haiku-субагент со стабами в /tmp и mtime/diff-инструментацией дал сильнейшее
evidence сессии (relayhub: политика правлена раньше кода). Дешевле любого
ревью-рассуждения о том же.
