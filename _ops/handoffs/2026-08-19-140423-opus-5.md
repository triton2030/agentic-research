---
description: "Хендоф 2026-08-19-140423: 1codex перекроен по владению после волны двух окон; поведение под новой топологией не измерено"
model: opus-5
date: 2026-08-19-140423
---

Читай файл целиком. Live-файлы и runtime сильнее этого снимка. Первый ход —
назови следующий шаг с причиной и сверь HEAD-якорь с `git log`.

## Где мы сейчас

Сессия началась с вопроса владельца «почему Codex плодит проекты на каждую
задачу» и кончилась перекройкой скила `1codex` по владению. Между ними —
четыре захода починки по одной строке, каждый находил новую неправду.

**Сделано и запушено:**

- `codex-bridge`: тред воркера архивируется вместе со своим деревом при
  закрытии волны (`f7cbe01`); один тред — один исход в отчёте (`812d3a7`);
  `--doctor` считает след моста снаружи + `codex_footprint.py` +
  `codex_threads.py archive --orphaned` (`6df4f7b`); сводка показывает цель
  вместо служебной рамки, `--board` не глотает `RUN_DIR`/`--steer`
  (`00c9b49`). **Чем проверено:** 155 тестов зелёные, pyflakes чист, плюс живой
  цикл «доктор нашёл 1 сироту → команда убрала → доктор показал 0».
- `skills/claude/1codex`: перекройка по оси «предмет старше фазы» (`4ddf274`),
  шесть неправд снято, шесть дублей сведено к одному владельцу.
  **Чем проверено:** токен-сверка «до/после» по 176 единицам — одна настоящая
  потеря найдена и возвращена; факты сверены по коду с адресами `файл:строка`.
  **Непроверено:** поведение агента под новой топологией.
- 34 осиротевших треда моста архивированы штатным `thread_archive` (обратимо
  через `codex_threads.py unarchive`). Владелец подтвердил глазами: карточки
  из списка проектов исчезли.

## Следующий шаг

**Замерить, работает ли новая топология** — единственное, что осталось
непроверенным. Свежее окно (субагент, голый промпт про волну в полёте, без
подсказок про файлы): открывает ли оно один файл вместо двух. Причина: сегодня
доказаны факты и полнота переноса, но не поведение — а весь смысл перекройки в
том, что следующий агент кладёт правило в файл-владелец без подсказки.

Замер записан как невыполненный в `skills/claude/1codex/cut.md`, раздел
«Перекройка по владению, 2026-08-19».

### Решения владельца — якоря

Все в `_ops/chat-recall/2026-08-18-143416-claude-9d58ffa2.md`:

- `2026-08-18T14:34:16` — в скиле многое дешевле написать инструкцией агенту,
  чем чинить кодом.
- `2026-08-19T14:02:39` — сверять выводы с официальной документацией вендора
  через интернет, не только с локальным кодом.
- `2026-08-19T14:02:39` — при повторяющейся починке по строке поднимать
  субагентов, а не заходить пятый раз.
- `2026-08-19T14:02:40` (selection) — квитанция волны живёт только в
  `fleet.md`; **вытесняет** `2026-08-18T14:34:25`, где полный список полей
  стоял в теле `SKILL.md`.

### Отвергнутые маршруты — не повторяй

- **Слить `fleet.md` и `orchestration.md` в один файл.** Отвергнуто
  арифметикой: 78% `orchestration.md` универсально, условие вырождения не
  выполняется. Расчёт —
  `_workspace/orchestration/2026-08-19-1codex-truth-and-ownership/decisions.md`.
- **Чистить `[projects.*]` в `~/.codex/config.toml` кодом.** 69 мёртвых
  записей; правка живого конфига владельца с хуками и плагинами дороже
  косметики, которую уберёт. Доктор их считает — этого достаточно.
- **Правило «`--run-dir` только внутри проекта».** Признак не тот (важна
  долговечность) и отменял бы живую строку `orchestration.md` про хуки,
  чистящие `_workspace/`.

### Инцидент: черновик скила был готов к записи и оказался неверен

- **До:** два черновика строк `fleet.md` утверждены владельцем дословно,
  оставалось записать.
- **Trigger:** независимый аудит окном Codex — прочитал живые пакеты
  `1skill-shaping`/`1instruction-shaping` и код моста.
- **Цена:** ~16 минут wall time на прогон плюс второй круг согласования.
  Записанный текст стоил бы отмены живого правила соседнего файла.
- **Почему поздно:** я вывел черновик из полевого отчёта и проверил его против
  кода, но не прочитал соседний reference того же скила.
- **Предотвращение:** перед правкой любого reference читать остальные файлы
  того же скила целиком — владельцем смысла может быть сосед.

### Сюрпризы инструментов

- `--doctor` (`config/read`) **не** создаёт запись `[projects.*]` — проба в
  свежей папке показала нулевой diff конфига. Регистрация проекта происходит
  только на настоящем треде. Полезно: диагностику можно гонять свободно.
- `thread_archive` из SDK переносит rollout в `~/.codex/archived_sessions/` и
  обратим. Ручной перенос файлов я сделал первым и он был лишним.
- `timeout` на macOS нет — в командах не использовать.

## Совет следующему агенту

- **Наблюдал:** четыре захода подряд «нашёл строку → проверил → починил» дали
  четыре находки и создали две новые ошибки; одна волна двух окон дала восемь
  находок и знаменатель (~120 проверенных утверждений). → работа со скилом,
  где текст описывает чужой код → ручная внимательность здесь дороже и хуже
  веера.
- **Наблюдал:** я записал в `cut.md` фразу как снятую ложь и оставил её жить
  в `fleet.md` абзацем выше. → правка reference по одному месту → ложь
  пережила собственное опровержение на сутки.
- **Наблюдал:** мой новый тест-класс молча затенил существующий и съел три его
  теста; поймал `pyflakes`, а не unittest (155 → 152 тестов выглядело
  нормально). → `tests/test_codex_progress.py` → без линтера потеря покрытия
  была бы невидима.
- **Наблюдал:** токен-сверка «до/после» по бэктикам поймала настоящую потерю
  (`active` в пульсе флота), которую глазами я не увидел. → любая перекройка
  markdown-артефакта → дешёвый скрипт вместо вычитки.
- **Наблюдал:** первый греп по точной фразе дал 2 совпадения и чуть не отверг
  находку критика о четырёхкратном дубле; пересчёт с перефразировками дал 4. →
  проверка чужих находок → точная строка не равна смыслу.
- **Помогало незаметно:** `experiments/codex-bridge/cut.md` и `evidence.md`
  скила — в них уже лежали разборы прежних правок, и они же показали, что
  ложь была известна раньше.

## Якоря

- **Где живая правда.** Скил: tracked owner — `skills/claude/1codex/`, живая
  установка `~/.claude/skills/1codex/` — его проекция, правь owner и
  синхронизируй (зеркала у Codex нет). Runtime моста: только код и тесты в
  `experiments/codex-bridge/`; его `README.md` за эту неделю врал дважды —
  доказательством не является.
- HEAD на момент хендофа: `ec1a82c`.
- Recall: **captured** — 3 записи в
  `_ops/chat-recall/2026-08-18-143416-claude-9d58ffa2.md` (2026-08-19T14:02).
- Уборка: **починено** — `codex_footprint.py` добавлен в карту кода
  `experiments/codex-bridge/AGENTS.md`; всё моё закоммичено и запушено.
  Осталось untracked не моё: `Workspace/` (40K, `Design/`) и
  `_ops/findings/2026-08-19-133516-98436-4135.md` (сессия Graphiti) — оба были
  до начала этой сессии.
- Папка волны:
  `_workspace/orchestration/2026-08-19-1codex-truth-and-ownership/` —
  `context.md`, `decisions.md` с расчётом 78%, `inventory.md`, `before/`
  (копии шести файлов скила до перекройки).
- Предыдущий хендоф этой сессии не получал.
