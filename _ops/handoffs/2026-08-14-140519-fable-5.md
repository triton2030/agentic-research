---
description: "Хендоф 2026-08-14-140519: 1codex стал скилом оркестратора, мост получил worktree-изоляцию с самозакрывающейся волной; 25 аудит-находок закрыто, живые волны чужой сессии уже на новом коде"
model: fable-5
date: 2026-08-14-140519
---

# Хендоф — 1codex как пульт оркестратора + worktree-флот

## Где мы сейчас

Сессия целиком про одно: владелец заказал переработку `1codex` и моста
`experiments/codex-bridge/` под роль «Claude — оркестратор, Codex — исполнитель».
Сделано, проверено, запушено (последний коммит — момент советника на старте
работы). Все 16 решений владельца записаны в
`_ops/chat-recall/2026-08-14-122215-claude-2e4e5183.md` — при любом
продолжении этой темы сначала читай его целиком: решения разворачивались
внутри дня (один воркер → группы задач; треды эфемерны → видимы).

Что теперь правда (live owners: `skills/claude/1codex/**`, его projection
`~/.claude/skills/1codex/**` — копия, не симлинк; `experiments/codex-bridge/AGENTS.md`,
`README.md`):

- Флот: каждый пишущий воркер — свой git worktree от HEAD + персистентный тред
  (чат в Codex Desktop — монитор владельца). Волна закрывается сама:
  коммит всего изменённого в ветку воркера → merge --no-ff записанного SHA
  (только чистого воркера) → уборка деревьев/веток. Незабранное живёт в ветках
  `codex-fleet/<run>/<task>`.
- Инвариант ресурсов: **дерево ⟺ тред**, только у пишущих; читающие роли — ни
  того, ни другого. Ярусов два: sol+xhigh и luna; **max/ultra сняты**.
- Тёплый ремонт: задача ремонтной волны несёт `thread_id` воркера прошлой.
- Единица деления у пишущих — группа дел на воркера (20 дел = 4×5 с
  `subagents: true`), не задача.
- Промптинг переписан от цели (4 блока: Цель/Контекст/Границы/Готово-когда);
  советник запускается фоном НА СТАРТЕ работы, цель без плана.

Проверено: 118 тестов (все инварианты флота — на настоящем git, включая
rescue при прерывании и mutation-чувствительный сквозной), pyflakes
обязателен (в AGENTS.md моста — почему), 3 боевых прогона.

## Следующий шаг

Снять результат живых волн чужой сессии в
`/Users/triton/Documents/My_projects/mavo-short2/_workspace/codex-artifacts/wave-frames-{history,worktree}/`
(первый чужой боевой прогон новой механики: 3 воркера worktree + 1 shared) и
сверить `wave.cleanup_done`, `kept_branches`, `main_tree_drift` с обещаниями
скила. Причина: единственное, что не проверено в бою нами самими, —
многоворкерная волна и `subagents: true`; чужая волна это закрывает бесплатно.

## Незакрытое (отложено осознанно, не забыто)

- `--verify` бежит ПОСЛЕ merge и не откатывает его. Решение: ждать первого
  реального падения verify в бою, тогда — интеграция через временную ветку
  волны. Не строить заранее.
- SDK: стоит `openai-codex 0.1.0b3`/bin `0.137.0a4`, доступен `0.144.4`,
  движок ChatGPT.app `0.148.0-alpha.9`. Апгрейд — решение владельца, не делан.
- 7.3 ГБ старых деревьев в `~/.codex/worktrees` — это Codex Desktop, не мост;
  там может быть незабранная работа владельца, сносить только с его «да».
- Из аудитов отложено: symlink-гонка на `cleanup_done` (низкий), e2e
  shared-режима и падающего `--verify`, доска видит только 12 свежих прогонов
  default-каталога (задокументировано).
- `codex_review.py`: `main()` на 552 строки — не тронут (вне заказа, самый
  рискованный файл).

## Инциденты

### Рефактор прошёл 106 тестов со сломанным живым путём

**До:** разложил `main()` (329 строк) на этапы, `py_compile` и вся батарея
зелёные. **Trigger:** pyflakes нашёл undefined name `codex_bin` — имя уехало в
`_plan_run`. **Цена:** ~0 (поймано до боевого прогона), но первый живой флот
упал бы `NameError`. **Почему поздно:** ни один из 106 тестов не исполнял
`main()` на не-dry-run пути — вся батарея доказывала dry-run и моки. 
**Предотвращение:** pyflakes в обязательной проверке (AGENTS.md моста,
раздел «Проверка») + `test_full_run_path_end_to_end_with_isolation`; правя
живой путь — держи его зелёным, он там один такой.

### Собственный дизайн дважды ломал свой же инвариант «работа не теряется»

**До:** написал изоляцию с union-allowlist и «отбраковкой» вне списка;
позже — удержание воркера без коммита внесписочного. **Trigger:** два
раунда blind-аудита Codex (первый — 2 ревьюера, второй — 3 с осью
«инварианты → контрпримеры git-командами»). **Цена:** день ушёл волнами
правок; без аудита — потерянные правки воркеров в первом же бою.
**Почему поздно:** проверял чтением и своими тестами — они проверяли новое
правило, не его стык со старым. **Предотвращение:** для каждого терминального
статуса воркера — вопрос «где физически лежит его работа после уборки?»
(таблица 5×2); аудит механики заказывать осью «построй контрпример
последовательностью git-команд», не «найди ошибки».

### Скрипты 1chat-recall изменились под ногами посреди сессии

**До:** capture работал; **trigger:** параллельная сессия владельца
переписала `chat_capture.py`/`chat_digest.py` — появился обязательный
`--session-context`; мой вызов упал с error. **Цена:** ~2 мин на повтор.
**Почему поздно:** узнать было неоткуда — drift основного дерева виден только
постфактум. **Предотвращение:** нет дешёвого; при падении общего скрипта
сначала перечитай его help/контракт — вероятно, его правит соседняя сессия.

### Чужой агент отчитался по устаревшей доктрине против фактов на диске

**До:** владелец передал отчёт другой сессии: «в манифесте прямо стоит
thread_ephemeral: true». **Trigger:** проверка манифестов её же волн —
`False`, все 4 воркера с персистентными тредами. **Цена:** ~10 мин проверки;
без неё — ложный вывод «есть способ обойти треды». **Почему поздно:** сессия
той стороны стартовала до нашей правки и цитировала старый AGENTS.md из
своего окна. **Предотвращение:** любое утверждение чужого агента о runtime —
только против manifest/events на диске (это и есть P-005 владельца).

## Совет следующему агенту

- Аудиторы-Codex с узкой осью нашли то, что не нашёл ни я, ни первая пара →
  тип работы: приёмка git-механики → формула задания «атакуй named-инварианты
  последовательностями git-команд, максимум N, по убыванию вреда» окупилась
  шестью реальными дефектами.
- `result.json` ревьюера кладёт текст в `final_response`, у флота — в
  `response`/`results[].response` → чтение вердиктов → полчаса ушло на «пустые»
  ответы из-за неправильного ключа; бери `d.get('response') or
  d.get('final_response')`.
- Три параллельных ревьюера фоном через `run_in_background` Bash — ноль
  расхода окна, уведомления сами пришли → любой оплаченный прогон моста.
- Владелец слал решения mid-turn пачками по 1–3 → тип работы: длинная сессия
  с живым владельцем → записывай КАЖДОЕ сразу в recall в том же ходе, иначе
  следующий ход уже строит на устаревшем (меня дважды разворачивало:
  «один воркер» → «группы задач»; эфемерные → видимые треды).
- Правка глобального скила: tracked owner `skills/claude/1codex/` — копия;
  каждая правка = `cp` + `diff -rq` parity → забыть = рассинхрон двух правд.
- `git branch --list` префиксует ветку в чужом worktree знаком `+` → парсинг
  в тестах/скриптах → стоило одного красного теста.
- `git worktree remove` требует `--force` на непустом дереве; ветка «empty»
  воркера с его же самокоммитом — ловушка diff-от-HEAD → все git-операции
  волны считать от `base_commit`, не от подвижных ссылок.

## Якоря

- Скил: `skills/claude/1codex/SKILL.md` + `references/{fleet,orchestration,advisor}.md`
  — сегодня переписаны; полная runtime-правда: `experiments/codex-bridge/{AGENTS,README}.md`.
- Тесты: `experiments/codex-bridge/.venv/bin/python -m unittest discover
  experiments/codex-bridge/tests` (118) + `-m pyflakes codex_*.py`.
- Доска прогонов: `codex_progress.py --board <проект>`.
- Recall сессии: `_ops/chat-recall/2026-08-14-122215-claude-2e4e5183.md` (16 решений).
- Память проекта обновлена: `project_codex_bridge.md`, разделы 2026-08-14.
- Вердикты аудитов (эфемерный scratchpad, может исчезнуть):
  `/tmp/claude-501/…2e4e5183…/scratchpad/{final-mech,final-docs,final-tests}/result.json`;
  суть влита в коммит-месседжи `git log --since="2026-08-14" -- experiments/codex-bridge`.
- Живые волны чужой сессии: mavo-short2 `wave-frames-history` (shared, 1 задача),
  `wave-frames-worktree` (worktree, 3 воркера, треды `019fff7c-…`).
