---
description: "Хендоф 2026-08-23-154529: библиотека знаний из цитат собрана Ox Alpha до указателя; 1hermes починен и получил волну агентов"
model: opus-5
date: 2026-08-23-154529
---

Читай файл целиком. Живые файлы и runtime сильнее этого снимка. Первый ход —
назови следующий шаг с причиной и сверь якорь HEAD с `git log`.

## Где мы сейчас

Библиотека знаний из `_ops/chat-recall/` собрана до предпоследней стадии.
347 страниц в `experiments/openviking-chat-recall/artifacts/wiki-v1/`,
39 тем из 40. Проверено: скрипт по шапкам страниц, все несут type, title,
description, topic.

Одна тема, `owner-intent`, переписывается в фоне — её прежний прогон не смог
доказать, что каждый вызов остался на заявленном маршруте, страницы удалены,
запущен чистый прогон. **Непроверено:** завершился ли он.

Метод записан как версионированный протокол:
`experiments/openviking-chat-recall/PROTOCOL.md`, версия v1. Владелец решил
превратить его в глобальный скил, если приёмка подтвердит пользу — условие
превращения записано в самом протоколе.

### Что показал корпус, а не мы

Схлопывание дало 1015 фактов из 1072: **корпус почти не повторяется**. Ожидание
сжатия в 5–10 раз не оправдается, и не из-за качества работы. Ценность
библиотеки — навигация, а не объём. Проверено счётом пунктов до и после слияния.

### Три класса отказа маршрута Ox

Различать обязательно, лечатся по-разному:

1. **маршрут не стартовал** — provider null, ноль вызовов, пустой ответ,
   примерно раз на три-четыре прогона. Причина неизвестна; CLI молчит, stderr
   пуст, четыре verbose-прогона отказ не воспроизвели. Обход — повтор по
   отпечатку, `experiments/hermes-ox-alpha/route_started.py`;
2. **отказ гейта цены по ярлыку** — был мой регресс, исправлен: гейт судит
   сумму, а не название статуса. Тест `test_ox_zero_cost_is_accepted…`;
3. **маршрут не доказан** — вызовы есть, но обёртка не подтвердила, что все
   ушли на Ox. Встретился один раз, лечится чистым перезапуском темы.

### Решения владельца этой сессии

Все якорями в `_ops/chat-recall/2026-08-22-140532-claude-efb50744.md` (20 записей):

- закрыть OpenViking только моделью Ox Alpha; root оркестрирует, работает Ox;
- сначала отшлифовать и починить скил гермес, потом задача;
- полная автономия, ничего не спрашивать, часто коммитить и пушить;
- работать методами APoSD, Poor Charlie's Almanack и Холмса, объясняя выбор
  метода прямо в чате — владелец учится по объяснениям;
- симулировать постепенный приход цитат, а не строить карту по всему корпусу
  сразу; систему обновления документов не переизобретать, следовать OpenViking;
- модальность записи брать из поля `type`, а не угадывать (следствие правки,
  закрывшей дефект, переживший четыре переделки механизма).

### Отвергнутые маршруты, не повторять

- **батчи по десять holder-ов сразу в страницы** — четыре кандидата подряд
  отклонены, находки шли 29 -> 17 -> 7 -> 15: механизм качается между
  смешиванием вопросов и дроблением страниц. Причина: от одного агента требуют
  одновременно не исказить и разложить, работы тянут в разные стороны;
- **слой заявки** changeset/manifest/хеш-эхо/196 проверок формы — снят по
  решению владельца: за четыре кандидата не поймал ни одного решающего дефекта.
  Осталось сорок строк `scripts/check_wiki.py` и git как откат;
- **отдельный allocation-артефакт** — снят: дублировал фазу 4 upstream
  OpenViking как вторую местную правду.

## Следующий шаг

Запустить сборку указателя: бриф `_workspace/ox-index/tasks/index.txt` уже
написан, вход `artifacts/wiki-v1-catalog.md`. Причина: указатель — единственный
взгляд на библиотеку целиком, и это первый момент, когда межтемные дубли вообще
видимы; без него приёмку проводить не по чему.

Дождаться завершения `owner-intent` до запуска, иначе указатель соберётся по
неполной библиотеке.

Команда: `TASKS=_workspace/ox-index/tasks OUT=_workspace/ox-index/runs PAR=1
MODE=write CWD="$PWD" bash experiments/hermes-ox-alpha/ox_wave.sh`, **только
фоном**.

## Совет следующему агенту

- **Проверяй собственные посылки, а не только чужие.** За сессию я дважды
  построил рассуждение на том, что считал наблюдением: «обёртка выбрасывает
  stderr» — неправда, она кладёт его в warnings; «два сообщения в сессии значит
  ответа не было» — неправда, успешный прогон даёт ровно столько же. Цена: два
  ложных диагноза и одна неверная находка, записанная в корпус.
- **Ложная находка хуже отсутствующей.** Первую редакцию
  `_ops/findings/2026-08-23-134711-*` пришлось переписывать: она пережила бы
  разговор и послала бы кого-то чинить нетронутое.
- **Прогон Hermes запускай только фоном.** Нарушил дважды, оба раза оборвал
  оплаченную работу своим же двухминутным лимитом. Правило вписано в шапку
  `ox_wave.sh` — там, где набирается команда, а не в скиле, прочитанном часами
  раньше.
- **Тест, выросший из той же выборки, что и код, ничего не доказывает.** Гейт
  цены доверял одному написанию статуса, и фейк в тестах подставлял то же самое
  написание. Зелёные тесты при живом регрессе, отклонившем семь тем.
- **Смотри состояние диска до перезапуска.** Восемь тем имели страницы, хотя их
  приёмка была отклонена: модель успевает писать до вердикта. Перезапускать
  пришлось шесть, а не тринадцать.
- **Помогало незаметно:** `check_topics.py` и `check_wiki.py` — сорок строк,
  ловят то, что глазами не видно (переписанные по памяти якоря выглядят
  правдоподобно). `_workspace/ox-*/runs/*.err` содержат сырой stderr прогонов —
  я вспомнил о них поздно.
- **Дорого и без результата:** поиск причины отказов маршрута через
  developer-index — Hermes закрытый продукт, в индексе его нет. Четыре
  verbose-прогона отказ не воспроизвели. Тема закрыта по заранее названному
  лимиту, не по результату.

## Якоря

- HEAD на момент хендофа: `96b2974b`
- Библиотека: `experiments/openviking-chat-recall/artifacts/wiki-v1/`
- Протокол метода: `experiments/openviking-chat-recall/PROTOCOL.md`
- Приёмка: `experiments/openviking-chat-recall/ACCEPTANCE.md`
- Эксперимент Ox: `experiments/hermes-ox-alpha/` — `ox_wave.sh`,
  `route_started.py`, брифы всех 40 тем в `tasks/`
- План: `_ops/plans/openviking-chat-recall/status.md` — обновлён этой сессией
- Скил: `skills/claude/1hermes/`, живой пакет `~/.claude/skills/1hermes` —
  симлинк сюда; 34 теста, проверены после каждой правки
- Recall-ветка: captured, `_ops/chat-recall/2026-08-22-140532-claude-efb50744.md`
- Уборка: удалён дамп сессии 41 МБ из корня репо; статус плана переписан;
  находка о stderr исправлена; в `INDEX.md` добавлены четыре оплаченных
  маршрута к доказательствам прогонов Ox; всё закоммичено и запушено.
  **Не сделано:** форма `status.md` не сверена с контрактом `1planning` —
  файл правился руками, скил не открывался
- Предыдущий хендоф этой сессии не получался
