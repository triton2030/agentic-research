---
description: "Хендоф 2026-08-22-140223: clean Wiki batch-001 v2 ожидает root check и независимый semantic audit"
model: gpt-5
date: 2026-08-22-140223
---

Прочитай этот файл целиком.
Live-файлы и runtime сильнее этого снимка.
Первый ход: назови следующий шаг с причиной и сверь HEAD-якорь через `git log`.

## Где мы сейчас

Control plane OpenViking очищен и prompt v2 принят для новой попытки. Live truth:
`_ops/plans/openviking-chat-recall/{task,context,status}.md`; история отдельно в
`HISTORY.md`. Проверено pushed commits `34e0aa0` и
`fc287ecf22834593c1a82b4b2a4445273e6ac377`, `git log --oneline`.

Visible Luna Max task `01a02899-63da-7ef2-99f1-b1f0e6559a6a` (`hostId=local`)
завершилась без worktree. Единственный её task-output:
`experiments/openviking-chat-recall/artifacts/chronological-v1/batch-001/changeset.json`.
Terminal SHA-256 =
`5b6d2a0e12df4f4a55273640b6f33621100cd8e718b67db7d3c84da2459f7701`;
Luna сообщила builder PASS, materializer `--check-only` PASS и after-tree
`5439df57ebf0cbfbe22b87610bb5ef57c06ba481405ebdf8f0953a2016ec4381`.
Проверено `wait_threads` terminal packet; это всё ещё **непроверенный root-ом
кандидат**, не принятая Wiki. Receipt и materialized Wiki не создавались.

### Veto до приёмки

Не коммить, не материализуй и не ремонтируй candidate вручную до двух gates:
root `--check-only` и свежий независимый semantic audit всех 32 records против
полных bytes ровно десяти frozen holders. Самоотчёт Luna и mechanical PASS не
доказывают смысл. Любой semantic FAIL отклоняет кандидат целиком; новый заход
начинается новым prompt/новой Luna, а не скрытой ручной правкой.

## Следующий шаг

Независимо пересчитай terminal candidate SHA, запусти root materializer
`--check-only` и проверь frozen provenance. Причина: Luna self-report доказывает
только её собственный run, а не root verification или semantic acceptance.
Затем:

1. root запускает materializer `--check-only` и проверяет frozen provenance;
2. свежий read-only auditor читает полные десять holders и строит PASS/FAIL по
   каждому из 32 records;
3. FAIL → сохранить rejected attempt и перезапустить, PASS → deterministic
   materialization, receipt, commit/push, обновление `status.md`, archive task;
4. до batch-002 добавить prior-page binding, observed read-set и body-change
   gate; после batch-002 провести ранний matched retrieval comparator.

Owner blocker отсутствует; вопросов владельцу перед этим шагом нет. Проверено
полным текущим recall-holder и live `status.md`.

## Оплаченные инциденты и тупики

### Prompt v1 прошёл форму и провалил смысл

До: подробные prose-правила считались достаточными. Trigger: independent audit
отклонил mechanically valid candidate по пяти классам — merged subjects,
topical H1, strengthened modality, inverted repetition и false reject reason.
Цена: одна полная попытка и повторный prompt/test cycle; точный wall time
не измерен. Поздно замечено потому, что output не нёс per-record semantic trace.
Предотвращение: `prompts/wiki-writer.v2.md` + strict v5 `source_alignment` +
независимое чтение full holders.

### В первом Luna v2 brief был выдуман полный SHA

До: short commit `fc287ec` был вручную расширен до неверного адреса. Trigger:
Luna fail-closed до semantic reading. Цена: один provenance correction; wall
time не измерен. Поздно замечено потому, что brief не был собран из
`git rev-parse`. Предотвращение: всегда вставлять commit только из
`git rev-parse <rev>` и проверять пути `git ls-tree` до dispatch.

### Не повторять

- Не возвращаться к полировке документации вместо terminal batch-001 evidence.
- Не принимать mechanical checks как semantic acceptance.
- Не давать batch N старые quotes: после batch-001 вход — только десять новых
  holders + current Wiki + deterministic prior-page bindings.
- Не превращать ожидаемое сжатие 5–10x в hard gate и не задавать лимит страниц.

## Совет следующему агенту

- Подробные правила без наблюдаемого record-to-claim следа дали правдоподобную,
  но неверную Wiki → semantic generation и prompt design → цена: целый rejected
  batch; реально помогли literal `supporting_words` и full-holder audit.
- Visible Luna хорошо ловила JSON/hash/coverage ошибки до сдачи → repetitive
  batch writing → выгода: root получает более чистый candidate, но её PASS всё
  равно не заменяет независимого reader-а.
- Shared main менялся параллельно и dirty tree содержит чужие recall/findings,
  skills и pilot artifacts → Git cleanup → риск присвоить или удалить чужую
  работу; staging оставлять path-specific и сверять `git status` перед commit.
- Fresh Eyes и Opus остановили method-as-goal → strategy/system work → выгода:
  следующий лучший probe снова стал реальным batch, а не новая документация.

## Якоря

- HEAD последней финальной сверки перед terminal correction:
  `d4eaae72b59bb347e37367cfed233a71743ed495`; проверено
  `git log -1 --format='%H %s'` и `git ls-remote origin refs/heads/main`.
  Свежая сессия обязана сверить более новый HEAD после correction commit.
- Plan truth: `_ops/plans/openviking-chat-recall/task.md`, `context.md`,
  `status.md`; history: `HISTORY.md`.
- Prompt: `experiments/openviking-chat-recall/prompts/wiki-writer.v2.md`,
  SHA-256 `e5c4389374911239551f3157bce2b03e878dcd9981dae48a60831af856c8eeba`.
- Manifest: `experiments/openviking-chat-recall/artifacts/chronological-v1/batch-001-input.json`,
  SHA-256 `1b7e51536b2b488ad2b8f4e16c4ab3ac47f1b72182e318bd2bcb1171951f32fc`.
- Rejected bytes and verdict:
  `experiments/openviking-chat-recall/artifacts/chronological-v1/batch-001/attempt-001/`.
- Owner decisions: `_ops/chat-recall/2026-08-21-133152-codex-01a0236d.md`;
  особенно timestamps `2026-08-22T05:42:05+05:00`,
  `2026-08-22T05:44:41+05:00`, `2026-08-22T12:31:19+05:00` и
  `2026-08-22T12:32:48+05:00`.
- Recall closeout: новых qualifying theses после holder-а нет; `$123-explain`
  и `$1handoff` — process invocations, не продуктовые решения.
- Cleanup closeout: обновлены только OpenViking `status.md` и этот packet;
  чужой dirty overlay не тронут; terminal candidate намеренно не принят;
  завершённая visible Luna task архивирована.
- Previous received handoff: отсутствует.

## Consumer proof

- Следующий шаг: root check terminal bytes, затем fresh semantic audit; Luna
  self-report не является независимым evidence.
- Pending/veto: candidate нельзя materialize/commit до двух gates.
- Тупик: не повторять prompt-only confidence и ручной repair failed candidate.
- Live truth: triad плана, prompt v2, manifest и runtime visible task по точным
  путям/ID выше.
