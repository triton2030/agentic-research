---
description: "Handoff 2026-08-30-144830: 1skill-creation v6 — черновик собран и прогнан (гейт на разносе, рефактор=создание, agents/ пара, правило цели), НЕ записан; ждёт ответов владельца на 4 вопроса сокращения"
model: claude-fable-5
date: 2026-08-30-144830
---

1. Read this file in full.
2. Live files and runtime state override this dated snapshot.
3. First state the next action and why, then compare the recorded HEAD with
   `git log`. If they diverge, re-derive the action from live state before
   starting it.

Веты и блокеры — сразу:

- **v6 НЕ записан.** Живые пакеты и tracked owner — v5. Запись только после
  «да» владельца через гейт `check-approve` черновика; «да» с оговоркой —
  правка. knowledge: `~/.claude/skills/1skill-creation/SKILL.md` = v5 (без
  пунктов в цели); consequence: blocker.
- **Четыре открытых вопроса владельцу** (сокращение накопленного текста
  прогона): 1) заменить ли «до 500 символов» у Уникального контекста на
  «несколько чётких предложений»; 2) убрать ли чтение `canon.md` из гейта;
  3) слить ли `refactor-check.md` в `check-approve.md` (отменяет решение
  владельца 2026-08-25 о дельте-зеркале с `1context-refactor`); 4) сжать ли
  `description.md` вдвое. Последний ответ владельца — «не понял»; упрощённое
  объяснение отправлено, решений нет. consequence: blocker — переспроси
  простыми словами, по номерам.
- **После кругов черновик менялся** (правило цели, recall L25–L26) —
  «находок нет» последнего круга не покрывает текущий `draft-v6`. Перед
  разбором обязателен повтор шагов 2–3 (два проверяющих + исполнитель) по
  протоколу самого черновика. consequence: blocker для записи.
- **Правки 1skill-creation — только его собственным протоколом**; дословные
  формулировки владельца — защищённый материал: перед принятием правок
  субагентов сверяй с цитатами в папке-истории и `cut.md` (рецидив урезания
  «максимально понятно, простыми словами» случился в этой сессии в третий
  раз; гейт его поймал). consequence: reframe.
- **Рабочее дерево грязное чужой работой** (~370 файлов): рефактор v3
  `1instruction-authoring` и правки `1context-refactor` параллельной сессии
  не закоммичены; мои механические двухстрочные починки ссылок вшиты в те же
  файлы (tracked owners + все проекции + live, sync `--check` зелёный) и
  тоже не закоммичены. Не откатывай и не «убирай» их. knowledge:
  `git status`; consequence: accepted assumption.

## Terrain Model

### Механика внимания: копится, не гаснет

Моя модель «прочитанные файлы гаснут, активны только текущие обязательства»
-> коррекция владельца: «агент, как чат, хранит все в истории… они не
выгружаются, они копятся… надо сокращать» (recall L25) -> считать надо
суммарный текст, прочитываемый за прогон. Посчитано по `draft-v6`:
обязательный маршрут создания ~290 строк правил (тело 71 + writing-rules 33
+ goal-context 34 + description 78 + check-approve 44 + canon 28), худший
рефактор ~600. Крупнейший рычаг — `description.md` (78 строк, читается при
каждом создании), его не заметил и внешний ревьюер.

### Вердикт «резать три файла» переоценён этой механикой

Внешний агент предложил снять canon / refactor-failures / refactor-check ->
бухгалтерия накопления -> `refactor-failures` резать не нужно (читается
только субагентом вреда в чистом окне и при совпавшем сбое — в главном окне
не копится); `canon` и `refactor-check` — реальные кандидаты, но canon —
семейная строка пяти скилов (снимать семейным проходом, см. `cut.md` v5), а
слияние refactor-check отменяет решение владельца 2026-08-25.

### Жёсткость держат артефакты и владелец, не текст

Аудит v5 по `science/how-to-make-llm-obey.md` (0–4% без предъявляемого
артефакта против 97% с ним) -> v6 строит наблюдаемые следы: гейт на
наблюдаемом действии (разнос по официальным папкам, черновик свободен),
сверка принятых правок с историей, повтор шагов 2–3 включая исполнителя.
PreToolUse hook как enforcement владелец отклонил («нет не надо», L18).

### Рефактор = ускоренное создание

Короткий путь «точный diff» снят владельцем: «мы в любом случае прогоняем
весь процесс полностью» (L16). Маршрут: прочитать скил целиком → история →
goal-context с заменой «предложи УК/ЦП сам» → полный маршрут создания →
гейт. Абстрагирование живёт шагом 4 рефактора (на него ссылается
reference-files).

### Пара проверяющих living в agents/

Мандаты в файлах пакета: `agents/check-instructions.md` — матрица
обязанностей свода письма + проверка добавок; `agents/check-harm.md` —
оценщик строгости на двух уровнях (цель→поведение агента · протокол↔цель,
L19) + шаги протокола по артефактам + буквальное послушание. Агенты — «по
сути референс-файлы… только по запросу пользователя» (L21); файлы тонкие —
указатели на существующие references, не копии (L20).

## Where We Are

- `versions/v6/` — полный самодостаточный пакет (тело + 14 references + 2
  agents) в `skills/1skill-creation/versions/v6/`. knowledge: `ls`, содержимое в
  HEAD; consequence: none.
- Прогнано два захода по протоколу самого скила: заход 1 — 7+1 → 7+2
  находок; заход 2 (после дельт владельца и внешнего ревью) — 5+2 → «находок
  нет» у обоих проверяющих; исполнители прошли оба маршрута (создание
  «Downloads», рефактор «1html») без расхождений с целью. knowledge:
  `reviews/v6.md` — все списки и решения; consequence: none.
- После заходов внесены: правило «цель — максимум три пункта,
  самодостаточна» в `goal-context.md` шаг 5; цель тела переоформлена тремя
  пунктами (оказалась ровно тремя предложениями — резать не пришлось).
  knowledge: diff в HEAD; consequence: blocker (повтор шагов 2–3 не делался).
- Механика соседей выполнена: ссылки на снятые `1skill-refactor` /
  `1skill-routing` переправлены на режимы `1skill-creation` в
  `1instruction-authoring`, `1context-refactor` (owners + tracked + installed,
  `sync_simple_projections.py --check` = «all match»), `1local-rules` (оба
  рантайма, tracked owner-а нет); `/raw/` убран из `user-said.md`/`origin.md`;
  ссылка на базу в `refactor-check.md` черновика переписана прозой владения.
  knowledge: grep по `1skill-refactor|1skill-routing` в этих папках пуст +
  sync exit 0; consequence: none, кроме «не закоммичено» (см. веты).
- Отклонено и почему — `cut.md` (раздел v6) и `reviews/v6.md`; главное:
  «не ослабляй намеренную строгость» — владельцем («лишнее усложение»);
  счёт повторов при смене мандата — налог на строгость; урезание цитат
  владельца — рецидив.
- no-skill comparator объяснён владельцу: отдельный eval (два чистых окна, с
  скилом и без, одна задача), не третий субагент и не шаг протокола.
  knowledge: unverified (реакции владельца нет); consequence: none.
- Режим `112-learning-mode` действовал в этой сессии; на новую сессию не
  переносится. consequence: none.

## Next Step

Задать владельцу четыре вопроса сокращения заново — простыми словами, по
номерам, по одному предложению на вопрос (последний ответ был «не понял»;
полные формулировки всех четырёх — во втором пункте вет выше и в
`reviews/v6.md`, раздел «ВНИМАНИЕ»). Затем: внести решённое в `versions/v6` →
повтор шагов 2–3 по протоколу черновика → разбор в чат → «да» → запись в
tracked owner
`skills/shared/1skill-creation/portable/` и оба живых пакета → sync →
обновить запись в `skills/shared/README.md`. Причина: всё остальное сделано
и прогнано; работа стоит ровно на ответах владельца.

## Anchors

- HEAD: `f43bbb7e` (main, запушен). Рабочее дерево грязное чужой работой —
  см. веты.
- recall: `captured` — `_ops/chat-recall/2026-08-28-183116-claude-0713a127.md`
  L15–L27: гейт на разносе (L15) · рефактор=создание (L16) · agents/ (L17) ·
  отказ от hook (L18) · мандат агента вреда (L19) · тонкие агенты (L20) ·
  агенты в правилах референсов (L21) · механика накопления (L23, L25) ·
  правило цели (L26) · номерные решения по ревью (L27).
- cleanup: названные изменения (`reviews/v6.md` — предупреждение о
  непокрытых правках; коммит `f43bbb7e`) и `handed off`: незакоммиченные
  смешанные правки в `skills/shared/1instruction-authoring/**`,
  `skills/shared/1context-refactor/**` и их проекциях (чужой v3-рефактор +
  мои починки ссылок + sync catch-up) — последствие: `git restore` этих папок
  потеряет и мои механические починки; коммитить должен владелец или сессия
  v3-рефактора.
- предыдущий полученный хендоф: `_ops/handoffs/2026-08-28-180116-claude-fable-5.md`
  — его Next Step (битые ссылки соседей) закрыт этой сессией.
- Черновик: `skills/1skill-creation/versions/v6/` (весь пакет). История:
  `skills/1skill-creation/{user-said,cut,evidence,origin}.md` и
  `skills/1skill-creation/reviews/v6.md` —
  обязательны при любой правке.
- Наука для споров о сокращении: `science/how-to-make-llm-obey.md` (правила
  2–4, «Числовые ориентиры») и `knowledge/guides/perfect-context-engineering.md`
  («Эмпирика и Сигналы Деградации»).
- Параллельный коммит `5fc9f475` («1handoff: находки двух чистых окон по
  черновику») — сессия внешнего ревьюера; его предложения и мои вердикты по
  ним — `reviews/v6.md`, раздел «Внешнее ревью».

## Incidents

### Модель «прочитанное гаснет» опровергнута владельцем

- прежняя модель: считал только одновременно активные обязательства, пики по
  моментам; накопление прочитанного считал безвредным по дизайну стадий.
- триггер: владелец: «они не выгружаются, они копятся… надо сокращать»
  (recall L25) — окно сессии держит всё физически, деградация от объёма
  (lost-in-middle, distraction) реальна отдельно от активных обязательств.
- оплаченная цена: один круг обсуждения (~30 мин) + перестройка анализа.
- почему нашлось поздно: применил науку об active constraints, не спросив,
  какую механику имеет в виду владелец.
- durable исход уборки: бухгалтерия накопления в этом пакете (Terrain);
  вопросы сокращения — блокером сверху.

### Ответ-гроссбух получил «не понял»

- прежняя модель: подробная таблица с четырьмя вопросами в одном сообщении —
  достаточная форма для решений владельца.
- триггер: ответ владельца «не понял»; после пересказа простыми словами по
  шагам возражений не было (но и ответов пока нет).
- оплаченная цена: один круг.
- почему нашлось поздно: нарушил собственную норму «Chat Is Speech» из
  глобальной инструкции владельца.
- durable исход уборки: сжатая формулировка вопросов — в ветах этого пакета.

## Advice to the Next Agent

### Пара agents/ + исполнитель сходится за два круга

Род работ: проверка черновика 1skill-creation его же протоколом. Два захода
подряд: первый круг даёт 5–9 находок, второй — ноль или около. Исполнителя
гоняй на обоих маршрутах (создание и рефактор) — они ловят разное: создание
проверяет порядок writing-rules → вопросы, рефактор — чтение целиком, tracked
owner и предложение снятия. Стоимость захода ~3 субагента, полезность высокая.

### Сверка с cut.md ловит рецидивы урезания цитат — уже трижды

Файл: `check-approve.md` шаг 4 черновика. Перефразировщик стабильно
предлагает урезать «максимально понятно, простыми словами» и «реально
попросил» — это третий и четвёртый заходы одного и того же совета. Без
операции сверки с историей правка проходит (случалось дважды до бортика);
с ней — ловится за секунды.

### Счёт единиц у субагентов плавает

Род работ: бюджеты. Разные чистые окна насчитывают телу 14–21 единицу — не
принимай один счёт за истину и не оптимизируй под точное число; порог
владельца мягкий («это не строгие правила»).
