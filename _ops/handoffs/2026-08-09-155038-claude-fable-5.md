---
description: "Хендоф 2026-08-09-155038: день полной перестройки мета-семьи — провенанс/карта/refactor.md в обоих мета-скилах, рефакторы 1document-system и 1handoff; очередь — 1chat-recall"
model: claude-fable-5
date: 2026-08-09-155038
---

# Хендоф — перестройка мета-семьи и первые два рефактора по новой процедуре

## Где мы сейчас

Коммиты сессии `05897e0 → cc0824b` (плюс параллельная сессия владельца
`c8f13be` — продуктовая пара проекта). Всё запушено, обе установки
(~/.claude, ~/.codex) синхронны с owner-ами.

- Оба мета-скила (`1skill-shaping`, `1instruction-shaping`) прошли
  4-линзовый аудит (я + свежее окно + Брукс + Codex xhigh) и получили:
  раздел/файл-владельца смысла + операцию 7а, `reference.md` (бюджет правил),
  провенанс-контур (источник каждой строки, «да» на точный текст,
  deletion-only), карту черновика с дефолтом модели, `refactor.md`
  (9 шагов, зеркальная пара), аксиому направления.
- `1document-system`: первый боевой прогон refactor.md — 95.6→45.6KB, tracked
  owner создан, английский (решение владельца), инверсия шаблона из MAVO.
- `1handoff`: второй прогон — летопись+«Совет следующему агенту», самовызов
  с лимитом 1/сессию; этот файл — первый хендоф по новому контракту.

## Следующий шаг

Рефактор `1chat-recall` по refactor.md. Причина: №1 в очереди хендофа
2026-08-08-203843 (питает всю мета-семью, каждый capture), процедура
обкатана дважды, туда же ложится Р1 — внешний след «допуск → черновик»,
главная дыра adherence-аудита. Baseline закоммитить ДО первой правки.

### Pending decision владельца

Строка в refactor.md обоих мета-скилов: «шаг 1 = зафиксируй baseline
коммитом до первой правки» — предложена после инцидента ниже, владелец не
ответил (переключился на 1handoff). Не вносить без «да».

### Открытый вопрос

Связка 1handoff → `1index` (старая очередь предполагала, контракт молчит) —
владелец дважды не решал.

## Инцидент: baseline 1document-system перезаписан до коммита

- **До:** cp живого пакета в tracked owner → сразу начал пересборку файлов
  на месте; живые установки тоже перезаписал установкой.
- **Trigger:** `git commit` вывел только `create mode` — оригинала нет ни в
  одной ревизии; моя строка в cut.md «baseline цел в git-истории» оказалась
  ложью.
- **Цена:** ~40 минут восстановления, два парсера транскриптов, лишний
  коммит; wall time по таймстампам коммитов 9abd662→beaf987.
- **Почему поздно:** refactor.md не требует коммита baseline; порядок
  «cp → rewrite» выглядел безопасным, проверка не была предусмотрена.
- **Предотвращение:** pending decision выше; до него — руками: baseline
  коммитится до первой правки (в 1handoff-рефакторе уже сделано так).

## Совет следующему агенту

- Транскрипты (`~/.claude/projects/<proj>/<session>.jsonl`, task-output
  субагентов) хранят прочитанные файлы дословно → восстановление затёртого
  baseline байт-в-байт двумя python-парсерами → спасло от полной потери.
- `chat_recall.py --session-id` падает на сессиях с AskUserQuestion
  («answer is not structured») → фиксация цитат: обход — capture с
  `--timestamp-source turn-context --timestamp-precision minute`, exact-ts
  недоступен.
- Opus-исполнитель по узкому контракту (сжатие 14 шаблонов) → сам назвал мой
  пропуск (файлов было 15) и свои превышения размера → делегирование
  механической пересборки надёжно при точном контракте + выборочной проверке.
- Карта групп/карточек вместо чтения полного пакета → владелец курировал
  95KB-рефактор за минуты, решения по группам → интерфейс работает.
- Перечисление файлов субагенту по памяти без `ls` → пропущенный
  template-sem.md → всегда давать список из команды, не из головы.
- Длинный кириллический JSON в AskUserQuestion (~3.5KB) оборвался с
  InputValidationError → повторная сборка короче прошла → опросы держать
  компактными.
- Зеркальные правки «источник → производная» (audit/check/refactor пары) →
  дисциплина держалась только моим вниманием; счёт помеченных копий за день
  ≈ полтора десятка → тип работы: мета-семья.

## Якоря

- Owner-ы: `skills/shared/{1skill-shaping,1instruction-shaping}/portable/` —
  sync скриптом `skills/shared/sync_simple_projections.py <имена> --write
  --install` и `--check`; `skills/shared/{1document-system,1handoff}/portable/`
  — вне sync-скрипта, установка вручную cp (дельты Codex-варианта 1handoff —
  в `skills/codex/1handoff/SKILL.md`).
- Baseline-ы: `skills/1document-system/baseline-2026-08-09/`,
  `skills/shared/1handoff/portable/SKILL.md@ba25ffa` + codex-вариант в
  `skills/1handoff/baseline-2026-08-09-codex-SKILL.md`.
- Дословные решения владельца сегодня: `_ops/chat-recall/
  2026-08-09-122011-claude-08445ad4.md` (15 записей: дефект разделов, супер-
  принцип, большие мета-скилы, карта, рефактор-монолог, интент
  1document-system), `-175000` (язык EN), `-203500` (хендоф-летопись),
  `-205000` (самовызов).
- Продуктовая пара проекта: `_ops/product-frames/agentic-research{,.principles}.md`
  — появилась сегодня параллельной сессией, читать до суждений.
- Вердикты аудитов: Брукс/свежее окно — в транскрипте сессии; Codex —
  `_workspace/codex-artifacts/20260809T{082220Z-ea329340,082945Z-e2e8dde9}/`.

## Residual risk

Ничто из сегодняшнего не прогнано поведенчески (решение владельца: прогоны
лично). Пары «источник→производная» проверяются только дисциплиной — первый
неперенесённый фикс это докажет; кандидат на `--check` sync-скрипта назван в
чате, владелец не решал.
