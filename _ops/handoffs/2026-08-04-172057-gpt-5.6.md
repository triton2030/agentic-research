---
description: "Хендоф 2026-08-04-172057: 1chat-recall стал продуктом decision context"
model: gpt-5.6
date: 2026-08-04-172057
---

# Хендоф — продуктовый контракт 1chat-recall

## Где мы сейчас

Переработка завершена и отправлена в `origin/main`: commit `6c66d7e` делает
decision-ready context главным продуктом `1chat-recall`, а quote search —
обслуживающей механикой. Codex/Claude tracked owners и live projections были
синхронизированы; structural validation, 62 теста на каждой из трёх копий,
strict corpus checks и hybrid regression прошли.

Четыре fresh consumer cases показали нужную траекторию: конкретная развилка,
широкий вопрос, near-miss без recall и отделение owner evidence от инженерного
inference. Это доказывает возможность переноса, но не частотный uplift.
Clean handoff consumer без старого чата восстановил правильный default next
action, optional benchmark, veto и оплаченный инцидент.

Рабочее дерево по-прежнему содержит чужие modified/untracked артефакты, включая
`_ops/**` и параллельные edits в `skills/claude/1fresh-eyes/**`. Не очищать, не
массово stage-ить и не включать их в следующий commit.

## Следующий шаг

Если потребуется доказать relative improvement, выполнить matched resampling на
одинаковых непоказанных prompts и одинаковом resolved model/settings для parent
`4224084` и нового `6c66d7e`; измерить частоту первого действия
`claim cluster → applicability gate`, а также quote-dump/false-current failures.
До такого evidence не расширять skill новыми правилами.

Если вероятностный benchmark не нужен, implementation закрыта: начать следующую
задачу от live Product Frame и текущего runtime, не продолжать polishing по
инерции.

## Якоря

- `_ops/product-frames/1chat-recall.md`: подтверждённый продуктовый promise,
  тайбрейкеры и appetite.
- `skills/codex/1chat-recall/SKILL.md`: tracked Codex owner; live projection —
  `/Users/triton/.codex/skills/1chat-recall/SKILL.md`.
- `skills/claude/1chat-recall/SKILL.md`: tracked Claude owner и target symlink.
- `skills/codex/1chat-recall/agents/openai.yaml`: обновлённый discovery prompt.
- `6c66d7e`: реализованный и pushed product/causal contract;
  `4224084`: ближайший previous-version comparator.
- `cr-7d68b6863c42d53a`: exact owner requirement о смысловой parity Codex/Claude
  projections; context-note восстановлен и strict check зелёный.

### Инцидент — shell съел имя skill в context-note

- **до:** capture-команда содержала backticks вокруг имени `1chat-recall`.
- **trigger:** zsh выполнил содержимое как command substitution и записал
  усечённую agent-authored delta; owner quote и timestamp остались точными.
- **цена:** один повреждённый context-note и шесть follow-up tool calls на
  диагностику, failed first patch, точечный repair и strict verification;
  wall time `unknown`.
- **почему поздно:** первая возможность была до exec; правило безопасного
  shell-escaping не было применено к аргументу `--context-note`.
- **предотвращение:** не передавать backticks в shell arguments; использовать
  plain text и сразу проверять consequential capture через `--show`.
