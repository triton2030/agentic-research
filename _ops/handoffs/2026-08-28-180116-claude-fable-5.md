---
description: "Handoff 2026-08-28-180116: 1skill-creation v5 записан — нарезка по разделам создаваемого скила, режимы рефактора и кнопки запуска, поглощены 1skill-refactor/1skill-routing; корпус chat-recall перестроен параллельной сессией"
model: claude-fable-5
date: 2026-08-28-180116
---

1. Read this file in full.
2. Live files and runtime state override this dated snapshot.
3. First state the next action and why, then compare the recorded HEAD with
   `git log`.

Веты и незакрытое — сразу:

- **Правки 1skill-creation — только его собственным протоколом**, включая
  рефактор (режим `references/refactor.md`) и правку description (режим
  `references/routing.md`): черновик → пара субагентов + исполнитель-
  наблюдатель → разбор со счётом → «да» владельца → запись. knowledge: сам
  скил `skills/shared/1skill-creation/portable/`; consequence: blocker для
  любой правки без «да».
- **Дословные формулировки владельца — защищённый материал.** Guardrail
  теперь в самом скиле: «Правку, меняющую дословные цитаты пользователя, сам
  не принимай — неси в разбор» (`check-approve.md`). За сессию рецидив
  случился дважды — см. Incidents. consequence: reframe.
- **Корпус chat-recall перестроен параллельной сессией — НЕ восстанавливать
  raw/ и topics/.** Один слой: файлы разговоров в корне `_ops/chat-recall/`,
  `topics.md` — словарь тем; слой тем и reconcile-машинерия сняты владельцем
  (коммиты `bf4327a7`, `4a994c85`, сессия d84f79af). Глобальный скил
  `1chat-recall` мог не успеть перестроиться — при расхождении правда у
  `_ops/chat-recall/AGENTS.md`. knowledge: `git log`, новый AGENTS.md корпуса;
  consequence: reframe.
- **Битые перекрёстные ссылки на поглощённые скилы — не чинены.** grep нашёл
  `1skill-refactor`/`1skill-routing` в `1instruction-authoring` (SKILL.md,
  placement.md), `1context-refactor/SKILL.md`, `1local-rules/SKILL.md` — в
  tracked и live. knowledge: grep в сессии (см. Next Step); consequence:
  blocker для корректной маршрутизации семьи.

## Terrain Model

- **1skill-creation v5** — три функции одним пакетом: создание + режим
  рефактора + режим кнопки запуска. Тело — дирижёр (~20 строк): УК · ЦП ·
  предусловие «нулевой» `writing-rules.md` (правила письма + адреса, действует
  с вызова до записи) · разделы по разделам создаваемого скила
  (`goal-context` → `behavior-protocol` условно → `reference-files` условно)
  · режимы · общие правила (сквозной гейт записи `check-approve`, механика
  «читай только файл раздела, переход по следу», автономия, техдиректор,
  репо, ссылки в чат). 14 референсов, каждый смысл у одного владельца.
- **Нарезка — владельца**: «каждый раздел скилла должен быть одним
  референс-файлом»; имена по действию; формат-грамматика: порядковое —
  номера, прочее — bullet, проза — только общая информация; единица счёта —
  несущее указание; порог <20, желательно ~10.
- **Пять осей приёмки инструкций** (владелец, raw L30): размещение · дубли ·
  однозначность для свежего агента · когнитивные пробелы между шагами ·
  симметрия усилий. Проверено веером из трёх Codex-ревьюеров по осям —
  расхождение осей само по себе находка.
- **Проверка скила** = пара субагентов (перефразировщик + буквальное
  послушание) + субагент-исполнитель с наблюдением фактических шагов против
  цели (новый критерий владельца, сессия d84f79af).
- Семья теперь: `1skill-creation` + `1instruction-authoring` +
  `1context-refactor`. Снятые `1skill-refactor`/`1skill-routing` — снапшоты в
  `skills/1skill-refactor/`, `skills/1skill-routing/` c cut.md-записями.
- Ложное чтение, опровергнутое сессией: «raw/ и topics/ исчезли — внешний
  сбой, надо восстанавливать». На деле — намеренная перестройка корпуса
  параллельной сессией владельца, закоммиченная. При дисковой аномалии сперва
  `git log`, потом тревога.

## Where We Are

- HEAD `536162cc` (main, запушен; до хэндофф-коммита). knowledge:
  `git status -sb` = `main...origin/main`; consequence: none.
- v5 записан: tracked owner + живые пакеты Claude/Codex;
  `sync_simple_projections.py 1skill-creation --check` зелёный. knowledge:
  вывод sync в сессии; consequence: none.
- Живые `1skill-refactor`/`1skill-routing` удалены из `~/.claude/skills/` и
  `~/.codex/skills/`; skill-лист рантайма показывает только 1skill-creation с
  новым description. knowledge: ls + системный список скилов; consequence:
  none.
- История актуальна: `skills/1skill-creation/{cut,evidence,user-said}.md` —
  v5-записи; `draft-v5/` оставлен в папке-истории как архив черновика (копия
  owner-а). knowledge: git show 66fcdb54; consequence: none.
- Продуктовая рамка 1skill-creation по-прежнему не создана (владелец не
  заказывал). consequence: accepted assumption.
- `_ops/chat-recall/2026-08-26-170908-codex-01a03ded.md` — uncommitted M,
  правит параллельная сессия; не трогать. consequence: none.
- Кандидаты следующего захода: тело и `check-approve.md` на пределе ~20
  (счёт в evidence.md); чистка перекрёстных ссылок (Next Step).

## Next Step

Прочесать репо и глобальные инструкции на ссылки на снятые
`1skill-refactor`/`1skill-routing` (grep из Anchors) и переправить их на
режимы `1skill-creation` (`references/refactor.md`, `references/routing.md`).
Причина: поглощение оставило непроверенные рёбра — соседние скилы маршрутизируют
в несуществующие адреса. Правки чужих скилов — короткий путь режима рефактора
(точный diff) с разбором и «да» владельца.

## Anchors

- Owner: `skills/shared/1skill-creation/portable/SKILL.md` + 14 references.
- История и слова владельца (обязательны при любой правке):
  `skills/1skill-creation/{user-said,cut,evidence,origin}.md`.
- Слова владельца этой сессии: `_ops/chat-recall/2026-08-28-053552-claude-9bb215e3.md`
  L15–L32 (корпус плоский, без raw/); критерий наблюдения — 
  `2026-08-28-061115-claude-d84f79af.md` L28.
- Снапшоты снятых: `skills/1skill-refactor/tracked-owner-2026-08-28/`,
  `skills/1skill-routing/tracked-owner-2026-08-28/`.
- Реестр: `skills/shared/README.md` (запись v5).
- Грязные ссылки: `grep -rln "1skill-refactor\|1skill-routing" ~/.claude/skills/
  skills/shared/ --include="*.md"` (исключая 1skill-creation и снапшоты).
- recall: captured — адреса выше; отдельного `no recall address` нет.
- cleanup: named changes — коммиты `66fcdb54`, `536162cc`, запушены; ссылки
  соседей — handed off (Next Step).
- Предыдущий полученный handoff: `_ops/handoffs/2026-08-28-052527-claude-fable-5.md`
  — использован, его Next Step (вопрос о разложении тела) закрыт этой сессией.

## Incidents

### Рецидив: совет субагента перекрыл дословную формулировку владельца

Прежняя модель: принятые по существу правки проверяющих безопасны. Триггер:
Codex-аудит потерь нашёл, что «реально попросил» (CAPS владельца) заменено на
«продиктовал шаги» — замену, которую cut.md уже отклонял 27.08. Цена: один
дополнительный круг правок (~30 мин). Почему поздно: правка пришла от
проверщика послушания и была принята без сверки с cut.md. Cleanup: guardrail в
самом скиле — «правку, меняющую дословные цитаты пользователя, сам не
принимай — неси в разбор» (`check-approve.md`, записано с «да» владельца).

### Ложная тревога: перестройку корпуса принял за внешний сбой

Прежняя модель: raw/ и topics/ удалены неизвестным процессом — предложил
владельцу восстановление из git. Триггер: `git log` показал коммиты
параллельной сессии d84f79af («снят слой тем») и её session-файл. Цена: ~2
хода диагностики и лишний вопрос владельцу. Почему поздно: смотрел
`git status` рабочего дерева, но не `git log` перед тревогой. Cleanup:
терраин-коррекция в этом пакете; правило «сперва git log» — в Terrain Model.

## Advice to the Next Agent

### Веер Codex по осям критики — окупается на инструкциях

Тип работы: приёмка скила/инструкции. Три параллельных ревьюера по одной оси
каждый (размещение+дубли · однозначность · пробелы+симметрия) дали 78 находок
за один цикл; совпадение находок разных осей — самый надёжный сигнал. Схема
выхода — одна на веер, иначе нормализация съест окно.

### Отклоняй «минимальные правки» оси однозначности пачкой

Файл: любой SKILL.md. Ось однозначности предлагает юридические уточнения
краёв; ~30 из 44 отклонено «налогом на строгость» (владелец нормы —
`agent-defaults.md`). Принимай только правки с правдоподобным неверным
разбором на реальном маршруте; остальное дороже пользы.
