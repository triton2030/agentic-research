---
description: "Хендоф 2026-08-19-022406: Graphiti transport закрыт, полный корпус продолжает retained task"
model: gpt-5
date: 2026-08-19-022406
---

Прочитай этот файл целиком.
Живые файлы и runtime сильнее этого снимка.
Первым ходом назови следующий шаг и его причину, затем сверь HEAD-якорь с `git log`.

## Где мы сейчас

`experiments/graphiti-codex` — законченный тонкий adapter над stock
`graphiti-core==0.29.3`: Luna/low заменяет LLM provider, локальный
`multilingual-e5-small` заменяет embeddings API, FalkorDBLite хранит граф.
Метод и границы принадлежат
`experiments/graphiti-codex/docs/PROC — Operational Procedure — Graphiti quote ingestion.md`;
карта пяти code owners и команды находятся в `experiments/graphiti-codex/README.md`.

Полный frozen-корпус ещё загружается. Retained Codex task
`01a01480-61ba-77b3-a876-01f3b50b15a5` активен; последний подтверждённый
checkpoint — 195/693 unique episodes, 300 facts, 33 invalidated, 498 remaining,
duplicates 0, BGSAVE `ok`, changes 0. Проверено task-сообщением после reopen;
следующая отдельная пачка уже выполнялась при closeout. Основная DB:
`experiments/graphiti-codex/.data/owner-quotes-2026-08-04_2026-08-18-luna-low-ordered.db`.

Текущий план и единственный Next принадлежат
`_ops/plans/graphiti-codex-finish/{task,status,context}.md`. Snapshot входных
117 holders / 693 quotes зафиксирован на `692d894` в retained worktree
`/Users/triton/.codex/worktrees/105a/agentic-research`; runtime task сильнее
записанного checkpoint.

### Принятая LLM-граница

Решение владельца закреплено в
`_ops/chat-recall/2026-08-18-151822-codex-01a0145e.md:67`: один тёплый
`codex app-server` на CLI-run, но новый ephemeral thread на каждый Graphiti
LLM-call. Episodes одного owner corpus строго последовательны; Graphiti может
держать до четырёх Luna-turn внутри одного episode. Tests этой границы:
`experiments/graphiti-codex/tests/test_codex_llm.py`.

### Предел stock Graphiti

Явный current/as-of filter гарантированно скрывает edge, которому Graphiti уже
выставил `invalid_at`. Stock extraction/resolution иногда не распознаёт
коррекцию или инвалидирует лишнее; adapter не добавляет custom resolver,
ontology, summary или coverage verdict. Это принятая граница, а не pending
рефактор; доказательства и control-пары сведены в
`_ops/plans/graphiti-codex-finish/status.md`.

### Отвергнутые маршруты

- Shared conversation thread на episode быстрее на 30–45.5%, но на девяти
  независимых quotes сохранил 12 edges против 23 и потерял owner-facts во всех
  трёх сериях. Не возвращаться к нему без нового слова владельца.
- Luna/medium была медленнее low и хуже разрешила temporal control-пару.
- Concurrent episodes, `add_episode_bulk`, multi-quote extraction, custom
  ontology и control-plane меняют штатную temporal семантику и не входят.

### Инцидент: неверный первый shared-thread probe

**До:** гипотеза владельца была прочитана как два одновременных turns в одном
conversation. **Trigger:** владелец указал, что нужен последовательный reuse
всех Graphiti calls одного episode. **Цена:** один нерелевантный probe и
повторная серия; точный wall time не отделён. **Почему поздно:** concurrency
была проверена раньше, чем сформулирована единица памяти. **Предотвращение:**
граница корректного A/B и ручные критерии fact recall/attribution/history теперь
зафиксированы в `_ops/plans/graphiti-codex-finish/status.md:50`.

## Следующий шаг

Сначала прочитать retained task `01a01480-61ba-77b3-a876-01f3b50b15a5` и
дать ему продолжить единственным writer до 693/693. Причина: следующая quote
должна видеть предыдущий graph state; второй процесс или параллельные episodes
ломают deduplication/invalidation и могут повредить DB. После каждого
`batch-size 5` retained task сам делает reopen, duplicate-check и BGSAVE.

Если task завершился или запросил внимание, действовать только от его точного
последнего результата. После 693/693 выполнить записанный в plan current/history
query audit на разных типах вопросов и сдать систему. Blocker сейчас отсутствует;
не реконструировать огромную holder-команду и не запускать второго writer, пока
retained task жив.

## Совет следующему агенту

- Счётчики и latency выглядели убедительно, но ручное чтение raw extracted
  edges сменило transport verdict → Graphiti A/B требует проверки owner-facts,
  attribution и history, а не только числа edges.
- Официальная документация и pinned upstream source быстро сняли saga,
  custom ontology и bulk/concurrent ingest → локальная правда собрана в PROC,
  повторный широкий web-поиск не нужен.
- Один `add_episode()` буферизует результат до конца, поэтому длинное молчание
  само по себе не ошибка → batch 5 дал наблюдаемую точку восстановления каждые
  несколько минут.
- Аудит пяти source-модулей не нашёл смешанного владения или дублированной
  семантики → новый façade/refactor сейчас увеличил бы interface без уменьшения
  знания caller; карта модулей в README дешевле и точнее.

## Якоря

- HEAD перед созданием packet: `5d22204486f27ecdb01c86444598e12d4bf785a5`;
  проверено `git rev-parse HEAD`, commit запушен в `origin/main`.
- Проверка кода: `cd experiments/graphiti-codex && uv run ruff check . && uv run pytest -q`
  → `All checks passed`, `26 passed`; один upstream Pydantic deprecation warning.
- Recall: `captured` — финальный выбор ephemeral записан в
  `_ops/chat-recall/2026-08-18-151822-codex-01a0145e.md:67`.
- Уборка: обновлены plan status, README, PROC и закрыта находка
  `_ops/findings/2026-08-18-201742-55354-22544.md`; stale orphan A/B processes
  26303/26304 завершены после `ps`/`lsof` проверки без открытой DB; live retained
  task не тронут.
- A/B databases `experiments/graphiti-codex/.data/live-thread-*` сохранены как
  дорогое reproducible evidence, не мусор. Backup до дедупликации основной DB:
  `.data/owner-quotes-2026-08-04_2026-08-18-luna-low-ordered.before-dedup-20260819T0110.db`.
- INDEX не менялся: оплаченный маршрут уже находится в локальных README/PROC.
- Чужие незакоммиченные владельцы сохранены без изменений: `Workspace/`,
  `_ops/chat-recall/2026-08-18-143951-claude-524d8920.md`,
  `_ops/chat-recall/2026-08-19-020820-claude-7658d2a8.md`,
  `skills/1instruction-shaping/cut.md` и пять файлов под
  `skills/claude/1instruction-shaping/` (`SKILL.md`, `references/audit.md`,
  `references/knowledge-out.md`, `references/placement.md`,
  `references/simplify.md`).
- Agent cleanup: новых subagents не создавалось; один старый subagent уже
  completed, один interrupted. Пользовательский retained Codex task намеренно
  остаётся active.
