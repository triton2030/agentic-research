---
description: "Handoff 2026-08-28-052527: 1skill-authoring переписан в 1skill-creation (v4.3) и 1skill-refactor перекроен; тело нарушает собственный порог, разложение по референсам ждёт решения владельца"
model: claude-fable-5
date: 2026-08-28-052527
---

1. Read this file in full.
2. Live files and runtime state override this dated snapshot.
3. First state the next action and why, then compare the recorded HEAD with
   `git log`.

Веты и незакрытое — сразу:

- **Файлы скила пишутся только после «да» владельца** (гейт в самом
  1skill-creation, «Разбор и запись»). Черновик-файл в репо — можно до «да»;
  live-установка и сам скил — после. knowledge: строки скила
  `skills/shared/1skill-creation/portable/SKILL.md`; consequence: blocker для
  любой правки тела без утверждения.
- **Открытое предложение владельцу без ответа:** тело v4.3 держит ~40
  строк-инструкций против собственного порога «меньше 20»; предложено
  разложить «Проверку» и «Черновик» по референс-файлам по правилам
  `references/reference-files.md`. Владелец не ответил. knowledge: последний
  ход сессии; consequence: blocker — без его «да» не резать.
- **Дословные формулировки владельца — защищённый материал.** Советы
  субагентов их не перекрывают: в этой сессии принятие таких советов стоило
  полного аудита и переписывания (см. Incidents). consequence: reframe.

## Terrain Model

- Пятёрка авторинг-скилов: `1skill-creation` (бывший `1skill-authoring`,
  архив в `skills/1skill-authoring/`), `1skill-refactor`, `1skill-routing`,
  `1instruction-authoring`, `1context-refactor`. Владельцы —
  `skills/shared/<name>/portable/`; проекции и установки собирает
  `python3 skills/shared/sync_simple_projections.py <имена> --write --install`
  и проверяет `--check`.
- `1skill-creation` v4.3 — продиктованный владельцем протокол: спроси
  функцию → разделы создаваемого скила («Уникальный контекст» ≤500 симв. без
  инструкций · «Цель пользователя» — трансляция воли, каждая цель =
  инструкция · «Протокол поведения» только если реально попросил, его
  цитатами) → черновик в репо `agentic-research` (до «да» — черновик, цитаты,
  cut.md, рамка) → пара субагентов (перефразировщик + буквальное послушание,
  пустой список законен) → разбор в чат (просто, markdown, с количеством
  инструкций, каждый пункт с цитатой держащей строки) → «да» → запись + ссылки.
- Критерии качества: без дублей; каждая инструкция с новой строки; тело <20
  единиц (желательно ~10), единица — несущее указание предложение включая
  контекст и цель; референсы — по случаю, тема целиком, ≤20 единиц, в теле
  только «когда читать» (`references/reference-files.md` — owner правил).
- `1skill-refactor` — сверка со `1skill-creation` + пошаговый протокол
  абстрагирования (плоский список → группы → инструкция высшего порядка →
  невыводимое остаётся явным); наследует разбор/«да»/ссылки по шагу «Разбор и
  запись» `1skill-creation`.
- Слой тем `_ops/chat-recall/topics/skill-instruction-authoring.md` обновлён
  typed-операциями под все решения сессии; сырые слова владельца — в
  `_ops/chat-recall/raw/2026-08-26-215943-claude-1a12f637.md` (L15–L31) и
  `raw/2026-08-27-190611-claude-029784d5.md` (L15–L23).
- Ложное чтение, опровергнутое сессией: «advice субагентов = улучшение».
  Перефразировщик «чинит» и дословные цитаты владельца; принятие без фильтра
  провенанса даёт искажения (11 штук за один цикл).

## Where We Are

- HEAD `af2d1856` (main, запушен; GitHub — backup, ветки не используются).
  knowledge: `git log --oneline -6`, `git status` чист; consequence: none.
- v4.3 установлена в `~/.claude/skills/1skill-creation` и
  `~/.codex/skills/1skill-creation`; `--check` зелёный. knowledge: вывод
  sync-скрипта в сессии; consequence: none.
- Старые `1skill-authoring` сняты отовсюду (tracked, live, ссылки соседей);
  снапшот — `skills/1skill-authoring/tracked-owner-2026-08-26/`. knowledge:
  grep по репо и live-папкам в сессии; consequence: none.
- История нового скила: `skills/1skill-creation/{origin,cut,evidence,user-said}.md`
  — актуальны по v4.3. knowledge: записи этой сессии; consequence: none.
- Продуктовая рамка для 1skill-creation не создана (шаг «Черновик» её
  упоминает). knowledge: `ls skills/shared/1skill-creation/` — нет
  product-frame файла; consequence: accepted assumption — владелец не
  заказывал явно, кандидат при следующем заходе.
- Побочная находка о невыполнимом стандарте базового `check.md`
  (`1context-refactor`) — `_ops/findings/2026-08-26-*.md`. consequence: none
  для текущей работы.

## Next Step

Спросить владельца (или дождаться его ответа): раскладывать ли «Проверку» и
«Черновик» из тела v4.3 по референс-файлам до порога <20. Причина: тело
нарушает собственный критерий вдвое, но весь объём — его продиктованные
строки, а их снятие без «да» запрещено самим скилом. При «да» — путь по
самому скилу: черновик в чат → пара субагентов → разбор с количеством
инструкций → «да» → запись + ссылки.

## Anchors

- Owner тела: `skills/shared/1skill-creation/portable/SKILL.md`; референсы —
  `references/{formulation,agent-defaults,skills-science,canon,reference-files}.md`.
- Слова владельца о скиле (обязан читать при любой правке):
  `skills/1skill-creation/user-said.md` и raw-якоря из Terrain Model.
- Уроки и отклонённое с причинами: `skills/1skill-creation/cut.md`,
  `evidence.md`.
- Сосед: `skills/shared/1skill-refactor/portable/SKILL.md` (наследование
  разбора — строка «по шагу „Разбор и запись“»).
- Реестр семьи: `skills/shared/README.md`; маршрут в корне: `AGENTS.md:74`.
- recall: captured — адреса выше; отдельного `no recall address` нет.
- cleanup: named changes — коммиты `4759e1ae..af2d1856`, все запушены.
- Предыдущий полученный handoff: не использовался в этой сессии.

## Incidents

### Советы субагентов перекрыли дословные цитаты владельца

Прежняя модель: советы перефразировщика («короче и однозначнее») можно
принимать по существу. Триггер: владелец попросил проверить всё сказанное —
аудит нашёл 11 расхождений v4.1 с его буквальными словами (черновик в репо,
«агент мощный», «не лечение проблем», скил-знание, первая строчка-команда и
др.). Цена: полный цикл анализа и переписывания, ~2 хода сессии
(2026-08-27→28). Почему поздно: советы фильтровались по качеству формулировки,
не по провенансу строки. Cleanup: v4.2 восстановила все 11 по команде
«исправь»; урок закреплён в `skills/1skill-creation/cut.md` и в самом скиле
(«Протокол поведения — буквально его цитатами»).

### Упущенный capture правила владельца

Правило «каждая инструкция описывается в новой строке» владелец считает
сказанным ранее; в raw его не было — пришлось переспрашивать примером. Цена:
один дополнительный цикл правки (2026-08-28). Почему поздно: unknown (в
сохранённых репликах строки нет — либо не прозвучало дословно, либо не
captured). Cleanup: зафиксировано в raw (029784d5 L21, L23), внесено в
критерии тела и применено ко всему телу.

## Advice to the Next Agent

### Правки 1skill-creation — только через его собственный протокол

Тип работы: любые изменения пятёрки. Черновик в чат, пара субагентов, разбор
с количеством инструкций, «да», запись, ссылки. Выгода, оплаченная сессией:
пара реально ловит (битые указатели, потерянные механизмы, самонарушения);
цена пропуска гейта — переписывание.

### Счёт инструкций — по строкам, вслух, в каждом отчёте

Файл: тело любого скила. Владелец требует называть количество инструкций в
отчёте; построчный формат делает счёт видимым. Занижение через склейку
предложений — известный путь побега, владелец его прямо запретил.
