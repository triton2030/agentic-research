---
эпик: "самостоятельный experiment: openviking-chat-recall"
план: "[[task|Библиотека знаний из chat-recall]]"
состояние: 🔨 в работе
режим: Execution
вех-готово: 2
вех-всего: 7
обновлено: 2026-08-23
kind: status
---

# Статус — библиотека знаний из chat-recall

## Next

Дождаться рукавов `library` и `topics` слепой приёмки и вынести вердикт
ослеплённым судьёй. Причина: судьба стадии 4 (354 страницы против 40
тематических файлов) решается этим замером, а не спором, и до вердикта
указатель строить нельзя — он собирается по слою, который может не пережить
проверку.

Планка известна: рукав `digest` — действующий маршрут скила `1chat-recall` —
ответил на 15 вопросов из 15, ноль промахов, медиана 3 открытых файла.

## Свидетельства и статус

Подтверждено проверками:

- 351 страница в `artifacts/wiki-v1/`, 40 тем из 40; шапки полные, типы в
  пределах пяти, темы в пределах сорока; `scripts/check_wiki.py` даёт одну
  проблему — ещё нет `index.md`;
- провенанс слияния чист: `scripts/check_topics.py` — ноль проблем;
- покрытие корпуса считается, а не оценивается: `scripts/check_coverage.py`
  против коммита снимка `dd1ff113` — 1085 записей из 1207 (89,9%);
- все 1693 ссылки библиотеки открываются; до 2026-08-23 не открывалась ни одна.

Опровергнуто и починено этой сессией:

- **ссылки**: контракт страниц требовал семь уровней вверх вместо пяти и прямо
  запрещал считать глубину. Незамеченным осталось потому, что `check_wiki.py`
  выходил из себя первой строкой при отсутствии `index.md`, а указатель
  собирается последним;
- **дубль**: две страницы темы `gemini` с одним заголовком и одним якорем; у
  снятой лишняя фраза опиралась на не свой источник. Близких пар в разных
  темах — ноль;
- **пропуски**: 122 записи корпуса не дошли до библиотеки. Потеря не в слиянии
  (оно не потеряло ничего), а в сжатии файлов (90) и написании страниц (32).

Заблокировано: ничего.

Активные прогоны (снимок 2026-08-23 16:45, источник — `ps` и файлы вывода):

- указатель — `_workspace/ox-index/runs/index.json`, идёт;
- добор 23 тем — `_workspace/ox-backfill/runs/`, готово 1 из 23;
- вопросы слепой приёмки — `_workspace/ox-accept/runs/questions.json`, идёт.

Открыто, с адресом:

- якоря вида `файл.md#Lстрока` протухают: писатель `1chat-recall` дополняет
  списки в шапке файла и сдвигает все записи —
  `_ops/findings/2026-08-23-161926-4784-anchors.md`;
- веха 4 (operations proof) не начата;
- 8 якорей библиотеки не попадают ни в одну запись; 14 разговоров дают дрейф
  на соседнюю запись.
