---
эпик: "[[../../GOAL#Definition of done]]"
план: "[[task|1html — design-free visual explainer]]"
состояние: ✅ завершено
вех-готово: 5
вех-всего: 5
обновлено: 2026-08-20
kind: status
---

# Статус — 1html visual explainer

## Итог

Exact candidate установлен в Claude/Codex; реальный WhatsApp artifact прошёл
финальный legacy finish без изменения source hash и старого visual style.

## Свидетельства и статус

- Подтверждено: design-free harness — `124` checks; blank scaffold и
  произвольные narrative/dashboard designs проходят один blocking gate.
- Подтверждено потребителем поведения: fresh-agent создал timeline plan и
  nested system flow в `/tmp/1html-forward-proof`; оба `finish` green.
- Подтверждено: React Flow bridge не задаёт node anatomy; strict config/source
  contract входит в общие `124` checks.
- Подтверждено browser-ом: `/tmp/1html-react-freedom.KXGpyn/evidence` — три
  icon-only nodes и две rich nodes с `2` и `3` disclosures, `4` edges, zero
  remote requests/errors, inherited font/color, edge recompute, Pause/Play,
  desktop/mobile без document overflow.
- Подтверждено на живом WhatsApp artifact: SHA `7f9b2923…b4598b30`, theme
  `editorial`, фон `rgb(244, 240, 231)`, serif h1; rail `0`, header padding
  `12px 16px`, radius `18px`, border `1px`.
- Подтверждено: Apache ECharts 6.1.0 установлен как opt-in local runtime; пять
  independent JSON recipes лежат в `references/echarts.md`, page template нет.
- Подтверждено browser-ом: `/tmp/1html-echarts-proof.LsbgV5/evidence` — bar,
  line, scatter, Sankey и treemap render в SVG desktop/mobile, zero remote
  requests/errors, palette inherited, role/ARIA present, reduced motion
  `animation=false`, document overflow false.
- Исправлено по browser finding: generic `.hero` столкнулся с DaisyUI grid;
  локальный класс namespaced, а blocking gate теперь отклоняет неосознанный
  `.hero` и текстовый `.card` без direct content wrapper.
- Подтверждено: add-on wiring разбирает реальные HTML tags и работает на
  one-line/minified source; comment/code markers не активируют runtime.
- Подтверждено browser-ом: `/tmp/1html-mermaid-final-proof` — Mermaid наследует
  serif/palette; malformed source остаётся читаемым, pageerror/remote requests
  `0`.
- Подтверждено: свежий `claude-opus-5` (`max`) прочитал exact candidate и
  вернул `APPROVE`; material findings architecture/developer атак закрыты до
  вызова.
- Подтверждено: Claude/Codex common tree одинаков (`diff -qr
  --exclude=agents` пуст), оба `qv-skill` green, installed owner harness —
  `PASS: 124`.
- Подтверждено: installed Codex `finish --legacy` на WhatsApp artifact —
  `1` live page, `0` violations; SHA до/после
  `7f9b292383b52296ef8b494bdef4a6e403880a45e6c91f59a71fdb24b4598b30`.
- Подтверждено browser-ом после установки: rail `0`, header padding `12×16`,
  radius `18`, border `1`, warm background и serif h1 сохранены, overflow/page
  errors/remote responses `0`.
