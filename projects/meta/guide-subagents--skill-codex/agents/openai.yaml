interface:
  display_name: "Guide Subagents"
  short_description: "Prepare and integrate Codex subagents"
  default_prompt: "Use $guide-subagents to decide whether native Codex subagents are worthwhile, keep the main agent on blocking or integration work, assign workers only disjoint clean scopes, write launch-ready briefs, and verify returned work on disk before trusting status messages."
