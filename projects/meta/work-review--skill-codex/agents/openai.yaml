interface:
  display_name: "Work Review"
  short_description: "Verify work before closeout"
  default_prompt: >
    Use $work-review to verify work before closeout and route newly discovered
    branches to $1strategy-discussion when needed.

policy:
  allow_implicit_invocation: true
