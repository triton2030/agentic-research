interface:
  display_name: "Work Review"
  short_description: "Verify work before closeout"
  default_prompt: >
    Use $work-review to verify work before closeout when the task matches its trigger.

policy:
  allow_implicit_invocation: true
