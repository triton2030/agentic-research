interface:
  display_name: "Strategy Discussion"
  short_description: "Discuss branches before planning"
  default_prompt: >
    Use $strategy-discussion to reveal expert approach branches, tradeoffs,
    rejected routes, and consequential questions before planning or execution.

policy:
  allow_implicit_invocation: true
