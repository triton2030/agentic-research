interface:
  display_name: "Task Contract"
  short_description: "Own active task contracts"
  default_prompt: >
    Use $task-contract to own active task contracts when the task matches its trigger.

policy:
  allow_implicit_invocation: true
