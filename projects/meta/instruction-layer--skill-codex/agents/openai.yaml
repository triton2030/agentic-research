interface:
  display_name: "Instruction Layer"
  short_description: "Place routing instructions"
  default_prompt: >
    Use $instruction-layer to place routing instructions after unresolved
    control-surface tradeoffs are discussed through $strategy-discussion.

policy:
  allow_implicit_invocation: true
