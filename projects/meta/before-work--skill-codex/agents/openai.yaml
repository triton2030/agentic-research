interface:
  display_name: "Before Work"
  short_description: "Re-anchor work against plan"
  default_prompt: >
    Use $before-work to re-anchor the next action against the active plan and
    route unresolved branches to $strategy-discussion before non-trivial work.

policy:
  allow_implicit_invocation: true
