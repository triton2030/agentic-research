interface:
  display_name: "Repo Shape"
  short_description: "Design repo runtime shape"
  default_prompt: >
    Use $repo-shape to design repo runtime shape after unresolved shape
    tradeoffs are discussed through $strategy-discussion.

policy:
  allow_implicit_invocation: true
