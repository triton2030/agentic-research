interface:
  display_name: "Before Write"
  short_description: "Check scope before file edits"
  default_prompt: >
    Use $before-write to check scope before file edits when the task matches its trigger.

policy:
  allow_implicit_invocation: true
