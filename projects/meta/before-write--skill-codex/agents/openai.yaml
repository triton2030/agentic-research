interface:
  display_name: "Before Write"
  short_description: "Check scope before file edits"
  default_prompt: >
    Use $before-write to check scope before file edits and route unresolved
    branches to $1strategy-discussion when needed.

policy:
  allow_implicit_invocation: true
